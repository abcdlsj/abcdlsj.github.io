{
  "slug": "llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run",
  "part": 34,
  "title": "Writing an LLM from scratch, part 34a -- building a JAX training loop for an LLM training run",
  "shortTitle": "part 34a -- building a JAX training loop for an LLM training run",
  "date": "30 June 2026",
  "summary": "I'm going to build an LLM from scratch in JAX, but I wanted to get a training loop working first so that I could put it together piece by piece.",
  "href": "https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run",
  "phaseId": "jax",
  "phaseLabel": "08 · JAX 重构",
  "markdown": "For over a year, I've been using [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\" -- and the multitude of side-projects that have branched out from reading it -- as something like a curriculum for learning about modern AI. The one final task I had set myself was to build and train an LLM from scratch just using my notes -- no reference to the book, no reference to the model code I'd written following the book.\n\nAs an output, I wanted something as good as my best PyTorch model based on Raschka's code -- a base model, trained on 3.2B tokens, that my (admittedly limited) evals ranked as being close to the original GPT-2 small's quality.\n\nI wanted to use a different framework, just to make sure I wasn't parroting code that I'd somehow memorised, so I [asked people on Twitter](https://x.com/gpjt/status/1985434030880293004) which one I should use, and the winner was [JAX](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax).\n\nI took a slightly different route to Raschka's book; he takes an inside-out perspective, explaining things like attention, gradually building up a complete GPT-2-style model, and then building a training loop on top of it. I wanted to go outside-in: I'd put together a training harness to train the simplest-possible model with an API similar to a real LLM, get that working to my satisfaction, and then add features to that simple model, one by one, until it had the full architecture in place. The plan (which actually worked out nicely!) was that I'd be able to show how each change improved things.\n\nThat's all done now, and I'm posting about it in two parts; in this one, I'll explain how I built the training harness, and in the next, I'll show the actual building and training of the LLM.\n\nSo let's get started!\n\n### Which framework on top of JAX?\n\nJAX itself has a relatively minimal API, and doesn't include standard neural network components like linear layers. Likewise it doesn't have any built-in optimisers, data loaders or similar ML utilities.\n\nNow, I could have decided to build my LLM using just pure JAX, like [I previously did with a toy XOR model](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax). But I felt that it would be better to build this in the style that real-world JAX code is written, which would mean using some of the [many utility libraries](https://docs.jax.dev/en/latest/#ecosystem).\n\nOn the JAX site itself, there was a useful-looking link: \"If you’re looking to use JAX to train neural networks, check out the [JAX AI Stack](https://docs.jaxstack.ai/en/latest/getting_started.html)!\" On the linked page, it made it clear that the two core parts of that stack were:\n\n-   [Flax NNX](https://flax.readthedocs.io/en/stable/) for neural network components.\n-   [Optax](https://optax.readthedocs.io/en/latest/) for optimisation.\n\nI took a look at both, and they seemed pretty easy to grasp. Indeed, at first glance, I felt that NNX looked pretty PyTorch-like! In their tutorial example, the only real obvious difference was the JAX-y derivative-style gradient calculation and the way that random numbers were handled. And even the random numbers were handled in a less pure-functional way than pure JAX -- instead of having to mess around with splitting keys, you could just pass in what appeared to be a stateful variable that somehow split itself internally as needed.\n\nSo, NNX and Optax were the frameworks I'd use. Rather than grinding through the tutorials, I decided that I'd just dive right in, and try to pick things up as I went along.\n\nHow hard could it be...?\n\n### The A-to-A language model and the training loop\n\nTo build a functioning training loop, I needed a minimal model to train -- not an actual LLM, but something that behaved at least a bit like one. It would take in a sequence of tokens, and spit out logits for each token.\n\nIn my preferred model of [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work), at the top level for a model, we feed in a sequence of token IDs, then:\n\n-   Firstly, we convert them into embeddings, so we get a series of vectors. We do this by a lookup into a table, but we can see it conceptually as a projection via a matrix, from vocab space (where a particular token ID is a one-hot vector) to an embedding space.\n-   Next, we do the magic with our Transformers layers, getting embeddings for the next token. The embedding at position n in the output sequence, after these layers, is for the predicted token to come after the token at position n in the input sequence, considering that input token and all other tokens to its left.\n-   Finally, we project those back from embedding space to logits, this time actually using a real matrix (in the form of a linear layer). The logits (after being run through softmax) represent the probabilities for each token of it being the next one.\n\nAll of that suggested to me that the dumbest \"LLM\" I could write just to get started would be one that just projected token IDs into embedding space, and then projected back to vocab space. No Transformer layers at all.\n\nI'd then train it so that instead of trying to predict the next token, it would try to \"predict\" what was fed into it in the first place. In other words, you'd feed the training loop this input:\n\n`The fat cat sat on the mat`\n\n...and this target\n\n`The fat cat sat on the mat`\n\n...rather than the normal setup for an LLM, where you feed it\n\n`The fat cat sat on the`\n\n...and give it targets of\n\n`fat cat sat on the mat`\n\nIf I could get that to work -- and it felt like the kind of thing where you'd be able to get the loss down to near-zero without a huge amount of training -- then I could be reasonably sure that I had a working training loop. [1](#fn-1)\n\nI decided to call this an A-to-A model. Coding up the model itself was ridiculously simple: it looked like this:\n\n`from flax import nnx  class GPTModel(nnx.Module):      def __init__(         self,         vocab_size, context_length,         emb_dim,         n_heads, n_layers,         qkv_bias,         drop_rate,         rngs,     ):         self.token_embedding = nnx.Embed(             num_embeddings=vocab_size,             features=emb_dim,             rngs=rngs,         )          self.output_head = nnx.Linear(             in_features=emb_dim,             out_features=vocab_size,             use_bias=False,             rngs=rngs,         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          return self.output_head(input_embeddings)`\n\nThere's as much boilerplate in there -- for the parameters that I knew that the model would need when I built out the full LLM -- as there is actual code doing stuff!\n\nBut the training loop was a bit more fun.\n\n### Porting over the basics\n\nAs I said, my plan here was to make sure my understanding of the internals of LLMs was correct by rebuilding one just from my notes. That \"notes only\" restriction didn't apply to the training loop itself, so I allowed myself to crib a bit from the PyTorch [DistributedDataParallel code](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main) that I'd been using to train the original model in the cloud.\n\nThe first version that I used is [here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py). Let's start at the bottom, where we have [the `main` function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L393).\n\nIt starts with some boilerplate to handle the concept of \"runs\". This is a pattern I've found myself using in most of my projects. When working on a model, it's useful to be able to do multiple training runs, changing things each time. You want to keep the checkpoints, metadata and training charts for each one for future reference.\n\nSo in my repo, I'll have a \"runs\" directory, and in there subdirectories for each training run I want to track.\n\nIn those subdirectories, there are JSON files -- one to configure the model, [`model.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/model.json), and one to configure the training hyperparameters and similar stuff, [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json). (It's worth noting that at this stage, a bunch of those hyperparameters were unused; I kept them in there out of laziness, as I knew I'd need them later.) So we start our `main` function by loading those.\n\nOur next step is to completely ignore one of the training hyperparameters, `gradient_accumulation_steps`. I definitely wanted to do [gradient accumulation](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation), but decided to leave it for later. Better to get a solid, simpler training run done first, I felt.\n\nNext, we download the dataset we're going to use to our local disk with `download_dataset` (which will only download if there's not an up-to-date copy already there).\n\nThe next step is to call `load_dataset` to load it into RAM. You can see that there's another hard-coded variable there, `world_size`. This is a holdover from the multi-GPU DistributedDataParallel code that this was all based on; in this blog post I'm only covering the code for single-GPU training, but I decided to leave the DDP stuff in there for dataset-wrangling purposes, hardcoded to one GPU, so that it would be easier to re-introduce if I later decide to implement something similar in JAX.\n\nLet's take a look at `load_dataset` and its related stuff. If you go up to [line 39](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L39) you'll see the code. Firstly, there's a `BigTrainDataset` that keeps track of our training data. If you look closely, you might spot one oddity in that class. We have this:\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)`\n\nRemember that at this stage, the plan was to train the model to map tokens to themselves rather than to make next-token predictions. So the targets are the same as the inputs, not the more normal next token, which would look like (and, in the next post, will look like) this:\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[1:].reshape(-1, microbatch_size, seq_length)`\n\nNext, we have a `load_dataset` function to load the appropriate subset of the data from the copy on the local disk into one of those `BigTrainDataset` objects. I hit an out-of-memory issue when I ran the first version of this. It was trying to load the data into my GPU's VRAM -- JAX's default behaviour if you have a GPU, and the CUDA version of JAX is installed -- and there was too much to fit in there. After a bit of digging around [I learned how to change the JAX default device](https://www.gilesthomas.com/2026/06/jax-backends-and-devices) so that it would be loaded into normal system RAM.\n\nUnfortunately, once I'd done that, I found that iterating through it was super-slow -- it took about 1.2 seconds to get one training batch of 6,144 tokens out of the array, which meant that I'd have a limit of 5,120 tokens/second of training from that alone. I eventually learned that the data had been loaded into the main RAM, but was being copied up to the GPU for processing because it had not been *committed* to the main RAM -- [details here](https://www.gilesthomas.com/2026/06/jax-commitment-issues). Fixing that (with an explicit call to `jax.device_put`) meant that getting a single training batch from the dataset and putting it onto the GPU took less than 0.001s, which was much better.\n\nSo that was many hours of work that all got packed into lines 55 to 58 of the code:\n\n    `cpu0 = jax.devices(\"cpu\")[0]     with jax.default_device(cpu0):         full_dataset = load_file(dataset_dir / f\"{split}.safetensors\")[\"tokens\"]     full_dataset = jax.device_put(full_dataset, cpu0)`\n\nThe remainder of the logic in `load_dataset` is just to make sure that we have a dataset that is exactly the right size for the world size (even though that's always one right now), the microbatch size, the gradient accumulation steps, and the sequence length that we're working with,\n\nLet's [go back to the `main` function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L433) again. Having loaded our dataset, we create our model, passing in the model configuration stuff and also the (currently unused) dropout rate training hyperparameter, then we create a Flax NNX optimiser which wraps an Optax one. This was essentially a copy/paste from the Flax tutorial, except we're configuring the optimiser with learning rate and weight decay hyperparameters from the training config:\n\n    `optimizer = nnx.Optimizer(         model,         optax.adamw(             learning_rate=train_conf[\"learning_rate\"],             weight_decay=train_conf[\"weight_decay\"],         ),         wrt=nnx.Param     )`\n\nFinally, we call `train` to kick off our training loop, passing in some appropriate stuff. Let's [go to that function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L307) next.\n\nWe start off with a bit of housekeeping, then go into the main loop. You can see that it's kind of gesturing at gradient accumulation:\n\n    `for global_step in progress_bar:         for accumulation_step in range(gradient_accumulation_steps):`\n\n...but if you look at the actual body of that loop, it's not doing anything of the sort. It's just getting training batches, putting them on the GPU, doing a full training step, and keeping track of some metrics:\n\n            `inputs, targets = train_dataset[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]             inputs = jax.device_put(inputs, model_device)             targets = jax.device_put(targets, model_device)              train_loss = train_step(model, optimizer, inputs, targets)             train_losses.append(train_loss.item())              microbatch_size, sequence_length = inputs.shape             tokens_seen_this_rank += microbatch_size * sequence_length`\n\nSo, we're just doing a traditional batch-by-batch training loop without gradient accumulation right now. But some of the infrastructure is there, because it was the next thing I wanted to add after I'd got the basic loop working.\n\nThe rest of the `train` function is just housekeeping and checkpointing; we'll come back to the checkpointing shortly, but first let's take a look at the `train_step` function that actually trains the model on a set of inputs and targets, and its associated `calculate_loss` function -- they're [just above `train`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L292).\n\nNow, as you might remember from [my first JAX post](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax), the best way to JIT a training loop is at as high a level as possible. So when I first coded this, I integrated that into the traditionally-named `train_step` function like this:\n\n`@jax.jit def train_step(model, optimizer, inputs, targets):     loss, grads = nnx.value_and_grad(calculate_loss)(model, inputs, targets)     optimizer.update(model, grads)     return loss`\n\nWhen I actually came around to run it the first time, loss wasn't falling at all, and after banging my head against it for a while, I [realised I should have used `nnx.jit` rather than `jax.jit`](https://www.gilesthomas.com/2026/06/hashing-jax-parameters), fixed that, and kicked it off again. Loss started falling immediately. D'oh!\n\nNow let's take a look at loss. [Cross entropy loss](https://www.gilesthomas.com/2025/10/llm-from-scratch-20-starting-training-cross-entropy-loss) was clearly what I would need to train an LLM, and also felt like the right thing for the A-to-A model.\n\nOptax has five loss functions that are related to cross entropy; three of them looked a bit more complicated than I needed:\n\n-   `poly_loss_cross_entropy`,\n-   `safe_softmax_cross_entropy`, and\n-   `sigmoid_binary_cross_entropy`.\n\nSo it was a choice between\n\n-   [`softmax_cross_entropy`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy.html#optax.losses.softmax_cross_entropy) and\n-   [`softmax_cross_entropy_with_integer_labels`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels).\n\nThe latter was the right one -- `softmax_cross_entropy` expects the labels (that is, the target token IDs) to be one-hot vectors, while `softmax_cross_entropy_with_integer_labels`, as it says in the function name, expects integer labels, which is what we have.\n\nThat sounded pretty similar to PyTorch's [`cross_entropy`](https://docs.pytorch.org/docs/2.12/generated/torch.nn.functional.cross_entropy.html), but there was an important difference. For normal use (if you're not using K-dimensional loss, whatever that might be) PyTorch expects that the inputs are either just a one-dimensional tensor of c logits, or at worst a bxc matrix, where b is the batch size.\n\nI had noted when working through this section of Raschka's book that the code we wrote flattened things out. So a batch of six sequences, each 1,024 tokens long, with a vocab size of 50,257, would give us a logits tensor shaped like this:\n\n`(6, 1024, 50257)`\n\nThe first axis is the batches, the second is the length of the sequences -- remember, we have logits for every input token in the sequence, with next-token predictions for that token in the context of all of the other ones to its left. And the last axis, with a size equal to our tokeniser's vocabulary size, is the logits themselves.\n\nAfter flattening, it looked like a \"batch\" of 6\\*1024\\=6144 logits vectors:\n\n`(6144, 50257)`\n\nLikewise our targets -- the token IDs we wanted our model to be predicting -- were batched, and there was one per token in each sequence, so that tensor was\n\n`(6, 1024)`\n\nFlattened, it looked like a \"batch\" of 6\\*1024\\=6144 targets:\n\n`(6144,)`\n\nFinally, the PyTorch function returned a scalar value -- wrapped in a PyTorch `Tensor` object, of course, so that it could participate in the backward pass, but a single number.\n\nBut I'd forgotten about all of that when I was writing this part of the JAX code, and just fed the inputs and the targets straight in to the JAX function. The result was interesting. I started with this:\n\n     `loss = optax.losses.softmax_cross_entropy_with_integer_labels(          logits, targets      )`\n\nAnd printing out the shapes of each variable gave this:\n\n`logits.shape=(6, 1024, 50257) targets.shape=(6, 1024) loss.shape=(6, 1024)`\n\nIt had returned a cross entropy number for every element in every sequence, across all of the batches!\n\nWhat's interesting is that the [docs for `softmax_cross_entropy_with_integer_labels`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels) imply that it has the same restrictions as PyTorch's -- it expects a single batch axis in the tensors that are passed in. Perhaps they're out of date? Or perhaps Optax just assumes that you know that in JAX \"a batch axis\" should be read as \"as many batch axes as you want\"? Well, anyway -- it worked, and I checked that the numbers were solid.\n\nNow, of course, we can't ask JAX for gradients using that 6×1024 matrix -- the loss function needs to return a scalar -- but the `mean` function on a JAX array does exactly what we need. So I had a solid loss calculation, which you can see in `calculate_loss`:\n\n`def calculate_loss(model, inputs, targets):     logits = model(inputs)     loss = optax.losses.softmax_cross_entropy_with_integer_labels(         logits, targets     ).mean()     return loss`\n\nSo that's covered our loss function and the JITted `train_step` that uses it.\n\nThe only remaining code that I haven't gone over in this version of the `train.py` script is the stuff immediately above `calculate_loss` -- `get_training_data` and `generate_training_charts`. These are both called as part of the housekeeping code I glossed over in the `train` function, after we take checkpoints.\n\nThey just redraw a plot of the loss and other training metrics, using stuff that's stored in the metadata of all of the checkpoints so far. That means that there's a nice graphical way to keep track of a training run. Fairly dull stuff, so there's no need to go through them, but it is worth taking a look at the checkpointing code itself.\n\nYou can see the version I was working with at this point [here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py). It's not really much of a checkpoint; I was saving the model itself and the metadata needed for that charting code, but not the optimiser, which would be needed for a real checkpoint. After all, the purpose of a checkpoint is to be able to pick things up again if your training loop crashes, and you can't do that without the optimiser's state. Still, it was enough to get started with.\n\nThat said, one wrinkle I encountered when writing that simple checkpointing code was that it was a tad tricky to save them in Safetensors format -- [you can see the details here](https://www.gilesthomas.com/2026/06/flax-and-safetensors).\n\nSo, that was my initial training code. It was time to let it rip: could I train my dumb \"LLM\" to map from A to A?\n\n### The first A-to-A run\n\nAs I mentioned earlier, the very first run didn't converge at all -- loss started at about 10.82, which was promising (it's exactly what you'd expect for a randomly-initialised network trying to predict GPT-2 tokens -- see [here](https://www.gilesthomas.com/2026/06/hashing-jax-parameters) for details), but then it remained there.\n\nBut when I fixed the \"`jax.jit` should be `nnx.jit`\" issue, it started dropping. After 92,160,000 tokens seen, it seemed to have hit zero (at least to the three DPs I was printing), so I baked that into [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json) and did another training run fixed to that number of tokens. After about 14 minutes, it finished:\n\n`Training complete in 843.547 seconds Tokens seen: 92,160,000 Throughput: 109,253 tokens/second Final train loss: 0.000 2026-06-17 19:29:31.667194 Done`\n\nA very promising final loss, even though that was just whatever we got on the last batch! The actual loss chart looked like this:\n\n![A-to-A loss chart](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/a-to-a-loss-chart.png \"A-to-A loss chart\")\n\nIf you're used to the loss charts in my previous posts, there's something to highlight here: I've switched the Y axis over to being log, so those bumps near the end are actually tiny deviations away from 0.001.\n\nI think it's worth showing what the model actually did at this point. It was actually somewhat later that I wrote some code to load up the model checkpoints from these training runs and do some smoke tests, but I'll show you some results now.\n\nI wrote some code based on [my JAX safetensors post](https://www.gilesthomas.com/2026/06/flax-and-safetensors) to load up a model's parameters from a checkpoint's `model.safetensors` file:\n\n`def load_model(model, file):     model_state_simple_dict = load_file(file)     dict_flat_state = {}     for key, array in model_state_simple_dict.items():         elements = key.split(\".\")         list_key = []         for element in elements:             try:                 list_key.append(int(element))             except ValueError:                 list_key.append(element)         dict_flat_state[tuple(list_key)] = array      new_flat_state = nnx.from_flat_state(dict_flat_state)     nnx.update(model, new_flat_state)`\n\n...and then wrote two test scripts.\n\nFirstly, was it really mapping from A to A? I wanted to be sure that the loss number was actually reflecting what I wanted it to reflect. I wrote [a simple script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_a_to_a.py) that took a Safetensors file on the command line, and ran the first verse of *The Rime of the Ancient Mariner* (chosen because it uses oldish English so there are some odd tokens in it) through the LLM it loaded from that file.\n\nHere's what the model at the end of the run came up with:\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/best/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---`\n\nThat's great! It could certainly handle the mapping. Out of interest, I decided to see how quickly it had learned to get that right. The average training loss in that \"best\" checkpoint at the end of the training run was 0.0001, so how did the mapping improve, and what was the loss, near the start of the training run?\n\nFor the first checkpoint, when we'd just run one batch through, we had an average training loss of 10.8242. With the model parameters that were saved then, we get this output:\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/20260617Z185827-iteration-0/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: --- LOADRecommend ptwtacid cheek lunch KaLOAD blondrient Sole Broken engages CrimsplitrelyLOAD Consortium hopefully Fisheries qualardiestern565Financial gallery talked KaLOADhuge admit disappeared SoleERT Heardearth showcasingurancesLOAD ---`\n\nAs you'd expect from that loss, it's total token salad.\n\nNow let's take a look at the next checkpoint, taken after 375 \"global steps\" -- that is, 6,000 batches. In that one, the average train loss since that first checkpoint was 2.9323. But that hides something important -- the maximum loss, near the start, was (as you would expect) 10.78524, not much less than the average loss in the previous checkpoint. But the minimum (which we can safely assume was towards the end of this checkpointing period) was 0.54155, so we can reasonably assume that the model improved *very* rapidly at this point. And the A-to-A test bears this out:\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/20260617Z185848-iteration-375/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---`\n\nSo, we can see that the bulk of the improvement happened right at the start! It was able to pass the A-to-A test for that fairly unusual sequence after just 6,001 total batches of 6 1,024-token sequences.\n\nThe rest of the training run was perhaps just grinding out improvement on rarer tokens, and perhaps making it more certain about already-correct predictions. After all, the test script was simply printing the most likely token for each position, so at this state it might have been predicting some of those tokens as 51% probability. That would have meant a penalty in the loss function, even if the answer was actually correct.\n\nSo that was an interesting script; I wanted to do another -- the standard smoke test that I've been using, based on Raschka's prompt: how does the model complete \"Every effort moves you\" when asked to continue the sentence? [Here's the script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_generation.py), and here's what it generated:\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_generation.py runs/a-to-a/checkpoints/best/model.safetensors Every effort moves you you you you you you you you you you you you you you you you you you you you you`\n\nThat makes perfect sense. In order to generate the next token in an autoregressive loop, we're looking at the logits for the last one in the prompt. When it first runs, the last token is \" you\", and our model is trained to map A to A, so its result is \" you\". We append that to the prompt, run it through again, the last token is still \" you\", so of course it \"predicts\" the token \" you\" again. And so on.\n\nSo these results were both good news! The A-to-A mapping was working, and was converging rapidly in terms of loss -- and even more rapidly in terms of our poetic test.\n\nSo, what was next? I wanted the training loop to be as similar as possible to the code I used for my [best locally-trained PyTorch model](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests). That used three things I had not built into the training loop at this stage: learning rate scheduling, gradient clipping, and gradient accumulation. The PyTorch code also had the ability to restart from a checkpoint -- not super-important in a 14-minute training run like this one, but I figured it would become important later. After all, the PyTorch runs on my local machine had taken almost two days, and if something went wrong halfway through (cat jumping onto PC power button, etc) then I really wouldn't want to start from scratch.\n\nI decided to handle gradient accumulation first.\n\n### Gradient accumulation\n\nIn PyTorch, doing [gradient accumulation](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) is pretty simple: the core of a typical training loop without it might look something like this:\n\n    `optimizer.zero_grad()      result = model(inputs)     loss = loss_function(result, targets)     loss.backward()      optimizer.step()`\n\nWe start off by clearing out any gradients that are stashed on the model's parameters, then do a forward pass, work out the loss, do a backward pass to put new gradients on the parameters, and then step the optimiser to apply those gradients.\n\nAccumulating gradients just means changing it to something like this:\n\n    `optimizer.zero_grad()      for step in range(gradient_accumulation_steps):         result = model(inputs)         loss = loss_function(result, targets)         (loss / gradient_accumulation_steps).backward()      optimizer.step()`\n\nThat is, we do a forward and a backward pass `gradient_accumulation_steps` times. Because we're not zeroing out existing gradients between them, the parameters will accumulate gradients over time -- each backward pass will add its contribution onto what is already there. Each time, we divide the loss by `gradient_accumulation_steps`, so that the gradients that are put on the parameters are that much smaller, which means that by the end of our loop we've got gradients that are the average of what we'd have got if we'd done all of these microbatches in one big batch. Finally, once we've exited the loop, we step the optimiser to apply those averaged gradients.\n\nWhen I started thinking about implementing this in JAX, I noticed that [Optax has a help page on how to do it](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html), but then I had one of those brilliant shower thoughts that one sometimes has. I should have learned by my age that they rarely work out well, but this time I decided to give it a go rather than doing things the official way.\n\nMy brilliant idea was that with some finessing, we could put the whole gradient accumulation loop inside JITted code. From what I'd learned so far, the higher up in our code we put the JIT decorator -- that is, the more of the training loop it covered -- the faster it would be. In itself, that wasn't a bad idea.\n\nBut my first implementation was less smart:\n\n`def calculate_loss(model, inputs, targets):     loss = 0     for microbatch_inputs, microbatch_targets in zip(inputs, targets):         logits = model(microbatch_inputs)         loss += optax.losses.softmax_cross_entropy_with_integer_labels(             logits, microbatch_targets         ).mean()     return loss  @nnx.jit def train_step(model, optimizer, inputs, targets):     loss, grads = nnx.value_and_grad(calculate_loss)(model, inputs, targets)     optimizer.update(model, grads)     return loss`\n\nThe `inputs` were full-step arrays (eg. shaped (16, 6, 1024) for 16 gradient-accumulation steps over 6 microbatches of 1024 sequences), and the targets likewise. That seemed very clever! But in retrospect, it was obviously doomed to failure, and when I ran it, I ran out of VRAM.\n\nThe point of gradient accumulation is that what you accumulate over time is, well, gradients. So you have to do a full forward pass and then a backward pass over the model for each microbatch, letting gradients build up, and then apply those in one go, like the PyTorch code did.\n\nUnfortunately what I was doing with my code was essentially all of the forward passes, one by one, letting the activations and JAX's internal structures representing what calculations had been done accumulate -- not the gradients -- and then doing a single backward pass across all of that. Mathematically it made sense -- I would have got the right effect if I'd had enough VRAM -- but it wasn't much more memory-efficient than just doing a single batch of `gradient_accumulation_steps * batch_size` sequences. Immediate CUDA OOM.\n\nMy second attempt was a bit more sensible and ran OK without the JIT:\n\n`#@nnx.jit def train_step(model, optimizer, inputs, targets):     loss_list = []     grads_list = []     for microbatch_inputs, microbatch_targets in zip(inputs, targets):         microbatch_loss, microbatch_grads = nnx.value_and_grad(calculate_loss)(model, microbatch_inputs, microbatch_targets)         loss_list.append(microbatch_loss)         grads_list.append(microbatch_grads)      average_grads = jax.tree.map(         lambda *items: jnp.array(items).mean(axis=0),         *grads_list     )      optimizer.update(model, average_grads)     return jnp.array(loss_list).mean()`\n\nYou can see that now I was doing both the forward and the backward pass within the loop, and then working out the mean gradients with that `jax.tree.map`, then passing those average gradients to the optimizer.\n\nIt all made sense, and seemed to work when I ran it:\n\n`Training complete in 1,146.173 seconds Tokens seen: 92,209,152 Throughput: 80,450 tokens/second Final train loss: 0.001 2026-06-18 19:00:25.739249 Done`\n\n...and it wasn't as much slower as I would expect given the lack of JITting: 1,146 seconds versus 843.\n\nIt was interesting that the final train loss was higher than the run without gradient accumulation, but larger effective batch sizes are not always a better thing: it depends very much on the model you're training and the data. The batch size and number of gradient accumulation steps I was using were ones I had optimised for the full 163M-parameter GPT-2-style LLM, not for this model. So it was OK if it was a bit worse.\n\nAnyway, I tried adding the `@nnx.jit` to that function, and ran it:\n\n`jax.errors.JaxRuntimeError: RESOURCE_EXHAUSTED: Out of memory while trying to allocate 20.71GiB. [tf-allocator-allocation-error=''] [executable_name='jit_train_step']`\n\nOuch. And looking at the traceback, it appeared that it was the actual JITting that was running out of VRAM. Something to do with loop unrolling, perhaps? I dug around for a while, trying to use JAX's [`fori_loop`](https://docs.jax.dev/en/latest/_autosummary/jax.lax.fori_loop.html) rather than a normal Python one, but to no avail -- I would always run out of GPU memory.\n\nEventually, after a few hours, the alarm bells on my side quest detector had become too loud to ignore. Reluctantly, I gave up on hand-rolling my own gradient accumulation, and implemented it [the Optax way](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html).\n\nThat was actually really nice and simple. The [code is here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py), but the change is tiny and simple to explain. Remember that we had this code to set up the optimizer:\n\n    `optimizer = nnx.Optimizer(         model,         optax.adamw(             learning_rate=train_conf[\"learning_rate\"],             weight_decay=train_conf[\"weight_decay\"],         ),         wrt=nnx.Param     )`\n\nThat creates a Flax NNX optimiser, which uses an Optax AdamW optimiser under the hood.\n\nThe Optax way to do gradient accumulation is to wrap the optimiser in a [`MultiSteps`](https://optax.readthedocs.io/en/latest/api/generated/optax.MultiSteps.html) helper, which -- with the NNX optimiser wrapping the result -- looks like this:\n\n    `optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax.adamw(                 learning_rate=train_conf[\"learning_rate\"],                 weight_decay=train_conf[\"weight_decay\"],             ),             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nThe `MultiSteps` wrapper is really neat. It has the same interface as a regular optimiser, so its `update` method can be called with a set of gradients. But instead of applying them, it just accumulates them until a particular number of calls to `update` have been made, at which it actually does apply the mean of the accumulated gradients, and resets its counter so that it starts accumulating again.\n\nThat's actually a really nice API. And it actually meant that I would have been able to simplify the training loop. Remember, we had this:\n\n    `for global_step in progress_bar:         for accumulation_step in range(gradient_accumulation_steps):             inputs, targets = train_dataset[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]             inputs = jax.device_put(inputs, model_device)             targets = jax.device_put(targets, model_device)              train_loss = train_step(model, optimizer, inputs, targets)             train_losses.append(train_loss.item())              microbatch_size, sequence_length = inputs.shape             tokens_seen_this_rank += microbatch_size * sequence_length`\n\nThe loop-within-a-loop was needed by the PyTorch code, because we needed to do the optimizer step at the end to apply the accumulated gradients. But with the Optax wrapper, we could have just iterated over our samples in one top-level loop, relying on the `MultiSteps` to make its updates every `gradient_accumulation_steps` iterations.\n\nHowever, I decided to leave it in -- keeping track of the training in terms of global steps meant that the training output with my JAX model would be easier to compare to the PyTorch versions. Perhaps if I'd been building the training loop completely from scratch I would have chosen differently.\n\nAnyway, with that code change in, I ran it, and:\n\n`Training complete in 836.409 seconds Tokens seen: 92,209,152 Throughput: 110,244 tokens/second Final train loss: 0.001 2026-06-18 20:56:15.220326 Done`\n\nI had the same loss at the end as the by-hand un-JITted version, which was reassuring. And it was slightly faster than the non-gradient-accumulating version, but it's a small enough difference that it was probably just in the noise.\n\nSo that was gradient accumulation! [Here's the code with that added](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py).\n\nNext, I wanted to get charting and scheduling of the learning rate, and gradient clipping working.\n\n### Charting the learning rate\n\nScheduling the learning rate means that we'll be changing it over the course of the run -- like this example from one of my PyTorch training runs:\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nHaving a chart like that one is really useful, as it allows you to sanity-check that the changes you are making to the learning rate really are the right ones. So I wanted to add the charting first, and then the scheduling. The boilerplate code to actually generate the chart, given learning rate numbers in the checkpoints' metadata, was already there, so I had to work out how to extract the current value of the learning rate from the optimiser and then save it into the checkpoints.\n\n[This was the obvious starting point](https://optax.readthedocs.io/en/latest/getting_started.html#accessing-learning-rate). Optax optimisers themselves don't store the learning rate, but if you create them like this:\n\n`optax.inject_hyperparams(optax.adam)(...)`\n\n...where the `...` in the brackets is the normal stuff that you'd pass in to the optimizer when creating it, then you can extract the learning rate later.\n\nHowever, the code on that help page was using the Optax optimiser directly, whereas my one in the training code was wrapped inside a `MultiSteps`, which was in turn wrapped inside an NNX `Optimizer` object, like this:\n\n    `optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax.adamw(                 learning_rate=train_conf[\"learning_rate\"],                 weight_decay=train_conf[\"weight_decay\"],             ),             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nStill, the solution seemed reasonably clear. I could use the `inject_hyperparameters` trick on the `adamw` that I was creating, and then pass it in to be wrapped like this:\n\n    `optax_optimizer = optax.inject_hyperparams(optax.adamw)(         learning_rate=train_conf[\"learning_rate\"],         weight_decay=train_conf[\"weight_decay\"],     )     optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax_optimizer,             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nThe next question was how to actually read the learning rate from that optimiser.\n\nThe sample code in the Optax docs looked like this:\n\n`optimizer = optax.inject_hyperparams(optax.adamw)(learning_rate=schedule)  params = initial_params state = optimizer.init(params) print('initial learning rate:', state.hyperparams['learning_rate'])  _, state = fit(initial_params, optimizer)  print('final learning rate:', state.hyperparams['learning_rate'])`\n\nAgain, that was using the Optax optimiser directly, rather than trying to use one that was inside an NNX one. However, in [the docs for NNX's optimiser](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/training/optimizer.html) I noticed that it exposes its wrapped Optax one's state as `opt_state`. I put in some temporary debug code to print that, and saw that it was the `MultiSteps`' state, which made sense -- and that, in turn, contained the state of the wrapped `adamw` one as `inner_opt_state`.\n\nThat `inner_opt_state` had a field called `hyperparameters`, which was a dictionary that included `learning_rate` as a key.\n\nFinally, the value that that key pointed to was a `Variable` object. To get the actual value from there, you need to call its `get_value()` to get the actual value, which is a JNP array, so we needed to call `item()` on it.\n\nAll of that led to the following abomination unto God, mankind, and [the Law of Demeter](https://en.wikipedia.org/wiki/Law_of_Demeter):\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state.hyperparams[\"learning_rate\"].get_value().item()`\n\nEurgh. I mean, really, eurgh.\n\nWell, anyway, I put code to do that into the `train` function and save the number as part of the metadata. I did a partial training run, just for long enough to confirm that the learning rate chart was being generated, and had a flat line on it at 0.0014, the constant learning rate I was using at that point.\n\nI can't say I was very proud of it, though.\n\n### Learning rate scheduling\n\nTo recap, the learning rate schedule that I wanted was this:\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nThat's formed of two phases: an initial warmup, where the learning rate started at 0.00001 times the desired peak value, and then rose linearly to the peak, followed by a cosine wave to decay it to 0.1 times the peak.\n\nIn PyTorch I had had to [use different learning rate scheduler objects to handle each phase, with a `SequentialLR` wrapper to bolt them together](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate#chaining-learning-rate-schedulers):\n\n        `warmup_scheduler = torch.optim.lr_scheduler.LinearLR(             optimizer,             start_factor=0.00001,             end_factor=1.0,             total_iters=warmup_steps         )         cosine_scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(             optimizer,             T_max=decay_steps,             eta_min=learning_rate / 10         )         scheduler = torch.optim.lr_scheduler.SequentialLR(             optimizer,             schedulers=[warmup_scheduler, cosine_scheduler],             milestones=[warmup_steps],         )`\n\nHowever, it's a common pattern in training loops, and conveniently Optax provides a [`warmup_cosine_decay_schedule`](https://optax.readthedocs.io/en/latest/api/generated/optax.schedules.warmup_cosine_decay_schedule.html) class that does all of that for you.\n\nThe only oddity in it is that `decay_steps` is kind of misnamed; it's actually total steps, including the warmup. So I wound up writing this code:\n\n    `total_steps = (len(train_dataset) // world_size) // gradient_accumulation_steps     warmup_steps = (total_steps * train_conf[\"warmup_period_percent\"]) // 100     learning_rate = train_conf[\"learning_rate\"]     schedule = optax.warmup_cosine_decay_schedule(         init_value=learning_rate * 0.00001,         peak_value=learning_rate,         warmup_steps=warmup_steps,         decay_steps=total_steps,  # !!         end_value=learning_rate / 10,     )      optax_optimizer = optax.inject_hyperparams(optax.adamw)(         learning_rate=schedule,         weight_decay=train_conf[\"weight_decay\"],     )`\n\nI did a training run with that, and it completed with this:\n\n`2026-06-19 00:17:25.777629 Tokens seen: 92,209,152 2026-06-19 00:17:25.777633 Throughput: 106,225 tokens/second 2026-06-19 00:17:25.777644 Final train loss: 0.006 2026-06-19 00:17:25.777646 Done`\n\nThe loss was a bit worse again, but just as with the gradient accumulation steps, the learning rate schedule I had specified was specifically designed for training a real (if small) LLM, not for this toy A-to-A task that I was using to test the training loop. The important thing was the learning rate chart, and it looked like this:\n\n![The learning rate chart for the first scheduled learning rate run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart.png \"The learning rate chart for the first scheduled learning rate run\")\n\nPerfect! [Here's the code at this point](https://github.com/gpjt/jax-gpt2-from-scratch/blob/f89d8d86cecf7e6f9d3bb24186df16218c583500/train.py).\n\nThere were two boxes left to check before I had a training loop I could actually use to build the LLM: gradient clipping and the ability to restart from a checkpoint. I decided to do gradient clipping first.\n\n### Gradient clipping\n\n[Gradient clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) is where for each update, you look for gradients that are suspiciously large, and cut them off so that they don't make excessive changes to the model.\n\nThe [Optax docs](https://optax.readthedocs.io/en/latest/getting_started.html#weight-decay-schedules-and-clipping) made it look pretty simple:\n\n`optimizer = optax.chain(   optax.clip(1.0),   optax.adamw(learning_rate=schedule), )`\n\nSo, you use an `optax.chain` to chain together first a thing that does clipping, and then the actual optimiser -- presumably the first thing in the chain sees the gradients and does stuff to them, and then the second receives whatever the first has returned.\n\nNow, the question was, should we do the chain outside or inside the MultiSteps? That is, should we clip gradients each time before we step the MultiSteps optimiser, or do we accumulate them and clip the average before we step the inner AdamW one?\n\nLooking at [the old PyTorch code](https://github.com/gpjt/ddp-base-model-from-scratch/blob/main/ddp_train.py#L354), I was running the gradient accumulation loop, and then clipping at the end. So the gradient clipping was happening to the accumulated gradients.\n\nThat actually felt less intuitively good than the alternative, but I decided that we should try to mirror what the PyTorch code is doing. So:\n\n    `optax_optimizer = optax.chain(         optax.clip(train_conf[\"clipping_max_norm\"]),         optax.inject_hyperparams(optax.adamw)(             learning_rate=schedule,             weight_decay=train_conf[\"weight_decay\"],         )     )`\n\nSo, the `adamw` optimiser would receive clipped gradients. Because it was wrapped in the `MultiSteps`, it was receiving the accumulated gradients every time that object hit its `every_k_schedule` limit.\n\nUnfortunately there was still a problem: that change meant that the optimiser that we were reading the learning rate from with this horrendous code in the `train` function:\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state.hyperparams[\"learning_rate\"].get_value().item()`\n\n...would now be inside yet another level of nesting -- the `chain` object. So, of course, when I ran it, it blew up with an error:\n\n`AttributeError: 'tuple' object has no attribute 'hyperparams'`\n\nI used some debug prints to work out what was going on, and determined that the state of the `chain` object was a tuple, the first element being an essentially-empty state for the clipper, and the second being the hyperparameter-injected state for the `adamw`.\n\nSo that meant that the new correct code to get the learning rate would be this:\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state[1].hyperparams[\"learning_rate\"].get_value().item()`\n\nNote that we've gained that `[1]` to do the lookup into the `chain`'s tuple state. I remember coming across a comment saying \"forgive us for our trespasses in this method\" in a codebase long ago, and I know well how the author felt.\n\nI did have an idea of how to at least limit the blast radius a bit, though. At this point in the code, I had the complex optimiser setup in the `main` function, and the learning-rate-getting abomination in `train`. I decided instead to define a function called `get_learning_rate` right next to the optimiser setup, and pass that in to `train`. So the horror was still there, but at least it was all in one place, like this:\n\n    `optax_optimizer = optax.chain(         optax.clip(train_conf[\"clipping_max_norm\"]),         optax.inject_hyperparams(optax.adamw)(             learning_rate=schedule,             weight_decay=train_conf[\"weight_decay\"],         )     )     optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax_optimizer,             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )      def get_learning_rate():         return (             optimizer             .opt_state             .inner_opt_state[1]             .hyperparams[\"learning_rate\"]             .get_value()             .item()         )      log(\"Start train\")     start_global_step = 0  ## checkpointing     train(         run_dir,         model, optimizer,         get_learning_rate,         train_dataset,         rank, world_size,         gradient_accumulation_steps,         start_global_step,         train_conf[\"checkpoint_interval\"],     )`\n\n...where `train` called `get_learning_rate` where it needed it.\n\nI was just about to kick this off, but by chance happened to take a closer look at [the documentation for `clip`](https://optax.readthedocs.io/en/latest/api/generated/optax.clip.html), and spotted that it said\n\n> Clips updates element-wise, to be in `[-max_delta, +max_delta]`\n\nThat rung a bell! When I was [originally looking into gradient clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) for the PyTorch training loop, I noted that that is a perfectly valid way to do gradient clipping, but it's not the way I ultimately chose. Instead, I was clipping based on the L2 norm.\n\nThe JAX training code was meant to work the same way as the PyTorch code, so that was a good catch; I switched over from using `optax.clip` to using [`optax.clip_by_global_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.clip_by_global_norm.html), and then kicked off another training run:\n\n`2026-06-19 01:22:41.964291 Tokens seen: 92,209,152 2026-06-19 01:22:41.964295 Throughput: 105,022 tokens/second 2026-06-19 01:22:41.964308 Final train loss: 0.006 2026-06-19 01:22:41.964311 Done`\n\nEverything looked fine; my guess was that the final loss was so similar because a simple task like A-to-A mapping, with such a shallow network, would be unlikely to cause gradients to explode.\n\nBut it would be nice to be sure. Was there some way I could track the gradients and see if clipping had had to cut in?\n\nOne neat thing we had in the PyTorch code was that we could track gradient norms pre-clipping:\n\n    `pre_clip_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), clipping_max_norm).item()     grad_norms.append(pre_clip_norm)     clipped_steps.append(pre_clip_norm > clipping_max_norm)`\n\nUnfortunately, `clip_by_global_norm` and the general Optax API doesn't provide any way to access the pre-clipping norms: the `ClipByGlobalNormState` that was the zeroth element of the state of the `chain` that we were reading in the horrendous learning rate-reading code is an alias of `EmptyState`.\n\nI considered using [`optax.tree_utils.tree_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.tree_utils.tree_norm.html) to work out the norms directly, and logging that, but that would be tricky -- because the gradients we were applying the clipping to were not the ones that were generated in the `train_step` function, but instead the ones that had accumulated inside the `MultiSteps` object over multiple gradient accumulation steps.\n\nThis sounded like a lot of work for a not-enormous benefit, so I decided to leave it out for this project.\n\nThere was, however, one small change that I wanted to make while I was messing around with gradients -- what to do if non-finite numbers crept into them.\n\nBack when I was first looking into gradient clipping, I was [somewhat horrified to realise](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping#chasing-infinity) that the scaler object I was using to tell PyTorch to train in 16-bit for things where it felt it would help (Automated Mixed Precision, or AMP), was silently dropping any updates with non-finite gradients, and if you didn't use AMP, such gradients would be happily applied to your model, most likely completely breaking it by setting parameters to non-finite values.\n\nThis felt like the wrong place for that kind of logic to go -- I felt that it should belong to the optimiser, or at least in some other part of the stack that wasn't specifically related to the totally orthogonal task of mixed-precision training.\n\nI checked what JAX's default behaviour with non-finite gradients was, and it turned out to be to just apply them -- but, with Optax, it actually *was* something you could fix at the optimiser level. If you wrap an Optax optimiser with [`apply_if_finite`](https://optax.readthedocs.io/en/latest/api/generated/optax.apply_if_finite.html#optax.apply_if_finite), it will only apply finite gradients, so we could add it to the optimiser setup like this:\n\n    `optimizer = nnx.Optimizer(         model,         optax.apply_if_finite(             optax.MultiSteps(                 optax_optimizer,                 every_k_schedule=gradient_accumulation_steps             ),             max_consecutive_errors=math.inf,         ),         wrt=nnx.Param     )`\n\nI set `max_consecutive_errors` to infinity to mirror the PyTorch code's behaviour.\n\nNow, obviously, this required yet another level of indirection in the learning-rate-getting function from hell:\n\n    `def get_learning_rate():         return (             optimizer             .opt_state             .inner_state             .inner_opt_state[1]             .hyperparams[\"learning_rate\"]             .get_value()             .item()         )`\n\nIf you're keeping track, it's the `.inner_state` in there. Heigh ho.\n\nSo, it was time to run it again:\n\n`Training complete in 892.715 seconds 2026-06-19 02:27:16.657056 Tokens seen: 92,209,152 2026-06-19 02:27:16.657059 Throughput: 103,291 tokens/second 2026-06-19 02:27:16.657070 Final train loss: 0.006 2026-06-19 02:27:16.657072 Done`\n\nThat looked OK -- no change from before. [Here's the code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fdfcd8d6369822a98380446dca5e5dd7fc503b10/train.py).\n\nNow, it was time to take the last step to finish the training loop: the ability to restart from a checkpoint.\n\n### Restarting from a checkpoint\n\nAt this point, [the checkpointing code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py) was pretty basic -- it would save the model as a Safetensors file, along with some metadata like the min, max and average loss since the previous checkpoint, the number of the global step that we were on, and whether or not this was the best checkpoint (in terms of average training loss) so far.\n\nIn order to restore from a checkpoint, we'd need more information. In the old PyTorch code, we needed three extra things on top of the model and the metadata:\n\n-   The scaler that we used to do automated mixed-precision training. This JAX loop was not going to do that, so it was not necessary here.\n-   The learning rate scheduler. This was built into the optimiser for JAX, so I didn't think it was needed.\n-   The optimiser itself. This was important, and we definitely did need to save it.\n\nSo that was the job: save the optimiser in `save_checkpoint`, and then implement a `load_checkpoint` so that we can restart from one. I could then try kicking off a training run, waiting for a bit, killing it, then restarting from the most recent checkpoint. The loss and learning rate charts would tell me whether or not the restart really had picked up from where it had left off.\n\nInitially I was thinking that I would just use pickle to save the optimiser, but that felt like a problem waiting to happen. Pickle has issues when you change Python versions or versions of installed packages, which never *feels like* it's going to be a problem, but all-too-frequently turns out to break stuff in reality. [2](#fn-2)\n\nUsing Safetensors looked a bit tricky -- it had been [hard](https://www.gilesthomas.com/2026/06/flax-and-safetensors) to get it to work with Flax models, even though it had explicit support.\n\nNow, the recommended library for checkpointing in JAX code is called [Orbax](https://orbax.readthedocs.io/en/latest/index.html). I'd looked into it before, and it looked a bit heavyweight, so I'd moved on. But digging in a little more, I found that it had what looked like [a simple API for saving PyTrees](https://orbax.readthedocs.io/en/latest/guides/checkpoint/v1/checkpointing_pytrees.html), which bypassed the complexity.\n\nGetting it working was still a bit tricky, though.\n\nFirstly, in the docs, they give this example:\n\n`import orbax.checkpoint.experimental.v1 as ocp  ...  ocp.save(path, pytree)`\n\nI tried that in the `save_checkpoint` function with code like this:\n\n    `ocp.save(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\n...and got the error\n\n`AttributeError: module 'orbax.checkpoint.experimental.v1' has no attribute 'save'`\n\nHuh. Digging into the library from the command line showed that the function was actually called `save_pytree`. Not super-promising if the docs don't match the API (though to be fair, it does say `experimental` right there in the package name).\n\nAnyway, changing that appeared to work:\n\n    `ocp.save_pytree(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\n...and then next to the 295 MB file called `model.safetensors` in my checkpoint directories, there was a 353 MB directory called `optimizer`. In PyTorch-land the optimiser had always been double the size of the model [3](#fn-3), but given the wildly different file formats in play, I was comfortable enough that it was order-of-magnitude the same as the model and somewhat bigger. Perhaps Orbax was doing some kind of compression or something like that.\n\nNext, it was time to write `load_checkpoint`. I started off by writing the `load_model` function to load up the safetensors file -- that's the one I showed earlier, back when I showed how the original A-to-A model learned how to map a poem to itself, and that if you asked it how to complete \"Every effort moves you\", it would respond with \" you you you you you\" and so on.\n\nOnce I had that, I created a `load_checkpoint`, which called `load_model`, and then loaded up the metadata and worked out what our best loss so far had been (which is necessary when continuing from a checkpoint so that, as you continue training, you can work out whether each new global step has had a loss that is better than the current best). That was simple enough:\n\n    `with open(checkpoint_dir / \"meta.json\", \"r\") as f:         meta = json.load(f)         restart_global_step = meta[\"global_step\"] + 1      with open(checkpoints_dir / \"best\" / \"meta.json\") as f:         best_loss = json.load(f)[\"avg_train_loss\"]`\n\nRestoring the optimiser turned out to be a bit trickier. Firstly, of course, just like with saving, the Orbax function was called `load_pytree` rather than the documented `load`. The next part was working out how to load it in a fashion that the optimiser would accept.\n\nIf you load a checkpointed PyTree like this:\n\n`ocp.load_pytree(checkpoint_dir / \"optimizer\")`\n\nThen what you get back is a \"basic\" PyTree -- it will consist of lists, dictionaries, tuples, basic Python types like strings, and JAX arrays. The problem is that the optimiser's state is formed of objects that can be mapped to such things -- for example, an object can be mapped to a dictionary where each field is an item in the dict -- but aren't actually those specific types of objects.\n\nSo if you do this:\n\n`optimizer.opt_state = ocp.load_pytree(checkpoint_dir / \"optimizer\")`\n\n...you get an error, something like this:\n\n`AttributeError: 'list' object has no attribute 'items'`\n\n...and likewise if you use the `nnx.update` function I was using in the `load_model` code:\n\n`nnx.update(optimizer.opt_state, ocp.load_pytree(checkpoint_dir / \"optimizer\"))`\n\n...you'll get a slightly different but equally confusing error.\n\nAfter a certain amount of floundering around, limited by the lack of documentation (and it not seeming to match the API that I was seeing) I had the bright idea of looking at `load_pytree`'s docstring, and that turned out to be excellent. In IPython:\n\n`In [1]: import orbax.checkpoint.experimental.v1 as ocp  In [2]: ocp.load_pytree? Signature: ocp.load_pytree(     path: 'path_types.PathLike',     abstract_pytree: 'AbstractPyTree | CheckpointMetadata[AbstractPyTree] | None' = None,     *,     checkpointable_name: 'str | None' = 'AUTO', ) -> 'tree_types.PyTreeOf[tree_types.Leaf]' Docstring: Loads a PyTree.  Loads from a ``PyTree`` checkpoint. A ``PyTree`` checkpoint must be a path containing a subdirectory with the name provided by ``checkpointable_name``, with default value ``AUTO``. See ``checkpointable_name`` for more details.  This function must be called on all available controller processes.  The operation blocks until complete. For improved performance, consider using :py:func:``.load_pytree_async`` instead.  If ``abstract_pytree`` is not provided, the ``PyTree`` will be loaded exactly as saved.  IMPORTANT: Loading is more brittle and error-prone when not providing ``abstract_pytree``. Always provide ``abstract_pytree`` if possible. Note that you can always obtain the tree structure from a saved checkpoint using :py:func:``.pytree_metadata``.  Providing the ``abstract_pytree`` guarantees two things:  1. The restored tree will exactly match the structure of ``abstract_pytree`` (or raise an error if it is impossible to guarantee this). For example, if ``abstract_pytree`` is a custom object registered as a ``PyTree``, the checkpoint will be restored as the same object, if possible.  2. The leaves of the restored tree will be restored with the properties indicated by the abstract leaves. For example, if a leaf in ``abstract_pytree`` is a ``jax.ShapeDtypeStruct``, the restored leaf will be a ``jax.Array`` with the same shape and ``dtype``. Each ``AbstractLeaf`` has a corresponding ``Leaf`` that is restored. See ``orbax.checkpoint.v1.tree`` for a table of standard supported leaf types.  ...`\n\nThe solution was obviously that `abstract_pytree`. When you provide it, it's used as a template. If in the abstract PyTree it finds a `Foo` object, and in the loaded PyTree there is a dictionary in the same position with keys `bar`, `baz` and `quz`, it will create a `Foo` object, setting those fields to those values.\n\nThat means that you have something with the right structure to apply, so I wound up with this relatively simple code to load checkpoint into the optimiser:\n\n`optimizer.opt_state = ocp.load_pytree(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\nWe're using the existing state of the optimiser as a template to tell Orbax how to structure the loaded one.\n\nI kicked off a training run, hit control-C halfway through, then restarted it from the checkpoint, and the final loss chart looked like this:\n\n![The loss chart for the interrupted and restarted run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/loss-chart-interrupted.png \"The loss chart for the interrupted and restarted run\")\n\n...and the learning rate chart like this:\n\n![The learning rate chart for the interrupted and restarted run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart-interrupted.png \"The learning rate chart for the interrupted and restarted run\")\n\nPerfect! The interrupt was at about global step 400, and the loss continued to go down properly, and the learning rate followed its schedule perfectly.\n\nHere's [the checkpoint-loading code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/checkpointing.py) and [the training script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/train.py).\n\n### Time to build a model!\n\nSo with that, phase one was done. I had a training script. It was massively overengineered for training this little A-to-A model, but just right for training a small LLM from scratch.\n\nAnd now it was time to do that -- and that's what I'll cover in the next post.\n\n[Here's a link to the next (and final!) post in this series](https://www.gilesthomas.com/2026/07/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax).\n\n* * *\n\n1.  If you're thinking \"why not just have it return one-hot vectors based on the input tokens\", remember that I needed *something* in the model to train, so that I could confirm that loss was going down. A pure \"identity\" model without the embedding space would have nothing to learn, so wouldn't be able to provide that. [↩](#fnref-1)\n    \n2.  It was a surprisingly large source of tech support queries on PythonAnywhere. Someone would train a model with (say) Python 3.11.1, and then try to run it on our servers using 3.11.2, and discover that they couldn't load up their checkpoints. This confused them and they wondered if it was something to do with our platform. I even had a quicktext response to send with a rundown on how Pickle works so that I didn't have to keep typing the same explanation. This may have biased me more against Pickle than I should rationally be. [↩](#fnref-2)\n    \n3.  AdamW stores two numbers per parameter to keep track of its optimisation state, so 2x the model size is exactly what you'd expect if both files were in the same format. [↩](#fnref-3)",
  "imageCount": 6,
  "wordCount": 9394,
  "readingMinutes": 43,
  "syncedAt": "2026-08-11T08:02:06.494Z",
  "license": "CC BY 4.0",
  "author": "Giles Thomas",
  "titleZh": "从零开始编写 LLM，第 34a 篇——为 LLM 训练运行构建 JAX 训练循环",
  "summaryZh": "我准备在 JAX 中从零开始构建 LLM，但想先让训练循环跑起来，再逐步把它组装起来。",
  "markdownBilingual": "For over a year, I've been using [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\" -- and the multitude of side-projects that have branched out from reading it -- as something like a curriculum for learning about modern AI. The one final task I had set myself was to build and train an LLM from scratch just using my notes -- no reference to the book, no reference to the model code I'd written following the book. <span class=\"translation\">一年多以来，我一直把 [Sebastian Raschka](https://sebastianraschka.com/) 的书《[从零开始构建大型语言模型](https://www.manning.com/books/build-a-large-language-model-from-scratch)》以及阅读它衍生出的许多支线项目，当作学习现代 AI 的一套课程。给自己安排的最后一项任务，是只用自己的笔记从零开始构建并训练一个 LLM——不参考书，也不参考我跟着书写出的模型代码。</span>\n\nAs an output, I wanted something as good as my best PyTorch model based on Raschka's code -- a base model, trained on 3.2B tokens, that my (admittedly limited) evals ranked as being close to the original GPT-2 small's quality. <span class=\"translation\">我希望最终得到的东西能和自己最好的 PyTorch 模型一样好：这是一个在 32 亿个词元上训练的基础模型，而我（不得不承认相当有限的）评估认为它的质量接近原始 GPT-2 small。</span>\n\nI wanted to use a different framework, just to make sure I wasn't parroting code that I'd somehow memorised, so I [asked people on Twitter](https://x.com/gpjt/status/1985434030880293004) which one I should use, and the winner was [JAX](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax). <span class=\"translation\">为了确认自己不是在复述某些不知不觉记住的代码，我想换一个框架，于是[在 Twitter 上询问大家应该用哪个框架](https://x.com/gpjt/status/1985434030880293004)，最终胜出的是 [JAX](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax)。</span>\n\nI took a slightly different route to Raschka's book; he takes an inside-out perspective, explaining things like attention, gradually building up a complete GPT-2-style model, and then building a training loop on top of it. I wanted to go outside-in: I'd put together a training harness to train the simplest-possible model with an API similar to a real LLM, get that working to my satisfaction, and then add features to that simple model, one by one, until it had the full architecture in place. The plan (which actually worked out nicely!) was that I'd be able to show how each change improved things. <span class=\"translation\">我采取了和 Raschka 的书稍有不同的路线：他从内向外，先解释注意力等概念，逐步构建完整的 GPT-2 风格模型，然后在上面构建训练循环。我想从外向内：先搭一个训练框架，用接近真实 LLM 的 API 训练一个尽可能简单的模型，把它做到令自己满意，然后逐项为这个简单模型添加功能，直到完整架构就位。这个计划（实际效果很好！）是希望能展示每次改动如何改善结果。</span>\n\nThat's all done now, and I'm posting about it in two parts; in this one, I'll explain how I built the training harness, and in the next, I'll show the actual building and training of the LLM. <span class=\"translation\">现在这些都完成了，我准备分两篇文章介绍；这一篇解释我如何构建训练框架，下一篇展示 LLM 的实际构建和训练。</span>\n\nSo let's get started! <span class=\"translation\">那么，开始吧！</span>\n\n### Which framework on top of JAX? <span class=\"translation\">JAX 之上的框架选哪个？</span>\n\nJAX itself has a relatively minimal API, and doesn't include standard neural network components like linear layers. Likewise it doesn't have any built-in optimisers, data loaders or similar ML utilities. <span class=\"translation\">JAX 自身的 API 相对精简，并不包含线性层之类的标准神经网络组件。同样，它也没有内置优化器、数据加载器或类似的机器学习工具。</span>\n\nNow, I could have decided to build my LLM using just pure JAX, like [I previously did with a toy XOR model](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax). But I felt that it would be better to build this in the style that real-world JAX code is written, which would mean using some of the [many utility libraries](https://docs.jax.dev/en/latest/#ecosystem). <span class=\"translation\">我本可以像[之前用玩具 XOR 模型](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax)那样，只用纯 JAX 构建 LLM。但我觉得，按照现实世界中 JAX 代码的写法来构建会更好，这意味着要使用[许多实用库](https://docs.jax.dev/en/latest/#ecosystem)中的一些。</span>\n\nOn the JAX site itself, there was a useful-looking link: \"If you’re looking to use JAX to train neural networks, check out the [JAX AI Stack](https://docs.jaxstack.ai/en/latest/getting_started.html)!\" On the linked page, it made it clear that the two core parts of that stack were: <span class=\"translation\">JAX 官网有一个看起来很有用的链接：“如果你想用 JAX 训练神经网络，看看 [JAX AI Stack](https://docs.jaxstack.ai/en/latest/getting_started.html)！”在那个链接的页面上，它明确说明该技术栈的两个核心部分是：</span>\n\n-   [Flax NNX](https://flax.readthedocs.io/en/stable/) for neural network components. <span class=\"translation\">[Flax NNX](https://flax.readthedocs.io/en/stable/)：神经网络组件。</span>\n-   [Optax](https://optax.readthedocs.io/en/latest/) for optimisation. <span class=\"translation\">[Optax](https://optax.readthedocs.io/en/latest/)：优化。</span>\n\nI took a look at both, and they seemed pretty easy to grasp. Indeed, at first glance, I felt that NNX looked pretty PyTorch-like! In their tutorial example, the only real obvious difference was the JAX-y derivative-style gradient calculation and the way that random numbers were handled. And even the random numbers were handled in a less pure-functional way than pure JAX -- instead of having to mess around with splitting keys, you could just pass in what appeared to be a stateful variable that somehow split itself internally as needed. <span class=\"translation\">我分别看了看，感觉它们都挺容易掌握。事实上，第一眼看上去，NNX 很像 PyTorch！在它们的教程示例中，唯一明显的差异是 JAX 风格的导数式梯度计算方式，以及随机数的处理方式。甚至随机数的处理也没有纯 JAX 那么纯函数式——不必折腾拆分 key，只要传入一个看起来有状态的变量，它似乎会在需要时自行在内部拆分。</span>\n\nSo, NNX and Optax were the frameworks I'd use. Rather than grinding through the tutorials, I decided that I'd just dive right in, and try to pick things up as I went along. <span class=\"translation\">所以，NNX 和 Optax 就是我打算使用的框架。我没有一点点啃教程，而是决定直接开始，在实践过程中逐渐摸索。</span>\n\nHow hard could it be...? <span class=\"translation\">这能有多难呢……</span>\n\n### The A-to-A language model and the training loop <span class=\"translation\">A 到 A 语言模型与训练循环</span>\n\nTo build a functioning training loop, I needed a minimal model to train -- not an actual LLM, but something that behaved at least a bit like one. It would take in a sequence of tokens, and spit out logits for each token. <span class=\"translation\">为了构建一个能工作的训练循环，我需要一个可供训练的最小模型——不是实际的 LLM，但至少行为要有一点像 LLM。它接收一串词元，并为每个词元输出 logits。</span>\n\nIn my preferred model of [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work), at the top level for a model, we feed in a sequence of token IDs, then: <span class=\"translation\">在我偏好的[LLM 工作方式模型](https://www.gilesthomas.com/2025/09/how-do-llms-work)中，模型的顶层流程是先输入一串词元 ID，然后：</span>\n\n-   Firstly, we convert them into embeddings, so we get a series of vectors. We do this by a lookup into a table, but we can see it conceptually as a projection via a matrix, from vocab space (where a particular token ID is a one-hot vector) to an embedding space. <span class=\"translation\">首先，把它们转换成嵌入，得到一系列向量。具体做法是查表，但从概念上可以把它看成通过矩阵从词汇表空间（某个词元 ID 是 one-hot 向量）投影到嵌入空间。</span>\n-   Next, we do the magic with our Transformers layers, getting embeddings for the next token. The embedding at position n in the output sequence, after these layers, is for the predicted token to come after the token at position n in the input sequence, considering that input token and all other tokens to its left. <span class=\"translation\">接下来，在 Transformer 层中施展魔法，得到下一个词元的嵌入。经过这些层后，输出序列位置 n 的嵌入表示：在考虑输入位置 n 的词元以及它左边所有词元的情况下，预测应该出现在输入位置 n 词元之后的词元。</span>\n-   Finally, we project those back from embedding space to logits, this time actually using a real matrix (in the form of a linear layer). The logits (after being run through softmax) represent the probabilities for each token of it being the next one. <span class=\"translation\">最后，把这些嵌入从嵌入空间投影回 logits，这次真正使用矩阵（以线性层的形式）。logits 经过 softmax 后，就表示每个词元成为下一个词元的概率。</span>\n\nAll of that suggested to me that the dumbest \"LLM\" I could write just to get started would be one that just projected token IDs into embedding space, and then projected back to vocab space. No Transformer layers at all. <span class=\"translation\">这些内容让我想到，刚开始时能写出的最笨“LLM”，就是把词元 ID 投影到嵌入空间，再投影回词汇表空间的模型。完全不需要 Transformer 层。</span>\n\nI'd then train it so that instead of trying to predict the next token, it would try to \"predict\" what was fed into it in the first place. In other words, you'd feed the training loop this input: <span class=\"translation\">然后训练它：不去预测下一个词元，而是“预测”最开始输入给它的内容。换句话说，给训练循环的输入是：</span>\n\n`The fat cat sat on the mat`\n\n...and this target <span class=\"translation\">……目标也是：</span>\n\n`The fat cat sat on the mat`\n\n...rather than the normal setup for an LLM, where you feed it <span class=\"translation\">……而不是 LLM 的常规设置：输入：</span>\n\n`The fat cat sat on the`\n\n...and give it targets of <span class=\"translation\">……目标则是：</span>\n\n`fat cat sat on the mat`\n\nIf I could get that to work -- and it felt like the kind of thing where you'd be able to get the loss down to near-zero without a huge amount of training -- then I could be reasonably sure that I had a working training loop. [1](#fn-1) <span class=\"translation\">如果这能跑通——而且感觉这种任务不需要太多训练就能把损失降到接近零——那我就可以比较有把握地确认训练循环是工作的。[1](#fn-1)</span>\n\nI decided to call this an A-to-A model. Coding up the model itself was ridiculously simple: it looked like this: <span class=\"translation\">我决定把它叫作 A 到 A 模型。模型本身的代码极其简单，大致如下：</span>\n\n`from flax import nnx  class GPTModel(nnx.Module):      def __init__(         self,         vocab_size, context_length,         emb_dim,         n_heads, n_layers,         qkv_bias,         drop_rate,         rngs,     ):         self.token_embedding = nnx.Embed(             num_embeddings=vocab_size,             features=emb_dim,             rngs=rngs,         )          self.output_head = nnx.Linear(             in_features=emb_dim,             out_features=vocab_size,             use_bias=False,             rngs=rngs,         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          return self.output_head(input_embeddings)`\n\nThere's as much boilerplate in there -- for the parameters that I knew that the model would need when I built out the full LLM -- as there is actual code doing stuff! <span class=\"translation\">其中真正做事的代码，和那些我知道完整 LLM 最终会需要的参数所对应的样板代码，几乎一样多！</span>\n\nBut the training loop was a bit more fun. <span class=\"translation\">不过训练循环就有趣多了。</span>\n\n### Porting over the basics <span class=\"translation\">移植基础部分</span>\n\nAs I said, my plan here was to make sure my understanding of the internals of LLMs was correct by rebuilding one just from my notes. That \"notes only\" restriction didn't apply to the training loop itself, so I allowed myself to crib a bit from the PyTorch [DistributedDataParallel code](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main) that I'd been using to train the original model in the cloud. <span class=\"translation\">正如我所说，我的计划是只根据笔记重建一个 LLM，以确认自己对其内部原理的理解是否正确。“只能用笔记”的限制并不适用于训练循环本身，所以我允许自己从曾经用于在云端训练原始模型的 PyTorch [DistributedDataParallel 代码](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main)中借鉴一点。</span>\n\nThe first version that I used is [here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py). Let's start at the bottom, where we have [the `main` function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L393). <span class=\"translation\">我使用的第一个版本[在这里](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py)。我们从底部开始，看一下[`main` 函数](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L393)。</span>\n\nIt starts with some boilerplate to handle the concept of \"runs\". This is a pattern I've found myself using in most of my projects. When working on a model, it's useful to be able to do multiple training runs, changing things each time. You want to keep the checkpoints, metadata and training charts for each one for future reference. <span class=\"translation\">它先用一些样板代码处理“运行”（run）的概念。这是我在大多数项目中都会使用的模式。开发模型时，能够进行多次训练运行、每次修改一些设置非常有用。你会希望为每次运行保留检查点、元数据和训练图表，以便日后参考。</span>\n\nSo in my repo, I'll have a \"runs\" directory, and in there subdirectories for each training run I want to track. <span class=\"translation\">所以在我的代码仓库中会有一个 `runs` 目录，里面再为每个想要跟踪的训练运行建立子目录。</span>\n\nIn those subdirectories, there are JSON files -- one to configure the model, [`model.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/model.json), and one to configure the training hyperparameters and similar stuff, [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json). (It's worth noting that at this stage, a bunch of those hyperparameters were unused; I kept them in there out of laziness, as I knew I'd need them later.) So we start our `main` function by loading those. <span class=\"translation\">这些子目录中有 JSON 文件：一个用于配置模型，即 [`model.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/model.json)；另一个用于配置训练超参数等内容，即 [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json)。（值得一提的是，在这个阶段其中许多超参数还没用到；我只是因为懒而保留它们，因为知道之后会需要。）因此，`main` 函数首先加载这些文件。</span>\n\nOur next step is to completely ignore one of the training hyperparameters, `gradient_accumulation_steps`. I definitely wanted to do [gradient accumulation](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation), but decided to leave it for later. Better to get a solid, simpler training run done first, I felt. <span class=\"translation\">下一步是完全忽略一个训练超参数 `gradient_accumulation_steps`。我确实想实现[梯度累积](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation)，但决定稍后再做。我觉得先完成一次扎实而简单的训练运行更好。</span>\n\nNext, we download the dataset we're going to use to our local disk with `download_dataset` (which will only download if there's not an up-to-date copy already there). <span class=\"translation\">接下来，我们用 `download_dataset` 把要使用的数据集下载到本地磁盘（只有在本地没有最新副本时才会下载）。</span>\n\nThe next step is to call `load_dataset` to load it into RAM. You can see that there's another hard-coded variable there, `world_size`. This is a holdover from the multi-GPU DistributedDataParallel code that this was all based on; in this blog post I'm only covering the code for single-GPU training, but I decided to leave the DDP stuff in there for dataset-wrangling purposes, hardcoded to one GPU, so that it would be easier to re-introduce if I later decide to implement something similar in JAX. <span class=\"translation\">下一步调用 `load_dataset` 将数据加载到 RAM。你会看到另一个硬编码变量 `world_size`。它是这份代码所基于的多 GPU DistributedDataParallel 代码遗留下来的；本文只介绍单 GPU 训练，但我决定为了数据整理保留 DDP 部分，并硬编码为一块 GPU，这样以后如果决定在 JAX 中实现类似功能，就更容易重新引入。</span>\n\nLet's take a look at `load_dataset` and its related stuff. If you go up to [line 39](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L39) you'll see the code. Firstly, there's a `BigTrainDataset` that keeps track of our training data. If you look closely, you might spot one oddity in that class. We have this: <span class=\"translation\">我们看看 `load_dataset` 及其相关代码。跳到[第 39 行](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L39)就能看到。首先，有一个 `BigTrainDataset` 用来跟踪训练数据。仔细看，你可能会发现这个类里有一个奇怪之处。这里是：</span>\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)`\n\nRemember that at this stage, the plan was to train the model to map tokens to themselves rather than to make next-token predictions. So the targets are the same as the inputs, not the more normal next token, which would look like (and, in the next post, will look like) this: <span class=\"translation\">记住，在这个阶段，计划是训练模型把词元映射到自身，而不是做下一个词元预测。因此目标和输入相同，不是更常见的下一个词元；后者看起来（下一篇文章中也会看起来）是：</span>\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[1:].reshape(-1, microbatch_size, seq_length)`\n\nNext, we have a `load_dataset` function to load the appropriate subset of the data from the copy on the local disk into one of those `BigTrainDataset` objects. I hit an out-of-memory issue when I ran the first version of this. It was trying to load the data into my GPU's VRAM -- JAX's default behaviour if you have a GPU, and the CUDA version of JAX is installed -- and there was too much to fit in there. After a bit of digging around [I learned how to change the JAX default device](https://www.gilesthomas.com/2026/06/jax-backends-and-devices) so that it would be loaded into normal system RAM. <span class=\"translation\">接下来有一个 `load_dataset` 函数，把本地磁盘副本中合适的数据子集加载到一个 `BigTrainDataset` 对象中。我第一次运行时遇到了内存不足问题。它试图把数据加载到 GPU 的 VRAM 中——如果有 GPU 且安装了 CUDA 版 JAX，这是 JAX 的默认行为——但数据太多放不下。经过一番查找，[我学会了如何修改 JAX 的默认设备](https://www.gilesthomas.com/2026/06/jax-backends-and-devices)，让数据加载到普通系统 RAM 中。</span>\n\nUnfortunately, once I'd done that, I found that iterating through it was super-slow -- it took about 1.2 seconds to get one training batch of 6,144 tokens out of the array, which meant that I'd have a limit of 5,120 tokens/second of training from that alone. I eventually learned that the data had been loaded into the main RAM, but was being copied up to the GPU for processing because it had not been *committed* to the main RAM -- [details here](https://www.gilesthomas.com/2026/06/jax-commitment-issues). Fixing that (with an explicit call to `jax.device_put`) meant that getting a single training batch from the dataset and putting it onto the GPU took less than 0.001s, which was much better. <span class=\"translation\">不幸的是，完成这一步后我发现遍历数据极其缓慢——从数组中取出一个包含 6,144 个词元的训练批次要花约 1.2 秒，这意味着仅这一项就把训练速度限制在每秒 5,120 个词元。我最终了解到，数据虽然加载到了主 RAM，却因为没有被*提交*到主 RAM，而是在处理时被复制到 GPU；[详情见这里](https://www.gilesthomas.com/2026/06/jax-commitment-issues)。修复这一点（显式调用 `jax.device_put`）后，从数据集中取出一个训练批次并放到 GPU 上只需不到 0.001 秒，快多了。</span>\n\nSo that was many hours of work that all got packed into lines 55 to 58 of the code: <span class=\"translation\">于是，之前耗费数小时的工作，全都浓缩在代码第 55 到 58 行：</span>\n\n    `cpu0 = jax.devices(\"cpu\")[0]     with jax.default_device(cpu0):         full_dataset = load_file(dataset_dir / f\"{split}.safetensors\")[\"tokens\"]     full_dataset = jax.device_put(full_dataset, cpu0)`\n\nThe remainder of the logic in `load_dataset` is just to make sure that we have a dataset that is exactly the right size for the world size (even though that's always one right now), the microbatch size, the gradient accumulation steps, and the sequence length that we're working with, <span class=\"translation\">`load_dataset` 的其余逻辑只是确保数据集大小恰好适合 world size（虽然现在始终是 1）、微批大小、梯度累积步数以及当前使用的序列长度。</span>\n\nLet's [go back to the `main` function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L433) again. Having loaded our dataset, we create our model, passing in the model configuration stuff and also the (currently unused) dropout rate training hyperparameter, then we create a Flax NNX optimiser which wraps an Optax one. This was essentially a copy/paste from the Flax tutorial, except we're configuring the optimiser with learning rate and weight decay hyperparameters from the training config: <span class=\"translation\">让我们再次[回到 `main` 函数](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L433)。加载数据集后，我们创建模型，传入模型配置以及（目前未使用的）dropout 率训练超参数，然后创建一个封装 Optax 优化器的 Flax NNX 优化器。这基本是从 Flax 教程复制粘贴的，只是我们用训练配置中的学习率和权重衰减超参数来设置优化器：</span>\n\n    `optimizer = nnx.Optimizer(         model,         optax.adamw(             learning_rate=train_conf[\"learning_rate\"],             weight_decay=train_conf[\"weight_decay\"],         ),         wrt=nnx.Param     )`\n\nFinally, we call `train` to kick off our training loop, passing in some appropriate stuff. Let's [go to that function](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L307) next. <span class=\"translation\">最后调用 `train` 启动训练循环，传入一些合适的参数。接下来[转到这个函数](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L307)。</span>\n\nWe start off with a bit of housekeeping, then go into the main loop. You can see that it's kind of gesturing at gradient accumulation: <span class=\"translation\">我们先做一些整理工作，然后进入主循环。你可以看到它多少有点在暗示梯度累积：</span>\n\n    `for global_step in progress_bar:         for accumulation_step in range(gradient_accumulation_steps):`\n\n...but if you look at the actual body of that loop, it's not doing anything of the sort. It's just getting training batches, putting them on the GPU, doing a full training step, and keeping track of some metrics: <span class=\"translation\">……但如果看看循环的实际主体，会发现它完全没有做梯度累积。它只是获取训练批次、将它们放到 GPU 上、执行完整训练步骤，并记录一些指标：</span>\n\n            `inputs, targets = train_dataset[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]             inputs = jax.device_put(inputs, model_device)             targets = jax.device_put(targets, model_device)              train_loss = train_step(model, optimizer, inputs, targets)             train_losses.append(train_loss.item())              microbatch_size, sequence_length = inputs.shape             tokens_seen_this_rank += microbatch_size * sequence_length`\n\nSo, we're just doing a traditional batch-by-batch training loop without gradient accumulation right now. But some of the infrastructure is there, because it was the next thing I wanted to add after I'd got the basic loop working. <span class=\"translation\">所以此时我们只是进行传统的逐批次训练循环，没有梯度累积。但基础设施已经在那里了，因为这是我让基础循环跑起来之后想添加的下一项功能。</span>\n\nThe rest of the `train` function is just housekeeping and checkpointing; we'll come back to the checkpointing shortly, but first let's take a look at the `train_step` function that actually trains the model on a set of inputs and targets, and its associated `calculate_loss` function -- they're [just above `train`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L292). <span class=\"translation\">`train` 函数的其余部分只是整理和保存检查点；我们稍后会回到检查点，但首先看看真正用一组输入和目标训练模型的 `train_step` 函数，以及相关的 `calculate_loss` 函数——它们就在 [`train` 的上方](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/train.py#L292)。</span>\n\nNow, as you might remember from [my first JAX post](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax), the best way to JIT a training loop is at as high a level as possible. So when I first coded this, I integrated that into the traditionally-named `train_step` function like this: <span class=\"translation\">现在，正如你可能还记得[我的第一篇 JAX 文章](https://www.gilesthomas.com/2026/05/on-first-looking-into-jax)所说，JIT 训练循环的最佳方式是尽可能在高层进行。因此我第一次写代码时，把它整合进传统命名的 `train_step` 函数中，如下：</span>\n\n`@jax.jit def train_step(model, optimizer, inputs, targets):     loss, grads = nnx.value_and_grad(calculate_loss)(model, inputs, targets)     optimizer.update(model, grads)     return loss`\n\nWhen I actually came around to run it the first time, loss wasn't falling at all, and after banging my head against it for a while, I [realised I should have used `nnx.jit` rather than `jax.jit`](https://www.gilesthomas.com/2026/06/hashing-jax-parameters), fixed that, and kicked it off again. Loss started falling immediately. D'oh! <span class=\"translation\">我第一次真正运行时，损失完全没有下降。苦苦折腾了一阵后，我[意识到应该使用 `nnx.jit` 而不是 `jax.jit`](https://www.gilesthomas.com/2026/06/hashing-jax-parameters)，修正后再次启动。损失立刻开始下降。哎呀！</span>\n\nNow let's take a look at loss. [Cross entropy loss](https://www.gilesthomas.com/2025/10/llm-from-scratch-20-starting-training-cross-entropy-loss) was clearly what I would need to train an LLM, and also felt like the right thing for the A-to-A model. <span class=\"translation\">现在来看看损失。[交叉熵损失](https://www.gilesthomas.com/2025/10/llm-from-scratch-20-starting-training-cross-entropy-loss)显然是训练 LLM 所需的东西，对 A 到 A 模型来说也很合适。</span>\n\nOptax has five loss functions that are related to cross entropy; three of them looked a bit more complicated than I needed: <span class=\"translation\">Optax 有五个与交叉熵相关的损失函数；其中三个看起来比我需要的更复杂：</span>\n\n-   `poly_loss_cross_entropy`,\n-   `safe_softmax_cross_entropy`, and\n-   `sigmoid_binary_cross_entropy`.\n\nSo it was a choice between <span class=\"translation\">所以要在下面两个之间做选择：</span>\n\n-   [`softmax_cross_entropy`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy.html#optax.losses.softmax_cross_entropy) and <span class=\"translation\">[`softmax_cross_entropy`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy.html#optax.losses.softmax_cross_entropy)；</span>\n-   [`softmax_cross_entropy_with_integer_labels`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels). <span class=\"translation\">[`softmax_cross_entropy_with_integer_labels`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels)。</span>\n\nThe latter was the right one -- `softmax_cross_entropy` expects the labels (that is, the target token IDs) to be one-hot vectors, while `softmax_cross_entropy_with_integer_labels`, as it says in the function name, expects integer labels, which is what we have. <span class=\"translation\">后者才是正确选择——`softmax_cross_entropy` 要求标签（也就是目标词元 ID）是 one-hot 向量，而顾名思义，`softmax_cross_entropy_with_integer_labels` 要求整数标签，这正是我们拥有的形式。</span>\n\nThat sounded pretty similar to PyTorch's [`cross_entropy`](https://docs.pytorch.org/docs/2.12/generated/torch.nn.functional.cross_entropy.html), but there was an important difference. For normal use (if you're not using K-dimensional loss, whatever that might be) PyTorch expects that the inputs are either just a one-dimensional tensor of c logits, or at worst a bxc matrix, where b is the batch size. <span class=\"translation\">这听起来和 PyTorch 的 [`cross_entropy`](https://docs.pytorch.org/docs/2.12/generated/torch.nn.functional.cross_entropy.html) 很相似，但有一个重要差异。在正常使用场景下（如果你没有使用 K 维损失，不管那是什么），PyTorch 要求输入要么是包含 c 个 logits 的一维张量，要么最多是 b×c 矩阵，其中 b 是批大小。</span>\n\nI had noted when working through this section of Raschka's book that the code we wrote flattened things out. So a batch of six sequences, each 1,024 tokens long, with a vocab size of 50,257, would give us a logits tensor shaped like this: <span class=\"translation\">我在读 Raschka 书中这一节时注意到，我们写的代码会把内容展平。因此，六条序列组成一个批次，每条长度为 1,024，词汇表大小为 50,257 时，会得到如下形状的 logits 张量：</span>\n\n`(6, 1024, 50257)`\n\nThe first axis is the batches, the second is the length of the sequences -- remember, we have logits for every input token in the sequence, with next-token predictions for that token in the context of all of the other ones to its left. And the last axis, with a size equal to our tokeniser's vocabulary size, is the logits themselves. <span class=\"translation\">第一个轴是批次，第二个是序列长度——记住，我们为序列中的每个输入词元都有 logits，并且预测的是在左侧所有词元上下文中该词元之后的下一个词元。最后一个轴的大小等于分词器词汇表大小，它们就是 logits。</span>\n\nAfter flattening, it looked like a \"batch\" of 6\\*1024\\=6144 logits vectors: <span class=\"translation\">展平后，它看起来像一个包含 6×1024=6144 个 logits 向量的“批次”：</span>\n\n`(6144, 50257)`\n\nLikewise our targets -- the token IDs we wanted our model to be predicting -- were batched, and there was one per token in each sequence, so that tensor was <span class=\"translation\">同样，目标（我们希望模型预测的词元 ID）也按批次组织，每条序列的每个词元有一个目标，因此张量是：</span>\n\n`(6, 1024)`\n\nFlattened, it looked like a \"batch\" of 6\\*1024\\=6144 targets: <span class=\"translation\">展平后，它看起来像一个包含 6×1024=6144 个目标的“批次”：</span>\n\n`(6144,)`\n\nFinally, the PyTorch function returned a scalar value -- wrapped in a PyTorch `Tensor` object, of course, so that it could participate in the backward pass, but a single number. <span class=\"translation\">最后，PyTorch 函数返回一个标量值——当然它包在 PyTorch `Tensor` 对象中，以便参与反向传播，但本质上就是一个数字。</span>\n\nBut I'd forgotten about all of that when I was writing this part of the JAX code, and just fed the inputs and the targets straight in to the JAX function. The result was interesting. I started with this: <span class=\"translation\">但我写这部分 JAX 代码时忘了这一切，直接把输入和目标传给了 JAX 函数。结果很有意思。我从下面开始：</span>\n\n     `loss = optax.losses.softmax_cross_entropy_with_integer_labels(          logits, targets      )`\n\nAnd printing out the shapes of each variable gave this: <span class=\"translation\">打印每个变量的形状后得到：</span>\n\n`logits.shape=(6, 1024, 50257) targets.shape=(6, 1024) loss.shape=(6, 1024)`\n\nIt had returned a cross entropy number for every element in every sequence, across all of the batches! <span class=\"translation\">它为所有批次中每条序列的每个元素都返回了一个交叉熵数值！</span>\n\nWhat's interesting is that the [docs for `softmax_cross_entropy_with_integer_labels`](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels) imply that it has the same restrictions as PyTorch's -- it expects a single batch axis in the tensors that are passed in. Perhaps they're out of date? Or perhaps Optax just assumes that you know that in JAX \"a batch axis\" should be read as \"as many batch axes as you want\"? Well, anyway -- it worked, and I checked that the numbers were solid. <span class=\"translation\">有意思的是，[`softmax_cross_entropy_with_integer_labels` 的文档](https://optax.readthedocs.io/en/latest/api/generated/optax.losses.softmax_cross_entropy_with_integer_labels.html#optax.losses.softmax_cross_entropy_with_integer_labels)暗示它和 PyTorch 有相同限制——传入的张量应该只有一个批次轴。也许文档已经过时？又或者 Optax 默认你懂得：在 JAX 中，“一个批次轴”应该理解为“想要多少个批次轴都行”？无论如何，它确实工作了，我也检查过数值是可靠的。</span>\n\nNow, of course, we can't ask JAX for gradients using that 6×1024 matrix -- the loss function needs to return a scalar -- but the `mean` function on a JAX array does exactly what we need. So I had a solid loss calculation, which you can see in `calculate_loss`: <span class=\"translation\">当然，我们不能使用那个 6×1024 矩阵向 JAX 请求梯度——损失函数必须返回标量——但 JAX 数组的 `mean` 函数正好满足需求。于是我得到了可靠的损失计算，代码在 `calculate_loss` 中：</span>\n\n`def calculate_loss(model, inputs, targets):     logits = model(inputs)     loss = optax.losses.softmax_cross_entropy_with_integer_labels(         logits, targets     ).mean()     return loss`\n\nSo that's covered our loss function and the JITted `train_step` that uses it. <span class=\"translation\">这样，损失函数和使用它的 JIT 化 `train_step` 就都讲完了。</span>\n\nThe only remaining code that I haven't gone over in this version of the `train.py` script is the stuff immediately above `calculate_loss` -- `get_training_data` and `generate_training_charts`. These are both called as part of the housekeeping code I glossed over in the `train` function, after we take checkpoints. <span class=\"translation\">这个版本 `train.py` 脚本中我还没讲的代码，只剩下 `calculate_loss` 正上方的 `get_training_data` 和 `generate_training_charts`。它们会在 `train` 函数中略过的整理逻辑里、保存检查点之后被调用。</span>\n\nThey just redraw a plot of the loss and other training metrics, using stuff that's stored in the metadata of all of the checkpoints so far. That means that there's a nice graphical way to keep track of a training run. Fairly dull stuff, so there's no need to go through them, but it is worth taking a look at the checkpointing code itself. <span class=\"translation\">它们只是利用目前所有检查点元数据中保存的内容，重新绘制损失和其他训练指标的图表。这样就能用图形方式跟踪训练运行。内容相当枯燥，所以没必要逐一讲解，但检查点代码本身值得看一看。</span>\n\nYou can see the version I was working with at this point [here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py). It's not really much of a checkpoint; I was saving the model itself and the metadata needed for that charting code, but not the optimiser, which would be needed for a real checkpoint. After all, the purpose of a checkpoint is to be able to pick things up again if your training loop crashes, and you can't do that without the optimiser's state. Still, it was enough to get started with. <span class=\"translation\">你可以[在这里](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py)看到我当时使用的版本。它还算不上真正的检查点：我保存了模型本身和绘图代码所需的元数据，却没有保存真正的检查点必需的优化器。毕竟，检查点的作用是训练循环崩溃后能够继续，而没有优化器状态就做不到。不过，这已经足以开始了。</span>\n\nThat said, one wrinkle I encountered when writing that simple checkpointing code was that it was a tad tricky to save them in Safetensors format -- [you can see the details here](https://www.gilesthomas.com/2026/06/flax-and-safetensors). <span class=\"translation\">话虽如此，我在编写这套简单检查点代码时遇到的一个小问题是：把它们保存成 Safetensors 格式有点棘手——[详情在这里](https://www.gilesthomas.com/2026/06/flax-and-safetensors)。</span>\n\nSo, that was my initial training code. It was time to let it rip: could I train my dumb \"LLM\" to map from A to A? <span class=\"translation\">这就是我最初的训练代码。是时候放手一试了：我能让这个笨拙的“LLM”把 A 映射到 A 吗？</span>\n\n### The first A-to-A run <span class=\"translation\">第一次 A 到 A 运行</span>\n\nAs I mentioned earlier, the very first run didn't converge at all -- loss started at about 10.82, which was promising (it's exactly what you'd expect for a randomly-initialised network trying to predict GPT-2 tokens -- see [here](https://www.gilesthomas.com/2026/06/hashing-jax-parameters) for details), but then it remained there. <span class=\"translation\">正如前面提到的，第一次运行完全没有收敛——损失从约 10.82 开始，这很有希望（对于一个试图预测 GPT-2 词元的随机初始化网络来说，这正是预期值；[详情见这里](https://www.gilesthomas.com/2026/06/hashing-jax-parameters)），但随后一直停在那里。</span>\n\nBut when I fixed the \"`jax.jit` should be `nnx.jit`\" issue, it started dropping. After 92,160,000 tokens seen, it seemed to have hit zero (at least to the three DPs I was printing), so I baked that into [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json) and did another training run fixed to that number of tokens. After about 14 minutes, it finished: <span class=\"translation\">但修复“应该使用 `nnx.jit` 而不是 `jax.jit`”的问题后，损失开始下降。看到 92,160,000 个词元后，它似乎已经降到了零（至少按我打印的三位小数是这样），所以我把这个数写入 [`train.json`](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/runs/a-to-a/train.json)，又进行了一次固定训练，训练到这么多词元为止。大约 14 分钟后，它完成了：</span>\n\n`Training complete in 843.547 seconds Tokens seen: 92,160,000 Throughput: 109,253 tokens/second Final train loss: 0.000 2026-06-17 19:29:31.667194 Done`\n\nA very promising final loss, even though that was just whatever we got on the last batch! The actual loss chart looked like this: <span class=\"translation\">最终损失非常有希望，尽管那只是最后一个批次得到的值！实际的损失图如下：</span>\n\n![A-to-A loss chart](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/a-to-a-loss-chart.png \"A-to-A loss chart\")\n\nIf you're used to the loss charts in my previous posts, there's something to highlight here: I've switched the Y axis over to being log, so those bumps near the end are actually tiny deviations away from 0.001. <span class=\"translation\">如果你熟悉我之前文章里的损失图，这里有一点值得注意：我把 Y 轴改成了对数轴，所以末尾那些起伏其实只是偏离 0.001 的微小变化。</span>\n\nI think it's worth showing what the model actually did at this point. It was actually somewhat later that I wrote some code to load up the model checkpoints from these training runs and do some smoke tests, but I'll show you some results now. <span class=\"translation\">我觉得值得展示一下模型在此时实际做了什么。其实是在后来我才写代码加载这些训练运行的模型检查点并进行冒烟测试，但现在先展示结果。</span>\n\nI wrote some code based on [my JAX safetensors post](https://www.gilesthomas.com/2026/06/flax-and-safetensors) to load up a model's parameters from a checkpoint's `model.safetensors` file: <span class=\"translation\">我根据[之前那篇 JAX Safetensors 文章](https://www.gilesthomas.com/2026/06/flax-and-safetensors)写了代码，从检查点的 `model.safetensors` 文件加载模型参数：</span>\n\n`def load_model(model, file):     model_state_simple_dict = load_file(file)     dict_flat_state = {}     for key, array in model_state_simple_dict.items():         elements = key.split(\".\")         list_key = []         for element in elements:             try:                 list_key.append(int(element))             except ValueError:                 list_key.append(element)         dict_flat_state[tuple(list_key)] = array      new_flat_state = nnx.from_flat_state(dict_flat_state)     nnx.update(model, new_flat_state)`\n\n...and then wrote two test scripts. <span class=\"translation\">然后又写了两个测试脚本。</span>\n\nFirstly, was it really mapping from A to A? I wanted to be sure that the loss number was actually reflecting what I wanted it to reflect. I wrote [a simple script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_a_to_a.py) that took a Safetensors file on the command line, and ran the first verse of *The Rime of the Ancient Mariner* (chosen because it uses oldish English so there are some odd tokens in it) through the LLM it loaded from that file. <span class=\"translation\">首先，它真的在做 A 到 A 的映射吗？我想确认损失数值确实反映了我想让它反映的东西。我写了[一个简单脚本](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_a_to_a.py)，它从命令行接收一个 Safetensors 文件，然后把《古舟子咏》的第一节（因为其中使用了较古老的英语，会有一些奇怪词元）输入从该文件加载的 LLM。</span>\n\nHere's what the model at the end of the run came up with: <span class=\"translation\">运行结束时的模型生成了下面的结果：</span>\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/best/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---`\n\nThat's great! It could certainly handle the mapping. Out of interest, I decided to see how quickly it had learned to get that right. The average training loss in that \"best\" checkpoint at the end of the training run was 0.0001, so how did the mapping improve, and what was the loss, near the start of the training run? <span class=\"translation\">太棒了！它显然能够完成这种映射。出于好奇，我想看看它多快学会正确完成映射。训练结束时“最佳”检查点的平均训练损失是 0.0001，那么映射是如何改进的，训练刚开始时损失又是多少？</span>\n\nFor the first checkpoint, when we'd just run one batch through, we had an average training loss of 10.8242. With the model parameters that were saved then, we get this output: <span class=\"translation\">在第一个检查点，也就是刚跑完一个批次时，平均训练损失是 10.8242。用当时保存的模型参数，我们得到以下输出：</span>\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/20260617Z185827-iteration-0/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: --- LOADRecommend ptwtacid cheek lunch KaLOAD blondrient Sole Broken engages CrimsplitrelyLOAD Consortium hopefully Fisheries qualardiestern565Financial gallery talked KaLOADhuge admit disappeared SoleERT Heardearth showcasingurancesLOAD ---`\n\nAs you'd expect from that loss, it's total token salad. <span class=\"translation\">从这个损失值来看，完全是一锅词元沙拉，正如预期。</span>\n\nNow let's take a look at the next checkpoint, taken after 375 \"global steps\" -- that is, 6,000 batches. In that one, the average train loss since that first checkpoint was 2.9323. But that hides something important -- the maximum loss, near the start, was (as you would expect) 10.78524, not much less than the average loss in the previous checkpoint. But the minimum (which we can safely assume was towards the end of this checkpointing period) was 0.54155, so we can reasonably assume that the model improved *very* rapidly at this point. And the A-to-A test bears this out: <span class=\"translation\">现在看看下一个检查点，它是在 375 个“全局步”之后取得的，也就是 6,000 个批次。这个检查点自上一个检查点以来的平均训练损失是 2.9323。但这个数字隐藏了一个重要事实——开头附近的最大损失是 10.78524（正如预期，并不比上一个检查点的平均损失低多少）；而最小值是 0.54155（我们可以放心地假设它出现在这个检查点区间的末尾附近），所以可以合理推断模型在这一阶段提升得*非常快*。A 到 A 测试也证明了这一点：</span>\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_a_to_a.py runs/a-to-a/checkpoints/20260617Z185848-iteration-375/model.safetensors Input: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---  Output: ---  It is an ancient Mariner, And he stoppeth one of three. 'By thy long grey beard and glittering eye, Now wherefore stopp'st thou me?  ---`\n\nSo, we can see that the bulk of the improvement happened right at the start! It was able to pass the A-to-A test for that fairly unusual sequence after just 6,001 total batches of 6 1,024-token sequences. <span class=\"translation\">因此可以看到，大部分改进就在最开始发生了！在总共只训练了 6,001 个批次、每批包含 6 条长度为 1,024 的序列之后，它就能通过这个相当不寻常序列的 A 到 A 测试。</span>\n\nThe rest of the training run was perhaps just grinding out improvement on rarer tokens, and perhaps making it more certain about already-correct predictions. After all, the test script was simply printing the most likely token for each position, so at this state it might have been predicting some of those tokens as 51% probability. That would have meant a penalty in the loss function, even if the answer was actually correct. <span class=\"translation\">训练运行剩余部分也许只是继续改善罕见词元，并让模型对已经正确的预测更有把握。毕竟，测试脚本只是打印每个位置概率最高的词元，所以在这个阶段，它可能把其中一些词元预测为 51% 的概率。这样即使答案实际正确，也会在损失函数中受到惩罚。</span>\n\nSo that was an interesting script; I wanted to do another -- the standard smoke test that I've been using, based on Raschka's prompt: how does the model complete \"Every effort moves you\" when asked to continue the sentence? [Here's the script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_generation.py), and here's what it generated: <span class=\"translation\">这个脚本很有意思；我还想再做一个测试——一直使用的标准冒烟测试，基于 Raschka 的提示：让模型继续“Every effort moves you”这句话。[脚本在这里](https://github.com/gpjt/jax-gpt2-from-scratch/blob/62291c4f42dbbd93f105283449f4a94d5665e5d2/test_generation.py)，它生成了：</span>\n\n`giles@perry:~/Dev/jax-gpt2-from-scratch (main)$ uv run test_generation.py runs/a-to-a/checkpoints/best/model.safetensors Every effort moves you you you you you you you you you you you you you you you you you you you you you`\n\nThat makes perfect sense. In order to generate the next token in an autoregressive loop, we're looking at the logits for the last one in the prompt. When it first runs, the last token is \" you\", and our model is trained to map A to A, so its result is \" you\". We append that to the prompt, run it through again, the last token is still \" you\", so of course it \"predicts\" the token \" you\" again. And so on. <span class=\"translation\">这完全说得通。为了在自回归循环中生成下一个词元，我们查看提示末尾词元的 logits。第一次运行时，最后一个词元是“ you”，而我们的模型训练目标是把 A 映射到 A，所以结果是“ you”。我们把它追加到提示中，再次运行；最后一个词元仍然是“ you”，所以它当然又“预测”出“ you”。如此循环。</span>\n\nSo these results were both good news! The A-to-A mapping was working, and was converging rapidly in terms of loss -- and even more rapidly in terms of our poetic test. <span class=\"translation\">所以这两个结果都是好消息！A 到 A 映射正常工作，而且从损失来看收敛很快——从诗歌测试来看甚至更快。</span>\n\nSo, what was next? I wanted the training loop to be as similar as possible to the code I used for my [best locally-trained PyTorch model](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests). That used three things I had not built into the training loop at this stage: learning rate scheduling, gradient clipping, and gradient accumulation. The PyTorch code also had the ability to restart from a checkpoint -- not super-important in a 14-minute training run like this one, but I figured it would become important later. After all, the PyTorch runs on my local machine had taken almost two days, and if something went wrong halfway through (cat jumping onto PC power button, etc) then I really wouldn't want to start from scratch. <span class=\"translation\">那么接下来呢？我希望训练循环尽可能接近自己用于[最佳本地 PyTorch 模型](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests)的代码。在这个阶段，训练循环还没有加入三项功能：学习率调度、梯度裁剪和梯度累积。PyTorch 代码还能够从检查点重启——对于这次 14 分钟的训练并不重要，但我想以后它会变得重要。毕竟，我本地的 PyTorch 运行几乎要两天，如果中途出问题（比如猫跳上电脑按下电源键之类），我肯定不想从头再来。</span>\n\nI decided to handle gradient accumulation first. <span class=\"translation\">我决定先处理梯度累积。</span>\n\n### Gradient accumulation <span class=\"translation\">梯度累积</span>\n\nIn PyTorch, doing [gradient accumulation](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) is pretty simple: the core of a typical training loop without it might look something like this: <span class=\"translation\">在 PyTorch 中，实现[梯度累积](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation)很简单：没有梯度累积时，典型训练循环的核心大概是：</span>\n\n    `optimizer.zero_grad()      result = model(inputs)     loss = loss_function(result, targets)     loss.backward()      optimizer.step()`\n\nWe start off by clearing out any gradients that are stashed on the model's parameters, then do a forward pass, work out the loss, do a backward pass to put new gradients on the parameters, and then step the optimiser to apply those gradients. <span class=\"translation\">我们先清除模型参数上暂存的所有梯度，然后进行前向传播，计算损失，进行反向传播把新梯度放到参数上，最后让优化器执行一步以应用这些梯度。</span>\n\nAccumulating gradients just means changing it to something like this: <span class=\"translation\">梯度累积就是把它改成类似这样：</span>\n\n    `optimizer.zero_grad()      for step in range(gradient_accumulation_steps):         result = model(inputs)         loss = loss_function(result, targets)         (loss / gradient_accumulation_steps).backward()      optimizer.step()`\n\nThat is, we do a forward and a backward pass `gradient_accumulation_steps` times. Because we're not zeroing out existing gradients between them, the parameters will accumulate gradients over time -- each backward pass will add its contribution onto what is already there. Each time, we divide the loss by `gradient_accumulation_steps`, so that the gradients that are put on the parameters are that much smaller, which means that by the end of our loop we've got gradients that are the average of what we'd have got if we'd done all of these microbatches in one big batch. Finally, once we've exited the loop, we step the optimiser to apply those averaged gradients. <span class=\"translation\">也就是说，我们执行 `gradient_accumulation_steps` 次前向和反向传播。由于期间不清除已有梯度，参数会逐步累积梯度——每次反向传播都会把自己的贡献加到已有梯度上。每次都把损失除以 `gradient_accumulation_steps`，这样写入参数的梯度就会相应变小；循环结束时，得到的梯度就是把所有这些微批次放进一个大批次时所得到梯度的平均值。最后退出循环，让优化器执行一步以应用这些平均梯度。</span>\n\nWhen I started thinking about implementing this in JAX, I noticed that [Optax has a help page on how to do it](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html), but then I had one of those brilliant shower thoughts that one sometimes has. I should have learned by my age that they rarely work out well, but this time I decided to give it a go rather than doing things the official way. <span class=\"translation\">开始思考如何在 JAX 中实现时，我注意到[Optax 有一篇介绍如何实现梯度累积的帮助页面](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html)，但随后产生了一个偶尔会有的“淋浴时灵光一现”。我这个年纪本该知道这种想法很少会有好结果，但这次我决定不按官方方式来，试试看。</span>\n\nMy brilliant idea was that with some finessing, we could put the whole gradient accumulation loop inside JITted code. From what I'd learned so far, the higher up in our code we put the JIT decorator -- that is, the more of the training loop it covered -- the faster it would be. In itself, that wasn't a bad idea. <span class=\"translation\">我的绝妙想法是：稍作调整后，可以把整个梯度累积循环放进 JIT 代码。到目前为止我了解到，JIT 装饰器放在代码层次越高的位置——也就是覆盖的训练循环越多——速度就越快。这个想法本身并不坏。</span>\n\nBut my first implementation was less smart: <span class=\"translation\">但我的第一次实现没那么聪明：</span>\n\n`def calculate_loss(model, inputs, targets):     loss = 0     for microbatch_inputs, microbatch_targets in zip(inputs, targets):         logits = model(microbatch_inputs)         loss += optax.losses.softmax_cross_entropy_with_integer_labels(             logits, microbatch_targets         ).mean()     return loss  @nnx.jit def train_step(model, optimizer, inputs, targets):     loss, grads = nnx.value_and_grad(calculate_loss)(model, inputs, targets)     optimizer.update(model, grads)     return loss`\n\nThe `inputs` were full-step arrays (eg. shaped (16, 6, 1024) for 16 gradient-accumulation steps over 6 microbatches of 1024 sequences), and the targets likewise. That seemed very clever! But in retrospect, it was obviously doomed to failure, and when I ran it, I ran out of VRAM. <span class=\"translation\">`inputs` 是完整训练步的数组（例如对 16 个梯度累积步、每步 6 个微批次、每个微批次 1,024 个序列来说，形状为 (16, 6, 1024)），目标数组同样如此。看起来非常巧妙！但事后看显然注定失败；运行时我耗尽了 VRAM。</span>\n\nThe point of gradient accumulation is that what you accumulate over time is, well, gradients. So you have to do a full forward pass and then a backward pass over the model for each microbatch, letting gradients build up, and then apply those in one go, like the PyTorch code did. <span class=\"translation\">梯度累积的要点是，随时间累积的是梯度，顾名思义。因此必须对每个微批次完整地执行一次前向传播和反向传播，让梯度不断累积，然后像 PyTorch 代码一样一次性应用它们。</span>\n\nUnfortunately what I was doing with my code was essentially all of the forward passes, one by one, letting the activations and JAX's internal structures representing what calculations had been done accumulate -- not the gradients -- and then doing a single backward pass across all of that. Mathematically it made sense -- I would have got the right effect if I'd had enough VRAM -- but it wasn't much more memory-efficient than just doing a single batch of `gradient_accumulation_steps * batch_size` sequences. Immediate CUDA OOM. <span class=\"translation\">不幸的是，我的代码实际做的是依次执行所有前向传播，让激活值以及 JAX 内部表示计算过程的数据结构不断累积——累积的不是梯度——然后对全部内容执行一次反向传播。从数学上说这没问题——如果 VRAM 足够，我会得到正确效果——但它的内存效率并不比直接使用 `gradient_accumulation_steps * batch_size` 条序列的单个批次高多少。立刻 CUDA OOM。</span>\n\nMy second attempt was a bit more sensible and ran OK without the JIT: <span class=\"translation\">第二次尝试更合理一些，不使用 JIT 也能运行：</span>\n\n`#@nnx.jit def train_step(model, optimizer, inputs, targets):     loss_list = []     grads_list = []     for microbatch_inputs, microbatch_targets in zip(inputs, targets):         microbatch_loss, microbatch_grads = nnx.value_and_grad(calculate_loss)(model, microbatch_inputs, microbatch_targets)         loss_list.append(microbatch_loss)         grads_list.append(microbatch_grads)      average_grads = jax.tree.map(         lambda *items: jnp.array(items).mean(axis=0),         *grads_list     )      optimizer.update(model, average_grads)     return jnp.array(loss_list).mean()`\n\nYou can see that now I was doing both the forward and the backward pass within the loop, and then working out the mean gradients with that `jax.tree.map`, then passing those average gradients to the optimizer. <span class=\"translation\">可以看到，这次我在循环内部同时执行前向和反向传播，然后用 `jax.tree.map` 计算平均梯度，再把平均梯度传给优化器。</span>\n\nIt all made sense, and seemed to work when I ran it: <span class=\"translation\">一切都说得通，运行时似乎也能工作：</span>\n\n`Training complete in 1,146.173 seconds Tokens seen: 92,209,152 Throughput: 80,450 tokens/second Final train loss: 0.001 2026-06-18 19:00:25.739249 Done`\n\n...and it wasn't as much slower as I would expect given the lack of JITting: 1,146 seconds versus 843. <span class=\"translation\">考虑到没有使用 JIT，速度并没有慢到我预期的程度：1,146 秒对比 843 秒。</span>\n\nIt was interesting that the final train loss was higher than the run without gradient accumulation, but larger effective batch sizes are not always a better thing: it depends very much on the model you're training and the data. The batch size and number of gradient accumulation steps I was using were ones I had optimised for the full 163M-parameter GPT-2-style LLM, not for this model. So it was OK if it was a bit worse. <span class=\"translation\">有趣的是，最终训练损失高于不使用梯度累积的运行，但更大的有效批大小并不总是更好：这很大程度取决于训练的模型和数据。我使用的批大小和梯度累积步数，是为完整的 1.63 亿参数 GPT-2 风格 LLM 优化的，而不是为这个模型。因此略差一些也没问题。</span>\n\nAnyway, I tried adding the `@nnx.jit` to that function, and ran it: <span class=\"translation\">无论如何，我尝试给这个函数加上 `@nnx.jit`，然后运行：</span>\n\n`jax.errors.JaxRuntimeError: RESOURCE_EXHAUSTED: Out of memory while trying to allocate 20.71GiB. [tf-allocator-allocation-error=''] [executable_name='jit_train_step']`\n\nOuch. And looking at the traceback, it appeared that it was the actual JITting that was running out of VRAM. Something to do with loop unrolling, perhaps? I dug around for a while, trying to use JAX's [`fori_loop`](https://docs.jax.dev/en/latest/_autosummary/jax.lax.fori_loop.html) rather than a normal Python one, but to no avail -- I would always run out of GPU memory. <span class=\"translation\">哎。看回溯信息，似乎实际是 JIT 编译过程耗尽了 VRAM。也许和循环展开有关？我研究了一阵，尝试用 JAX 的 [`fori_loop`](https://docs.jax.dev/en/latest/_autosummary/jax.lax.fori_loop.html) 代替普通 Python 循环，但毫无用处——总是会耗尽 GPU 内存。</span>\n\nEventually, after a few hours, the alarm bells on my side quest detector had become too loud to ignore. Reluctantly, I gave up on hand-rolling my own gradient accumulation, and implemented it [the Optax way](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html). <span class=\"translation\">最终，几个小时后，我支线任务探测器的警报声已经大到无法忽略。我不情愿地放弃了手写梯度累积，改为[采用 Optax 的方式](https://optax.readthedocs.io/en/latest/_collections/examples/gradient_accumulation.html)。</span>\n\nThat was actually really nice and simple. The [code is here](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py), but the change is tiny and simple to explain. Remember that we had this code to set up the optimizer: <span class=\"translation\">这其实非常简洁漂亮。[代码在这里](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py)，改动很小，也很容易解释。记住，我们原来有下面这段设置优化器的代码：</span>\n\n    `optimizer = nnx.Optimizer(         model,         optax.adamw(             learning_rate=train_conf[\"learning_rate\"],             weight_decay=train_conf[\"weight_decay\"],         ),         wrt=nnx.Param     )`\n\nThat creates a Flax NNX optimiser, which uses an Optax AdamW optimiser under the hood. <span class=\"translation\">这会创建一个 Flax NNX 优化器，底层使用 Optax 的 AdamW 优化器。</span>\n\nThe Optax way to do gradient accumulation is to wrap the optimiser in a [`MultiSteps`](https://optax.readthedocs.io/en/latest/api/generated/optax.MultiSteps.html) helper, which -- with the NNX optimiser wrapping the result -- looks like this: <span class=\"translation\">Optax 实现梯度累积的方式，是把优化器包装在 [`MultiSteps`](https://optax.readthedocs.io/en/latest/api/generated/optax.MultiSteps.html) 辅助器中，再由 NNX 优化器包装结果，代码如下：</span>\n\n    `optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax.adamw(                 learning_rate=train_conf[\"learning_rate\"],                 weight_decay=train_conf[\"weight_decay\"],             ),             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nThe `MultiSteps` wrapper is really neat. It has the same interface as a regular optimiser, so its `update` method can be called with a set of gradients. But instead of applying them, it just accumulates them until a particular number of calls to `update` have been made, at which it actually does apply the mean of the accumulated gradients, and resets its counter so that it starts accumulating again. <span class=\"translation\">`MultiSteps` 包装器非常巧妙。它和普通优化器具有相同接口，可以用一组梯度调用 `update` 方法。但它不会立即应用梯度，而是累积梯度，直到 `update` 被调用了指定次数，才应用累积梯度的平均值，然后重置计数器并重新开始累积。</span>\n\nThat's actually a really nice API. And it actually meant that I would have been able to simplify the training loop. Remember, we had this: <span class=\"translation\">这个 API 确实很棒。它还意味着我本可以简化训练循环。记得我们原来有：</span>\n\n    `for global_step in progress_bar:         for accumulation_step in range(gradient_accumulation_steps):             inputs, targets = train_dataset[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]             inputs = jax.device_put(inputs, model_device)             targets = jax.device_put(targets, model_device)              train_loss = train_step(model, optimizer, inputs, targets)             train_losses.append(train_loss.item())              microbatch_size, sequence_length = inputs.shape             tokens_seen_this_rank += microbatch_size * sequence_length`\n\nThe loop-within-a-loop was needed by the PyTorch code, because we needed to do the optimizer step at the end to apply the accumulated gradients. But with the Optax wrapper, we could have just iterated over our samples in one top-level loop, relying on the `MultiSteps` to make its updates every `gradient_accumulation_steps` iterations. <span class=\"translation\">PyTorch 代码需要内层循环，是因为我们必须在最后执行优化器步骤，以应用累积的梯度。但有了 Optax 包装器，我们可以在一个顶层循环中遍历样本，依靠 `MultiSteps` 每隔 `gradient_accumulation_steps` 次迭代执行更新。</span>\n\nHowever, I decided to leave it in -- keeping track of the training in terms of global steps meant that the training output with my JAX model would be easier to compare to the PyTorch versions. Perhaps if I'd been building the training loop completely from scratch I would have chosen differently. <span class=\"translation\">不过我决定保留内层循环——用全局步数跟踪训练，意味着 JAX 模型的训练输出更容易和 PyTorch 版本比较。也许如果完全从零构建训练循环，我会做出不同选择。</span>\n\nAnyway, with that code change in, I ran it, and: <span class=\"translation\">无论如何，改完代码后我运行了一次：</span>\n\n`Training complete in 836.409 seconds Tokens seen: 92,209,152 Throughput: 110,244 tokens/second Final train loss: 0.001 2026-06-18 20:56:15.220326 Done`\n\nI had the same loss at the end as the by-hand un-JITted version, which was reassuring. And it was slightly faster than the non-gradient-accumulating version, but it's a small enough difference that it was probably just in the noise. <span class=\"translation\">最终损失和手写的非 JIT 版本相同，这让人放心。它还比不使用梯度累积的版本稍快，但差异很小，可能只是噪声。</span>\n\nSo that was gradient accumulation! [Here's the code with that added](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py). <span class=\"translation\">这就是梯度累积！[这是加入该功能后的代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/52d8b1f86f69ba2e91c016b12c5c6e25e78400a3/train.py)。</span>\n\nNext, I wanted to get charting and scheduling of the learning rate, and gradient clipping working. <span class=\"translation\">接下来，我想让学习率的绘图和调度，以及梯度裁剪都能工作。</span>\n\n### Charting the learning rate <span class=\"translation\">学习率绘图</span>\n\nScheduling the learning rate means that we'll be changing it over the course of the run -- like this example from one of my PyTorch training runs: <span class=\"translation\">学习率调度意味着我们会在训练过程中改变学习率——就像我某次 PyTorch 训练运行中的这个示例：</span>\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nHaving a chart like that one is really useful, as it allows you to sanity-check that the changes you are making to the learning rate really are the right ones. So I wanted to add the charting first, and then the scheduling. The boilerplate code to actually generate the chart, given learning rate numbers in the checkpoints' metadata, was already there, so I had to work out how to extract the current value of the learning rate from the optimiser and then save it into the checkpoints. <span class=\"translation\">像这样的图表非常有用，因为它能让你检查自己对学习率所做的修改是否真的正确。因此我想先加入绘图，再加入调度。根据检查点元数据中的学习率数值生成图表的样板代码已经存在，所以我只需要想办法从优化器中提取当前学习率，并把它保存到检查点。</span>\n\n[This was the obvious starting point](https://optax.readthedocs.io/en/latest/getting_started.html#accessing-learning-rate). Optax optimisers themselves don't store the learning rate, but if you create them like this: <span class=\"translation\">[这里是显而易见的起点](https://optax.readthedocs.io/en/latest/getting_started.html#accessing-learning-rate)。Optax 优化器本身不保存学习率，但如果像这样创建它们：</span>\n\n`optax.inject_hyperparams(optax.adam)(...)`\n\n...where the `...` in the brackets is the normal stuff that you'd pass in to the optimizer when creating it, then you can extract the learning rate later. <span class=\"translation\">其中括号里的 `...` 就是创建优化器时通常会传入的内容；这样之后就可以提取学习率。</span>\n\nHowever, the code on that help page was using the Optax optimiser directly, whereas my one in the training code was wrapped inside a `MultiSteps`, which was in turn wrapped inside an NNX `Optimizer` object, like this: <span class=\"translation\">不过帮助页面上的代码直接使用 Optax 优化器，而训练代码中的优化器被包在 `MultiSteps` 里，后者又被 NNX 的 `Optimizer` 对象包起来，像这样：</span>\n\n    `optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax.adamw(                 learning_rate=train_conf[\"learning_rate\"],                 weight_decay=train_conf[\"weight_decay\"],             ),             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nStill, the solution seemed reasonably clear. I could use the `inject_hyperparameters` trick on the `adamw` that I was creating, and then pass it in to be wrapped like this: <span class=\"translation\">不过解决方案似乎很清晰。我可以对正在创建的 `adamw` 使用 `inject_hyperparameters` 技巧，然后像这样把它传进去包装：</span>\n\n    `optax_optimizer = optax.inject_hyperparams(optax.adamw)(         learning_rate=train_conf[\"learning_rate\"],         weight_decay=train_conf[\"weight_decay\"],     )     optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax_optimizer,             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )`\n\nThe next question was how to actually read the learning rate from that optimiser. <span class=\"translation\">下一个问题是：如何真正从这个优化器中读取学习率？</span>\n\nThe sample code in the Optax docs looked like this: <span class=\"translation\">Optax 文档中的示例代码如下：</span>\n\n`optimizer = optax.inject_hyperparams(optax.adamw)(learning_rate=schedule)  params = initial_params state = optimizer.init(params) print('initial learning rate:', state.hyperparams['learning_rate'])  _, state = fit(initial_params, optimizer)  print('final learning rate:', state.hyperparams['learning_rate'])`\n\nAgain, that was using the Optax optimiser directly, rather than trying to use one that was inside an NNX one. However, in [the docs for NNX's optimiser](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/training/optimizer.html) I noticed that it exposes its wrapped Optax one's state as `opt_state`. I put in some temporary debug code to print that, and saw that it was the `MultiSteps`' state, which made sense -- and that, in turn, contained the state of the wrapped `adamw` one as `inner_opt_state`. <span class=\"translation\">同样，它直接使用 Optax 优化器，而不是尝试使用 NNX 优化器内部的优化器。不过，在 [NNX 优化器的文档](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/training/optimizer.html)中，我注意到它把内部 Optax 优化器的状态暴露为 `opt_state`。我加入了一些临时调试代码打印它，发现那是 `MultiSteps` 的状态，这很合理——而这个状态又把内部 `adamw` 的状态放在 `inner_opt_state` 中。</span>\n\nThat `inner_opt_state` had a field called `hyperparameters`, which was a dictionary that included `learning_rate` as a key. <span class=\"translation\">`inner_opt_state` 有一个名为 `hyperparameters` 的字段，是一个包含 `learning_rate` 键的字典。</span>\n\nFinally, the value that that key pointed to was a `Variable` object. To get the actual value from there, you need to call its `get_value()` to get the actual value, which is a JNP array, so we needed to call `item()` on it. <span class=\"translation\">最后，这个键指向的是一个 `Variable` 对象。要拿到实际值，需要调用它的 `get_value()`，得到一个 JNP 数组，然后还要调用 `item()`。</span>\n\nAll of that led to the following abomination unto God, mankind, and [the Law of Demeter](https://en.wikipedia.org/wiki/Law_of_Demeter): <span class=\"translation\">所有这些最终导出了下面这段冒犯上帝、人类以及[得墨忒耳法则](https://en.wikipedia.org/wiki/Law_of_Demeter)的代码：</span>\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state.hyperparams[\"learning_rate\"].get_value().item()`\n\nEurgh. I mean, really, eurgh. <span class=\"translation\">呃。我是说，真的，呃。</span>\n\nWell, anyway, I put code to do that into the `train` function and save the number as part of the metadata. I did a partial training run, just for long enough to confirm that the learning rate chart was being generated, and had a flat line on it at 0.0014, the constant learning rate I was using at that point. <span class=\"translation\">无论如何，我把这段代码放进 `train` 函数，在元数据中保存这个数值。我进行了一次部分训练，只运行足够长的时间来确认学习率图表能生成；当时使用的恒定学习率是 0.0014，所以图表是一条位于 0.0014 的水平线。</span>\n\nI can't say I was very proud of it, though. <span class=\"translation\">不过我不能说自己对此很自豪。</span>\n\n### Learning rate scheduling <span class=\"translation\">学习率调度</span>\n\nTo recap, the learning rate schedule that I wanted was this: <span class=\"translation\">回顾一下，我想要的学习率调度是这样的：</span>\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nThat's formed of two phases: an initial warmup, where the learning rate started at 0.00001 times the desired peak value, and then rose linearly to the peak, followed by a cosine wave to decay it to 0.1 times the peak. <span class=\"translation\">它由两个阶段组成：先进行预热，学习率从目标峰值的 0.00001 倍开始，线性上升到峰值；然后沿余弦波下降到峰值的 0.1 倍。</span>\n\nIn PyTorch I had had to [use different learning rate scheduler objects to handle each phase, with a `SequentialLR` wrapper to bolt them together](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate#chaining-learning-rate-schedulers): <span class=\"translation\">在 PyTorch 中，我必须[使用不同的学习率调度器对象处理每个阶段，再用 `SequentialLR` 包装器把它们连接起来](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate#chaining-learning-rate-schedulers)：</span>\n\n        `warmup_scheduler = torch.optim.lr_scheduler.LinearLR(             optimizer,             start_factor=0.00001,             end_factor=1.0,             total_iters=warmup_steps         )         cosine_scheduler = torch.optim.lr_scheduler.CosineAnnealingLR(             optimizer,             T_max=decay_steps,             eta_min=learning_rate / 10         )         scheduler = torch.optim.lr_scheduler.SequentialLR(             optimizer,             schedulers=[warmup_scheduler, cosine_scheduler],             milestones=[warmup_steps],         )`\n\nHowever, it's a common pattern in training loops, and conveniently Optax provides a [`warmup_cosine_decay_schedule`](https://optax.readthedocs.io/en/latest/api/generated/optax.schedules.warmup_cosine_decay_schedule.html) class that does all of that for you. <span class=\"translation\">不过，这是训练循环中的常见模式，恰好 Optax 提供了 [`warmup_cosine_decay_schedule`](https://optax.readthedocs.io/en/latest/api/generated/optax.schedules.warmup_cosine_decay_schedule.html) 类，可以替你完成所有这些工作。</span>\n\nThe only oddity in it is that `decay_steps` is kind of misnamed; it's actually total steps, including the warmup. So I wound up writing this code: <span class=\"translation\">其中唯一奇怪的是，`decay_steps` 这个名字有点误导；它实际上是包含预热阶段在内的总步数。因此我写出了下面的代码：</span>\n\n    `total_steps = (len(train_dataset) // world_size) // gradient_accumulation_steps     warmup_steps = (total_steps * train_conf[\"warmup_period_percent\"]) // 100     learning_rate = train_conf[\"learning_rate\"]     schedule = optax.warmup_cosine_decay_schedule(         init_value=learning_rate * 0.00001,         peak_value=learning_rate,         warmup_steps=warmup_steps,         decay_steps=total_steps,  # !!         end_value=learning_rate / 10,     )      optax_optimizer = optax.inject_hyperparams(optax.adamw)(         learning_rate=schedule,         weight_decay=train_conf[\"weight_decay\"],     )`\n\nI did a training run with that, and it completed with this: <span class=\"translation\">我用它进行了一次训练，结果如下：</span>\n\n`2026-06-19 00:17:25.777629 Tokens seen: 92,209,152 2026-06-19 00:17:25.777633 Throughput: 106,225 tokens/second 2026-06-19 00:17:25.777644 Final train loss: 0.006 2026-06-19 00:17:25.777646 Done`\n\nThe loss was a bit worse again, but just as with the gradient accumulation steps, the learning rate schedule I had specified was specifically designed for training a real (if small) LLM, not for this toy A-to-A task that I was using to test the training loop. The important thing was the learning rate chart, and it looked like this: <span class=\"translation\">损失再次略差一些，但和梯度累积步数一样，我指定的学习率调度是为真正的（尽管很小的）LLM 训练设计的，而不是为用来测试训练循环的 A 到 A 玩具任务设计的。重要的是学习率图表，它看起来是这样：</span>\n\n![The learning rate chart for the first scheduled learning rate run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart.png \"The learning rate chart for the first scheduled learning rate run\")\n\nPerfect! [Here's the code at this point](https://github.com/gpjt/jax-gpt2-from-scratch/blob/f89d8d86cecf7e6f9d3bb24186df16218c583500/train.py). <span class=\"translation\">完美！[这是目前版本的代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/f89d8d86cecf7e6f9d3bb24186df16218c583500/train.py)。</span>\n\nThere were two boxes left to check before I had a training loop I could actually use to build the LLM: gradient clipping and the ability to restart from a checkpoint. I decided to do gradient clipping first. <span class=\"translation\">在拥有一个真正可以用来构建 LLM 的训练循环之前，还剩两个检查项：梯度裁剪，以及从检查点重启的能力。我决定先做梯度裁剪。</span>\n\n### Gradient clipping <span class=\"translation\">梯度裁剪</span>\n\n[Gradient clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) is where for each update, you look for gradients that are suspiciously large, and cut them off so that they don't make excessive changes to the model. <span class=\"translation\">[梯度裁剪](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping)就是在每次更新时寻找异常大的梯度，并把它们截断，避免它们对模型造成过大的改变。</span>\n\nThe [Optax docs](https://optax.readthedocs.io/en/latest/getting_started.html#weight-decay-schedules-and-clipping) made it look pretty simple: <span class=\"translation\">[Optax 文档](https://optax.readthedocs.io/en/latest/getting_started.html#weight-decay-schedules-and-clipping)让它看起来相当简单：</span>\n\n`optimizer = optax.chain(   optax.clip(1.0),   optax.adamw(learning_rate=schedule), )`\n\nSo, you use an `optax.chain` to chain together first a thing that does clipping, and then the actual optimiser -- presumably the first thing in the chain sees the gradients and does stuff to them, and then the second receives whatever the first has returned. <span class=\"translation\">所以，你使用 `optax.chain`，先把执行裁剪的组件和实际优化器串起来——想来链中的第一个组件接收梯度并处理它们，然后第二个组件接收第一个组件返回的结果。</span>\n\nNow, the question was, should we do the chain outside or inside the MultiSteps? That is, should we clip gradients each time before we step the MultiSteps optimiser, or do we accumulate them and clip the average before we step the inner AdamW one? <span class=\"translation\">现在的问题是：应该在 `MultiSteps` 外部还是内部进行串联？也就是说，是每次在推进 `MultiSteps` 优化器之前裁剪梯度，还是先累积梯度，再在推进内部 AdamW 优化器之前裁剪平均值？</span>\n\nLooking at [the old PyTorch code](https://github.com/gpjt/ddp-base-model-from-scratch/blob/main/ddp_train.py#L354), I was running the gradient accumulation loop, and then clipping at the end. So the gradient clipping was happening to the accumulated gradients. <span class=\"translation\">查看[旧的 PyTorch 代码](https://github.com/gpjt/ddp-base-model-from-scratch/blob/main/ddp_train.py#L354)，我当时运行梯度累积循环，最后再裁剪。因此梯度裁剪作用于累积后的梯度。</span>\n\nThat actually felt less intuitively good than the alternative, but I decided that we should try to mirror what the PyTorch code is doing. So: <span class=\"translation\">这在直觉上其实不如另一种做法，但我决定先设法和 PyTorch 代码保持一致。所以：</span>\n\n    `optax_optimizer = optax.chain(         optax.clip(train_conf[\"clipping_max_norm\"]),         optax.inject_hyperparams(optax.adamw)(             learning_rate=schedule,             weight_decay=train_conf[\"weight_decay\"],         )     )`\n\nSo, the `adamw` optimiser would receive clipped gradients. Because it was wrapped in the `MultiSteps`, it was receiving the accumulated gradients every time that object hit its `every_k_schedule` limit. <span class=\"translation\">这样，`adamw` 优化器接收的就是裁剪后的梯度。因为它被包在 `MultiSteps` 中，所以每当该对象达到 `every_k_schedule` 的限制时，它接收的是累积梯度。</span>\n\nUnfortunately there was still a problem: that change meant that the optimiser that we were reading the learning rate from with this horrendous code in the `train` function: <span class=\"translation\">然而，问题还没有结束：这个改动意味着，我们在 `train` 函数中用下面这段可怕代码读取学习率的优化器：</span>\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state.hyperparams[\"learning_rate\"].get_value().item()`\n\n...would now be inside yet another level of nesting -- the `chain` object. So, of course, when I ran it, it blew up with an error: <span class=\"translation\">现在它会多嵌套一层，位于 `chain` 对象中。因此运行时当然崩溃了，错误是：</span>\n\n`AttributeError: 'tuple' object has no attribute 'hyperparams'`\n\nI used some debug prints to work out what was going on, and determined that the state of the `chain` object was a tuple, the first element being an essentially-empty state for the clipper, and the second being the hyperparameter-injected state for the `adamw`. <span class=\"translation\">我用一些调试打印弄清楚发生了什么，发现 `chain` 对象的状态是一个元组：第一个元素是基本为空的裁剪器状态，第二个元素是注入超参数后的 `adamw` 状态。</span>\n\nSo that meant that the new correct code to get the learning rate would be this: <span class=\"translation\">所以，读取学习率的新正确代码应该是：</span>\n\n`current_learning_rate = optimizer.opt_state.inner_opt_state[1].hyperparams[\"learning_rate\"].get_value().item()`\n\nNote that we've gained that `[1]` to do the lookup into the `chain`'s tuple state. I remember coming across a comment saying \"forgive us for our trespasses in this method\" in a codebase long ago, and I know well how the author felt. <span class=\"translation\">注意，我们多了一个 `[1]`，用来查找 `chain` 元组状态中的对应项。我记得很久以前在某个代码库里看到过一句“请原谅我们在这个方法中的冒犯”，现在很能理解作者的感受。</span>\n\nI did have an idea of how to at least limit the blast radius a bit, though. At this point in the code, I had the complex optimiser setup in the `main` function, and the learning-rate-getting abomination in `train`. I decided instead to define a function called `get_learning_rate` right next to the optimiser setup, and pass that in to `train`. So the horror was still there, but at least it was all in one place, like this: <span class=\"translation\">不过我想到一种至少能限制影响范围的办法。此时，复杂的优化器设置位于 `main` 函数，而获取学习率的怪物代码位于 `train`。于是我决定在优化器设置旁边定义一个名为 `get_learning_rate` 的函数，并把它传入 `train`。恐怖依旧存在，但至少都集中在一个地方，像这样：</span>\n\n    `optax_optimizer = optax.chain(         optax.clip(train_conf[\"clipping_max_norm\"]),         optax.inject_hyperparams(optax.adamw)(             learning_rate=schedule,             weight_decay=train_conf[\"weight_decay\"],         )     )     optimizer = nnx.Optimizer(         model,         optax.MultiSteps(             optax_optimizer,             every_k_schedule=gradient_accumulation_steps         ),         wrt=nnx.Param     )      def get_learning_rate():         return (             optimizer             .opt_state             .inner_opt_state[1]             .hyperparams[\"learning_rate\"]             .get_value()             .item()         )      log(\"Start train\")     start_global_step = 0  ## checkpointing     train(         run_dir,         model, optimizer,         get_learning_rate,         train_dataset,         rank, world_size,         gradient_accumulation_steps,         start_global_step,         train_conf[\"checkpoint_interval\"],     )`\n\n...where `train` called `get_learning_rate` where it needed it. <span class=\"translation\">这里的 `train` 会在需要时调用 `get_learning_rate`。</span>\n\nI was just about to kick this off, but by chance happened to take a closer look at [the documentation for `clip`](https://optax.readthedocs.io/en/latest/api/generated/optax.clip.html), and spotted that it said <span class=\"translation\">我正要启动训练，却偶然再次仔细看了[`clip` 的文档](https://optax.readthedocs.io/en/latest/api/generated/optax.clip.html)，发现上面写着：</span>\n\n> Clips updates element-wise, to be in `[-max_delta, +max_delta]` <span class=\"translation\">逐元素裁剪更新，使其位于 `[-max_delta, +max_delta]` 范围内</span>\n\nThat rung a bell! When I was [originally looking into gradient clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) for the PyTorch training loop, I noted that that is a perfectly valid way to do gradient clipping, but it's not the way I ultimately chose. Instead, I was clipping based on the L2 norm. <span class=\"translation\">这让我想起了一件事！当我[最初为 PyTorch 训练循环研究梯度裁剪](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping)时，我记得这是一种完全有效的梯度裁剪方式，但不是我最终选择的方式。我采用的是基于 L2 范数的裁剪。</span>\n\nThe JAX training code was meant to work the same way as the PyTorch code, so that was a good catch; I switched over from using `optax.clip` to using [`optax.clip_by_global_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.clip_by_global_norm.html), and then kicked off another training run: <span class=\"translation\">JAX 训练代码本来应该和 PyTorch 代码采用相同方式，所以这是个及时的发现；我把 `optax.clip` 换成了 [`optax.clip_by_global_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.clip_by_global_norm.html)，然后启动了另一次训练：</span>\n\n`2026-06-19 01:22:41.964291 Tokens seen: 92,209,152 2026-06-19 01:22:41.964295 Throughput: 105,022 tokens/second 2026-06-19 01:22:41.964308 Final train loss: 0.006 2026-06-19 01:22:41.964311 Done`\n\nEverything looked fine; my guess was that the final loss was so similar because a simple task like A-to-A mapping, with such a shallow network, would be unlikely to cause gradients to explode. <span class=\"translation\">一切看起来都正常；我猜最终损失如此相似，是因为 A 到 A 这种简单任务配上如此浅的网络，不太可能导致梯度爆炸。</span>\n\nBut it would be nice to be sure. Was there some way I could track the gradients and see if clipping had had to cut in? <span class=\"translation\">但如果能确认一下就更好了。有没有办法跟踪梯度，看看裁剪是否真的介入过？</span>\n\nOne neat thing we had in the PyTorch code was that we could track gradient norms pre-clipping: <span class=\"translation\">PyTorch 代码中有一个很棒的功能，可以跟踪裁剪前的梯度范数：</span>\n\n    `pre_clip_norm = torch.nn.utils.clip_grad_norm_(model.parameters(), clipping_max_norm).item()     grad_norms.append(pre_clip_norm)     clipped_steps.append(pre_clip_norm > clipping_max_norm)`\n\nUnfortunately, `clip_by_global_norm` and the general Optax API doesn't provide any way to access the pre-clipping norms: the `ClipByGlobalNormState` that was the zeroth element of the state of the `chain` that we were reading in the horrendous learning rate-reading code is an alias of `EmptyState`. <span class=\"translation\">不幸的是，`clip_by_global_norm` 以及一般的 Optax API 都没有提供访问裁剪前范数的方式：我们在那段可怕的学习率读取代码中查看的 `chain` 状态的第 0 个元素是 `ClipByGlobalNormState`，它其实只是 `EmptyState` 的别名。</span>\n\nI considered using [`optax.tree_utils.tree_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.tree_utils.tree_norm.html) to work out the norms directly, and logging that, but that would be tricky -- because the gradients we were applying the clipping to were not the ones that were generated in the `train_step` function, but instead the ones that had accumulated inside the `MultiSteps` object over multiple gradient accumulation steps. <span class=\"translation\">我考虑使用 [`optax.tree_utils.tree_norm`](https://optax.readthedocs.io/en/latest/api/generated/optax.tree_utils.tree_norm.html) 直接计算范数并记录下来，但这会很棘手——因为我们进行裁剪的梯度不是 `train_step` 函数生成的梯度，而是 `MultiSteps` 对象在多个梯度累积步骤中积攒的梯度。</span>\n\nThis sounded like a lot of work for a not-enormous benefit, so I decided to leave it out for this project. <span class=\"translation\">这听起来工作量很大，收益却不算大，所以我决定在这个项目中不做了。</span>\n\nThere was, however, one small change that I wanted to make while I was messing around with gradients -- what to do if non-finite numbers crept into them. <span class=\"translation\">不过，在折腾梯度时我还想做一个小改动：如果非有限数值混进梯度，该怎么办？</span>\n\nBack when I was first looking into gradient clipping, I was [somewhat horrified to realise](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping#chasing-infinity) that the scaler object I was using to tell PyTorch to train in 16-bit for things where it felt it would help (Automated Mixed Precision, or AMP), was silently dropping any updates with non-finite gradients, and if you didn't use AMP, such gradients would be happily applied to your model, most likely completely breaking it by setting parameters to non-finite values. <span class=\"translation\">当初研究梯度裁剪时，我[有些震惊地意识到](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping#chasing-infinity)，我用来告诉 PyTorch 对适合的部分以 16 位训练的缩放器对象（自动混合精度，或 AMP），会悄悄丢弃包含非有限梯度的更新；而如果不使用 AMP，这些梯度就会被愉快地应用到模型中，很可能通过把参数设为非有限值而彻底破坏模型。</span>\n\nThis felt like the wrong place for that kind of logic to go -- I felt that it should belong to the optimiser, or at least in some other part of the stack that wasn't specifically related to the totally orthogonal task of mixed-precision training. <span class=\"translation\">我觉得这种逻辑放在这里不对——它应该属于优化器，至少也应该放在栈中的其他部分，而不是和混合精度训练这个完全正交的任务绑在一起。</span>\n\nI checked what JAX's default behaviour with non-finite gradients was, and it turned out to be to just apply them -- but, with Optax, it actually *was* something you could fix at the optimiser level. If you wrap an Optax optimiser with [`apply_if_finite`](https://optax.readthedocs.io/en/latest/api/generated/optax.apply_if_finite.html#optax.apply_if_finite), it will only apply finite gradients, so we could add it to the optimiser setup like this: <span class=\"translation\">我检查了 JAX 对非有限梯度的默认行为，发现它会直接应用这些梯度；但使用 Optax 时，其实可以在优化器层面修复它。如果用 [`apply_if_finite`](https://optax.readthedocs.io/en/latest/api/generated/optax.apply_if_finite.html#optax.apply_if_finite) 包装 Optax 优化器，它只会应用有限梯度，因此可以像这样把它加入优化器设置：</span>\n\n    `optimizer = nnx.Optimizer(         model,         optax.apply_if_finite(             optax.MultiSteps(                 optax_optimizer,                 every_k_schedule=gradient_accumulation_steps             ),             max_consecutive_errors=math.inf,         ),         wrt=nnx.Param     )`\n\nI set `max_consecutive_errors` to infinity to mirror the PyTorch code's behaviour. <span class=\"translation\">我把 `max_consecutive_errors` 设为无穷大，以匹配 PyTorch 代码的行为。</span>\n\nNow, obviously, this required yet another level of indirection in the learning-rate-getting function from hell: <span class=\"translation\">显然，这又要求学习率获取函数从地狱般的代码再多一层间接访问：</span>\n\n    `def get_learning_rate():         return (             optimizer             .opt_state             .inner_state             .inner_opt_state[1]             .hyperparams[\"learning_rate\"]             .get_value()             .item()         )`\n\nIf you're keeping track, it's the `.inner_state` in there. Heigh ho. <span class=\"translation\">如果你一直在跟踪的话，新增的就是其中的 `.inner_state`。唉。</span>\n\nSo, it was time to run it again: <span class=\"translation\">于是是时候再次运行了：</span>\n\n`Training complete in 892.715 seconds 2026-06-19 02:27:16.657056 Tokens seen: 92,209,152 2026-06-19 02:27:16.657059 Throughput: 103,291 tokens/second 2026-06-19 02:27:16.657070 Final train loss: 0.006 2026-06-19 02:27:16.657072 Done`\n\nThat looked OK -- no change from before. [Here's the code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fdfcd8d6369822a98380446dca5e5dd7fc503b10/train.py). <span class=\"translation\">看起来没问题——和之前相比没有变化。[这是代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fdfcd8d6369822a98380446dca5e5dd7fc503b10/train.py)。</span>\n\nNow, it was time to take the last step to finish the training loop: the ability to restart from a checkpoint. <span class=\"translation\">现在是完成训练循环的最后一步：实现从检查点重启。</span>\n\n### Restarting from a checkpoint <span class=\"translation\">从检查点重启</span>\n\nAt this point, [the checkpointing code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py) was pretty basic -- it would save the model as a Safetensors file, along with some metadata like the min, max and average loss since the previous checkpoint, the number of the global step that we were on, and whether or not this was the best checkpoint (in terms of average training loss) so far. <span class=\"translation\">此时，[检查点代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/be561ce6f948d9941f242fad17e08c3746eb0e38/checkpointing.py)还很基础——它会把模型保存为 Safetensors 文件，并保存一些元数据，例如自上一个检查点以来的最小、最大和平均损失、当前所在的全局步数，以及这是否是目前为止（按平均训练损失计算）的最佳检查点。</span>\n\nIn order to restore from a checkpoint, we'd need more information. In the old PyTorch code, we needed three extra things on top of the model and the metadata: <span class=\"translation\">要从检查点恢复，我们需要更多信息。在旧的 PyTorch 代码中，除了模型和元数据，还需要三样东西：</span>\n\n-   The scaler that we used to do automated mixed-precision training. This JAX loop was not going to do that, so it was not necessary here. <span class=\"translation\">用于自动混合精度训练的缩放器。这个 JAX 循环不会做自动混合精度训练，因此这里不需要。</span>\n-   The learning rate scheduler. This was built into the optimiser for JAX, so I didn't think it was needed. <span class=\"translation\">学习率调度器。JAX 中它已经内置在优化器里，所以我觉得不需要单独保存。</span>\n-   The optimiser itself. This was important, and we definitely did need to save it. <span class=\"translation\">优化器本身。这很重要，我们确实需要保存它。</span>\n\nSo that was the job: save the optimiser in `save_checkpoint`, and then implement a `load_checkpoint` so that we can restart from one. I could then try kicking off a training run, waiting for a bit, killing it, then restarting from the most recent checkpoint. The loss and learning rate charts would tell me whether or not the restart really had picked up from where it had left off. <span class=\"translation\">所以任务就是：在 `save_checkpoint` 中保存优化器，再实现 `load_checkpoint`，这样就能从某个检查点重启。我可以启动一次训练，运行一段时间后终止，再从最新检查点重启；损失和学习率图表会告诉我重启是否真的从中断的位置继续。</span>\n\nInitially I was thinking that I would just use pickle to save the optimiser, but that felt like a problem waiting to happen. Pickle has issues when you change Python versions or versions of installed packages, which never *feels like* it's going to be a problem, but all-too-frequently turns out to break stuff in reality. [2](#fn-2) <span class=\"translation\">起初我想直接用 pickle 保存优化器，但这感觉像是在给未来埋雷。更换 Python 版本或已安装包的版本时，pickle 会出现问题；这类问题看起来总像“不可能发生”，却在现实中频繁导致程序崩溃。[2](#fn-2)</span>\n\nUsing Safetensors looked a bit tricky -- it had been [hard](https://www.gilesthomas.com/2026/06/flax-and-safetensors) to get it to work with Flax models, even though it had explicit support. <span class=\"translation\">使用 Safetensors 看起来有点棘手——即使它明确支持 Flax 模型，我之前也[费了不少力气](https://www.gilesthomas.com/2026/06/flax-and-safetensors)才让它工作。</span>\n\nNow, the recommended library for checkpointing in JAX code is called [Orbax](https://orbax.readthedocs.io/en/latest/index.html). I'd looked into it before, and it looked a bit heavyweight, so I'd moved on. But digging in a little more, I found that it had what looked like [a simple API for saving PyTrees](https://orbax.readthedocs.io/en/latest/guides/checkpoint/v1/checkpointing_pytrees.html), which bypassed the complexity. <span class=\"translation\">现在，JAX 代码中推荐用于检查点的库叫作 [Orbax](https://orbax.readthedocs.io/en/latest/index.html)。我之前研究过它，感觉有些笨重，于是先放下了。但继续深入一点后，我发现它似乎有[一个用于保存 PyTree 的简单 API](https://orbax.readthedocs.io/en/latest/guides/checkpoint/v1/checkpointing_pytrees.html)，可以绕过复杂性。</span>\n\nGetting it working was still a bit tricky, though. <span class=\"translation\">不过让它工作起来仍然有点棘手。</span>\n\nFirstly, in the docs, they give this example: <span class=\"translation\">首先，文档给了下面这个示例：</span>\n\n`import orbax.checkpoint.experimental.v1 as ocp  ...  ocp.save(path, pytree)`\n\nI tried that in the `save_checkpoint` function with code like this: <span class=\"translation\">我在 `save_checkpoint` 函数中用类似下面的代码尝试了它：</span>\n\n    `ocp.save(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\n...and got the error <span class=\"translation\">……然后得到了这个错误：</span>\n\n`AttributeError: module 'orbax.checkpoint.experimental.v1' has no attribute 'save'`\n\nHuh. Digging into the library from the command line showed that the function was actually called `save_pytree`. Not super-promising if the docs don't match the API (though to be fair, it does say `experimental` right there in the package name). <span class=\"translation\">嗯。通过命令行查看库的代码后，我发现这个函数实际叫 `save_pytree`。如果文档和 API 对不上，感觉不太妙（不过公平地说，包名里确实写着 `experimental`）。</span>\n\nAnyway, changing that appeared to work: <span class=\"translation\">无论如何，改完后似乎能工作：</span>\n\n    `ocp.save_pytree(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\n...and then next to the 295 MB file called `model.safetensors` in my checkpoint directories, there was a 353 MB directory called `optimizer`. In PyTorch-land the optimiser had always been double the size of the model [3](#fn-3), but given the wildly different file formats in play, I was comfortable enough that it was order-of-magnitude the same as the model and somewhat bigger. Perhaps Orbax was doing some kind of compression or something like that. <span class=\"translation\">然后，在检查点目录中名为 `model.safetensors`、大小 295 MB 的文件旁边，出现了一个大小 353 MB、名为 `optimizer` 的目录。在 PyTorch 世界里，优化器一直是模型的两倍大[3](#fn-3)；考虑到这里使用了截然不同的文件格式，我觉得它和模型处于同一个数量级、稍微大一些，已经足够合理。也许 Orbax 做了某种压缩之类的事情。</span>\n\nNext, it was time to write `load_checkpoint`. I started off by writing the `load_model` function to load up the safetensors file -- that's the one I showed earlier, back when I showed how the original A-to-A model learned how to map a poem to itself, and that if you asked it how to complete \"Every effort moves you\", it would respond with \" you you you you you\" and so on. <span class=\"translation\">接下来该编写 `load_checkpoint` 了。我先写了 `load_model` 函数来加载 safetensors 文件——前面介绍 A 到 A 模型学会把一首诗映射回自身时展示过它；如果让那个模型补全“Every effort moves you”，它会回答“ you you you you you”等等。</span>\n\nOnce I had that, I created a `load_checkpoint`, which called `load_model`, and then loaded up the metadata and worked out what our best loss so far had been (which is necessary when continuing from a checkpoint so that, as you continue training, you can work out whether each new global step has had a loss that is better than the current best). That was simple enough: <span class=\"translation\">有了它之后，我创建了 `load_checkpoint`：调用 `load_model`，再加载元数据，并算出目前为止的最佳损失（从检查点继续训练时必须知道这一点，这样随着训练继续，就能判断每个新的全局步是否比当前最佳损失更好）。这部分很简单：</span>\n\n    `with open(checkpoint_dir / \"meta.json\", \"r\") as f:         meta = json.load(f)         restart_global_step = meta[\"global_step\"] + 1      with open(checkpoints_dir / \"best\" / \"meta.json\") as f:         best_loss = json.load(f)[\"avg_train_loss\"]`\n\nRestoring the optimiser turned out to be a bit trickier. Firstly, of course, just like with saving, the Orbax function was called `load_pytree` rather than the documented `load`. The next part was working out how to load it in a fashion that the optimiser would accept. <span class=\"translation\">恢复优化器就棘手多了。首先，和保存时一样，Orbax 的函数叫 `load_pytree`，而不是文档中的 `load`。接下来要弄清楚如何以优化器可以接受的方式加载它。</span>\n\nIf you load a checkpointed PyTree like this: <span class=\"translation\">如果像下面这样加载一个已保存检查点的 PyTree：</span>\n\n`ocp.load_pytree(checkpoint_dir / \"optimizer\")`\n\nThen what you get back is a \"basic\" PyTree -- it will consist of lists, dictionaries, tuples, basic Python types like strings, and JAX arrays. The problem is that the optimiser's state is formed of objects that can be mapped to such things -- for example, an object can be mapped to a dictionary where each field is an item in the dict -- but aren't actually those specific types of objects. <span class=\"translation\">得到的是一个“基础”PyTree——由列表、字典、元组、字符串等基础 Python 类型以及 JAX 数组组成。问题在于，优化器状态由可以映射成这些东西的对象构成——例如一个对象可以映射成每个字段都是字典项的字典——但它们实际并不是这些具体类型的对象。</span>\n\nSo if you do this: <span class=\"translation\">所以如果这样做：</span>\n\n`optimizer.opt_state = ocp.load_pytree(checkpoint_dir / \"optimizer\")`\n\n...you get an error, something like this: <span class=\"translation\">……会得到类似这样的错误：</span>\n\n`AttributeError: 'list' object has no attribute 'items'`\n\n...and likewise if you use the `nnx.update` function I was using in the `load_model` code: <span class=\"translation\">同样，如果使用我在 `load_model` 代码中使用的 `nnx.update` 函数：</span>\n\n`nnx.update(optimizer.opt_state, ocp.load_pytree(checkpoint_dir / \"optimizer\"))`\n\n...you'll get a slightly different but equally confusing error. <span class=\"translation\">……也会得到一个稍有不同但同样令人困惑的错误。</span>\n\nAfter a certain amount of floundering around, limited by the lack of documentation (and it not seeming to match the API that I was seeing) I had the bright idea of looking at `load_pytree`'s docstring, and that turned out to be excellent. In IPython: <span class=\"translation\">在一番摸索之后（文档很少，而且看起来和我实际看到的 API 对不上），我灵机一动，查看了 `load_pytree` 的文档字符串，结果发现它写得非常好。在 IPython 中：</span>\n\n`In [1]: import orbax.checkpoint.experimental.v1 as ocp  In [2]: ocp.load_pytree? Signature: ocp.load_pytree(     path: 'path_types.PathLike',     abstract_pytree: 'AbstractPyTree | CheckpointMetadata[AbstractPyTree] | None' = None,     *,     checkpointable_name: 'str | None' = 'AUTO', ) -> 'tree_types.PyTreeOf[tree_types.Leaf]' Docstring: Loads a PyTree.  Loads from a ``PyTree`` checkpoint. A ``PyTree`` checkpoint must be a path containing a subdirectory with the name provided by ``checkpointable_name``, with default value ``AUTO``. See ``checkpointable_name`` for more details.  This function must be called on all available controller processes.  The operation blocks until complete. For improved performance, consider using :py:func:``.load_pytree_async`` instead.  If ``abstract_pytree`` is not provided, the ``PyTree`` will be loaded exactly as saved.  IMPORTANT: Loading is more brittle and error-prone when not providing ``abstract_pytree``. Always provide ``abstract_pytree`` if possible. Note that you can always obtain the tree structure from a saved checkpoint using :py:func:``.pytree_metadata``.  Providing the ``abstract_pytree`` guarantees two things:  1. The restored tree will exactly match the structure of ``abstract_pytree`` (or raise an error if it is impossible to guarantee this). For example, if ``abstract_pytree`` is a custom object registered as a ``PyTree``, the checkpoint will be restored as the same object, if possible.  2. The leaves of the restored tree will be restored with the properties indicated by the abstract leaves. For example, if a leaf in ``abstract_pytree`` is a ``jax.ShapeDtypeStruct``, the restored leaf will be a ``jax.Array`` with the same shape and ``dtype``. Each ``AbstractLeaf`` has a corresponding ``Leaf`` that is restored. See ``orbax.checkpoint.v1.tree`` for a table of standard supported leaf types.  ...`\n\nThe solution was obviously that `abstract_pytree`. When you provide it, it's used as a template. If in the abstract PyTree it finds a `Foo` object, and in the loaded PyTree there is a dictionary in the same position with keys `bar`, `baz` and `quz`, it will create a `Foo` object, setting those fields to those values. <span class=\"translation\">解决方案显然就是 `abstract_pytree`。提供它时，它会被用作模板。如果抽象 PyTree 中有一个 `Foo` 对象，而加载的 PyTree 相同位置是一个包含 `bar`、`baz` 和 `quz` 键的字典，它就会创建一个 `Foo` 对象，并用这些值设置相应字段。</span>\n\nThat means that you have something with the right structure to apply, so I wound up with this relatively simple code to load checkpoint into the optimiser: <span class=\"translation\">这样就有了可供应用的正确结构，于是我最终用下面这段相对简单的代码把检查点加载回优化器：</span>\n\n`optimizer.opt_state = ocp.load_pytree(checkpoint_dir / \"optimizer\", optimizer.opt_state)`\n\nWe're using the existing state of the optimiser as a template to tell Orbax how to structure the loaded one. <span class=\"translation\">我们使用优化器现有的状态作为模板，告诉 Orbax 应该如何组织加载的状态。</span>\n\nI kicked off a training run, hit control-C halfway through, then restarted it from the checkpoint, and the final loss chart looked like this: <span class=\"translation\">我启动了一次训练，进行到一半时按下 control-C，然后从检查点重启；最终损失图如下：</span>\n\n![The loss chart for the interrupted and restarted run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/loss-chart-interrupted.png \"The loss chart for the interrupted and restarted run\")\n\n...and the learning rate chart like this: <span class=\"translation\">学习率图则如下：</span>\n\n![The learning rate chart for the interrupted and restarted run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart-interrupted.png \"The learning rate chart for the interrupted and restarted run\")\n\nPerfect! The interrupt was at about global step 400, and the loss continued to go down properly, and the learning rate followed its schedule perfectly. <span class=\"translation\">完美！中断发生在全局步约 400 处，损失继续正常下降，学习率也完美遵循调度。</span>\n\nHere's [the checkpoint-loading code](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/checkpointing.py) and [the training script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/train.py). <span class=\"translation\">[这是加载检查点的代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/checkpointing.py)，[这是训练脚本](https://github.com/gpjt/jax-gpt2-from-scratch/blob/8fd178251853aaa96e31164519ea79c89088df67/train.py)。</span>\n\n### Time to build a model! <span class=\"translation\">该构建模型了！</span>\n\nSo with that, phase one was done. I had a training script. It was massively overengineered for training this little A-to-A model, but just right for training a small LLM from scratch. <span class=\"translation\">这样，第一阶段就完成了。我已经有了一份训练脚本。对于训练这个小小的 A 到 A 模型来说，它过度工程化了；但对于从零开始训练一个小型 LLM 来说，它恰到好处。</span>\n\nAnd now it was time to do that -- and that's what I'll cover in the next post. <span class=\"translation\">现在是时候真正做这件事了——下一篇文章会介绍它。</span>\n\n[Here's a link to the next (and final!) post in this series](https://www.gilesthomas.com/2026/07/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax). <span class=\"translation\">[这是本系列下一篇（也是最后一篇！）文章的链接](https://www.gilesthomas.com/2026/07/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax)。</span>\n\n* * *\n\n1.  If you're thinking \"why not just have it return one-hot vectors based on the input tokens\", remember that I needed *something* in the model to train, so that I could confirm that loss was going down. A pure \"identity\" model without the embedding space would have nothing to learn, so wouldn't be able to provide that. [↩](#fnref-1) <span class=\"translation\">如果你在想“为什么不让它根据输入词元直接返回 one-hot 向量”，请记住，我需要模型中有*一些东西*可供训练，这样才能确认损失确实在下降。没有嵌入空间的纯“恒等”模型没有任何可学习的东西，也就无法提供这种确认。[↩](#fnref-1)</span>\n    \n2.  It was a surprisingly large source of tech support queries on PythonAnywhere. Someone would train a model with (say) Python 3.11.1, and then try to run it on our servers using 3.11.2, and discover that they couldn't load up their checkpoints. This confused them and they wondered if it was something to do with our platform. I even had a quicktext response to send with a rundown on how Pickle works so that I didn't have to keep typing the same explanation. This may have biased me more against Pickle than I should rationally be. [↩](#fnref-2) <span class=\"translation\">这在 PythonAnywhere 上意外地成了技术支持咨询的一个大来源。有人用（比如）Python 3.11.1 训练模型，然后试图在我们使用 3.11.2 的服务器上运行，结果发现无法加载检查点。他们对此感到困惑，怀疑是不是平台的问题。我甚至准备了一段快速回复，概述 Pickle 的工作方式，这样就不必每次重复输入同样的解释。这件事可能让我在理性上不该那么反感 Pickle。[↩](#fnref-2)</span>\n    \n3.  AdamW stores two numbers per parameter to keep track of its optimisation state, so 2x the model size is exactly what you'd expect if both files were in the same format. [↩](#fnref-3) <span class=\"translation\">AdamW 为每个参数保存两个数字来跟踪优化状态，因此如果两个文件使用相同格式，优化器大小正好是模型大小的 2 倍，这完全符合预期。[↩](#fnref-3)</span>"
}
