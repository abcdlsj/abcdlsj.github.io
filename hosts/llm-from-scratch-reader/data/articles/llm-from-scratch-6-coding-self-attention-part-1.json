{
  "slug": "llm-from-scratch-6-coding-self-attention-part-1",
  "part": 6,
  "title": "Writing an LLM from scratch, part 6 -- starting to code self-attention",
  "shortTitle": "part 6 -- starting to code self-attention",
  "date": "21 January 2025",
  "summary": "Our first cut at an attention mechanism -- a non-trainable one.",
  "href": "https://www.gilesthomas.com/2025/01/llm-from-scratch-6-coding-self-attention-part-1",
  "phaseId": "attention",
  "phaseLabel": "02 · 注意力",
  "markdown": "This is the sixth in my series of notes on [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". Each time I read part of it, I'm posting about what I found interesting as a way to help get things straight in my own head -- and perhaps to help anyone else that is working through it too. This post covers just one subsection of the trickiest chapter in the book -- subsection 3.3.1, \"A simple self-attention mechanism without trainable weights\". I feel that there's enough in there to make up a post on its own. For me, it certainly gave me one key intuition that I think is a critical part of how everything fits together.\n\nAs always, there may be errors in my understanding below -- I've cross-checked and run the whole post through Claude, ChatGPT o1, and DeepSeek r1, so I'm reasonably confident, but *caveat lector* :-) With all that said, let's go!\n\nIn my last [two](https://www.gilesthomas.com/2024/12/llm-from-scratch-4) [posts](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention), I've spent a lot of time trying to get a solid, intuitive understanding of what self-attention mechanisms are for, and what they mean in an abstract sense. They're the core concept in the implementation of LLMs (using Raschka's specific meaning of transformers-based decoder-only language models, specifically GPTs), so this felt like an important foundation to get exactly right. This chapter is where things start to get a bit more concrete.\n\nLet's start with a summary at the abstract level, though. Using my normal simplification that words == tokens, an LLM is a model that, given some input text, works out what the next word should be. In order to do this, it considers all of the words in the input text. Attention is a system that tells it, when looking at word *x* in the input, how much it should also take into account each of the other words in that input -- that is, how much *attention* it should pay to each other word.\n\nSo, for the sentence\n\n```\nThe fat cat sat on the mat\n```\n\n...when the LLM is considering \"cat\", it needs to focus on the first \"the\" (because it's a specific cat, not \"a cat\" -- sorry, Slavic language speakers, that *is* important ;-), \"sat\" because that is the verb that the cat is doing, \"fat\" because it applies to the cat, and perhaps to a lesser extent \"on\", \"the\", and \"mat\". Likewise, if the LLM is looking at \"sat\", then it's likely that \"cat\" and \"mat\" are the important bits as the subject and indirect objects of the verb, with the other words playing lesser parts.\n\nYou can imagine building up for each word in the sentence a list of numbers -- one for each other token. Here's an example of what that might look like, based on my own intuition about the importance of each word to the others in that sentence:\n\n| Token | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\") |\n| --- | --- | --- | --- | --- | --- | --- | --- |\n| The | 1 | 0.3 | 0.75 | 0.1 | 0 | 0 | 0 |\n| fat | 0.2 | 1 | 0.8 | 0 | 0 | 0 | 0 |\n| cat | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4 |\n| sat | 0.1 | 0 | 0.85 | 1 | 0.3 | 0.2 | 0.75 |\n| on | 0 | 0.1 | 0.4 | 0.6 | 1 | 0.3 | 0.8 |\n| the | 0 | 0 | 0 | 0 | 0.1 | 1 | 0.75 |\n| mat | 0 | 0 | 0.2 | 0.8 | 0.7 | 0.6 | 1 |\n\nTo unpack that -- each row in the table is a word in the sentence, and the numbers in that row are how much attention one might want to pay to the other words when trying to understand it. I've used a number from 0 (\"you can ignore it\") to 1 (\"this is super-important\"). But you can see that I've tried to represent the relative importance that I gave above for \"cat\" and \"sat\" numerically, and then done likewise for the other words. Note that each word is most attentive to itself, which makes intuitive sense to me :-)\n\nNow, the cool thing is that this is exactly what attention mechanisms do. The reason I (slightly ab-)used the symbol ω to represent these numbers is because it's a symbol used to represent *attention scores*. When considering a particular token in the input sequence, the attention mechanism assigns every other token an attention score. This is then used to work out what to do with it.\n\nAttention is all about building up data like this so that neural networks can understand language enough to be able to predict the next token when given something to start with. So we really have two questions:\n\n-   How do we build up a system that can work out sets of attention scores across all tokens, for each token in our input sequence?\n-   What do we do with those lists of attention scores once we've got them?\n\nThe answer to the second one of those is actually the easiest, and Raschka explains that in this section -- this is the big \"aha\" moment for me in this post. We want to create, for each token, something called a *context vector*.\n\nSo far, our LLM has received a bunch of text, and has\n\n-   Split it into tokens.\n-   For each token, it has generated two embeddings -- a *token embedding* that represents (or rather, will be trained to represent) in some manner the meaning of that token in isolation, and a *position embedding*, which will represent simply whether it's the first, second, third, or whatever token in the sequence. The element-wise sum of these two embeddings is called the *input embedding* for this token.\n\nNow, for our token \"cat\" we have, at this stage, a token embedding that represents \"cat\" (and is probably similar to the embedding for \"kitten\", not too dissimilar to the embedding for \"dog\", and very different to the embedding for \"rain\"). We also have a position embedding that essentially says \"this is the third word in the input\". These two combined are the input embedding, and the purpose of the context vector is to produce a something numerical that represents \"cat\" in the context of all of the other words in the sentence, which not only needs to represent the token and its position but also the information from those other words.\n\nSo while the input embedding for \"cat\" just means \"cat in position 3\", the context vector for \"cat\" in this sentence also has some kind of overtones about it being a cat that is sitting, perhaps less strongly that it's a specific cat (\"the\" rather than \"a\"), and hints of it being sitting on a mat. Back at the start of the chapter, Raschka said that the attention mechanisms don't really understand the concept of ordering, so we need to pack in enough meaning here that that is also unimportant (rather like the endless declensions in Latin, I guess).\n\nBy contrast, the context for the first \"The\" would just be pretty much representing that it's the definite article and might also have some cattishness about it, and maybe a spot of fatness. And so on for the context vectors for all of the other words. As I understand it, it's the context vector that the next-token-prediction layer will care about -- not the word embedding. Which makes sense, it doesn't care about cats in general, it cares about this specific one in this sentence.\n\n> If you've read further on in the chapter you'll have noticed I'm playing a bit fast and loose in the above, because I'm not considering causal attention. But I think that's worth doing as an aid to intuition, at least my own, at this stage.\n\nThat means that the attention part of our LLM is basically a black box that takes in input embeddings, and for each token spits out a context vector that represents the token's meaning in the context of this particular input sequence, including those parts of the meaning that come from its position. (Multi-head attention, introduced later, will build on this a bit more.)\n\nTo me the context vector feels a little bit like the hidden states that were used to transfer meaning from the encoder to the decoder in those original translation systems that I was writing about in my [last post in this series](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention); perhaps that's how people got from there to where we are now. It's essentially something like an embedding for this token in this context.\n\nSo, given a set of attention scores for a particular token -- one for each token in the input -- how do we go about getting this context vector for the token we're considering?\n\nWell, for \"cat\" we want something that includes sitting, some hint of the mat, and so on, and we have these attention scores that relate how strongly the word \"cat\" is related to each other token. And each of those other tokens has an input embedding that is meant to represent its meaning and position. So we could do a pretty simple bit of maths to get something that represents them all taken together -- we just multiply all of the input embeddings by their respective attention weights and add them together! So to get the context vector for \"cat\" given this part of the attention score matrix above:\n\n| Token | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\") |\n| --- | --- | --- | --- | --- | --- | --- | --- |\n| cat | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4 |\n\nWe'd calculate 0.6 times the input embedding for \"The\", 0.8 times the input embedding for \"fat\", 1 times the input embedding for \"cat\", and so on, then add them all up. That might give us our context embedding for \"cat\" in this sentence.\n\nAnd that is pretty much what happens! That, to me, is beautifully simple.\n\nThere's only one extra wrinkle, which seems entirely reasonable -- the numbers I showed above would be called *attention scores*, and before multiplying the input embeddings for all of the tokens in the input sequence by their associated score, we convert them into *attention weights*, which are essentially the same numbers but normalised so that all of the weights for a given token sum up to 1 -- this is done by running them through the [Softmax function](https://en.wikipedia.org/wiki/Softmax_function#:~:text=The%20softmax%20function%2C%20also%20known,used%20in%20multinomial%20logistic%20regression.). This apparently works well with extreme values and turns negative numbers into small positive ones, both of which seem like good things (especially the second -- paying a negative amount of attention to one word when trying to understand another doesn't make any intuitive sense).\n\nSo, for each token -- or rather the input embedding for each token -- we work out a vector of attention scores, which is the length of the full input sequence and for each item in the sequence, says how much attention to pay to it. We then run that vector through softmax to normalise it into attention weights, multiply each input embedding in the input sequence by its attention weight, and add together the results element-wise to get the context vector for the token we're considering.\n\nMaybe it's worth sketching out some pseudocode to make this as clear as possible. Given a sequence of input embeddings (which, remember, are the token embeddings plus the position embeddings) called `inputs`, we can imagine it's doing this:\n\n```\noutput_context_vectors = []\nfor input_embedding in inputs:\n    attention_scores = get_attention_scores(input_embedding, inputs)\n    attention_weights = softmax(attention_scores)\n    context_vector = an empty vector of the same size as the input embeddings\n    for (other_input_embedding, attention_weight) in zip(inputs, attention_weights):\n        context_vector += attention_weight * other_input_embedding\n    output_context_vectors.append(context_vector)\n```\n\nNow, obviously this would not be the right way to implement it (all of that iterating over lists inside iterations over lists can be simplified into matrix multiplications). And of course, the `get_attention_scores` is magic -- and the next big thing to understand. But I think that pseudocode gives, at least for me, a clear idea of what is going on when building up a context vector given a vector of attention scores for each input token (or more strictly, its input embedding).\n\nThat's pretty magical :-) But how do we get the attention scores in the first place? Or more accurately, how do we train something to do that? In this section, Raschka keeps things super-simple. As he says in the section title, this is a \"simple self-attention mechanism without trainable weights\". So instead of trying to write a real `get_attention_scores`, he just uses the dot product of the input embeddings to generate a placeholder one. The dot product takes two vectors and returns a scalar, so that means that the attention scores for \"cat\" in position 3 is a vector made up of:\n\n-   The input embedding for \"The\" in position 1, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"fat\" in position 2, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"cat\" in position 3, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"sat\" in position 4, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"on\" in position 5, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"the\" in position 6, dot-product the input embedding for \"cat\" in position 3\n-   The input embedding for \"mat\" in position 7, dot-product the input embedding for \"cat\" in position 3\n\nNow, the dot product for vectors is worked out by multiplying their elements individually and then adding up the results -- that is,\n\n```\n[1, 2, 3] . [4, 5, 6]\n-> [1 * 4, 2 * 5, 3 * 6]\n=  [4, 10, 18]\n-> 4 + 10 + 18\n= 32\n```\n\nThis is a non-crazy \"toy\" calculation to work out attention scores, because the dot product of two vectors is related to how similar they are in terms of their direction in the vector space (though they apparently should be normalised in some way for this).\n\nBut it's not what we want for a real attention mechanism -- for example, in\n\n```\nThe fat cat sat on the mat\n```\n\n...if we ignore case, the two \"the\"s would be super-closely related to each other -- the position component of their input embeddings would be different, but the token component would be identical -- so they would have a really high attention score, probably much higher than any of the other pairs of words apart from each word's score against itself. There's almost nothing in there about the meaning of the sentence or how words relate to each other grammatically in this context. But it's a great starting point so that we can start implementing things before actually coding a real system for generating attention scores. And I'm pretty sure that the real attention score system will be using dot product somewhere, given their ability to say whether two things are similar -- it will just be a part of a larger mechanism.\n\nI'm going to wrap up there. The next section is working at a more implementational level -- given this simple dot-product way of working out the attention, how do we in practice calculate the context vectors for an input sequence in PyTorch, using matrix and other tensor operations -- so that everything can be done as a batch without complex loops like in my pseudocode above. That can be the topic for my next post.\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2025/02/llm-from-scratch-7-coding-self-attention-part-2).\n\n\\[Update: the original version of this post had an error -- I was only considering token embeddings as an input to the attention mechanism rather than thinking in terms of input embeddings. The text above has been revised to fix that. More details [here](https://www.gilesthomas.com/2025/01/llm-from-scratch-6b-correction).\\]",
  "imageCount": 0,
  "wordCount": 2719,
  "readingMinutes": 12,
  "syncedAt": "2026-08-11T08:01:47.579Z",
  "license": "CC BY 4.0",
  "author": "Giles Thomas",
  "markdownBilingual": "This is the sixth in my series of notes on [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". Each time I read part of it, I'm posting about what I found interesting as a way to help get things straight in my own head -- and perhaps to help anyone else that is working through it too. This post covers just one subsection of the trickiest chapter in the book -- subsection 3.3.1, \"A simple self-attention mechanism without trainable weights\". I feel that there's enough in there to make up a post on its own. For me, it certainly gave me one key intuition that I think is a critical part of how everything fits together. <span class=\"translation\">这是我记录阅读 [Sebastian Raschka](https://sebastianraschka.com/) 著作《[从零开始构建大型语言模型](https://www.manning.com/books/build-a-large-language-model-from-scratch)》心得系列的第六篇。每次读完一部分，我都会发文记录有趣的发现，既帮助自己理清思路，也可能帮助其他同样在读这本书的人。这篇只涵盖本书最棘手章节中的一个小节——第 3.3.1 节《一种没有可训练权重的简单自注意力机制》。我觉得其中内容足以单独成文；对我来说，它确实带来了一个关键直觉，我认为这是理解一切如何衔接的核心部分。</span>\n\nAs always, there may be errors in my understanding below -- I've cross-checked and run the whole post through Claude, ChatGPT o1, and DeepSeek r1, so I'm reasonably confident, but *caveat lector* :-) With all that said, let's go! <span class=\"translation\">一如既往，下面对内容的理解可能有错误——我已经交叉核对，并让 Claude、ChatGPT o1 和 DeepSeek r1 运行了整篇文章，所以自认为相当有把握，但仍请读者自行判断（*caveat lector*）:-)。说了这么多，开始吧！</span>\n\nIn my last [two](https://www.gilesthomas.com/2024/12/llm-from-scratch-4) [posts](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention), I've spent a lot of time trying to get a solid, intuitive understanding of what self-attention mechanisms are for, and what they mean in an abstract sense. They're the core concept in the implementation of LLMs (using Raschka's specific meaning of transformers-based decoder-only language models, specifically GPTs), so this felt like an important foundation to get exactly right. This chapter is where things start to get a bit more concrete. <span class=\"translation\">在前两篇[文章](https://www.gilesthomas.com/2024/12/llm-from-scratch-4)和[文章](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention)中，我花了很多时间试图建立对自注意力机制用途及其抽象含义的扎实直觉。它们是 LLM 实现的核心概念（这里采用 Raschka 的特定定义：基于 Transformer 的仅解码器语言模型，具体就是 GPT），所以弄准确这个基础非常重要。本章开始让一切变得更具体。</span>\n\nLet's start with a summary at the abstract level, though. Using my normal simplification that words == tokens, an LLM is a model that, given some input text, works out what the next word should be. In order to do this, it considers all of the words in the input text. Attention is a system that tells it, when looking at word *x* in the input, how much it should also take into account each of the other words in that input -- that is, how much *attention* it should pay to each other word. <span class=\"translation\">不过，我们先从抽象层面总结一下。沿用“单词 = 词元”的常规简化，LLM 是这样一种模型：给定一些输入文本，推断下一个词应该是什么。为此，它会考虑输入文本中的所有单词。注意力是一套系统：当模型查看输入中的单词 *x* 时，系统告诉它还应该在多大程度上考虑输入中的其他每个单词——也就是应该对每个其他词投入多少*注意力*。</span>\n\nSo, for the sentence <span class=\"translation\">所以，对于句子：</span>\n\n```\nThe fat cat sat on the mat\n```\n\n...when the LLM is considering \"cat\", it needs to focus on the first \"the\" (because it's a specific cat, not \"a cat\" -- sorry, Slavic language speakers, that *is* important ;-), \"sat\" because that is the verb that the cat is doing, \"fat\" because it applies to the cat, and perhaps to a lesser extent \"on\", \"the\", and \"mat\". Likewise, if the LLM is looking at \"sat\", then it's likely that \"cat\" and \"mat\" are the important bits as the subject and indirect objects of the verb, with the other words playing lesser parts. <span class=\"translation\">……当 LLM 考虑“cat”时，需要关注第一个“the”（因为这是特指的一只猫，不是“a cat”——抱歉，使用斯拉夫语的读者，这一点确实重要 ;-)）、“sat”（这是猫所做的动作）、“fat”（它修饰猫），并可能稍微关注“on”“the”和“mat”。同样，如果 LLM 查看“sat”，那么“cat”和“mat”很可能是作为动词主语和间接宾语的关键部分，其他词所起作用较小。</span>\n\nYou can imagine building up for each word in the sentence a list of numbers -- one for each other token. Here's an example of what that might look like, based on my own intuition about the importance of each word to the others in that sentence: <span class=\"translation\">你可以想象为句子中的每个词构建一个数字列表，每个其他词对应一个数字。下面是根据我对句中各词重要性的直觉得到的示例：</span>\n\n| Token | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\") | <span class=\"translation\">词元 | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\")</span>\n| --- | --- | --- | --- | --- | --- | --- | --- |\n| The | 1 | 0.3 | 0.75 | 0.1 | 0 | 0 | 0 | <span class=\"translation\">The（定冠词） | 1 | 0.3 | 0.75 | 0.1 | 0 | 0 | 0</span>\n| fat | 0.2 | 1 | 0.8 | 0 | 0 | 0 | 0 | <span class=\"translation\">fat（肥胖的） | 0.2 | 1 | 0.8 | 0 | 0 | 0 | 0</span>\n| cat | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4 | <span class=\"translation\">cat（猫） | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4</span>\n| sat | 0.1 | 0 | 0.85 | 1 | 0.3 | 0.2 | 0.75 | <span class=\"translation\">sat（坐下） | 0.1 | 0 | 0.85 | 1 | 0.3 | 0.2 | 0.75</span>\n| on | 0 | 0.1 | 0.4 | 0.6 | 1 | 0.3 | 0.8 | <span class=\"translation\">on（在……上） | 0 | 0.1 | 0.4 | 0.6 | 1 | 0.3 | 0.8</span>\n| the | 0 | 0 | 0 | 0 | 0.1 | 1 | 0.75 | <span class=\"translation\">the（定冠词） | 0 | 0 | 0 | 0 | 0.1 | 1 | 0.75</span>\n| mat | 0 | 0 | 0.2 | 0.8 | 0.7 | 0.6 | 1 | <span class=\"translation\">mat（垫子） | 0 | 0 | 0.2 | 0.8 | 0.7 | 0.6 | 1</span>\n\nTo unpack that -- each row in the table is a word in the sentence, and the numbers in that row are how much attention one might want to pay to the other words when trying to understand it. I've used a number from 0 (\"you can ignore it\") to 1 (\"this is super-important\"). But you can see that I've tried to represent the relative importance that I gave above for \"cat\" and \"sat\" numerically, and then done likewise for the other words. Note that each word is most attentive to itself, which makes intuitive sense to me :-) <span class=\"translation\">拆解一下：表格中的每一行对应句子里的一个词，该行数字表示在理解这个词时，可能希望对其他词投入多少注意力。我使用从 0（“可以忽略”）到 1（“极其重要”）的数值。你可以看到，我尝试把上文对“cat”和“sat”的相对重要性用数字表示出来，然后对其他词也做同样处理。注意，每个词对自己最关注，这对我来说很符合直觉 :-)。</span>\n\nNow, the cool thing is that this is exactly what attention mechanisms do. The reason I (slightly ab-)used the symbol ω to represent these numbers is because it's a symbol used to represent *attention scores*. When considering a particular token in the input sequence, the attention mechanism assigns every other token an attention score. This is then used to work out what to do with it. <span class=\"translation\">现在，酷的地方在于：这正是注意力机制所做的事情。我（稍微滥用地）用符号 ω 表示这些数字，是因为它通常用来表示*注意力分数*。当考虑输入序列中的某个词元时，注意力机制会为其他每个词元分配一个注意力分数，然后据此决定如何处理它。</span>\n\nAttention is all about building up data like this so that neural networks can understand language enough to be able to predict the next token when given something to start with. So we really have two questions: <span class=\"translation\">注意力的全部工作，就是构建这样的数据，让神经网络充分理解语言，从而在给定起始内容时预测下一个词元。因此我们实际上有两个问题：</span>\n\n-   How do we build up a system that can work out sets of attention scores across all tokens, for each token in our input sequence? <span class=\"translation\">如何构建一个系统，为输入序列中的每个词元计算所有词元的注意力分数集合？</span>\n-   What do we do with those lists of attention scores once we've got them? <span class=\"translation\">得到这些注意力分数列表后，要如何使用它们？</span>\n\nThe answer to the second one of those is actually the easiest, and Raschka explains that in this section -- this is the big \"aha\" moment for me in this post. We want to create, for each token, something called a *context vector*. <span class=\"translation\">第二个问题其实更容易回答，Raschka 在本节中解释了这一点——这是本文让我产生“啊哈”感觉的关键。我们希望为每个词元创建一种叫作*上下文向量*的东西。</span>\n\nSo far, our LLM has received a bunch of text, and has <span class=\"translation\">到目前为止，LLM 接收了一批文本，并且：</span>\n\n-   Split it into tokens. <span class=\"translation\">把文本拆分成词元。</span>\n-   For each token, it has generated two embeddings -- a *token embedding* that represents (or rather, will be trained to represent) in some manner the meaning of that token in isolation, and a *position embedding*, which will represent simply whether it's the first, second, third, or whatever token in the sequence. The element-wise sum of these two embeddings is called the *input embedding* for this token. <span class=\"translation\">为每个词元生成两个嵌入——一个是*词元嵌入*，以某种方式表示（或者说将通过训练来表示）该词元单独存在时的含义；另一个是*位置嵌入*，只表示它是序列中的第一个、第二个、第三个或其他位置。两个嵌入按元素相加，所得结果称为该词元的*输入嵌入*。</span>\n\nNow, for our token \"cat\" we have, at this stage, a token embedding that represents \"cat\" (and is probably similar to the embedding for \"kitten\", not too dissimilar to the embedding for \"dog\", and very different to the embedding for \"rain\"). We also have a position embedding that essentially says \"this is the third word in the input\". These two combined are the input embedding, and the purpose of the context vector is to produce a something numerical that represents \"cat\" in the context of all of the other words in the sentence, which not only needs to represent the token and its position but also the information from those other words. <span class=\"translation\">现在，对词元“cat”来说，我们有一个表示“cat”的词元嵌入（它可能与“kitten”的嵌入相似，与“dog”的嵌入不太相似，与“rain”的嵌入非常不同）。我们还有一个位置嵌入，大致表示“这是输入中的第三个词”。两者合在一起就是输入嵌入；上下文向量的目标，是生成某种数值表示，在整个句子其他词的背景下表达“cat”，既要包含词元及其位置，也要包含那些其他词提供的信息。</span>\n\nSo while the input embedding for \"cat\" just means \"cat in position 3\", the context vector for \"cat\" in this sentence also has some kind of overtones about it being a cat that is sitting, perhaps less strongly that it's a specific cat (\"the\" rather than \"a\"), and hints of it being sitting on a mat. Back at the start of the chapter, Raschka said that the attention mechanisms don't really understand the concept of ordering, so we need to pack in enough meaning here that that is also unimportant (rather like the endless declensions in Latin, I guess). <span class=\"translation\">因此，“cat”的输入嵌入只表示“位于第 3 个位置的 cat”，而这句话中“cat”的上下文向量还会带有它正在坐下、可能是特指的一只猫（“the”而不是“a”）以及坐在垫子上的暗示。回到本章开头，Raschka 说注意力机制并不真正理解顺序概念，所以我们需要在这里塞入足够多的含义，让顺序也变得不重要（有点像拉丁语无穷无尽的变格，我猜）。</span>\n\nBy contrast, the context for the first \"The\" would just be pretty much representing that it's the definite article and might also have some cattishness about it, and maybe a spot of fatness. And so on for the context vectors for all of the other words. As I understand it, it's the context vector that the next-token-prediction layer will care about -- not the word embedding. Which makes sense, it doesn't care about cats in general, it cares about this specific one in this sentence. <span class=\"translation\">相比之下，第一个“The”的上下文大致只表示它是定冠词，可能还带有一点与猫相关的意味，以及一点肥胖的意味。其他词的上下文向量也依此类推。据我理解，下一个词预测层关心的是上下文向量，而不是词嵌入。毕竟它关心的不是一般意义上的猫，而是这句话中的这只特定的猫。</span>\n\n> If you've read further on in the chapter you'll have noticed I'm playing a bit fast and loose in the above, because I'm not considering causal attention. But I think that's worth doing as an aid to intuition, at least my own, at this stage. <span class=\"translation\">如果你已经继续读了本章，会注意到上面的说法有点不严谨，因为我没有考虑因果注意力。但在现阶段，为了帮助直觉（至少帮助我自己的直觉），这样做是值得的。</span>\n\nThat means that the attention part of our LLM is basically a black box that takes in input embeddings, and for each token spits out a context vector that represents the token's meaning in the context of this particular input sequence, including those parts of the meaning that come from its position. (Multi-head attention, introduced later, will build on this a bit more.) <span class=\"translation\">这意味着，LLM 的注意力部分基本上是一个黑箱：接收输入嵌入，并为每个词元输出一个上下文向量，表示该词元在这段特定输入序列中的含义，包括来自其位置的那部分含义。（后文介绍的多头注意力会在此基础上进一步扩展。）</span>\n\nTo me the context vector feels a little bit like the hidden states that were used to transfer meaning from the encoder to the decoder in those original translation systems that I was writing about in my [last post in this series](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention); perhaps that's how people got from there to where we are now. It's essentially something like an embedding for this token in this context. <span class=\"translation\">对我来说，上下文向量有点像我在[本系列上一篇文章](https://www.gilesthomas.com/2025/01/llm-from-scratch-5-self-attention)中介绍的早期翻译系统里，用来把含义从编码器传给解码器的隐藏状态；也许人们就是从那里走到了今天。它本质上类似于“这个上下文中的这个词元”的嵌入。</span>\n\nSo, given a set of attention scores for a particular token -- one for each token in the input -- how do we go about getting this context vector for the token we're considering? <span class=\"translation\">那么，给定某个词元的一组注意力分数（输入中的每个词元对应一个），我们如何得到正在考虑的词元的上下文向量？</span>\n\nWell, for \"cat\" we want something that includes sitting, some hint of the mat, and so on, and we have these attention scores that relate how strongly the word \"cat\" is related to each other token. And each of those other tokens has an input embedding that is meant to represent its meaning and position. So we could do a pretty simple bit of maths to get something that represents them all taken together -- we just multiply all of the input embeddings by their respective attention weights and add them together! So to get the context vector for \"cat\" given this part of the attention score matrix above: <span class=\"translation\">对于“cat”，我们希望结果包含坐下、垫子的某种暗示等信息；而我们有注意力分数，表示“cat”与其他每个词元的关联强度。每个其他词元都有一个输入嵌入，表示它的含义和位置。因此可以做一段非常简单的数学运算，把它们整体表示出来——把所有输入嵌入分别乘以对应的注意力权重，然后相加！所以，根据上面注意力分数矩阵中的这一部分，要得到“cat”的上下文向量：</span>\n\n| Token | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\") | <span class=\"translation\">词元 | ω(\"The\") | ω(\"fat\") | ω(\"cat\") | ω(\"sat\") | ω(\"on\") | ω(\"the\") | ω(\"mat\")</span>\n| --- | --- | --- | --- | --- | --- | --- | --- |\n| cat | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4 | <span class=\"translation\">cat（猫） | 0.6 | 0.8 | 1 | 0.7 | 0.3 | 0.2 | 0.4</span>\n\nWe'd calculate 0.6 times the input embedding for \"The\", 0.8 times the input embedding for \"fat\", 1 times the input embedding for \"cat\", and so on, then add them all up. That might give us our context embedding for \"cat\" in this sentence. <span class=\"translation\">我们会计算 0.6 乘以“The”的输入嵌入、0.8 乘以“fat”的输入嵌入、1 乘以“cat”的输入嵌入，依此类推，然后把它们全部相加。这可能就得到这句话中“cat”的上下文嵌入。</span>\n\nAnd that is pretty much what happens! That, to me, is beautifully simple. <span class=\"translation\">实际情况基本就是这样！对我来说，这美妙地简单。</span>\n\nThere's only one extra wrinkle, which seems entirely reasonable -- the numbers I showed above would be called *attention scores*, and before multiplying the input embeddings for all of the tokens in the input sequence by their associated score, we convert them into *attention weights*, which are essentially the same numbers but normalised so that all of the weights for a given token sum up to 1 -- this is done by running them through the [Softmax function](https://en.wikipedia.org/wiki/Softmax_function#:~:text=The%20softmax%20function%2C%20also%20known,used%20in%20multinomial%20logistic%20regression.). This apparently works well with extreme values and turns negative numbers into small positive ones, both of which seem like good things (especially the second -- paying a negative amount of attention to one word when trying to understand another doesn't make any intuitive sense). <span class=\"translation\">只有一个额外的小变化，而且完全合理：我上面展示的数字叫作*注意力分数*；在把输入序列中所有词元的输入嵌入乘以各自分数之前，我们会把它们转换成*注意力权重*。它们本质上是相同的数字，但会归一化，使给定词元的所有权重之和为 1——具体做法是将它们送入 [Softmax 函数](https://en.wikipedia.org/wiki/Softmax_function#:~:text=The%20softmax%20function%2C%20also%20known,used%20in%20multinomial%20logistic%20regression.)。这种函数似乎能很好地处理极端值，并把负数变成较小的正数，这两点都很有用（尤其是后者：在理解一个词时对另一个词投入负的注意力，直觉上完全说不通）。</span>\n\nSo, for each token -- or rather the input embedding for each token -- we work out a vector of attention scores, which is the length of the full input sequence and for each item in the sequence, says how much attention to pay to it. We then run that vector through softmax to normalise it into attention weights, multiply each input embedding in the input sequence by its attention weight, and add together the results element-wise to get the context vector for the token we're considering. <span class=\"translation\">因此，对每个词元——更准确地说，对每个词元的输入嵌入——我们会计算一个注意力分数向量。它的长度等于完整输入序列的长度，向量中的每一项表示应对序列中的对应词元投入多少注意力。接着将该向量通过 softmax 归一化为注意力权重，用每个权重乘以输入序列中的相应输入嵌入，再按元素相加，得到当前词元的上下文向量。</span>\n\nMaybe it's worth sketching out some pseudocode to make this as clear as possible. Given a sequence of input embeddings (which, remember, are the token embeddings plus the position embeddings) called `inputs`, we can imagine it's doing this: <span class=\"translation\">也许可以写一些伪代码，让这一过程尽可能清楚。给定一个名为 `inputs` 的输入嵌入序列（记住，它们是词元嵌入加位置嵌入），可以想象代码这样运行：</span>\n\n```\noutput_context_vectors = []\nfor input_embedding in inputs:\n    attention_scores = get_attention_scores(input_embedding, inputs)\n    attention_weights = softmax(attention_scores)\n    context_vector = an empty vector of the same size as the input embeddings\n    for (other_input_embedding, attention_weight) in zip(inputs, attention_weights):\n        context_vector += attention_weight * other_input_embedding\n    output_context_vectors.append(context_vector)\n```\n\nNow, obviously this would not be the right way to implement it (all of that iterating over lists inside iterations over lists can be simplified into matrix multiplications). And of course, the `get_attention_scores` is magic -- and the next big thing to understand. But I think that pseudocode gives, at least for me, a clear idea of what is going on when building up a context vector given a vector of attention scores for each input token (or more strictly, its input embedding). <span class=\"translation\">显然，这不是正确的实现方式（在列表迭代中再进行列表迭代，完全可以简化成矩阵乘法）。当然，`get_attention_scores` 还是个魔法，也是接下来要理解的重点。但我认为，这段伪代码至少让我清楚看到：当每个输入词元（严格地说，是它的输入嵌入）都有一个注意力分数向量时，如何逐步构建上下文向量。</span>\n\nThat's pretty magical :-) But how do we get the attention scores in the first place? Or more accurately, how do we train something to do that? In this section, Raschka keeps things super-simple. As he says in the section title, this is a \"simple self-attention mechanism without trainable weights\". So instead of trying to write a real `get_attention_scores`, he just uses the dot product of the input embeddings to generate a placeholder one. The dot product takes two vectors and returns a scalar, so that means that the attention scores for \"cat\" in position 3 is a vector made up of: <span class=\"translation\">这相当神奇 :-)。但我们最初是如何得到注意力分数的？更准确地说，如何训练某个东西去完成这件事？本节中 Raschka 把问题保持得极其简单。正如节标题所说，这是“一种没有可训练权重的简单自注意力机制”。因此，他没有尝试编写真正的 `get_attention_scores`，而是直接使用输入嵌入的点积生成一个占位版本。点积接收两个向量并返回一个标量，所以位置 3 的“cat”对应的注意力分数是由以下各项组成的向量：</span>\n\n-   The input embedding for \"The\" in position 1, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 1 的“The”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"fat\" in position 2, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 2 的“fat”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"cat\" in position 3, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 3 的“cat”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"sat\" in position 4, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 4 的“sat”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"on\" in position 5, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 5 的“on”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"the\" in position 6, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 6 的“the”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n-   The input embedding for \"mat\" in position 7, dot-product the input embedding for \"cat\" in position 3 <span class=\"translation\">位置 7 的“mat”输入嵌入，与位置 3 的“cat”输入嵌入做点积</span>\n\nNow, the dot product for vectors is worked out by multiplying their elements individually and then adding up the results -- that is, <span class=\"translation\">向量点积的计算方式，是把对应元素分别相乘，再把结果相加，也就是：</span>\n\n```\n[1, 2, 3] . [4, 5, 6]\n-> [1 * 4, 2 * 5, 3 * 6]\n=  [4, 10, 18]\n-> 4 + 10 + 18\n= 32\n```\n\nThis is a non-crazy \"toy\" calculation to work out attention scores, because the dot product of two vectors is related to how similar they are in terms of their direction in the vector space (though they apparently should be normalised in some way for this). <span class=\"translation\">这是一个不疯狂的“玩具”注意力分数计算，因为两个向量的点积与它们在向量空间中的方向相似程度有关（不过显然还应该以某种方式进行归一化）。</span>\n\nBut it's not what we want for a real attention mechanism -- for example, in <span class=\"translation\">但它不是实际注意力机制想要的结果——例如在：</span>\n\n```\nThe fat cat sat on the mat\n```\n\n...if we ignore case, the two \"the\"s would be super-closely related to each other -- the position component of their input embeddings would be different, but the token component would be identical -- so they would have a really high attention score, probably much higher than any of the other pairs of words apart from each word's score against itself. There's almost nothing in there about the meaning of the sentence or how words relate to each other grammatically in this context. But it's a great starting point so that we can start implementing things before actually coding a real system for generating attention scores. And I'm pretty sure that the real attention score system will be using dot product somewhere, given their ability to say whether two things are similar -- it will just be a part of a larger mechanism. <span class=\"translation\">……如果忽略大小写，两个“the”会彼此高度相关——它们输入嵌入的位置成分不同，但词元成分相同——所以会得到非常高的注意力分数，可能远高于其他词对之间的分数（除了每个词与自身的分数）。其中几乎没有关于句子含义或词语在该语境中的语法关系的信息。不过，这是一个很好的起点，让我们可以在真正编写生成注意力分数的系统之前先开始实现。考虑到点积能够判断两个东西是否相似，我相当确定真实的注意力分数系统也会在某处使用点积，只是它会成为更大机制的一部分。</span>\n\nI'm going to wrap up there. The next section is working at a more implementational level -- given this simple dot-product way of working out the attention, how do we in practice calculate the context vectors for an input sequence in PyTorch, using matrix and other tensor operations -- so that everything can be done as a batch without complex loops like in my pseudocode above. That can be the topic for my next post. <span class=\"translation\">这篇文章就写到这里。下一节会进入更偏实现的层面：给定这种简单的点积注意力计算方式，如何在 PyTorch 中使用矩阵和其他张量运算，为输入序列计算上下文向量，从而无需像上面伪代码那样使用复杂循环，就能批量完成所有工作。这可以留到下一篇文章。</span>\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2025/02/llm-from-scratch-7-coding-self-attention-part-2). <span class=\"translation\">[这是本系列下一篇文章的链接](https://www.gilesthomas.com/2025/02/llm-from-scratch-7-coding-self-attention-part-2)。</span>\n\n\\[Update: the original version of this post had an error -- I was only considering token embeddings as an input to the attention mechanism rather than thinking in terms of input embeddings. The text above has been revised to fix that. More details [here](https://www.gilesthomas.com/2025/01/llm-from-scratch-6b-correction).\\]",
  "titleZh": "从零开始编写大型语言模型，第 6 部分——开始编写自注意力",
  "summaryZh": "第一次实现注意力机制：一个不可训练的版本。"
}
