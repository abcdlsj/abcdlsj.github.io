{"slug":"llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation","part":32,"title":"Writing an LLM from scratch, part 32k -- Interventions: training a better model locally with gradient accumulation","titleZh":"从零开始编写 LLM，第 32k 篇——干预：用梯度累积在本地训练最佳模型","shortTitle":"part 32k -- Interventions: training a better model locally with gradient accumulation","date":"15 April 2026","summary":"Having worked out which combination of interventions into my model and training run improved loss the most, it was time to see how well that worked with a local run, which meant I had to learn about gradient accumulation.","summaryZh":"我已经找出能最大幅度改善损失的干预组合，是时候在本地运行中验证它的效果了；这意味着我需要了解梯度累积。","href":"https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation","phaseId":"interventions","phaseLabel":"07 · 实验干预","markdown":"I've been working on a GPT-2-small-style LLM based on [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". I've trained various versions of it in the cloud to [work out which interventions to the model and training code](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) had the best effects on the loss it gets on a specific test dataset, and now I wanted to do a training run locally to match the best of those. For that, I wanted to match the batch size I was using for the cloud training runs.\n\nWhen I first started learning this stuff, batching seemed like a performance thing -- with highly parallel systems like GPUs, it generally turned out that you could run a batch of (say) two inputs through a model in less than twice the time you could run one, so it made sense to batch them up.\n\nFor inference, that is exactly the advantage you get, but when training, it's become increasingly clear to me that you can also get an improvement in the quality of the model from batching. The best intuitive model I have is that if you run inputs through one-by-one, adjusting parameters after each, then it's easy for the model to \"overcorrect\" each time. With batches, you get an average set of gradients across all of the items -- which smooths things out and stabilises the training.\n\nOf course, it's possible to overdo it. As an extreme example, imagine that you were somehow able to fit your whole training set into one batch -- then you could train by running that single batch through, doing a single backward pass, and then adjusting the parameters once. It's pretty clear that that would not work very well -- just one single update of the initially-random parameters.\n\nWhen training on my local machine, I could fit a batch of six sequences into my RTX 3090. I'd found that when I moved to cloud machines, it had a very positive effect on the loss I got out of the models when I tested them. From [a quick-and-dirty bit of curve-fitting](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud#the-results), I estimated that the optimal batch size for this model, with that training run, was somewhere around 97. Conveniently, that was close to the maximum I could fit onto an 8x A100 40 GiB/GPU machine, so I used a batch size of 96 to test the different interventions I was trying.\n\nAnd when I finally [put all of the interventions that helped with training together](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud), I found (somewhat to my surprise) that their combined effect -- an improvement in loss of 0.113765 -- was less than half of the loss improvement of 0.252474 that I had got from increasing the batch size.\n\nWhat that all made clear was that if I wanted to do a local training run that matched the quality of the cloud-trained model, I'd need to not only add on the interventions that I'd been testing in detail, but I'd need to match the cloud batch size. And for that, I needed to learn about gradient accumulation.\n\n### Gradient accumulation basics\n\nGradient accumulation is pretty much what it sounds like; instead of the normal technique of doing a forward pass, working out the loss, getting gradients with a backward pass, and then applying them by stepping the optimiser, you do multiple forward-backward phases, letting the gradients accumulate, and then do one optimiser step after that.\n\nWhen you do that, you're getting the training stabilisation benefits of a larger batch size, even though you're not getting the performance boost. Sounds simple enough, and it is, in theory, but implementation got a little more complicated.\n\nLet's work through it step-by-step.\n\nTo start with, imagine you have a really simple training loop:\n\n`for inputs, targets in batched_dataset:     # Other stuff      logits = model(inputs)     loss = calculate_loss(logits, targets)     loss.backward()      # Other stuff      optimizer.step()     optimizer.zero_grad()      # Other stuff`\n\nAdding gradient accumulation to that is really simple!\n\nLet's assume that `batched_dataset` has a length divisible by `gradient_accumulation_steps`, the number of steps we want to run through before we step the optimiser. As a first (not quite correct) cut, you could just do this:\n\n`for step, (inputs, targets) in enumerate(batched_dataset):     # Other stuff      logits = model(inputs)     loss = calculate_loss(logits, targets)     loss.backward()      # Other stuff      if (step + 1) % gradient_accumulation_steps == 0 or step == len(batched_dataset) - 1:         optimizer.step()         optimizer.zero_grad()      # Other stuff`\n\nYou can see that we're just stepping the optimiser every `gradient_accumulation_steps` steps. An alternative way to do it would be with an inner loop:\n\n`dataset_ix = 0 while dataset_ix < len(batched_dataset):     # Other stuff      for ii in range(gradient_accumulation_steps):         if dataset_ix >= len(batched_dataset):             break         inputs, targets = batched_dataset[dataset_ix]         logits = model(inputs)         loss = calculate_loss(logits, targets)         loss.backward()         dataset_ix += 1      # Other stuff      optimizer.step()     optimizer.zero_grad()      # Other stuff`\n\nWhich of those is better would depend on the details of the training loop -- in general, if you wanted the \"other stuff\" to be done once per training batch, then you'd want to use the first option, whereas if you wanted it to be done once per optimiser step, the second would be easier. As you'll see in a bit, I went for the second one for my code.\n\nHowever, there's one small correction that we need to do to make either of these properly. Remember that when you calculate loss across a batch -- for example, cross entropy loss like this:\n\n`torch.nn.functional.cross_entropy(logits.flatten(0, 1), targets.flatten())`\n\n...you're getting the *average* loss across the batch, so when you do the backward pass, you're getting the average gradients. By contrast, in the code above, we're doing a backward pass on the complete loss at each step, so the gradients that are being generated in each backward pass are being added to each other -- you wind up with the sum of all of them rather than the average. So the gradients that the optimizer applied would be `gradient_accumulation_steps` times larger than they should be -- it would be as if we'd multiplied the learning rate by that number!\n\nBut that's easy enough to fix. The average gradients over a number of steps are the sum divided by the number of steps, and we can do that division ahead of time just by scaling the loss down. Adding that into the first example above:\n\n`for step, (inputs, targets) in enumerate(batched_dataset):     logits = model(inputs)     loss = calculate_loss(logits, targets)     (loss / gradient_accumulation_steps).backward()      if (step + 1) % gradient_accumulation_steps == 0 or step == len(batched_dataset) - 1:         optimizer.step()         optimizer.zero_grad()`\n\nAnd that's basically it; with those changes, the original basic training loop becomes one that uses gradient accumulation. The effective batch size is whatever the real batch size is, times the number of gradient accumulation steps.\n\n### Gradient accumulation in practice\n\nHowever, the real training loop that I'm using for these experiments is a bit more complicated than that simple example. There's checkpointing, AMP, and -- most importantly -- it can handle multi-GPU training using DistributedDataParallel. That made things a little bit more complicated.\n\nThe first thing was to look into the way I was selecting the data to train on. My dataset was already in batches, but we had to split those batches up between GPUs. The solution in the code was to work out how many global steps there were -- each global step being one batch going through each GPU on the machine -- like this:\n\n`total_global_steps = len(train_ds) // world_size`\n\n`world_size`, if you remember from [the DDP post](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud), is the number of processes running in a multi-GPU training run -- one per GPU.\n\nNext, in the training loop, I iterated over the global steps:\n\n`progress_bar = tqdm(     range(start_global_step, total_global_steps),     disable=(rank != 0) ) for global_step in progress_bar:     # Get the data     # Forward and backward pass     # Step the optimiser.`\n\n...for each one, getting the appropriate batch out for the specific GPU that was running the code:\n\n    `inputs, targets = train_ds[global_step * world_size + rank]`\n\n`rank` is a zero-indexed number, unique to each of the per-GPU processes. So this basically split `train_ds` into chunks of length `world_size`, and then each GPU was fed the batch at its `rank`'s offset into the chunk.\n\nI wanted to keep things shaped such that when I was running with gradient accumulation locally, it would be similar to a cloud run with per-GPU batching. Specifically: when I was training in the cloud, I had eight GPUs with a per-GPU microbatch size of 12, giving a total batch size of 96. Locally, I could fit a batch size of six on my GPU, so I needed to do gradient accumulation over 96 / 6 = 16 steps.\n\nTo keep things as similar as possible, I decided that I wanted the concept of a \"global step\" to match between the runs. In other words, it would expand slightly, from meaning \"one batch per GPU\" to being \"one optimiser step per GPU\". So, each time through that `global_step` loop, we'd do multiple forward-backward passes, and then one optimiser step. That would mean that the best way to do things would be with something much more like the second of the two bits of sample code above -- the one with the inner loop rather than the modulus.\n\nMaybe that's easier to show in code:\n\n`total_global_steps = (len(train_ds) // world_size) // gradient_accumulation_steps  ...  for global_step in progress_bar:     ...     for accumulation_step in range(gradient_accumulation_steps):         # Forward and backward passes         ...      # Step the optimiser`\n\nThat required a change to the data lookup; I decided that `train_ds` would be split into chunks of size `gradient_accumulation_steps * world_size`, and then each of those would be split into chunks of size `world_size`, so the code to get the appropriate batch for a given run through the loop became this:\n\n`inputs, targets = train_ds[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]`\n\nThat required a corresponding change in `load_dataset` to make sure that `train_ds` was divisible by both the world size, the per-GPU batch (\"microbatch\") size, and the number of gradient accumulation steps, but that was easy:\n\n`one_full_batch_tokens = world_size * microbatch_size * seq_length`\n\n...became this:\n\n`one_full_batch_tokens = world_size * microbatch_size * gradient_accumulation_steps * seq_length`\n\nThat was enough to get the gradient accumulation happening! Next, I needed to change the backward pass code to scale down the loss so that we got averaged rather than summed gradients. Because we might be using AMP with a scaler, the code wasn't just a simple `loss.backward`:\n\n`if scaler is not None:     scaler.scale(train_loss).backward() else:     train_loss.backward()`\n\n...but the change was obvious enough:\n\n`if scaler is not None:     scaler.scale(train_loss / gradient_accumulation_steps).backward() else:     (train_loss / gradient_accumulation_steps).backward()`\n\nAll of those changes put together, plus a bit of shuffling around of code, were enough to get a correct gradient accumulation training loop! But there was one small tweak I needed to add.\n\nWhen you're using DDP, gradients need to be synchronised between the different per-GPU processes. As a reminder, what happens is:\n\n-   Each process does a forward pass.\n-   Each process does a backward pass.\n-   When they have the gradients, they essentially share them so that each process has an average of the gradients from all of those backward passes.\n-   Then they all step their optimisers to apply the average gradients to each process's copy of the model.\n\nNow, with my first cut of the gradient accumulation code above, what would have happened is this:\n\n-   For each gradient accumulation step:\n    -   Each process does a forward pass.\n    -   Each process does a backward pass.\n    -   The average is worked out\n-   They all step their optimisers based on the most recent average\n\nThat would be correct, but not very efficient. We're sending out gradients and averaging on every accumulation step. But because each of our per-GPU processes is keeping its own \"local\" average (by accumulating the scaled-down gradients), we only really need to send those local averages out and get a global average once, just before we step the optimiser. If we do that, we can save quite a lot of work.\n\nThe trick to avoid that was to use the method `no_sync` on the `DistributedDataParallel` class that our own model is wrapped in. What we wanted to do was suppress the gradient synchronisation for each of the accumulation steps apart from the last one.\n\nIt was easy to work out whether we were on the last gradient accumulation step:\n\n`is_last = accumulation_step == gradient_accumulation_steps - 1`\n\nNow, what we needed to do was to wrap this:\n\n`if scaler is not None:     scaler.scale(train_loss / gradient_accumulation_steps).backward() else:     (train_loss / gradient_accumulation_steps).backward()`\n\n...in `model.no_sync`, but *only if* `is_last` was false.\n\nConditional `with` statements can be a little fiddly, but Python has a \"do-nothing\" [`nullcontext` context manager in `contextlib`](https://docs.python.org/3/library/contextlib.html#contextlib.nullcontext) -- that is,\n\n`with nullcontext():     do_something()`\n\n...is identical to just:\n\n`do_something()`\n\nSo we can combine that with the ternary operator like this:\n\n    `with model.no_sync() if not is_last else nullcontext():         if scaler is not None:             scaler.scale(train_loss / gradient_accumulation_steps).backward()         else:             (train_loss / gradient_accumulation_steps).backward()`\n\n...which does exactly what we want [1](#fn-1).\n\nWith that change, I had something I was happy with; you can [see the diff here](https://github.com/gpjt/ddp-base-model-from-scratch/commit/c5d860e29848287abd2c1f9af11a8b849d260aba).\n\nSo now it was time to do a training run!\n\n### The new local baseline\n\nI'd originally been planning to jump right in and do a training run based on my [last cloud run](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud), with all of the interventions I'd decided were worth using, but locally with gradient accumulation.\n\nHowever, I decided that it would be interesting to try doing a new \"baseline\" train first. I'd done my local training runs, and then established a baseline version in the cloud by taking exactly the same configuration and doing the training run on an 8x A100 40 GiB with an overall batch size of 96. So I could repeat that locally with gradient accumulation, and that would show two things (or perhaps, the same thing but in different lights):\n\n-   Whether the increased effective batch size had as positive an effect on the loss as the increased real batch size did when I did my cloud runs.\n-   Whether the locally-trained gradient accumulation model was similar to the cloud-trained big-batch model in terms of its loss.\n\nThat would help confirm my understanding that it was the increased batch size that helped in the cloud, and not, say, some architectural difference -- and would also act as a good test of the gradient accumulation code.\n\nHere's [the training run config](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-baseline). I kicked it off:\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (main)$ uv run torchrun --nproc_per_node=1 ddp_train.py 1xrtx3090-baseline datasets/ Fetching 4 files: 100%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 4/4 [00:00<00:00, 810.65it/s] Starting rank 0 training at global step 0   0%|                                                                                                                  | 0/33165 [00:04<?, ?it/s, loss=10.991, tps=20,451]  Checkpoint  Continuing training   0%|                                                                                                       | 23/33165 [01:50<43:29:50,  4.72s/it, loss=7.625, tps=20,549]`\n\nThat looked like the right number of global steps; it matched the numbers I saw when training in the cloud. And 44 hours for the training run seemed correct: my original local runs took 48, but with them I was spending quite a lot of time on validation, which this code didn't do.\n\nJust less than two days later:\n\n```\nTraining complete in 155,402.289 seconds\nTokens seen: 3,260,252,160\nThroughput: 20,979 tokens/second\nFinal train loss: 3.738\n```\n\nThat all looked good. The loss chart looked like this:\n\n![The loss chart for the local baseline train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/baseline-loss-chart.png \"The loss chart for the local baseline train\")\n\nFor comparison, here's the one from the cloud training run with the same config (but using larger batches rather than gradient accumulation):\n\n![Baseline training run on an 8x A100 with 40 GiB/GPU](https://www.gilesthomas.com/post-assets/llm-from-scratch-32a-interventions-baseline-model/baseline-training-run-chart.png \"Baseline training run on an 8x A100 with 40 GiB/GPU\")\n\nYou can see that they're similar, but not identical. That's pretty much what you'd expect! The two training runs were on different architectures -- RTX 3090 vs A100 -- and so there will probably be differences in the CUDA kernels, and also PyTorch's AMP (which uses 16-bit instead of 32-bit in cases where it makes sense) might make different decisions. I think that if we'd run it on a machine with one A100, then the results of using gradient accumulation would be even closer (perhaps even identical) to a larger batch size, especially if we were training without AMP.\n\nI [uploaded the model to Hugging Face](https://huggingface.co/gpjt/1xrtx3090-baseline) and it was time for the evals. The smoke test first:\n\n```\nEvery effort moves you to a more realistic approach and is always welcome, especially for a younger child!\nI’\n```\n\nAs usual, reasonably coherent. But the important one was the loss on the test set:\n\n```\nLoss against our test dataset: 3.683835\n```\n\nThat's solid! The cloud-trained baseline model got 3.691526, so this local one was actually very slightly better, by 0.007691. But that's very close indeed, which is what we wanted to see :-)\n\nIt was time to see what effect adding on the interventions would have.\n\n### The local run with the interventions\n\nAs a reminder, here are the changes I made to the config for this run:\n\n-   Gradient clipping at 3.5\n-   Learning rate changed from 0.0004 to 0.0014, with a warmup over 5% of the run then a cosine decay to 0.00014.\n-   Weight decay changed from 0.1 to 0.01\n-   Dropout removed\n\nIt did not include QKV bias.\n\n[Here's the config](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-stacked-interventions). I kicked it off, and:\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (main)$ uv run torchrun --nproc_per_node=1 ddp_train.py 1xrtx3090-stacked-interventions datasets Fetching 4 files: 100%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 4/4 [00:00<00:00, 749.32it/s] Starting rank 0 training at global step 0   0%|                                                                                                                  | 0/33165 [00:04<?, ?it/s, loss=10.994, tps=21,589]  Checkpoint  Continuing training   0%|                                                                                                      | 19/33165 [01:25<40:52:06,  4.44s/it, loss=10.318, tps=21,867]`\n\nIt looked like it was going to take 40 hours; that matched what happened in the cloud runs, as removing dropout speeds things up quite a lot.\n\nJust less than two days later:\n\n```\nTraining complete in 146,299.816 seconds\nTokens seen: 3,260,252,160\nThroughput: 22,285 tokens/second\nFinal train loss: 3.519\n```\n\nThe loss chart over the training run looked like this:\n\n![The loss chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-loss-chart.png \"The loss chart for the local interventions train\")\n\nThat's very smooth, with no loss spikes. For comparison, here's the chart when we did the same training run in the cloud; you can see that it was a bit choppier than the local one.\n\n![The loss chart for the cloud interventions run](https://www.gilesthomas.com/post-assets/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud/stacked-interventions-1-loss-chart.png \"The loss chart for the cloud interventions run\")\n\nThe gradient norm chart was also interesting:\n\n![The gradient norm chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-grad-norm-chart.png \"The gradient norm chart for the local interventions train\")\n\nIf you compare it to the one from the cloud training run below, you can see that the local one was actually noisier -- the cloud run has a few gradient spikes near the start but calms down from around global step 6,000 or so, whereas the local one is spiky up to about 3,000, then calm, but has a massive spike at around 10,000.\n\n![The gradient norm chart for the cloud interventions run](https://www.gilesthomas.com/post-assets/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud/stacked-interventions-1-grad-norm-chart.png \"The gradient norm chart for the cloud interventions run\")\n\nThe learning rate we don't need to compare, but it was worth sanity checking to make sure we really did train the right way:\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nSo that all looked good. The training run did have some differences to the cloud one, but (as with the previous baseline train) it looked similar enough. Architectural differences between the A100s in the cloud and the local RTX 3090 seemed like a plausible cause.\n\n### Evals\n\nI [uploaded the model to Hugging Face](https://huggingface.co/gpjt/1xrtx3090-stacked-interventions), and it was time to run the evals. The smoke test first:\n\n```\nEvery effort moves you to give your customers the opportunity you give them in the event of a failure.<|endoftext|>A couple friends\n```\n\nReasonably coherent -- and I think that's the first time I've seen an `<|endoftext|>` token in a smoke test output! But the important one is, as ever, the loss, and:\n\n```\nLoss against our test dataset: 3.538161\n```\n\nLet's add both this one and the local baseline to the results table for all interventions:\n\n|  | Test set loss | Improvement vs baseline |\n| --- | --- | --- |\n| [8xa100m40-weight-tying](https://www.gilesthomas.com/2026/03/llm-from-scratch-32g-interventions-weight-tying) | 3.874305 | \\-0.182779 |\n| [8xa100m40-weight-decay-cerebras](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.813856 | \\-0.122330 |\n| [8xa100m40-baseline](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) | 3.691526 | \\- |\n| [1xrtx3090-baseline](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.683835 | 0.007691 |\n| [8xa100m80-no-amp](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32) | 3.678968 | 0.012558 |\n| [8xa100m40-gradient-clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) | 3.678317 | 0.013209 |\n| [8xa100m40-qkv-bias](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias) | 3.669385 | 0.022141 |\n| [8xa100m40-weight-decay-gpt2](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.642940 | 0.048586 |\n| [8xa100m40-remove-dropout](https://www.gilesthomas.com/2026/02/llm-from-scratch-32c-interventions-removing-dropout) | 3.641282 | 0.050244 |\n| [8xa100m40-stacked-interventions-2](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.604342 | 0.087184 |\n| [8xa100m40-schedule-learning-rate](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate) | 3.601917 | 0.089609 |\n| [8xa100m40-stacked-interventions-3](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.590266 | 0.101260 |\n| [8xa100m40-stacked-interventions-1](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.577761 | 0.113765 |\n| [1xrtx3090-stacked-interventions](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.538161 | 0.153365 |\n\nThat's really weird! The local run with the interventions, `1xrtx3090-stacked-interventions`, is 0.039600 points better than the cloud version of the same training run, `8xa100m40-stacked-interventions-1`. That's nice, in that lower loss is always better, but it's also rather confusing -- that's a bigger loss improvement than some of the interventions.\n\nIn theory, all that we changed between the cloud version of this training run, and the local one was the architecture. I was expecting that to have an effect, but thought that it would be small -- as, indeed, it was with the baseline trains `8xa100m40-baseline` and `1xrtx3090-baseline`, where you can see the loss difference was just 0.007691 -- about five times smaller.\n\nNow, when I was looking into [the effects of noise on training loss](https://www.gilesthomas.com/2026/04/llm-from-scratch-32i-interventions-what-is-in-the-noise), I found that changing the random seed that was used to initialise the weights (but starting the training run itself at the same random seed) had a much bigger effect on the resulting model quality than keeping the weights identical but varying the seed at the start of the post-initialisation phase of the training run. The standard deviation of the varied-weights, same-train models was about double the SD of the same-weights, varied-train.\n\nThat was interesting, though not directly comparable -- those tests were done with the same training run, but the architecture held constant -- a 8x A100 40 GiB machine for each test.\n\nHowever, it felt like it would be a good idea to at least see whether we started with the same weights locally and when training in the cloud. My suspicion was that we probably would; the weight initialisation uses deterministic non-GPU code, so with the same seed we'd expect the same weights regardless of the computer. The similarity of the loss results for the local and cloud baseline training runs also seemed to point in that direction.\n\nBut it was worth testing. I created a throwaway branch of the training code, which -- after creating the model -- just dumped the model weights to a file, then exited. I ran it locally using the `1xrtx3090-stacked-interventions` config, and then I fired up yet another 8x A100 40 GiB machine on Lambda, ran the same code there, this time with the `8xa100m40-stacked-interventions-1` config, and then `scp`ed down the weights.\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (dump-weights)$ diff cloud-init-weights.safetensors local-init-weights.safetensors giles@perry:~/Dev/ddp-base-model-from-scratch (dump-weights)$`\n\nIdentical. That was reassuring!\n\nI considered doing more analysis on this; for example, in my investigations into noise, I found that keeping the same weights but altering the random seed for the rest of the training run, I got results with a standard deviation of 0.008672 -- more than four times smaller than the difference between the local and cloud trains with the interventions. Might that be a number I could use for some kind of comparison?\n\nHowever, I decided that it's not really comparable. That number was from varying the random seed, but keeping the same architecture. There's not really any solid reason to believe that keeping the seed constant but changing the architecture would cause the same kind of differences. They might be more similar, they might be less.\n\nI think that all we can really say here is that the change of machine changed some aspects of the training dynamics in a way that happened to get us a lower loss. I can easily imagine that if I'd done something slightly different -- used a local RTX 4090, for example -- it could equally well have gone in the other direction.\n\nAnd at least it's reassuring that the improvement was smaller than the interventions I was most convinced by; the only smaller ones were full-fat float32, gradient clipping, and QKV bias -- ones that I'd already decided might have only been beneficial due to noise. Most importantly, it was orders of magnitude smaller than the 0.252474 improvement I originally saw when I moved from local training to larger-batch cloud training.\n\n### Conclusion\n\nSo, I think that that brings me to the end of this set of training experiments. We started with a locally-trained model that got a loss of 3.943522 on our test set, compared to the original GPT-2 small model, which got 3.499677 [2](#fn-2).\n\nI've tried a bunch of interventions to try to get my model closer, and finally I've managed to get almost all of the way there, to 3.538161. That's really pleasing!\n\nI think that there are two things to do before I can fully wrap up this \"interventions\" mini-series, and get back to the main-line LLM from scratch stuff.\n\nFirstly, I should revisit the instruction fine-tuning tests, which I put on hold while doing these training runs. That would give us some indication as to whether the loss improvement was just a technical improvement that made a number go down, or whether it actually improved the usefulness of the model.\n\nSecondly, I think I really need to write a wrap-up. I've been working on this stuff on and off since December, and I think a summary of what I did would be quite nice!\n\nI'll post soon; don't touch that dial :-)\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests).\n\n* * *\n\n1.  Thanks to [this Stack Overflow answer](https://stackoverflow.com/a/34798330) for that trick. [↩](#fnref-1)\n    \n2.  I'm going to switch to six decimal places from now on -- previously I was rounding it to three, hence 3.500. [↩](#fnref-2)","markdownBilingual":"I've been working on a GPT-2-small-style LLM based on [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". I've trained various versions of it in the cloud to [work out which interventions to the model and training code](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) had the best effects on the loss it gets on a specific test dataset, and now I wanted to do a training run locally to match the best of those. For that, I wanted to match the batch size I was using for the cloud training runs. <span class=\"translation\">我一直在根据[塞巴斯蒂安·拉斯卡](https://sebastianraschka.com/)的书“[构建大型语言模型（从头开始）](https://www.manning.com/books/build-a-large-language-model-from-scratch)”开发GPT-2-small-style大语言模型（LLM）。我在云端有训练过的各种版本，[找出对模型和训练代码进行哪些干预](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) 在特定测试数据集上对损失具有最佳效果，现在我想在本地做一个训练运行以匹配其中最好的版本。为此，我想匹配我用于云的批大小训练运行。</span>\n\nWhen I first started learning this stuff, batching seemed like a performance thing -- with highly parallel systems like GPUs, it generally turned out that you could run a batch of (say) two inputs through a model in less than twice the time you could run one, so it made sense to batch them up. <span class=\"translation\">当我第一次开始学习这些东西时，批处理似乎是一个性能问题——对于像 GPU 这样的高度并行系统，通常结果表明，您可以通过模型处理一批（比如说）两个输入，所需时间不到运行一个输入的两倍，因此对它们进行批处理是有意义的。</span>\n\nFor inference, that is exactly the advantage you get, but when training, it's become increasingly clear to me that you can also get an improvement in the quality of the model from batching. The best intuitive model I have is that if you run inputs through one-by-one, adjusting parameters after each, then it's easy for the model to \"overcorrect\" each time. With batches, you get an average set of gradients across all of the items -- which smooths things out and stabilises the training. <span class=\"translation\">作为推断，这正是您获得的优势，但是当训练时，我越来越清楚，您还可以通过批处理来提高模型的质量。我最直观的模型是，如果你运行通过one-by-one输入，每次调整参数，那么模型每次都很容易“矫枉过正”。通过批次，您可以获得所有项目的平均梯度集 - 这可以使事情变得平滑并稳定训练。</span>\n\nOf course, it's possible to overdo it. As an extreme example, imagine that you were somehow able to fit your whole training set into one batch -- then you could train by running that single batch through, doing a single backward pass, and then adjusting the parameters once. It's pretty clear that that would not work very well -- just one single update of the initially-random parameters. <span class=\"translation\">当然，也有可能做得太过分。举一个极端的例子，假设您能够以某种方式将整个训练集放入一个批次中 - 然后您可以通过运行该批次，执行一次向后传递，然后调整参数一次来实现训练。很明显，这不会很好地工作——只需对最初随机的参数进行一次更新。</span>\n\nWhen training on my local machine, I could fit a batch of six sequences into my RTX 3090. I'd found that when I moved to cloud machines, it had a very positive effect on the loss I got out of the models when I tested them. From [a quick-and-dirty bit of curve-fitting](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud#the-results), I estimated that the optimal batch size for this model, with that training run, was somewhere around 97. Conveniently, that was close to the maximum I could fit onto an 8x A100 40 GiB/GPU machine, so I used a batch size of 96 to test the different interventions I was trying. <span class=\"translation\">当训练在我的本地机器上时，我可以将一批六个序列放入我的RTX 3090中。我发现当我转移到云机器时，它对我在测试模型时得到的损失产生了非常积极的影响。从[一些 quick-and-dirty 曲线拟合](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud#the-results)开始，我估计这个模型和训练运行的最佳批大小约为97。方便的是，这接近我可以安装到8x A100 40 GiB/GPU机器上的最大值，因此我使用96的批大小来测试我的不同干预措施正在尝试。</span>\n\nAnd when I finally [put all of the interventions that helped with training together](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud), I found (somewhat to my surprise) that their combined effect -- an improvement in loss of 0.113765 -- was less than half of the loss improvement of 0.252474 that I had got from increasing the batch size. <span class=\"translation\">当我最终[将所有有助于训练的干预措施放在一起](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud)时，我发现（有点令我惊讶）它们的综合效果——损失改善了0.113765——还不到我通过增加批大小而获得的损失改善0.252474的一半。</span>\n\nWhat that all made clear was that if I wanted to do a local training run that matched the quality of the cloud-trained model, I'd need to not only add on the interventions that I'd been testing in detail, but I'd need to match the cloud batch size. And for that, I needed to learn about gradient accumulation. <span class=\"translation\">这一切都清楚地表明，如果我想做一个与云质量相匹配的本地训练运行-训练过模型，我不仅需要添加我一直在详细测试的干预措施，而且还需要与云批大小相匹配。为此，我需要了解梯度累积。</span>\n\n### Gradient accumulation basics <span class=\"translation\">梯度累积基础知识</span>\n\nGradient accumulation is pretty much what it sounds like; instead of the normal technique of doing a forward pass, working out the loss, getting gradients with a backward pass, and then applying them by stepping the optimiser, you do multiple forward-backward phases, letting the gradients accumulate, and then do one optimiser step after that. <span class=\"translation\">梯度累积就是它听起来的样子；与进行前向传递、计算损失、通过后向传递获取梯度、然后通过步进优化器应用它们的常规技术不同，您可以执行多个前向-后向阶段，让梯度累积，然后再执行一个优化器步骤。</span>\n\nWhen you do that, you're getting the training stabilisation benefits of a larger batch size, even though you're not getting the performance boost. Sounds simple enough, and it is, in theory, but implementation got a little more complicated. <span class=\"translation\">当您这样做时，您将获得较大批大小的训练稳定性优势，即使您没有获得性能提升。听起来很简单，理论上也确实如此，但实施起来有点复杂。</span>\n\nLet's work through it step-by-step. <span class=\"translation\">让我们来解决它step-by-step。</span>\n\nTo start with, imagine you have a really simple training loop: <span class=\"translation\">首先，假设您有一个非常简单的训练循环：</span>\n\n`for inputs, targets in batched_dataset:     # Other stuff      logits = model(inputs)     loss = calculate_loss(logits, targets)     loss.backward()      # Other stuff      optimizer.step()     optimizer.zero_grad()      # Other stuff`\n\nAdding gradient accumulation to that is really simple! <span class=\"translation\">添加梯度累积非常简单！</span>\n\nLet's assume that `batched_dataset` has a length divisible by `gradient_accumulation_steps`, the number of steps we want to run through before we step the optimiser. As a first (not quite correct) cut, you could just do this: <span class=\"translation\">假设 `batched_dataset` 的长度可被 `gradient_accumulation_steps` 整除，即我们在步进优化器之前需要运行执行的步数。作为第一次（不太正确）切割，你可以这样做：</span>\n\n`for step, (inputs, targets) in enumerate(batched_dataset):     # Other stuff      logits = model(inputs)     loss = calculate_loss(logits, targets)     loss.backward()      # Other stuff      if (step + 1) % gradient_accumulation_steps == 0 or step == len(batched_dataset) - 1:         optimizer.step()         optimizer.zero_grad()      # Other stuff`\n\nYou can see that we're just stepping the optimiser every `gradient_accumulation_steps` steps. An alternative way to do it would be with an inner loop: <span class=\"translation\">您可以看到，我们只是每 `gradient_accumulation_steps` 步就步进优化器。另一种方法是使用内部循环：</span>\n\n`dataset_ix = 0 while dataset_ix < len(batched_dataset):     # Other stuff      for ii in range(gradient_accumulation_steps):         if dataset_ix >= len(batched_dataset):             break         inputs, targets = batched_dataset[dataset_ix]         logits = model(inputs)         loss = calculate_loss(logits, targets)         loss.backward()         dataset_ix += 1      # Other stuff      optimizer.step()     optimizer.zero_grad()      # Other stuff`\n\nWhich of those is better would depend on the details of the training loop -- in general, if you wanted the \"other stuff\" to be done once per training batch, then you'd want to use the first option, whereas if you wanted it to be done once per optimiser step, the second would be easier. As you'll see in a bit, I went for the second one for my code. <span class=\"translation\">其中哪个更好取决于训练循环的细节 - 一般来说，如果您希望每个训练批次完成一次“其他内容”，那么您需要使用第一个选项，而如果您希望每个优化器步骤完成一次，第二个选项会更容易。正如您稍后会看到的，我为我的代码选择了第二个。</span>\n\nHowever, there's one small correction that we need to do to make either of these properly. Remember that when you calculate loss across a batch -- for example, cross entropy loss like this: <span class=\"translation\">然而，为了正确地实现其中任何一个，我们需要做一个小修正。请记住，当您跨批次计算损失时 - 例如，交叉熵损失如下所示：</span>\n\n`torch.nn.functional.cross_entropy(logits.flatten(0, 1), targets.flatten())`\n\n...you're getting the *average* loss across the batch, so when you do the backward pass, you're getting the average gradients. By contrast, in the code above, we're doing a backward pass on the complete loss at each step, so the gradients that are being generated in each backward pass are being added to each other -- you wind up with the sum of all of them rather than the average. So the gradients that the optimizer applied would be `gradient_accumulation_steps` times larger than they should be -- it would be as if we'd multiplied the learning rate by that number! <span class=\"translation\">...您将获得整个批次的*平均*损失，因此当您进行向后传递时，您将获得平均梯度。相比之下，在上面的代码中，我们在每一步都对完整的损失进行向后传递，因此每次向后传递中生成的梯度都会相互相加——最终得到的是所有梯度的总和，而不是平均值。因此，优化器应用的梯度将比应有的值大 `gradient_accumulation_steps` 倍——就好像我们将学习率乘以了该数字！</span>\n\nBut that's easy enough to fix. The average gradients over a number of steps are the sum divided by the number of steps, and we can do that division ahead of time just by scaling the loss down. Adding that into the first example above: <span class=\"translation\">但这很容易解决。多个步骤的平均梯度是总和除以步骤数，我们可以通过缩小损失来提前进行除法。将其添加到上面的第一个示例中：</span>\n\n`for step, (inputs, targets) in enumerate(batched_dataset):     logits = model(inputs)     loss = calculate_loss(logits, targets)     (loss / gradient_accumulation_steps).backward()      if (step + 1) % gradient_accumulation_steps == 0 or step == len(batched_dataset) - 1:         optimizer.step()         optimizer.zero_grad()`\n\nAnd that's basically it; with those changes, the original basic training loop becomes one that uses gradient accumulation. The effective batch size is whatever the real batch size is, times the number of gradient accumulation steps. <span class=\"translation\">基本上就是这样；经过这些更改，原来的基本训练循环变成了使用梯度累积的循环。有效批大小是实际的批大小乘以梯度累积步数。</span>\n\n### Gradient accumulation in practice <span class=\"translation\">梯度累积实践</span>\n\nHowever, the real training loop that I'm using for these experiments is a bit more complicated than that simple example. There's checkpointing, AMP, and -- most importantly -- it can handle multi-GPU training using DistributedDataParallel. That made things a little bit more complicated. <span class=\"translation\">然而，我用于这些实验的真正的训练循环比这个简单的示例要复杂一些。有检查点、AMP，最重要的是，它可以使用 DistributedDataParallel 处理多个GPU训练。这让事情变得有点复杂了。</span>\n\nThe first thing was to look into the way I was selecting the data to train on. My dataset was already in batches, but we had to split those batches up between GPUs. The solution in the code was to work out how many global steps there were -- each global step being one batch going through each GPU on the machine -- like this: <span class=\"translation\">第一件事是研究我选择数据到训练的方式。我的数据集已经成批了，但我们必须在 GPU 之间拆分这些批次。代码中的解决方案是计算出有多少个全局步骤——每个全局步骤都是一批遍历机器上的每个GPU——如下所示：</span>\n\n`total_global_steps = len(train_ds) // world_size`\n\n`world_size`, if you remember from [the DDP post](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud), is the number of processes running in a multi-GPU training run -- one per GPU. <span class=\"translation\">如果您还记得 [DDP 帖子](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud)，`world_size` 是多GPU训练运行中运行的进程数——每个 GPU 一个。</span>\n\nNext, in the training loop, I iterated over the global steps: <span class=\"translation\">接下来，在训练循环中，我迭代了全局步骤：</span>\n\n`progress_bar = tqdm(     range(start_global_step, total_global_steps),     disable=(rank != 0) ) for global_step in progress_bar:     # Get the data     # Forward and backward pass     # Step the optimiser.`\n\n...for each one, getting the appropriate batch out for the specific GPU that was running the code: <span class=\"translation\">...对于每一个，为运行代码的特定GPU获取适当的批次：</span>\n\n    `inputs, targets = train_ds[global_step * world_size + rank]`\n\n`rank` is a zero-indexed number, unique to each of the per-GPU processes. So this basically split `train_ds` into chunks of length `world_size`, and then each GPU was fed the batch at its `rank`'s offset into the chunk. <span class=\"translation\">`rank` 是一个零索引数字，对于每个GPU 进程都是唯一的。因此，这基本上将 `train_ds` 分割成长度为 `world_size` 的块，然后每个 GPU 都按照其 `rank` 的偏移量喂入该块。</span>\n\nI wanted to keep things shaped such that when I was running with gradient accumulation locally, it would be similar to a cloud run with per-GPU batching. Specifically: when I was training in the cloud, I had eight GPUs with a per-GPU microbatch size of 12, giving a total batch size of 96. Locally, I could fit a batch size of six on my GPU, so I needed to do gradient accumulation over 96 / 6 = 16 steps. <span class=\"translation\">我希望保持事物的形状，以便当我在本地运行梯度累积时，它会类似于带有 per-GPU 批处理的云运行。具体来说：当我在云中训练时，我有八个GPU，每个GPU微批次大小为12，总共批大小为96。在本地，我可以在我的GPU上安装六个批大小，所以我需要在96 / 6 = 16上执行梯度累积步骤。</span>\n\nTo keep things as similar as possible, I decided that I wanted the concept of a \"global step\" to match between the runs. In other words, it would expand slightly, from meaning \"one batch per GPU\" to being \"one optimiser step per GPU\". So, each time through that `global_step` loop, we'd do multiple forward-backward passes, and then one optimiser step. That would mean that the best way to do things would be with something much more like the second of the two bits of sample code above -- the one with the inner loop rather than the modulus. <span class=\"translation\">为了使事情尽可能相似，我决定希望“全局步骤”的概念在运行之间匹配。换句话说，它会稍微扩展，从“每个GPU一批”到“每个GPU一个优化器步骤”。因此，每次通过 `global_step` 循环，我们都会执行多次前后传递，然后执行一次优化器步骤。那就是均值，最好的做法是使用更像上面两位示例代码中的第二位的东西——带有内部循环而不是模数的东西。</span>\n\nMaybe that's easier to show in code: <span class=\"translation\">也许用代码更容易显示：</span>\n\n`total_global_steps = (len(train_ds) // world_size) // gradient_accumulation_steps  ...  for global_step in progress_bar:     ...     for accumulation_step in range(gradient_accumulation_steps):         # Forward and backward passes         ...      # Step the optimiser`\n\nThat required a change to the data lookup; I decided that `train_ds` would be split into chunks of size `gradient_accumulation_steps * world_size`, and then each of those would be split into chunks of size `world_size`, so the code to get the appropriate batch for a given run through the loop became this: <span class=\"translation\">这需要更改数据查找；我决定将 `train_ds` 分割成大小为 `gradient_accumulation_steps * world_size` 的块，然后将每个块分割成大小为 `world_size` 的块，因此通过循环获取给定运行的适当批次的代码如下：</span>\n\n`inputs, targets = train_ds[((global_step * gradient_accumulation_steps) + accumulation_step) * world_size + rank]`\n\nThat required a corresponding change in `load_dataset` to make sure that `train_ds` was divisible by both the world size, the per-GPU batch (\"microbatch\") size, and the number of gradient accumulation steps, but that was easy: <span class=\"translation\">这需要对`load_dataset`进行相应的更改，以确保`train_ds`可以被世界大小、每个GPU批次（“微批次”）大小和梯度累积步数整除，但这很简单：</span>\n\n`one_full_batch_tokens = world_size * microbatch_size * seq_length`\n\n...became this: <span class=\"translation\">...变成了这样：</span>\n\n`one_full_batch_tokens = world_size * microbatch_size * gradient_accumulation_steps * seq_length`\n\nThat was enough to get the gradient accumulation happening! Next, I needed to change the backward pass code to scale down the loss so that we got averaged rather than summed gradients. Because we might be using AMP with a scaler, the code wasn't just a simple `loss.backward`: <span class=\"translation\">这足以让梯度累积发生！接下来，我需要更改后向传递代码以缩小损失，以便我们获得平均值而不是求和梯度。因为我们可能将 AMP 与缩放器一起使用，所以代码不仅仅是一个简单的 `loss.backward`：</span>\n\n`if scaler is not None:     scaler.scale(train_loss).backward() else:     train_loss.backward()`\n\n...but the change was obvious enough: <span class=\"translation\">...但变化是显而易见的：</span>\n\n`if scaler is not None:     scaler.scale(train_loss / gradient_accumulation_steps).backward() else:     (train_loss / gradient_accumulation_steps).backward()`\n\nAll of those changes put together, plus a bit of shuffling around of code, were enough to get a correct gradient accumulation training loop! But there was one small tweak I needed to add. <span class=\"translation\">所有这些更改放在一起，加上一些代码的改组，足以获​​得正确的梯度累积训练循环！但我需要添加一个小调整。</span>\n\nWhen you're using DDP, gradients need to be synchronised between the different per-GPU processes. As a reminder, what happens is: <span class=\"translation\">当您使用 DDP 时，需要在不同的 per-GPU 进程之间同步梯度。提醒一下，发生的情况是：</span>\n\n-   Each process does a forward pass. <span class=\"translation\">每个进程都会进行一次前向传递。</span>\n-   Each process does a backward pass. <span class=\"translation\">每个进程都会进行向后传递。</span>\n-   When they have the gradients, they essentially share them so that each process has an average of the gradients from all of those backward passes. <span class=\"translation\">当他们拥有梯度时，他们本质上是共享梯度，以便每个过程都有所有这些向后传递的梯度的平均值。</span>\n-   Then they all step their optimisers to apply the average gradients to each process's copy of the model. <span class=\"translation\">然后他们都逐步执行优化器，将平均梯度应用于每个进程的模型副本。</span>\n\nNow, with my first cut of the gradient accumulation code above, what would have happened is this: <span class=\"translation\">现在，根据我对上面的梯度累积代码的第一次剪辑，会发生的情况是这样的：</span>\n\n-   For each gradient accumulation step: <span class=\"translation\">对于每个梯度累积步骤：</span>\n    -   Each process does a forward pass. <span class=\"translation\">每个进程都会进行一次前向传递。</span>\n    -   Each process does a backward pass. <span class=\"translation\">每个进程都会进行向后传递。</span>\n    -   The average is worked out <span class=\"translation\">平均值已计算出来</span>\n-   They all step their optimisers based on the most recent average <span class=\"translation\">他们都根据最近的平均值来调整优化器</span>\n\nThat would be correct, but not very efficient. We're sending out gradients and averaging on every accumulation step. But because each of our per-GPU processes is keeping its own \"local\" average (by accumulating the scaled-down gradients), we only really need to send those local averages out and get a global average once, just before we step the optimiser. If we do that, we can save quite a lot of work. <span class=\"translation\">这是正确的，但效率不高。我们发送梯度并对每个累积步骤求平均值。但是因为我们的每个GPU进程都保持自己的“局部”平均值（通过累积按比例缩小的梯度），所以我们只需要在我们步进优化器之前将这些局部平均值发送出去并获得一次全局平均值。如果我们这样做，我们可以节省很多工作。</span>\n\nThe trick to avoid that was to use the method `no_sync` on the `DistributedDataParallel` class that our own model is wrapped in. What we wanted to do was suppress the gradient synchronisation for each of the accumulation steps apart from the last one. <span class=\"translation\">避免这种情况的技巧是在我们自己的模型所包装的 `DistributedDataParallel` 类上使用方法 `no_sync`。我们想要做的是抑制除了最后一个累积步骤之外的每个累积步骤的梯度同步。</span>\n\nIt was easy to work out whether we were on the last gradient accumulation step: <span class=\"translation\">很容易判断我们是否处于最后一个梯度累积步骤：</span>\n\n`is_last = accumulation_step == gradient_accumulation_steps - 1`\n\nNow, what we needed to do was to wrap this: <span class=\"translation\">现在，我们需要做的就是包装它：</span>\n\n`if scaler is not None:     scaler.scale(train_loss / gradient_accumulation_steps).backward() else:     (train_loss / gradient_accumulation_steps).backward()`\n\n...in `model.no_sync`, but *only if* `is_last` was false. <span class=\"translation\">...在 `model.no_sync` 中，但*仅当* `is_last` 为假时。</span>\n\nConditional `with` statements can be a little fiddly, but Python has a \"do-nothing\" [`nullcontext` context manager in `contextlib`](https://docs.python.org/3/library/contextlib.html#contextlib.nullcontext) -- that is, <span class=\"translation\">条件`with`语句可能有点繁琐，但Python有一个“什么也不做”[`nullcontext` `contextlib` 中的上下文管理器](https://docs.python.org/3/library/contextlib.html#contextlib.nullcontext)——也就是说，</span>\n\n`with nullcontext():     do_something()`\n\n...is identical to just: <span class=\"translation\">...与以下内容相同：</span>\n\n`do_something()`\n\nSo we can combine that with the ternary operator like this: <span class=\"translation\">所以我们可以将其与三元运算符结合起来，如下所示：</span>\n\n    `with model.no_sync() if not is_last else nullcontext():         if scaler is not None:             scaler.scale(train_loss / gradient_accumulation_steps).backward()         else:             (train_loss / gradient_accumulation_steps).backward()`\n\n...which does exactly what we want [1](#fn-1). <span class=\"translation\">...这正是我们想要的[1](#fn-1)。</span>\n\nWith that change, I had something I was happy with; you can [see the diff here](https://github.com/gpjt/ddp-base-model-from-scratch/commit/c5d860e29848287abd2c1f9af11a8b849d260aba). <span class=\"translation\">有了这个改变，我有了一些令我满意的事情；你可以[在这里查看差异](https://github.com/gpjt/ddp-base-model-from-scratch/commit/c5d860e29848287abd2c1f9af11a8b849d260aba)。</span>\n\nSo now it was time to do a training run! <span class=\"translation\">所以现在是时候做一个训练运行了！</span>\n\n### The new local baseline <span class=\"translation\">新的本地基线</span>\n\nI'd originally been planning to jump right in and do a training run based on my [last cloud run](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud), with all of the interventions I'd decided were worth using, but locally with gradient accumulation. <span class=\"translation\">我原本计划直接根据我的 [最后一片云运行](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) 进行训练运行，其中所有我认为值得使用的干预措施，但在本地使用梯度累积。</span>\n\nHowever, I decided that it would be interesting to try doing a new \"baseline\" train first. I'd done my local training runs, and then established a baseline version in the cloud by taking exactly the same configuration and doing the training run on an 8x A100 40 GiB with an overall batch size of 96. So I could repeat that locally with gradient accumulation, and that would show two things (or perhaps, the same thing but in different lights): <span class=\"translation\">然而，我认为首先尝试做一个新的“基线”训练会很有趣。我完成了本地训练运行，然后通过采用完全相同的配置并在 8x A100 40 GiB 上执行训练运行，总体批大小为 96，在云中建立了一个基线版本。因此，我可以使用梯度累积在本地重复该操作，这将显示两件事（或者可能是同一件事，但在不同的角度）：</span>\n\n-   Whether the increased effective batch size had as positive an effect on the loss as the increased real batch size did when I did my cloud runs. <span class=\"translation\">增加的有效批大小是否对损失产生了与当我做云运行时增加的实际批大小一样的积极影响。</span>\n-   Whether the locally-trained gradient accumulation model was similar to the cloud-trained big-batch model in terms of its loss. <span class=\"translation\">本地-训练过梯度累积模型与云端-训练过大批量模型的损失是否相似。</span>\n\nThat would help confirm my understanding that it was the increased batch size that helped in the cloud, and not, say, some architectural difference -- and would also act as a good test of the gradient accumulation code. <span class=\"translation\">这将有助于确认我的理解，即增加的批大小在云中有所帮助，而不是某种架构差异，并且也可以作为对梯度累积代码的良好测试。</span>\n\nHere's [the training run config](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-baseline). I kicked it off: <span class=\"translation\">这是[训练运行配置](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-baseline)。我把它踢开了：</span>\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (main)$ uv run torchrun --nproc_per_node=1 ddp_train.py 1xrtx3090-baseline datasets/ Fetching 4 files: 100%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 4/4 [00:00<00:00, 810.65it/s] Starting rank 0 training at global step 0   0%|                                                                                                                  | 0/33165 [00:04<?, ?it/s, loss=10.991, tps=20,451]  Checkpoint  Continuing training   0%|                                                                                                       | 23/33165 [01:50<43:29:50,  4.72s/it, loss=7.625, tps=20,549]`\n\nThat looked like the right number of global steps; it matched the numbers I saw when training in the cloud. And 44 hours for the training run seemed correct: my original local runs took 48, but with them I was spending quite a lot of time on validation, which this code didn't do. <span class=\"translation\">这看起来是正确的全局步数；它与我在云中训练时看到的数字相符。训练运行的 44 小时似乎是正确的：我原来的本地运行花了 48 小时，但对于它们，我花了相当多的时间进行验证，而这段代码没有做到这一点。</span>\n\nJust less than two days later: <span class=\"translation\">就在不到两天后：</span>\n\n```\nTraining complete in 155,402.289 seconds\nTokens seen: 3,260,252,160\nThroughput: 20,979 tokens/second\nFinal train loss: 3.738\n```\n\nThat all looked good. The loss chart looked like this: <span class=\"translation\">一切看起来都不错。损失图表如下所示：</span>\n\n![The loss chart for the local baseline train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/baseline-loss-chart.png \"The loss chart for the local baseline train\")\n\nFor comparison, here's the one from the cloud training run with the same config (but using larger batches rather than gradient accumulation): <span class=\"translation\">为了进行比较，这是来自云端的具有相同配置的训练运行（但使用更大的批次而不是梯度累积）：</span>\n\n![Baseline training run on an 8x A100 with 40 GiB/GPU](https://www.gilesthomas.com/post-assets/llm-from-scratch-32a-interventions-baseline-model/baseline-training-run-chart.png \"Baseline training run on an 8x A100 with 40 GiB/GPU\")\n\nYou can see that they're similar, but not identical. That's pretty much what you'd expect! The two training runs were on different architectures -- RTX 3090 vs A100 -- and so there will probably be differences in the CUDA kernels, and also PyTorch's AMP (which uses 16-bit instead of 32-bit in cases where it makes sense) might make different decisions. I think that if we'd run it on a machine with one A100, then the results of using gradient accumulation would be even closer (perhaps even identical) to a larger batch size, especially if we were training without AMP. <span class=\"translation\">您可以看到它们很相似，但并不完全相同。这正是您所期望的！两个训练运行位于不同的架构上——RTX 3090与A100——因此CUDA内核可能会存在差异，并且PyTorch的AMP（在有意义的情况下使用16位而不是32位）可能会做出不同的决定。我认为，如果我们在一台带有 A100 的机器上使用运行，那么使用梯度累积的结果会更接近（甚至可能相同）到更大的批大小，特别是如果我们在没有 AMP 的情况下使用训练。</span>\n\nI [uploaded the model to Hugging Face](https://huggingface.co/gpjt/1xrtx3090-baseline) and it was time for the evals. The smoke test first: <span class=\"translation\">我[上传了模型到拥抱脸](https://huggingface.co/gpjt/1xrtx3090-baseline)，是时候评估了。第一个冒烟测试：</span>\n\n```\nEvery effort moves you to a more realistic approach and is always welcome, especially for a younger child!\nI’\n```\n\nAs usual, reasonably coherent. But the important one was the loss on the test set: <span class=\"translation\">和往常一样，相当连贯。但最重要的是测试集上的损失：</span>\n\n```\nLoss against our test dataset: 3.683835\n```\n\nThat's solid! The cloud-trained baseline model got 3.691526, so this local one was actually very slightly better, by 0.007691. But that's very close indeed, which is what we wanted to see :-) <span class=\"translation\">那很扎实！云-训练过基线模型得到了3.691526，所以这个本地的实际上稍微好一点，高了0.007691。但这确实非常接近，这正是我们希望看到的:-)</span>\n\nIt was time to see what effect adding on the interventions would have. <span class=\"translation\">现在是时候看看增加干预措施会产生什么效果了。</span>\n\n### The local run with the interventions <span class=\"translation\">当地运行的干预</span>\n\nAs a reminder, here are the changes I made to the config for this run: <span class=\"translation\">提醒一下，以下是我对此运行的配置所做的更改：</span>\n\n-   Gradient clipping at 3.5 <span class=\"translation\">梯度裁剪3.5</span>\n-   Learning rate changed from 0.0004 to 0.0014, with a warmup over 5% of the run then a cosine decay to 0.00014. <span class=\"translation\">学习率从 0.0004 更改为 0.0014，预热超过运行的 5%，然后余弦衰减到 0.00014。</span>\n-   Weight decay changed from 0.1 to 0.01 <span class=\"translation\">权重衰减从0.1更改为0.01</span>\n-   Dropout removed <span class=\"translation\">随机失活（dropout）已移除</span>\n\nIt did not include QKV bias. <span class=\"translation\">它不包括QKV偏差。</span>\n\n[Here's the config](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-stacked-interventions). I kicked it off, and: <span class=\"translation\">[这是配置](https://github.com/gpjt/ddp-base-model-from-scratch/tree/main/runs/1xrtx3090-stacked-interventions)。我把它踢开了，然后：</span>\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (main)$ uv run torchrun --nproc_per_node=1 ddp_train.py 1xrtx3090-stacked-interventions datasets Fetching 4 files: 100%|████████████████████████████████████████████████████████████████████████████████████████████████████████████████████| 4/4 [00:00<00:00, 749.32it/s] Starting rank 0 training at global step 0   0%|                                                                                                                  | 0/33165 [00:04<?, ?it/s, loss=10.994, tps=21,589]  Checkpoint  Continuing training   0%|                                                                                                      | 19/33165 [01:25<40:52:06,  4.44s/it, loss=10.318, tps=21,867]`\n\nIt looked like it was going to take 40 hours; that matched what happened in the cloud runs, as removing dropout speeds things up quite a lot. <span class=\"translation\">看起来要花40个小时；这与云端运行中发生的情况相匹配，因为删除随机失活（dropout）可以大大加快速度。</span>\n\nJust less than two days later: <span class=\"translation\">就在不到两天后：</span>\n\n```\nTraining complete in 146,299.816 seconds\nTokens seen: 3,260,252,160\nThroughput: 22,285 tokens/second\nFinal train loss: 3.519\n```\n\nThe loss chart over the training run looked like this: <span class=\"translation\">训练运行上的损失图表如下所示：</span>\n\n![The loss chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-loss-chart.png \"The loss chart for the local interventions train\")\n\nThat's very smooth, with no loss spikes. For comparison, here's the chart when we did the same training run in the cloud; you can see that it was a bit choppier than the local one. <span class=\"translation\">非常顺利，没有损失尖峰。为了进行比较，这是我们在云端执行相同训练运行时的图表；你可以看到它比当地的有点不稳定。</span>\n\n![The loss chart for the cloud interventions run](https://www.gilesthomas.com/post-assets/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud/stacked-interventions-1-loss-chart.png \"The loss chart for the cloud interventions run\")\n\nThe gradient norm chart was also interesting: <span class=\"translation\">梯度范数图也很有趣：</span>\n\n![The gradient norm chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-grad-norm-chart.png \"The gradient norm chart for the local interventions train\")\n\nIf you compare it to the one from the cloud training run below, you can see that the local one was actually noisier -- the cloud run has a few gradient spikes near the start but calms down from around global step 6,000 or so, whereas the local one is spiky up to about 3,000, then calm, but has a massive spike at around 10,000. <span class=\"translation\">如果将其与下面云训练运行中的云进行比较，您可以看到本地云实际上更加嘈杂 - 云运行在开始附近有一些梯度尖峰，但从全局步骤 6,000 左右开始平静下来，而本地云则尖峰达到约 3,000，然后平静，但在 10,000 左右有一个巨大的尖峰。</span>\n\n![The gradient norm chart for the cloud interventions run](https://www.gilesthomas.com/post-assets/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud/stacked-interventions-1-grad-norm-chart.png \"The gradient norm chart for the cloud interventions run\")\n\nThe learning rate we don't need to compare, but it was worth sanity checking to make sure we really did train the right way: <span class=\"translation\">我们不需要比较学习率，但值得进行合理性检查，以确保我们确实以正确的方式完成了训练：</span>\n\n![The learning rate chart for the local interventions train](https://www.gilesthomas.com/post-assets/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation/interventions-learning-rate-chart.png \"The learning rate chart for the local interventions train\")\n\nSo that all looked good. The training run did have some differences to the cloud one, but (as with the previous baseline train) it looked similar enough. Architectural differences between the A100s in the cloud and the local RTX 3090 seemed like a plausible cause. <span class=\"translation\">所以一切看起来都不错。训练运行确实与云端有一些差异，但是（与之前的基线训练一样）它看起来足够相似。云中的 A100 与本地 RTX 3090 之间的架构差异似乎是一个合理的原因。</span>\n\n### Evals <span class=\"translation\">评估</span>\n\nI [uploaded the model to Hugging Face](https://huggingface.co/gpjt/1xrtx3090-stacked-interventions), and it was time to run the evals. The smoke test first: <span class=\"translation\">我[上传了模型到拥抱脸](https://huggingface.co/gpjt/1xrtx3090-stacked-interventions)，是时候运行评估了。第一个冒烟测试：</span>\n\n```\nEvery effort moves you to give your customers the opportunity you give them in the event of a failure.<|endoftext|>A couple friends\n```\n\nReasonably coherent -- and I think that's the first time I've seen an `<|endoftext|>` token in a smoke test output! But the important one is, as ever, the loss, and: <span class=\"translation\">相当连贯——我认为这是我第一次在冒烟测试输出中看到 `&lt;|endoftext|&gt;`词元！但与以往一样，最重要的是损失，并且：</span>\n\n```\nLoss against our test dataset: 3.538161\n```\n\nLet's add both this one and the local baseline to the results table for all interventions: <span class=\"translation\">让我们将这一基线和本地基线添加到所有干预措施的结果表中：</span>\n\n|  | Test set loss | Improvement vs baseline | <span class=\"translation\"> | 测试集损失 | 与基线相比的改进</span>\n| --- | --- | --- |\n| [8xa100m40-weight-tying](https://www.gilesthomas.com/2026/03/llm-from-scratch-32g-interventions-weight-tying) | 3.874305 | \\-0.182779 | <span class=\"translation\">[8xa100m40-weight-tying](https://www.gilesthomas.com/2026/03/llm-from-scratch-32g-interventions-weight-tying) | 3.874305 | \\-0.182779</span>\n| [8xa100m40-weight-decay-cerebras](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.813856 | \\-0.122330 | <span class=\"translation\">[8xa100m40-weight-decay-cerebras](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.813856 | \\-0.122330</span>\n| [8xa100m40-baseline](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) | 3.691526 | \\- | <span class=\"translation\">[8xa100m40-基线](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) | 3.691526 | \\-</span>\n| [1xrtx3090-baseline](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.683835 | 0.007691 | <span class=\"translation\">[1xrtx3090-基线](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.683835 | 0.007691</span>\n| [8xa100m80-no-amp](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32) | 3.678968 | 0.012558 | <span class=\"translation\">[8xa100m80-no-amp](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32) | 3.678968 | 0.012558</span>\n| [8xa100m40-gradient-clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) | 3.678317 | 0.013209 | <span class=\"translation\">[8xa100m40-gradient-clipping](https://www.gilesthomas.com/2026/02/llm-from-scratch-32b-interventions-gradient-clipping) | 3.678317 | 0.013209</span>\n| [8xa100m40-qkv-bias](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias) | 3.669385 | 0.022141 | <span class=\"translation\">[8xa100m40-qkv-bias](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias) | 3.669385 | 0.022141</span>\n| [8xa100m40-weight-decay-gpt2](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.642940 | 0.048586 | <span class=\"translation\">[8xa100m40-weight-decay-gpt2](https://www.gilesthomas.com/2026/03/llm-from-scratch-32f-interventions-weight-decay) | 3.642940 | 0.048586</span>\n| [8xa100m40-remove-dropout](https://www.gilesthomas.com/2026/02/llm-from-scratch-32c-interventions-removing-dropout) | 3.641282 | 0.050244 | <span class=\"translation\">[8xa100m40-remove-dropout](https://www.gilesthomas.com/2026/02/llm-from-scratch-32c-interventions-removing-dropout) | 3.641282 | 0.050244</span>\n| [8xa100m40-stacked-interventions-2](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.604342 | 0.087184 | <span class=\"translation\">[8xa100m40-stacked-interventions-2](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.604342 | 0.087184</span>\n| [8xa100m40-schedule-learning-rate](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate) | 3.601917 | 0.089609 | <span class=\"translation\">[8xa100m40-schedule-learning-rate](https://www.gilesthomas.com/2026/03/llm-from-scratch-32e-interventions-learning-rate) | 3.601917 | 0.089609</span>\n| [8xa100m40-stacked-interventions-3](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.590266 | 0.101260 | <span class=\"translation\">[8xa100m40-stacked-interventions-3](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.590266 | 0.101260</span>\n| [8xa100m40-stacked-interventions-1](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.577761 | 0.113765 | <span class=\"translation\">[8xa100m40-stacked-interventions-1](https://www.gilesthomas.com/2026/04/llm-from-scratch-32j-interventions-trying-to-train-a-better-model-in-the-cloud) | 3.577761 | 0.113765</span>\n| [1xrtx3090-stacked-interventions](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.538161 | 0.153365 | <span class=\"translation\">[1xrtx3090-stacked-interventions](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) | 3.538161 | 0.153365</span>\n\nThat's really weird! The local run with the interventions, `1xrtx3090-stacked-interventions`, is 0.039600 points better than the cloud version of the same training run, `8xa100m40-stacked-interventions-1`. That's nice, in that lower loss is always better, but it's also rather confusing -- that's a bigger loss improvement than some of the interventions. <span class=\"translation\">这实在是太奇怪了！具有干预措施的本地运行`1xrtx3090-stacked-interventions`比云版本的相同训练运行`8xa100m40-stacked-interventions-1`好0.039600点。这很好，因为较低的损失总是更好，但它也相当令人困惑——这比一些干预措施有更大的损失改善。</span>\n\nIn theory, all that we changed between the cloud version of this training run, and the local one was the architecture. I was expecting that to have an effect, but thought that it would be small -- as, indeed, it was with the baseline trains `8xa100m40-baseline` and `1xrtx3090-baseline`, where you can see the loss difference was just 0.007691 -- about five times smaller. <span class=\"translation\">理论上，我们在这个训练运行的云版本和本地版本之间改变的只是架构。我预计这会产生影响，但认为它会很小 - 事实上，它是基线列车 `8xa100m40-baseline` 和 `1xrtx3090-baseline`，您可以看到损失差异仅为 0.007691 - 大约小五倍。</span>\n\nNow, when I was looking into [the effects of noise on training loss](https://www.gilesthomas.com/2026/04/llm-from-scratch-32i-interventions-what-is-in-the-noise), I found that changing the random seed that was used to initialise the weights (but starting the training run itself at the same random seed) had a much bigger effect on the resulting model quality than keeping the weights identical but varying the seed at the start of the post-initialisation phase of the training run. The standard deviation of the varied-weights, same-train models was about double the SD of the same-weights, varied-train. <span class=\"translation\">现在，当我研究[噪声对训练损失的影响](https://www.gilesthomas.com/2026/04/llm-from-scratch-32i-interventions-what-is-in-the-noise)时，我发现更改用于初始化权重的随机种子（但在相同的随机种子处启动训练运行本身）对生成的模型质量的影响比保持权重相同但在初始化后阶段开始时改变种子要大得多。训练运行。不同重量、相同 -训练模型的标准差大约是相同重量、不同 -训练模型的 SD 的两倍。</span>\n\nThat was interesting, though not directly comparable -- those tests were done with the same training run, but the architecture held constant -- a 8x A100 40 GiB machine for each test. <span class=\"translation\">这很有趣，虽然不能直接比较——这些测试是使用相同的训练运行完成的，但架构保持不变——每次测试使用 8x A100 40 GiB 机器。</span>\n\nHowever, it felt like it would be a good idea to at least see whether we started with the same weights locally and when training in the cloud. My suspicion was that we probably would; the weight initialisation uses deterministic non-GPU code, so with the same seed we'd expect the same weights regardless of the computer. The similarity of the loss results for the local and cloud baseline training runs also seemed to point in that direction. <span class=\"translation\">然而，感觉至少看看我们是否在本地使用相同的权重以及何时在云中使用训练是一个好主意。我怀疑我们可能会这样做；权重初始化使用确定性非GPU代码，因此对于相同的种子，无论计算机如何，我们都期望相同的权重。本地和云基线训练运行损失结果的相似性似乎也指向了这个方向。</span>\n\nBut it was worth testing. I created a throwaway branch of the training code, which -- after creating the model -- just dumped the model weights to a file, then exited. I ran it locally using the `1xrtx3090-stacked-interventions` config, and then I fired up yet another 8x A100 40 GiB machine on Lambda, ran the same code there, this time with the `8xa100m40-stacked-interventions-1` config, and then `scp`ed down the weights. <span class=\"translation\">但这是值得测试的。我创建了训练代码的一次性分支，在创建模型之后，它只是将模型权重转储到文件中，然后退出。我使用`1xrtx3090-stacked-interventions`配置在本地运行它，然后在Lambda上启动另一台8x A100 40 GiB机器，在那里运行相同的代码，这次使用`8xa100m40-stacked-interventions-1`配置，然后`scp`降低权重。</span>\n\n`giles@perry:~/Dev/ddp-base-model-from-scratch (dump-weights)$ diff cloud-init-weights.safetensors local-init-weights.safetensors giles@perry:~/Dev/ddp-base-model-from-scratch (dump-weights)$`\n\nIdentical. That was reassuring! <span class=\"translation\">完全相同的。这才让人放心！</span>\n\nI considered doing more analysis on this; for example, in my investigations into noise, I found that keeping the same weights but altering the random seed for the rest of the training run, I got results with a standard deviation of 0.008672 -- more than four times smaller than the difference between the local and cloud trains with the interventions. Might that be a number I could use for some kind of comparison? <span class=\"translation\">我考虑对此进行更多分析；例如，在我对噪声的调查中，我发现保持相同的权重，但改变训练运行其余部分的随机种子，我得到的结果是标准差为 0.008672——比干预后本地和云端训练之间的差异小四倍多。这可能是我可以用来进行某种比较的数字吗？</span>\n\nHowever, I decided that it's not really comparable. That number was from varying the random seed, but keeping the same architecture. There's not really any solid reason to believe that keeping the seed constant but changing the architecture would cause the same kind of differences. They might be more similar, they might be less. <span class=\"translation\">但是，我认为这实际上没有可比性。该数字来自于改变随机种子，但保持相同的架构。没有任何充分的理由相信保持种子不变但改变架构会导致同样的差异。它们可能更相似，也可能更少。</span>\n\nI think that all we can really say here is that the change of machine changed some aspects of the training dynamics in a way that happened to get us a lower loss. I can easily imagine that if I'd done something slightly different -- used a local RTX 4090, for example -- it could equally well have gone in the other direction. <span class=\"translation\">我认为我们在这里真正能说的是，机器的改变改变了训练动态的某些方面，碰巧让我们降低了损失。我可以很容易地想象，如果我做了一些稍微不同的事情——例如使用本地 RTX 4090——它同样可能会走向另一个方向。</span>\n\nAnd at least it's reassuring that the improvement was smaller than the interventions I was most convinced by; the only smaller ones were full-fat float32, gradient clipping, and QKV bias -- ones that I'd already decided might have only been beneficial due to noise. Most importantly, it was orders of magnitude smaller than the 0.252474 improvement I originally saw when I moved from local training to larger-batch cloud training. <span class=\"translation\">至少令人放心的是，改善幅度小于我最确信的干预措施；唯一较小的偏差是全脂 float32、梯度裁剪和 QKV 偏差——我已经确定这些偏差可能只是由于噪声才有益。最重要的是，它比我最初从本地训练迁移到大批量云训练时看到的 0.252474 改进要小几个数量级。</span>\n\n### Conclusion <span class=\"translation\">结论</span>\n\nSo, I think that that brings me to the end of this set of training experiments. We started with a locally-trained model that got a loss of 3.943522 on our test set, compared to the original GPT-2 small model, which got 3.499677 [2](#fn-2). <span class=\"translation\">所以，我认为这组训练实验就到此结束了。我们从本地-训练过模型开始，在我们的测试集上得到了3.943522的损失，而原始的GPT-2小模型得到了3.499677[2](#fn-2)。</span>\n\nI've tried a bunch of interventions to try to get my model closer, and finally I've managed to get almost all of the way there, to 3.538161. That's really pleasing! <span class=\"translation\">我尝试了一系列干预措施，试图让我的模型更接近，最后我几乎成功到达了 3.538161。这真是令人高兴啊！</span>\n\nI think that there are two things to do before I can fully wrap up this \"interventions\" mini-series, and get back to the main-line LLM from scratch stuff. <span class=\"translation\">我认为在我能够完全结束这个“干预”迷你系列并从头开始回到主线大语言模型（LLM）之前，有两件事要做。</span>\n\nFirstly, I should revisit the instruction fine-tuning tests, which I put on hold while doing these training runs. That would give us some indication as to whether the loss improvement was just a technical improvement that made a number go down, or whether it actually improved the usefulness of the model. <span class=\"translation\">首先，我应该重新审视指令微调测试，我在做这些训练运行时暂停了这些测试。这将为我们提供一些线索，以确定损失的改进是否只是导致数字下降的技术改进，或者它是否确实提高了模型的实用性。</span>\n\nSecondly, I think I really need to write a wrap-up. I've been working on this stuff on and off since December, and I think a summary of what I did would be quite nice! <span class=\"translation\">其次，我觉得我真的有必要写一篇总结。自 12 月以来，我一直断断续续地致力于这些工作，我认为对我所做的事情进行总结会非常好！</span>\n\nI'll post soon; don't touch that dial :-) <span class=\"translation\">我很快就会发帖；不要碰那个表盘:-)</span>\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests). <span class=\"translation\">[这是本系列下一篇文章的链接](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests)。</span>\n\n* * *\n\n1.  Thanks to [this Stack Overflow answer](https://stackoverflow.com/a/34798330) for that trick. [↩](#fnref-1) <span class=\"translation\">感谢[这个堆栈溢出答案](https://stackoverflow.com/a/34798330)的这个技巧。 [↩](#fnref-1)</span>\n    \n2.  I'm going to switch to six decimal places from now on -- previously I was rounding it to three, hence 3.500. [↩](#fnref-2) <span class=\"translation\">从现在开始，我将改用小数点后六位——之前我将其四舍五入为三位，即 3.500。 [↩](#fnref-2)</span>","imageCount":7,"wordCount":4317,"readingMinutes":20,"syncedAt":"2026-08-11T08:02:04.106Z","license":"CC BY 4.0","author":"Giles Thomas"}