{
  "slug": "llm-from-scratch-11-batches",
  "part": 11,
  "title": "Writing an LLM from scratch, part 11 -- batches",
  "shortTitle": "part 11 -- batches",
  "date": "19 April 2025",
  "summary": "Batching speeds up training and inference, but for LLMs we can't just use matrices for it -- we need higher-order tensors.",
  "href": "https://www.gilesthomas.com/2025/04/llm-from-scratch-11-batches",
  "phaseId": "attention",
  "phaseLabel": "02 · 注意力",
  "markdown": "I'm still working through chapter 3 of [Sebastian Raschka](https://sebastianraschka.com/)'s \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". Last time I covered [dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), which was nice and easy.\n\nThis time I'm moving on to batches. Batches allow you to run a bunch of different input sequences through an LLM at the same time, generating outputs for each in parallel, which can make training and inference more efficient -- if you've read my series on [fine-tuning LLMs](https://www.gilesthomas.com/fine-tuning) you'll probably remember I spent a lot of time trying to find exactly the right batch sizes for speed and for the memory I had available.\n\nThis was something I was originally planning to go into in some depth, because there's some fundamental maths there that I really wanted to understand better. But the more time I spent reading into it, the more of a rabbit hole it became -- and I had decided on a strict \"no side quests\" rule when working through this book.\n\nSo in this post I'll just present the basic stuff, the stuff that was necessary for me to feel comfortable with the code and the operations described in the book. A full treatment of linear algebra and higher-order tensor operations will, sadly, have to wait for another day...\n\nLet's start off with the fundamental problem of why batches are a bit tricky in an LLM.\n\n### Batches in normal neural networks\n\n> If you're not familiar with batches in normal neural networks, check out my blog post on [basic matrix maths for neural networks](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-1). This bit is just a summary of part of that post.\n\nWith a traditional neural network, we have a bunch of inputs, we feed them through a number of layers, and get a number of outputs. That means that we're starting with an input vector, the first layer takes that vector and transforms it into a vector of activations, the second takes that activation vector and transforms it into a different activation vector, and so on until we have the last activation vector, which is the outputs.\n\nThe fact that a single input \"run\" of the network operates only on vectors means that we can \"stack\" those vectors into a matrix, and run the network on a bunch of different sets of inputs in parallel. That's batching.\n\n### Batches in LLMs\n\nNow, with LLMs we don't have that shortcut, or at least so it looked to me initially. Remember, we take our input tokens, and convert each one into an input embedding, which is a vector. The data -- for one single input sequence, no batches -- that is fed into the attention mechanism is a matrix already. It has one row per token, and the columns in each row make up the token's input embedding.\n\nWe then perform matrix maths to work out our attention weights -- another matrix with one row per input token, with each column saying how much attention to pay to each other token -- and then use that to generate our context vectors, which are yet another matrix with one row per token, each one somehow encoding the meaning of that token in the current context.\n\nSo if we're already using matrices for a single input sequence, we clearly need some kind of higher-order thing to handle batches, where we're handling multiple input sequences -- multiple matrices -- at the same time.\n\n### Tensors to the rescue\n\nTensors are the answer[1](#fn-1). They're not a big stretch conceptually, especially if you're a coder, because they're pretty much like arrays. Mathematically, a scalar -- ie. a number -- is an order-zero tensor, a vector is an order-one tensor (a list, or a one-dimensional array), a matrix is an order-two tensor (a list of lists, each of which is the same length -- or in other words, a two-dimensional array) -- and it's not hard to see that you could also have order-three tensors, order-four tensors, and so on, just like you could keep on adding dimensions to an array.\n\nI've seen code -- rarely *good* code, but sometimes -- with lines like this:\n\n`this_item = records[i][j][k][l][m];`\n\n...and there's no deep conceptual problem with that, even if the actual meaning in context might be a tad tricky. It's just indexing into a multi-dimensional array.\n\nThe blocker for me was what operations like multiplication mean on higher-order tensors. There is a clear definition of matrix multiplication:\n\n> If you have a matrix M which has a rows and b columns -- that is, it's an a×b matrix -- you can multiply it by any other matrix N, so long as N has b rows -- that is, the number of columns in the first matrix in a matrix multiplication has to equal the number of rows in the second one. If we say that N is an b×c matrix, the result of the matrix multiplication is a new matrix, with a rows and c columns -- that is, it has the same number of rows as the first one, and the same number of columns as the second one.\n> \n> To work out the values in the resulting matrix R\\=MN, we say that Ri,j -- that is, the element at row i, column j -- is the dot product of row i in the first matrix, taken as a vector, with column j in the second matrix, also considered as a vector.\n\nBut what happens if you want to multiply a (say) a×b×c order three tensor with an d×e×f one? What rules apply (like the columns-in-the-first, rows-in-the-second one for matrix multiplication) to say which dimensions have to match?\n\nThis was where I fell into a rabbit hole; Claude was an appalling enabler for this, giving me loads of fascinating information. However, after a while I realised that googling about Einstein summation notation, while great fun, was exactly the kind of side quest I'd sworn not to fall into with this book.\n\nSo it was time to focus on what the book says rather than reading around.\n\n### Follow the code\n\nI think that the best way to approach it is to start off by showing Raschka's code for self attention without batching, and then to describe just the differences.\n\nUnfortunately the last code listing in the book was before the sections about [causal attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention) and [dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), so it's pretty simple:\n\n`class SelfAttention(nn.Module):     def __init__(self, d_in, d_out, qkv_bias=False):         super().__init__()         self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)      def forward(self, x):         keys = self.W_key(x)         queries = self.W_query(x)         values = self.W_value(x)         attn_scores = queries @ keys.T         attn_weights = torch.softmax(             attn_scores / keys.shape[-1] ** 0.5,             dim=-1         )         context_vec = attn_weights @ values         return context_vec`\n\nIt's pretty clearly an expression in PyTorch of the maths that came out of the section on [self-attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-8-trainable-self-attention). The parameter `d_in` is the length of the input vectors, and `d_out` is the length of the context vectors we want it to produce.\n\nThere are two oddities; both are due to the use of the `nn.Linear` class, which I kind of glossed over in my earlier post, and probably should not have -- I suspect I was just a bit overwhelmed by finally getting my head around self-attention!\n\nRaschka's initial implementation used the `nn.Parameter` class for the three weight matrices. He then explained that `nn.Linear` has a better weight-initialisation algorithm for our purposes, and switched to using it, so I think it's worth looking at the changes that required.\n\nFirstly, there's that `qkv_bias` kwarg that has crept in; the reason for that is that the `nn.Linear` class is designed for use in \"classical\" neural networks. In that use case it would control whether or not we have a bias for each neuron (which we normally would, but might not want to). Here we're just trying to mirror the `nn.Parameter` that Raschka used previously, so we're setting it to `False` by default. It's kind of interesting that he's keeping the option open to set it to `True` later by putting it into a kwarg for the `__init__`, but if that's relevant I'm sure it will pop up later.\n\nThe other oddity is that we're doing this:\n\n`keys = self.W_key(x)`\n\n...and likewise for the queries and values. But in the mathematical treatment we had\n\nK\\=XWk\n\n\\-- note that they're in the opposite order. That's just a quirk of PyTorch, the actual calculation being performed is exactly what we have in the maths. Indeed, in the previous code listing where we used `nn.Parameter`, we did indeed have the code\n\n`keys = x @ self.W_key`\n\nThe important thing is that the PyTorch syntax for `nn.Linear` reverses the order, but it's the same matrix multiplication happening[2](#fn-2).\n\nSo, that's the class with basic self-attention. Now let's look at the equivalent class with causal attention, dropout and batching:\n\n`class CausalAttention(nn.Module):     def __init__(self, d_in, d_out, context_length, dropout, qkv_bias=False):         super().__init__()         self.d_out = d_out         self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)         self.dropout = nn.Dropout(dropout)         self.register_buffer(             'mask',             torch.triu(torch.ones(context_length, context_length), diagonal=1)         )      def forward(self, x):         b, num_tokens, d_in = x.shape         keys = self.W_key(x)         queries = self.W_query(x)         values = self.W_value(x)          attn_scores = queries @ keys.transpose(1, 2)         attn_scores.masked_fill_(             self.mask.bool()[:num_tokens, :num_tokens],             -torch.inf         )         attn_weights = torch.softmax(             attn_scores / keys.shape[-1] ** 0.5,             dim=-1         )         attn_weights = self.dropout(attn_weights)          context_vec = attn_weights @ values         return context_vec`\n\nAlthough Raschka introduces the concepts of causal attention, dropout and batching in that order[3](#fn-3), when breaking down the differences between the old code and the new, I think it's worth tweaking that. Let's look at dropout first.\n\n#### Dropout\n\nThis one is really easy -- we construct an `nn.Dropout` object, passing in the `dropout` level (from 0 - 1) that we got as a parameter when we create the `CausalAttention` object:\n\n`self.dropout = nn.Dropout(dropout)`\n\n...and then we apply it to the attention weights once we've calculated them:\n\n`attn_weights = self.dropout(attn_weights)`\n\nSimple enough -- it's really nifty that PyTorch makes that so easy (though, as I noted in my [post on dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), applying it to the weights is unusual and applying it to the attention scores pre-softmax is apparently more common).\n\n#### Batches\n\nNow we're into the core of it. In our unbatched example, the first bit of code, the input to the `forward` function, `x`, was a matrix; its size was `num_tokens` rows by `d_in` (the length of the input vectors) columns, eg. ntokens×din. Now we have batches.\n\nIf we look at the start of the `forward` function, we're extracting `x`'s dimensions:\n\n`b, num_tokens, d_in = x.shape`\n\nSo that makes it clear that it's a b×ntokens×din order-three tensor (where b is our batch size).\n\nMy mental model of this (which will of course become useless as soon as we need to deal with order-four tensors) is that it's a stack of b matrices, one on top of the other.\n\nSo what other changes do we need to handle this higher-order tensor? Well, as far as I can understand it, almost nothing! The multiplications to project the inputs into key, query and value space are exactly the same as they were in the unbatched version, eg.\n\n`keys = self.W_key(x)`\n\nThen there's the transpose, where there is a small change -- instead of having\n\n`attn_scores = queries @ keys.T`\n\n...we have\n\n`attn_scores = queries @ keys.transpose(1, 2)`\n\n...but the only obvious change there is that we're saying \"transpose the tensor in the 1st and 2nd dimensions, leaving the 0th untouched\" -- which makes sense, we're just transposing every matrix in the \"stack\" and leaving things unchanged between stacked matrices.\n\nSo what's going on in the multiplications? Let's start with the easy ones:\n\n```\nkeys = self.W_key(x)\nqueries = self.W_query(x)\nvalues = self.W_value(x)\n```\n\nTaking the first one, we're doing\n\nK\\=XWk\n\n\\-- remember that the ordering in the PyTorch code is reversed compared to mathematical notation because we're using `nn.Linear`, so `self.W_key(x)` is XWk.\n\nSo X is a b×ntokens×din third-order tensor, and Wk is a din×dout matrix.\n\nWhat does it mean to multiply two things of such different shapes -- a third-order and a second-order tensor? I was puzzling about this for a bit until I realised that it's just a broadcast.\n\nJust like we can use broadcasting to add a vector of bias terms to our matrix of batched activations in a traditional neural network (see [my post on matrix maths for neural networks in practice](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2) for the details):\n\n\\[z11z12z21z22\\]+\\[b1b2\\]\\=\\[z11+b1z12+b1z21+b2z22+b2\\]\n\n...if we multiply a 3-tensor by a 2-tensor, we're broadcasting the matrix multiplication across the stack of matrices that makes up the 3-tensor. The last dimension of the 3-tensor (the number of columns in each matrix in the \"stack\") is din, which is the same as the number of rows in the 2-tensor, so it's compatible.\n\nSo, `keys`, `queries` and `values` are all 3-tensors, with dimensions b×ntokens×dout.\n\nNow how about the attention scores? Now we are working with 3-tensors consistently:\n\n```\nattn_scores = queries @ keys.transpose(1, 2)\n```\n\nLet's look at the dimensionality.\n\n`queries` is, as we said, b×ntokens×dout.\n\n`keys.transpose(1, 2)` means that we've transposed dimensions 1 and 2 of `keys`, and dimensions are zero-indexed. So that means that we're changing it from b×ntokens×dout to b×dout×ntokens.\n\nIt was while I was trying to get my head around exactly what a multiplication of two 3-tensors like that would mean that I fell down my rabbit hole and started reading about Einstein summation notation -- but the answer to what is going on turns out to be much simpler than all of that.\n\nLet's think about what that `@` means in a Pythonic sense. Python allows you to overload mathematical operators; for example, if you define `__add__` on a class `A`, create an instance of it `a`, and then call\n\n`a + b`\n\n...that will be interpreted as `a.__add__(b)`.\n\nPython 3.5 made it possible to overload the `@` operator too, and defined it as meaning matrix multiplication. So, if you have a class that defines `__matmul__`, then it will be called if you do\n\n`a @ b`\n\n(For completeness -- if the class of `a` doesn't define `__matmul__`, it will check whether `b`'s class defines `__rmatmul__` and call `b.__rmatmul__(a)` if it does.)\n\nSo, what is going on here? When we do\n\n`queries @ keys.transpose(1, 2)`\n\n...what we're really saying is:\n\n`queries.__matmul__(keys.transpose(1, 2))`\n\nAlthough I couldn't find it expressed explicitly in [the PyTorch docs for `torch.Tensor`](https://pytorch.org/docs/stable/tensors.html), I'm pretty sure this winds up being the same as\n\n`queries.matmul(keys.transpose(1, 2))`\n\n\\-- that is, the dunder (double-underscore) method is an alias for the non-dunder one. This is in turn an alias for `torch.matmul`, so we're actually doing this:\n\n`torch.matmul(queries, keys.transpose(1, 2))`\n\n[The docs for that method](https://pytorch.org/docs/stable/generated/torch.matmul.html#torch.matmul) show that it's essentially a whole bunch of special cases to make it generally useful for the kind of stuff that people do in ML models (and let mathematical purity be damned ;-). The important bit is this, because it covers not only this case but also the broadcast case above:\n\n> If both arguments are at least 1-dimensional and at least one argument is N-dimensional (where N > 2), then a batched matrix multiply is returned.\n\nSo, no need for any complicated tensor maths. PyTorch knows that people want to do batched matrix multiplications for its normal use cases, and the `@` operator is designed for exactly that. We just make sure that our batch dimension (or indeed, per those docs, our batch dimensions) are at the start and the matrix dimensions are the last two, and it will happily broadcast the whole thing for us.\n\nIn a way I was a little disappointed when I worked that out -- it's just a special-case utility function rather than some kind of deep new maths. But never mind; deep knowledge of tensors can wait for another day.\n\nLet's get on to the last part of the differences between the old and the new code: the causal attention.\n\n#### Causal attention\n\nThe two remaining differences between Raschka's first code example and the second are both to do with the masked self-attention. The first is this bit from the end of the `__init__` method:\n\n`self.register_buffer(     'mask',     torch.triu(torch.ones(context_length, context_length), diagonal=1) )`\n\nThat `torch.triu` call is exactly what we had in the code in the [section on causal attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention) -- it's a mask that will be applied to the attention weights to clear the ones that would imply that a token was paying attention to tokens that came after it. Raschka somewhat glosses over what the whole `register_buffer` thing is all about, though he does mention that it means that the mask will automatically be moved to the same device as our model so that if (say) the model is on the GPU, so will the mask be, so we won't get errors when trying to use it.\n\nThe [docs](https://pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.register_buffer) didn't make things a whole lot clearer for me, but the impression I get is that the `CausalAttention` class that we're creating here is regarded as a single \"thing\" by PyTorch, an `nn.Module`, and will be moved between CPU and GPU as a unit. A variable that was a field of the module -- like, say, `W_query` -- would be treated as part of it, but would also be treated as a trainable parameter by default, and we certainly don't want this mask to be adjusted as part of training -- it should always be a triangular mask with negative infinities in the top right. So we register it as a buffer to make sure that it is part of the module, but isn't treated as a set of trainable parameters.\n\nI'm sure there's more depth there, but I think that's enough for now.\n\nLet's move on to the second bit of causal attention code, which is this from the `forward` method:\n\n`attn_scores.masked_fill_(     self.mask.bool()[:num_tokens, :num_tokens],     -torch.inf )`\n\nIn the original causal attention code we had this:\n\n`masked = attn_scores.masked_fill(mask.bool(), -torch.inf)`\n\nRaschka notes that the trailing underscore on `masked_fill_` in the new code is just PyTorch shorthand to say \"do this operation in place\", which explains why we don't need to bind it to a new variable -- `attn_scores` is being changed directly.\n\nThe next bit is this `[:num_tokens, :num_tokens]` at the end of the mask; this is just because the original code was assuming that the mask was the right size for the `attn_scores` matrix that we were working with -- that is, all of our inputs were the full `context_length` in length, whereas here we're allowing for variable lengths -- so we just take the sub-matrix of the mask that is required for this scores matrix.\n\nThe elephant in the room is, of course: what about the batches? [The docs](https://pytorch.org/docs/stable/generated/torch.Tensor.masked_fill_.html#torch.Tensor.masked_fill_) indicate what's going on -- it's just another broadcast operation, so it will do exactly what we expect.\n\nAnd that's it!\n\n### Wrapping it up\n\nSo, as it turned out, this was actually all pretty simple. There's a certain amount of depth there -- I still need to learn a bit more about PyTorch buffers -- but instead of the deep linear algebra that I thought I was going to have to learn to work out what multiplying order >2 tensors together actually means, we just have what amount to utility functions that know that dealing with batches of matrices is a common pattern, and allow us to treat them essentially the same as matrices, doing the sensible thing with them on that basis.\n\nThis is a good thing! You want the tools you're using to be optimised for the job that you're doing.\n\nI think that for me the biggest lesson from working through this section is actually to look out for AIs as an enabler for excessive curiosity. The reason I took so long to work through something so relatively simple is that I could see that there were operations with higher-order tensors going on, and started chatting about what that means, and this stuff is fascinating (for very geeky values of fascinating). There's a ton to learn there, and I suspect that much of it is useful as well as interesting.\n\nBut as it turned out, just RTFMing was the best way forward. What we're looking at here isn't really advanced maths, it's just engineering. The docs are your friends, and are probably the best first port of call, especially if you want to keep yourself grounded and avoid side quests.\n\n### Conclusion\n\nSo, that was batching. Not as simple as I thought it might be, but -- having avoided a full-on diversion into deep linear algebra -- not a huge lift either. Next step: multi-head attention. My suspicion is that even more tensors and batched matrix multiplication helper functions will be involved somewhere...\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention).\n\n* * *\n\n1.  In the interest of being maximally annoying, here's a niche joke that will infuriate those that get it because they'll be stuck with the world's worst earworm.\n    \n    ```\n    Eight, sir; seven, sir;\n    Six, sir; five, sir;\n    Four, sir; three, sir;\n    Two, sir, one!\n    \n    Tenser, said the Tensor.\n    Tenser, said the Tensor.\n    \n    Tension, apprehension,\n    And dissension have begun.\n    \n    <RIFF>\n    \n    Tension, apprehension,\n    And dissension have begun\n    \n    <RIFF>\n    \n    Tension...\n    ```\n    \n    [↩](#fnref-1)\n    \n2.  To be more precise, because the `nn.Linear` is actually designed to do a forward pass of a normal NN, so what's going on internally is something more like K\\=XWkT+b, where b is all zeros (because we set `bias` to `False`) and PyTorch is internally transposing Wk as part of its initialisation. But that's kind of a nit at this point, I think. [↩](#fnref-2)\n    \n3.  Correctly, IMO [↩](#fnref-3)",
  "imageCount": 0,
  "wordCount": 3581,
  "readingMinutes": 16,
  "syncedAt": "2026-08-11T08:01:49.484Z",
  "license": "CC BY 4.0",
  "author": "Giles Thomas",
  "markdownBilingual": "I'm still working through chapter 3 of [Sebastian Raschka](https://sebastianraschka.com/)'s \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\". Last time I covered [dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), which was nice and easy. <span class=\"translation\">我还在继续阅读第 3 章，书名是 [Sebastian Raschka](https://sebastianraschka.com/) 的《[从零开始构建大型语言模型](https://www.manning.com/books/build-a-large-language-model-from-scratch)》。上次讲了[dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)，轻松愉快。</span>\n\nThis time I'm moving on to batches. Batches allow you to run a bunch of different input sequences through an LLM at the same time, generating outputs for each in parallel, which can make training and inference more efficient -- if you've read my series on [fine-tuning LLMs](https://www.gilesthomas.com/fine-tuning) you'll probably remember I spent a lot of time trying to find exactly the right batch sizes for speed and for the memory I had available. <span class=\"translation\">这次转向批次。批次让你可以同时把许多不同的输入序列送入 LLM，并行生成各自的输出，从而提高训练和推理效率——如果你读过我的[微调 LLM 系列](https://www.gilesthomas.com/fine-tuning)，可能记得我花了很多时间寻找兼顾速度和可用内存的最佳批次大小。</span>\n\nThis was something I was originally planning to go into in some depth, because there's some fundamental maths there that I really wanted to understand better. But the more time I spent reading into it, the more of a rabbit hole it became -- and I had decided on a strict \"no side quests\" rule when working through this book. <span class=\"translation\">我原本打算深入讲解这一部分，因为其中有些基础数学我很想弄明白。但读得越多，越像掉进兔子洞；而我在读这本书时已经决定严格遵守“不搞支线任务”的规则。</span>\n\nSo in this post I'll just present the basic stuff, the stuff that was necessary for me to feel comfortable with the code and the operations described in the book. A full treatment of linear algebra and higher-order tensor operations will, sadly, have to wait for another day... <span class=\"translation\">所以本文只介绍基础内容，也就是让我能对书中代码和操作感到自在所必需的部分。完整讲解线性代数和高阶张量运算，只能遗憾地留到以后……</span>\n\nLet's start off with the fundamental problem of why batches are a bit tricky in an LLM. <span class=\"translation\">先从批次在 LLM 中为何有些棘手这个根本问题开始。</span>\n\n### Batches in normal neural networks <span class=\"translation\">普通神经网络中的批次</span>\n\n> If you're not familiar with batches in normal neural networks, check out my blog post on [basic matrix maths for neural networks](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-1). This bit is just a summary of part of that post. <span class=\"translation\">如果你不熟悉普通神经网络中的批次，可以看看我写的[神经网络基础矩阵数学](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-1)。这里仅总结那篇文章的一部分。</span>\n\nWith a traditional neural network, we have a bunch of inputs, we feed them through a number of layers, and get a number of outputs. That means that we're starting with an input vector, the first layer takes that vector and transforms it into a vector of activations, the second takes that activation vector and transforms it into a different activation vector, and so on until we have the last activation vector, which is the outputs. <span class=\"translation\">在传统神经网络中，我们有一批输入，把它们依次通过多个层，得到一批输出。也就是说，从一个输入向量开始，第一层把它转换成激活向量，第二层再把该激活向量转换成另一个激活向量，依此类推，直到最后一个激活向量，也就是输出。</span>\n\nThe fact that a single input \"run\" of the network operates only on vectors means that we can \"stack\" those vectors into a matrix, and run the network on a bunch of different sets of inputs in parallel. That's batching. <span class=\"translation\">由于网络单次输入“运行”只处理向量，我们可以把多个向量“堆叠”成矩阵，并行运行网络处理许多不同的输入集合。这就是批处理。</span>\n\n### Batches in LLMs <span class=\"translation\">LLM 中的批次</span>\n\nNow, with LLMs we don't have that shortcut, or at least so it looked to me initially. Remember, we take our input tokens, and convert each one into an input embedding, which is a vector. The data -- for one single input sequence, no batches -- that is fed into the attention mechanism is a matrix already. It has one row per token, and the columns in each row make up the token's input embedding. <span class=\"translation\">但在 LLM 中没有这个捷径，至少我一开始是这样认为的。记住，我们把输入词元转换成输入嵌入，每个嵌入是一个向量。单个输入序列（没有批次）送入注意力机制的数据，已经是一个矩阵：每个词元一行，每行的列组成该词元的输入嵌入。</span>\n\nWe then perform matrix maths to work out our attention weights -- another matrix with one row per input token, with each column saying how much attention to pay to each other token -- and then use that to generate our context vectors, which are yet another matrix with one row per token, each one somehow encoding the meaning of that token in the current context. <span class=\"translation\">接着进行矩阵运算，计算注意力权重——另一个矩阵，每个输入词元一行，每列表示应对其他每个词元投入多少注意力——然后用它生成上下文向量；上下文向量又是一个矩阵，每个词元一行，每行以某种方式编码该词元在当前上下文中的含义。</span>\n\nSo if we're already using matrices for a single input sequence, we clearly need some kind of higher-order thing to handle batches, where we're handling multiple input sequences -- multiple matrices -- at the same time. <span class=\"translation\">因此，既然单个输入序列已经使用矩阵，处理批次（同时处理多个输入序列、多个矩阵）显然需要某种高阶结构。</span>\n\n### Tensors to the rescue <span class=\"translation\">张量来救场</span>\n\nTensors are the answer[1](#fn-1). They're not a big stretch conceptually, especially if you're a coder, because they're pretty much like arrays. Mathematically, a scalar -- ie. a number -- is an order-zero tensor, a vector is an order-one tensor (a list, or a one-dimensional array), a matrix is an order-two tensor (a list of lists, each of which is the same length -- or in other words, a two-dimensional array) -- and it's not hard to see that you could also have order-three tensors, order-four tensors, and so on, just like you could keep on adding dimensions to an array. <span class=\"translation\">张量就是答案[1](#fn-1)。从概念上看它们并不难，尤其对程序员而言，因为它们很像数组。数学上，标量（即一个数字）是零阶张量，向量是一阶张量（列表或一维数组），矩阵是二阶张量（等长列表的列表，换句话说是二维数组）；不难看出，还可以有三阶、四阶张量，依此类推，就像可以不断给数组增加维度。</span>\n\nI've seen code -- rarely *good* code, but sometimes -- with lines like this: <span class=\"translation\">我见过这样的代码——很少是*好*代码，但偶尔会有：</span>\n\n`this_item = records[i][j][k][l][m];`\n\n...and there's no deep conceptual problem with that, even if the actual meaning in context might be a tad tricky. It's just indexing into a multi-dimensional array. <span class=\"translation\">……即使具体语境中的含义可能稍微棘手，也不存在深层概念问题。它只是在多维数组中进行索引。</span>\n\nThe blocker for me was what operations like multiplication mean on higher-order tensors. There is a clear definition of matrix multiplication: <span class=\"translation\">真正阻碍我的是，高阶张量上的乘法等操作意味着什么。矩阵乘法有明确的定义：</span>\n\n> If you have a matrix M which has a rows and b columns -- that is, it's an a×b matrix -- you can multiply it by any other matrix N, so long as N has b rows -- that is, the number of columns in the first matrix in a matrix multiplication has to equal the number of rows in the second one. If we say that N is an b×c matrix, the result of the matrix multiplication is a new matrix, with a rows and c columns -- that is, it has the same number of rows as the first one, and the same number of columns as the second one. <span class=\"translation\">如果有一个矩阵 M，它有 a 行 b 列——即 a×b 矩阵——那么只要另一个矩阵 N 有 b 行，就可以与之相乘；也就是说，矩阵乘法中第一个矩阵的列数必须等于第二个矩阵的行数。如果 N 是 b×c 矩阵，矩阵乘法结果就是一个新的矩阵，有 a 行 c 列——即行数取自第一个矩阵，列数取自第二个矩阵。</span>\n> \n> To work out the values in the resulting matrix R\\=MN, we say that Ri,j -- that is, the element at row i, column j -- is the dot product of row i in the first matrix, taken as a vector, with column j in the second matrix, also considered as a vector. <span class=\"translation\">要计算结果矩阵 R=MN 的值，Ri,j（即第 i 行第 j 列的元素）是第一个矩阵第 i 行（视为向量）与第二个矩阵第 j 列（同样视为向量）的点积。</span>\n\nBut what happens if you want to multiply a (say) a×b×c order three tensor with an d×e×f one? What rules apply (like the columns-in-the-first, rows-in-the-second one for matrix multiplication) to say which dimensions have to match? <span class=\"translation\">但如果想把一个（比如）a×b×c 的三阶张量与一个 d×e×f 的三阶张量相乘，会发生什么？有哪些规则（类似矩阵乘法中“第一个的列数等于第二个的行数”）来规定哪些维度必须匹配？</span>\n\nThis was where I fell into a rabbit hole; Claude was an appalling enabler for this, giving me loads of fascinating information. However, after a while I realised that googling about Einstein summation notation, while great fun, was exactly the kind of side quest I'd sworn not to fall into with this book. <span class=\"translation\">这就是我掉进兔子洞的地方；Claude 极其善于助推，给了我大量迷人的信息。但过了一阵我意识到，搜索爱因斯坦求和记号虽然很有趣，却正是我发誓不在本书中陷入的那类支线任务。</span>\n\nSo it was time to focus on what the book says rather than reading around. <span class=\"translation\">所以是时候关注书中的内容，而不是到处查阅了。</span>\n\n### Follow the code <span class=\"translation\">跟着代码走</span>\n\nI think that the best way to approach it is to start off by showing Raschka's code for self attention without batching, and then to describe just the differences. <span class=\"translation\">我认为最好的方法，是先展示 Raschka 那段不做批处理的自注意力代码，然后只描述差异。</span>\n\nUnfortunately the last code listing in the book was before the sections about [causal attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention) and [dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), so it's pretty simple: <span class=\"translation\">遗憾的是，书中最后一个代码清单早于[因果注意力](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention)和[dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)两节，所以相当简单：</span>\n\n`class SelfAttention(nn.Module):     def __init__(self, d_in, d_out, qkv_bias=False):         super().__init__()         self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)      def forward(self, x):         keys = self.W_key(x)         queries = self.W_query(x)         values = self.W_value(x)         attn_scores = queries @ keys.T         attn_weights = torch.softmax(             attn_scores / keys.shape[-1] ** 0.5,             dim=-1         )         context_vec = attn_weights @ values         return context_vec`\n\nIt's pretty clearly an expression in PyTorch of the maths that came out of the section on [self-attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-8-trainable-self-attention). The parameter `d_in` is the length of the input vectors, and `d_out` is the length of the context vectors we want it to produce. <span class=\"translation\">这显然是用 PyTorch 表达了[自注意力](https://www.gilesthomas.com/2025/03/llm-from-scratch-8-trainable-self-attention)一节数学的代码。参数 `d_in` 是输入向量长度，`d_out` 是希望生成的上下文向量长度。</span>\n\nThere are two oddities; both are due to the use of the `nn.Linear` class, which I kind of glossed over in my earlier post, and probably should not have -- I suspect I was just a bit overwhelmed by finally getting my head around self-attention! <span class=\"translation\">这里有两个奇怪之处，都是因为使用 `nn.Linear` 类；我在上一篇文章中对它一带而过，可能不该这样——我想当时只是终于理解自注意力，脑子有些超负荷。</span>\n\nRaschka's initial implementation used the `nn.Parameter` class for the three weight matrices. He then explained that `nn.Linear` has a better weight-initialisation algorithm for our purposes, and switched to using it, so I think it's worth looking at the changes that required. <span class=\"translation\">Raschka 最初用 `nn.Parameter` 类表示三个权重矩阵。然后他解释说，`nn.Linear` 有更适合我们用途的权重初始化算法，于是改用它；所以值得看看需要哪些改动。</span>\n\nFirstly, there's that `qkv_bias` kwarg that has crept in; the reason for that is that the `nn.Linear` class is designed for use in \"classical\" neural networks. In that use case it would control whether or not we have a bias for each neuron (which we normally would, but might not want to). Here we're just trying to mirror the `nn.Parameter` that Raschka used previously, so we're setting it to `False` by default. It's kind of interesting that he's keeping the option open to set it to `True` later by putting it into a kwarg for the `__init__`, but if that's relevant I'm sure it will pop up later. <span class=\"translation\">首先是出现的 `qkv_bias` 关键字参数；原因在于 `nn.Linear` 为“经典”神经网络设计。在那种用法中，它控制每个神经元是否有偏置（通常会有，但有时不需要）。这里我们只是想复现 Raschka 之前使用的 `nn.Parameter`，所以默认设为 `False`。有趣的是，他把它作为 `__init__` 的关键字参数，保留了以后设为 `True` 的选项；如果这很重要，后面应该会再出现。</span>\n\nThe other oddity is that we're doing this: <span class=\"translation\">另一个奇怪之处是我们这样写：</span>\n\n`keys = self.W_key(x)`\n\n...and likewise for the queries and values. But in the mathematical treatment we had <span class=\"translation\">……查询和值也同样如此。但数学处理是：</span>\n\nK\\=XWk\n\n\\-- note that they're in the opposite order. That's just a quirk of PyTorch, the actual calculation being performed is exactly what we have in the maths. Indeed, in the previous code listing where we used `nn.Parameter`, we did indeed have the code <span class=\"translation\">——注意，它们顺序相反。这只是 PyTorch 的一个小特点，实际执行的计算与数学表达完全相同。事实上，在之前使用 `nn.Parameter` 的代码清单中，我们确实写过：</span>\n\n`keys = x @ self.W_key`\n\nThe important thing is that the PyTorch syntax for `nn.Linear` reverses the order, but it's the same matrix multiplication happening[2](#fn-2). <span class=\"translation\">重要的是，PyTorch 中 `nn.Linear` 的语法会反转顺序，但执行的是同一个矩阵乘法[2](#fn-2)。</span>\n\nSo, that's the class with basic self-attention. Now let's look at the equivalent class with causal attention, dropout and batching: <span class=\"translation\">这就是基本自注意力类。现在来看看同时包含因果注意力、dropout 和批处理的等价类：</span>\n\n`class CausalAttention(nn.Module):     def __init__(self, d_in, d_out, context_length, dropout, qkv_bias=False):         super().__init__()         self.d_out = d_out         self.W_query = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_key = nn.Linear(d_in, d_out, bias=qkv_bias)         self.W_value = nn.Linear(d_in, d_out, bias=qkv_bias)         self.dropout = nn.Dropout(dropout)         self.register_buffer(             'mask',             torch.triu(torch.ones(context_length, context_length), diagonal=1)         )      def forward(self, x):         b, num_tokens, d_in = x.shape         keys = self.W_key(x)         queries = self.W_query(x)         values = self.W_value(x)          attn_scores = queries @ keys.transpose(1, 2)         attn_scores.masked_fill_(             self.mask.bool()[:num_tokens, :num_tokens],             -torch.inf         )         attn_weights = torch.softmax(             attn_scores / keys.shape[-1] ** 0.5,             dim=-1         )         attn_weights = self.dropout(attn_weights)          context_vec = attn_weights @ values         return context_vec`\n\nAlthough Raschka introduces the concepts of causal attention, dropout and batching in that order[3](#fn-3), when breaking down the differences between the old code and the new, I think it's worth tweaking that. Let's look at dropout first. <span class=\"translation\">虽然 Raschka 按因果注意力、dropout、批处理的顺序介绍这些概念[3](#fn-3)，但拆解新旧代码差异时，我觉得调整顺序更好。先看 dropout。</span>\n\n#### Dropout <span class=\"translation\">Dropout</span>\n\nThis one is really easy -- we construct an `nn.Dropout` object, passing in the `dropout` level (from 0 - 1) that we got as a parameter when we create the `CausalAttention` object: <span class=\"translation\">这一部分非常简单——构造一个 `nn.Dropout` 对象，传入创建 `CausalAttention` 对象时作为参数得到的 dropout 级别（0–1）：</span>\n\n`self.dropout = nn.Dropout(dropout)`\n\n...and then we apply it to the attention weights once we've calculated them: <span class=\"translation\">……然后在计算出注意力权重后应用它：</span>\n\n`attn_weights = self.dropout(attn_weights)`\n\nSimple enough -- it's really nifty that PyTorch makes that so easy (though, as I noted in my [post on dropout](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout), applying it to the weights is unusual and applying it to the attention scores pre-softmax is apparently more common). <span class=\"translation\">足够简单——PyTorch 让这件事变得非常方便（不过正如我在[dropout 文章](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)中提到的，对权重使用 dropout 并不常见，对 softmax 前的注意力分数使用 dropout 似乎更常见）。</span>\n\n#### Batches <span class=\"translation\">批次</span>\n\nNow we're into the core of it. In our unbatched example, the first bit of code, the input to the `forward` function, `x`, was a matrix; its size was `num_tokens` rows by `d_in` (the length of the input vectors) columns, eg. ntokens×din. Now we have batches. <span class=\"translation\">现在进入核心。在不做批处理的示例中，第一段代码里 `forward` 函数的输入 `x` 是一个矩阵；大小为 `num_tokens` 行、`d_in` 列（输入向量长度），例如 ntokens×din。现在加入批次。</span>\n\nIf we look at the start of the `forward` function, we're extracting `x`'s dimensions: <span class=\"translation\">看 `forward` 函数开头，我们提取 `x` 的维度：</span>\n\n`b, num_tokens, d_in = x.shape`\n\nSo that makes it clear that it's a b×ntokens×din order-three tensor (where b is our batch size). <span class=\"translation\">所以它是一个 b×ntokens×din 的三阶张量（b 是批次大小）。</span>\n\nMy mental model of this (which will of course become useless as soon as we need to deal with order-four tensors) is that it's a stack of b matrices, one on top of the other. <span class=\"translation\">我的心智模型是（当然，一旦需要处理四阶张量就会失效）：它是 b 个矩阵上下堆叠而成。</span>\n\nSo what other changes do we need to handle this higher-order tensor? Well, as far as I can understand it, almost nothing! The multiplications to project the inputs into key, query and value space are exactly the same as they were in the unbatched version, eg. <span class=\"translation\">那么要处理这个高阶张量，还需要哪些改动？据我理解，几乎没有！把输入投影到键、查询和值空间的乘法，与不做批处理时完全相同，例如：</span>\n\n`keys = self.W_key(x)`\n\nThen there's the transpose, where there is a small change -- instead of having <span class=\"translation\">然后是转置，这里有一个小变化——不再写：</span>\n\n`attn_scores = queries @ keys.T`\n\n...we have <span class=\"translation\">……而是写：</span>\n\n`attn_scores = queries @ keys.transpose(1, 2)`\n\n...but the only obvious change there is that we're saying \"transpose the tensor in the 1st and 2nd dimensions, leaving the 0th untouched\" -- which makes sense, we're just transposing every matrix in the \"stack\" and leaving things unchanged between stacked matrices. <span class=\"translation\">唯一明显的变化，是我们说“转置第 1、2 维的张量，保持第 0 维不变”——这很合理：我们只是转置“堆叠”中的每个矩阵，保持堆叠矩阵之间不变。</span>\n\nSo what's going on in the multiplications? Let's start with the easy ones: <span class=\"translation\">那么乘法中发生了什么？先看简单的部分：</span>\n\n```\nkeys = self.W_key(x)\nqueries = self.W_query(x)\nvalues = self.W_value(x)\n```\n\nTaking the first one, we're doing <span class=\"translation\">看第一行，我们执行的是：</span>\n\nK\\=XWk\n\n\\-- remember that the ordering in the PyTorch code is reversed compared to mathematical notation because we're using `nn.Linear`, so `self.W_key(x)` is XWk. <span class=\"translation\">——记住，由于使用 `nn.Linear`，PyTorch 代码中的顺序与数学记号相反，所以 `self.W_key(x)` 就是 XWk。</span>\n\nSo X is a b×ntokens×din third-order tensor, and Wk is a din×dout matrix. <span class=\"translation\">X 是 b×ntokens×din 的三阶张量，Wk 是 din×dout 的矩阵。</span>\n\nWhat does it mean to multiply two things of such different shapes -- a third-order and a second-order tensor? I was puzzling about this for a bit until I realised that it's just a broadcast. <span class=\"translation\">把形状差别这么大的两个东西——三阶张量和二阶张量——相乘，是什么意思？我困惑了一会儿，才意识到这只是广播。</span>\n\nJust like we can use broadcasting to add a vector of bias terms to our matrix of batched activations in a traditional neural network (see [my post on matrix maths for neural networks in practice](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2) for the details): <span class=\"translation\">就像在传统神经网络中，我们可以用广播把偏置项向量加到批量激活矩阵上（细节见我的[实践中的神经网络矩阵数学文章](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2)）：</span>\n\n\\[z11z12z21z22\\]+\\[b1b2\\]\\=\\[z11+b1z12+b1z21+b2z22+b2\\]\n\n...if we multiply a 3-tensor by a 2-tensor, we're broadcasting the matrix multiplication across the stack of matrices that makes up the 3-tensor. The last dimension of the 3-tensor (the number of columns in each matrix in the \"stack\") is din, which is the same as the number of rows in the 2-tensor, so it's compatible. <span class=\"translation\">……如果把三阶张量乘以二阶张量，就是在构成三阶张量的矩阵堆叠上广播矩阵乘法。三阶张量的最后一维（堆叠中每个矩阵的列数）是 din，与二阶张量的行数相同，因此兼容。</span>\n\nSo, `keys`, `queries` and `values` are all 3-tensors, with dimensions b×ntokens×dout. <span class=\"translation\">所以 `keys`、`queries` 和 `values` 都是三阶张量，维度为 b×ntokens×dout。</span>\n\nNow how about the attention scores? Now we are working with 3-tensors consistently: <span class=\"translation\">接下来是注意力分数。现在我们始终在处理三阶张量：</span>\n\n```\nattn_scores = queries @ keys.transpose(1, 2)\n```\n\nLet's look at the dimensionality. <span class=\"translation\">看看维度。</span>\n\n`queries` is, as we said, b×ntokens×dout. <span class=\"translation\">如前所述，`queries` 的大小是 b×ntokens×dout。</span>\n\n`keys.transpose(1, 2)` means that we've transposed dimensions 1 and 2 of `keys`, and dimensions are zero-indexed. So that means that we're changing it from b×ntokens×dout to b×dout×ntokens. <span class=\"translation\">`keys.transpose(1, 2)` 表示转置 `keys` 的第 1、2 维，维度从 0 开始计数。因此，它把 b×ntokens×dout 变成 b×dout×ntokens。</span>\n\nIt was while I was trying to get my head around exactly what a multiplication of two 3-tensors like that would mean that I fell down my rabbit hole and started reading about Einstein summation notation -- but the answer to what is going on turns out to be much simpler than all of that. <span class=\"translation\">我正是在努力理解两个这样的三阶张量相乘到底意味着什么时，掉进兔子洞、开始研究爱因斯坦求和记号的；但结果证明，这比那些内容简单得多。</span>\n\nLet's think about what that `@` means in a Pythonic sense. Python allows you to overload mathematical operators; for example, if you define `__add__` on a class `A`, create an instance of it `a`, and then call <span class=\"translation\">从 Python 的角度想想 `@` 表示什么。Python 允许重载数学运算符；例如，如果你在类 `A` 上定义 `__add__`，创建实例 `a`，然后调用：</span>\n\n`a + b`\n\n...that will be interpreted as `a.__add__(b)`. <span class=\"translation\">……这会被解释为 `a.__add__(b)`。</span>\n\nPython 3.5 made it possible to overload the `@` operator too, and defined it as meaning matrix multiplication. So, if you have a class that defines `__matmul__`, then it will be called if you do <span class=\"translation\">Python 3.5 也支持重载 `@` 运算符，并将其定义为矩阵乘法。因此，如果某个类定义了 `__matmul__`，执行下面操作时就会调用它：</span>\n\n`a @ b`\n\n(For completeness -- if the class of `a` doesn't define `__matmul__`, it will check whether `b`'s class defines `__rmatmul__` and call `b.__rmatmul__(a)` if it does.) <span class=\"translation\">（完整来说，如果 `a` 的类没有定义 `__matmul__`，它会检查 `b` 的类是否定义了 `__rmatmul__`，若有则调用 `b.__rmatmul__(a)`。）</span>\n\nSo, what is going on here? When we do <span class=\"translation\">那么这里发生了什么？执行：</span>\n\n`queries @ keys.transpose(1, 2)`\n\n...what we're really saying is: <span class=\"translation\">……真正表达的是：</span>\n\n`queries.__matmul__(keys.transpose(1, 2))`\n\nAlthough I couldn't find it expressed explicitly in [the PyTorch docs for `torch.Tensor`](https://pytorch.org/docs/stable/tensors.html), I'm pretty sure this winds up being the same as <span class=\"translation\">虽然我没能在 [PyTorch `torch.Tensor` 文档](https://pytorch.org/docs/stable/tensors.html)中找到明确表述，但我很确定这最终等价于：</span>\n\n`queries.matmul(keys.transpose(1, 2))`\n\n\\-- that is, the dunder (double-underscore) method is an alias for the non-dunder one. This is in turn an alias for `torch.matmul`, so we're actually doing this: <span class=\"translation\">——也就是说，双下划线方法是非双下划线方法的别名。而后者又是 `torch.matmul` 的别名，所以实际上执行的是：</span>\n\n`torch.matmul(queries, keys.transpose(1, 2))`\n\n[The docs for that method](https://pytorch.org/docs/stable/generated/torch.matmul.html#torch.matmul) show that it's essentially a whole bunch of special cases to make it generally useful for the kind of stuff that people do in ML models (and let mathematical purity be damned ;-). The important bit is this, because it covers not only this case but also the broadcast case above: <span class=\"translation\">[该方法的文档](https://pytorch.org/docs/stable/generated/torch.matmul.html#torch.matmul)显示，它本质上是一大堆特殊情况，使其对机器学习模型中的常见操作普遍有用（数学纯洁性见鬼去吧 ;-)）。重要的是，下面这条规则不仅涵盖当前情况，也涵盖上面的广播情况：</span>\n\n> If both arguments are at least 1-dimensional and at least one argument is N-dimensional (where N > 2), then a batched matrix multiply is returned. <span class=\"translation\">如果两个参数至少都是一维，并且至少有一个参数是 N 维（N &gt; 2），则返回批量矩阵乘法结果。</span>\n\nSo, no need for any complicated tensor maths. PyTorch knows that people want to do batched matrix multiplications for its normal use cases, and the `@` operator is designed for exactly that. We just make sure that our batch dimension (or indeed, per those docs, our batch dimensions) are at the start and the matrix dimensions are the last two, and it will happily broadcast the whole thing for us. <span class=\"translation\">因此，不需要复杂的张量数学。PyTorch 知道常见用法需要批量矩阵乘法，`@` 运算符正是为此设计的。只需确保批次维度（根据文档，也可能是多个批次维度）在最前面，矩阵维度在最后两位，它就会愉快地为我们广播整个操作。</span>\n\nIn a way I was a little disappointed when I worked that out -- it's just a special-case utility function rather than some kind of deep new maths. But never mind; deep knowledge of tensors can wait for another day. <span class=\"translation\">弄清之后我有点失望——这只是一个特殊情况的工具函数，而不是什么深奥的新数学。不过没关系，深入理解张量可以留到以后。</span>\n\nLet's get on to the last part of the differences between the old and the new code: the causal attention. <span class=\"translation\">下面进入新旧代码差异的最后一部分：因果注意力。</span>\n\n#### Causal attention <span class=\"translation\">因果注意力</span>\n\nThe two remaining differences between Raschka's first code example and the second are both to do with the masked self-attention. The first is this bit from the end of the `__init__` method: <span class=\"translation\">Raschka 第一段和第二段代码剩下的两个差异，都与掩码自注意力有关。第一处是 `__init__` 方法末尾的这段：</span>\n\n`self.register_buffer(     'mask',     torch.triu(torch.ones(context_length, context_length), diagonal=1) )`\n\nThat `torch.triu` call is exactly what we had in the code in the [section on causal attention](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention) -- it's a mask that will be applied to the attention weights to clear the ones that would imply that a token was paying attention to tokens that came after it. Raschka somewhat glosses over what the whole `register_buffer` thing is all about, though he does mention that it means that the mask will automatically be moved to the same device as our model so that if (say) the model is on the GPU, so will the mask be, so we won't get errors when trying to use it. <span class=\"translation\">`torch.triu` 调用正是[因果注意力章节](https://www.gilesthomas.com/2025/03/llm-from-scratch-9-causal-attention)代码中的内容——它是一个应用于注意力权重的遮罩，用于清除那些会暗示某词元关注后续词元的权重。Raschka 对整个 `register_buffer` 机制讲得有些简略；他提到遮罩会自动移动到与模型相同的设备，因此如果模型在 GPU 上，遮罩也会在 GPU 上，不会在使用时出错。</span>\n\nThe [docs](https://pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.register_buffer) didn't make things a whole lot clearer for me, but the impression I get is that the `CausalAttention` class that we're creating here is regarded as a single \"thing\" by PyTorch, an `nn.Module`, and will be moved between CPU and GPU as a unit. A variable that was a field of the module -- like, say, `W_query` -- would be treated as part of it, but would also be treated as a trainable parameter by default, and we certainly don't want this mask to be adjusted as part of training -- it should always be a triangular mask with negative infinities in the top right. So we register it as a buffer to make sure that it is part of the module, but isn't treated as a set of trainable parameters. <span class=\"translation\">[文档](https://pytorch.org/docs/stable/generated/torch.nn.Module.html#torch.nn.Module.register_buffer)没有让我更清楚多少，但我的理解是：我们创建的 `CausalAttention` 类在 PyTorch 中被视为一个单独的“东西”（`nn.Module`），会作为整体在 CPU 和 GPU 之间移动。模块的字段（例如 `W_query`）会被当作模块的一部分，但默认也会被当作可训练参数；我们当然不希望这个遮罩在训练中被调整——它应始终是右上角为负无穷的三角形遮罩。因此我们把它注册为 buffer，确保它属于模块，却不会被当成可训练参数。</span>\n\nI'm sure there's more depth there, but I think that's enough for now. <span class=\"translation\">其中肯定还有更深的内容，但目前这样就够了。</span>\n\nLet's move on to the second bit of causal attention code, which is this from the `forward` method: <span class=\"translation\">继续看因果注意力代码的第二处，这是 `forward` 方法中的：</span>\n\n`attn_scores.masked_fill_(     self.mask.bool()[:num_tokens, :num_tokens],     -torch.inf )`\n\nIn the original causal attention code we had this: <span class=\"translation\">原来的因果注意力代码是：</span>\n\n`masked = attn_scores.masked_fill(mask.bool(), -torch.inf)`\n\nRaschka notes that the trailing underscore on `masked_fill_` in the new code is just PyTorch shorthand to say \"do this operation in place\", which explains why we don't need to bind it to a new variable -- `attn_scores` is being changed directly. <span class=\"translation\">Raschka 指出，新代码中 `masked_fill_` 末尾的下划线只是 PyTorch 的简写，表示“原地执行该操作”；这解释了为什么不需要把结果绑定到新变量——`attn_scores` 会被直接修改。</span>\n\nThe next bit is this `[:num_tokens, :num_tokens]` at the end of the mask; this is just because the original code was assuming that the mask was the right size for the `attn_scores` matrix that we were working with -- that is, all of our inputs were the full `context_length` in length, whereas here we're allowing for variable lengths -- so we just take the sub-matrix of the mask that is required for this scores matrix. <span class=\"translation\">遮罩末尾的 `[:num_tokens, :num_tokens]` 是因为原代码假设遮罩大小正好与我们处理的 `attn_scores` 矩阵相同——所有输入长度都是完整的 `context_length`；而这里允许可变长度，所以只取遮罩中该分数矩阵所需的子矩阵。</span>\n\nThe elephant in the room is, of course: what about the batches? [The docs](https://pytorch.org/docs/stable/generated/torch.Tensor.masked_fill_.html#torch.Tensor.masked_fill_) indicate what's going on -- it's just another broadcast operation, so it will do exactly what we expect. <span class=\"translation\">房间里的大象当然是：批次怎么办？[文档](https://pytorch.org/docs/stable/generated/torch.Tensor.masked_fill_.html#torch.Tensor.masked_fill_)说明了情况——这又是一次广播操作，因此会完全按预期工作。</span>\n\nAnd that's it! <span class=\"translation\">就这样！</span>\n\n### Wrapping it up <span class=\"translation\">总结</span>\n\nSo, as it turned out, this was actually all pretty simple. There's a certain amount of depth there -- I still need to learn a bit more about PyTorch buffers -- but instead of the deep linear algebra that I thought I was going to have to learn to work out what multiplying order >2 tensors together actually means, we just have what amount to utility functions that know that dealing with batches of matrices is a common pattern, and allow us to treat them essentially the same as matrices, doing the sensible thing with them on that basis. <span class=\"translation\">事实证明，这一切其实相当简单。其中确实有一定深度——我还需要进一步了解 PyTorch 的 buffer——但没有我原以为需要学习的、用来理解高阶张量（阶数 &gt;2）相乘的深奥线性代数；我们只有一些实用函数，知道矩阵批次是常见模式，允许我们基本像处理矩阵一样处理它们，并以合理方式执行操作。</span>\n\nThis is a good thing! You want the tools you're using to be optimised for the job that you're doing. <span class=\"translation\">这是好事！你希望使用的工具针对手头的工作进行了优化。</span>\n\nI think that for me the biggest lesson from working through this section is actually to look out for AIs as an enabler for excessive curiosity. The reason I took so long to work through something so relatively simple is that I could see that there were operations with higher-order tensors going on, and started chatting about what that means, and this stuff is fascinating (for very geeky values of fascinating). There's a ton to learn there, and I suspect that much of it is useful as well as interesting. <span class=\"translation\">对我来说，通读本节最大的收获，其实是要警惕 AI 成为过度好奇的助推器。我花这么久处理一个相对简单的主题，是因为看到其中涉及高阶张量运算，就开始聊这意味着什么；这些东西非常迷人（以极客式的“迷人”而言）。那里有大量值得学习的内容，我怀疑其中很多既有用又有趣。</span>\n\nBut as it turned out, just RTFMing was the best way forward. What we're looking at here isn't really advanced maths, it's just engineering. The docs are your friends, and are probably the best first port of call, especially if you want to keep yourself grounded and avoid side quests. <span class=\"translation\">但事实证明，直接 RTFM（读他妈的手册）才是最好的前进方式。这里并不是高级数学，而是工程。文档是你的朋友，可能是最好的第一站，尤其当你想保持脚踏实地、避免支线任务时。</span>\n\n### Conclusion <span class=\"translation\">结语</span>\n\nSo, that was batching. Not as simple as I thought it might be, but -- having avoided a full-on diversion into deep linear algebra -- not a huge lift either. Next step: multi-head attention. My suspicion is that even more tensors and batched matrix multiplication helper functions will be involved somewhere... <span class=\"translation\">这就是批处理。没有我想象的那么简单，但避开深入线性代数的全面偏离后，也没那么费力。下一步：多头注意力。我猜某处还会涉及更多张量和批量矩阵乘法辅助函数……</span>\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention). <span class=\"translation\">[这是本系列下一篇文章的链接](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention)。</span>\n\n* * *\n\n1.  In the interest of being maximally annoying, here's a niche joke that will infuriate those that get it because they'll be stuck with the world's worst earworm. <span class=\"translation\">为了尽可能烦人，这里有一个小众笑话，会让听懂的人气恼，因为他们会被世界上最糟糕的洗脑神曲缠住。</span>\n    \n    ```\n    Eight, sir; seven, sir;\n    Six, sir; five, sir;\n    Four, sir; three, sir;\n    Two, sir, one!\n    \n    Tenser, said the Tensor.\n    Tenser, said the Tensor.\n    \n    Tension, apprehension,\n    And dissension have begun.\n    \n    <RIFF>\n    \n    Tension, apprehension,\n    And dissension have begun\n    \n    <RIFF>\n    \n    Tension...\n    ```\n    \n    [↩](#fnref-1)\n    \n2.  To be more precise, because the `nn.Linear` is actually designed to do a forward pass of a normal NN, so what's going on internally is something more like K\\=XWkT+b, where b is all zeros (because we set `bias` to `False`) and PyTorch is internally transposing Wk as part of its initialisation. But that's kind of a nit at this point, I think. [↩](#fnref-2) <span class=\"translation\">更准确地说，因为 `nn.Linear` 实际上是为普通神经网络的前向传播设计的，内部发生的事情更像 K=XWkᵀ+b，其中 b 全为 0（因为我们把 `bias` 设为 `False`），而 PyTorch 在初始化过程中会在内部转置 Wk。不过这在此刻算是吹毛求疵。 [↩](#fnref-2)</span>\n    \n3.  Correctly, IMO [↩](#fnref-3) <span class=\"translation\">严格来说，我认为如此。[↩](#fnref-3)</span>",
  "titleZh": "从零开始编写大型语言模型，第 11 部分——批次",
  "summaryZh": "批处理能加速训练和推理，但对 LLM 不能只用矩阵：我们需要高阶张量。"
}
