{
  "slug": "llm-from-scratch-2",
  "part": 2,
  "title": "Writing an LLM from scratch, part 2",
  "shortTitle": "part 2",
  "date": "23 December 2024",
  "summary": "The basics of embeddings and tokenisation.",
  "href": "https://www.gilesthomas.com/2024/12/llm-from-scratch-2",
  "phaseId": "foundations",
  "phaseLabel": "01 · 基础",
  "markdown": "I'm reading [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", and planning to post every day (or at least, every day I read some of it -- Christmas day I suspect I'll not be posting) with notes on what I found interesting.\n\n[Here's a link to the previous post in this series](https://www.gilesthomas.com/2024/12/llm-from-scratch-1).\n\nI had been planning to do a chapter a day, but that is looking optimistic for such a dense book! So today, I've read the first half or so of Chapter 2, \"Working with text data\". This gives an overview of the pre-processing that happens to text before it hits the LLM, goes on to describe a simple tokenization system (complete with source code), and then briefly covers the byte pair encoding method that we'll actually be using for the LLM.\n\n### The overview\n\nThe core idea here is that LLMs cannot actually process text directly -- not even words. I think it's pretty common knowledge that the starting point for inputting text to one is to tokenize it (there was a lot of that during [my adventures with fine-tuning](https://www.gilesthomas.com/fine-tuning)), but it turns out that that is only the first step.\n\nThe process is:\n\n-   Get the raw text, which is a series of characters (the purist in me wants to either say \"bytes\" here, or to add a preliminary step which is \"work out what encoding was used for the text and convert it into something consistent across documents\").\n-   Tokenize that, so that you get a series of token IDs -- integers, one per word or sub-word.\n-   For each token, generate an embedding.\n\n...and then it's the embeddings that are actually sent to the LLM.\n\n### Introduction to word embeddings\n\nThe embeddings in question are just one-per-token, not the big embeddings of a whole sentence or document that were being used in the encoder-decoder transformer presented in the [first chapter](https://www.gilesthomas.com/2024/12/llm-from-scratch-1). But they're still just high-dimensional vectors that represent in some abstract way the meaning of the tokens in question.\n\nRashka gives as an example the Word2vec embedding that was quite big news a while back, because you could do arithmetic with the vectors it produced and get reasonable results. For example, using `w2v` as an imaginary function that takes a word and returns the embedding vector, and treating `+` and `-` as element-wise operators on vectors, we can see stuff like this:\n\n```\nw2v(\"king\") - w2v(\"man\") + w2v(\"woman\") ~= w2v(\"queen\")\n```\n\nor:\n\n```\nw2v(\"Paris\") - w2v(\"France\") + w2v(\"Germany\") ~= w2v(\"Berlin\")\n```\n\n...but more problematically:\n\n```\nw2v(\"doctor\") - w2v(\"man\") + w2v(\"woman\") ~= w2v(\"nurse\")\n```\n\n...which made it clear that keeping training material bias-free was going to be important.\n\nAnyway, he notes that the embeddings used for LLMs are generally not pre-created ones like Word2vec but are instead generated by embedding engines that are trained alongside the LLM itself. One surprise for me was how many dimensions these have -- he says 768 dimensions for GPT-2 [1](#fn-1) but a massive 12,288 dimensions for GPT-3!\n\nHe also briefly touches on the existence of embedding models for audio and video, and while he doesn't mention it explicitly, that suggests to me how the multi-modal LLMs that we have now might work. After all, if an LLM just takes embeddings as an input, then so long as they're compatible in some sense, there's no reason why it might not reason about audio embeddings or image embeddings just like it does about word embeddings. That's not much more than speculation on my part, though.\n\nSomething else that occurred to me while reading this is that if an LLM is working specifically with embeddings for its calculations, it seems plausible that the immediate output might be an embedding for the next token rather than the token itself. So we'd somehow need to map from embedding to token, which would be hard (finding the nearest token to a random embedding in a space with thousands of dimensions is an expensive operation) so perhaps that symmetry embedding -> LLM -> embedding is just a mistake on my part, and the LLM really does just produce a next token (or rather, a set of tokens with associated probabilities). We'll see!\n\n### Tokenization: writing your own\n\nThis part was something with less new stuff for me -- the concept of tokenization, converting words (or parts of words) into numbers is something that I think anyone reading this will be familiar with, and the implementation that Raschka works through is a pretty simple and clear one.\n\nFor the first version, we split text into words and punctuation, eg.\n\n```\n\"Hello, how are you?\" -> [\"Hello\", \",\", \"how\", \"are\", \"you\", \"?\"]\n```\n\n(He notes that whitespace can be kept if required for the use case, eg. Python code.)\n\nOnce you've done that, assign a unique integer for each unique word/punctuation mark, and build a mapping from one to the other.\n\nWith that, you can build a simple class with an encode method to map text to lists of token IDs and a decode method to convert such a list back to text.\n\nHowever, if it encounters a word it hasn't seen before, it will break. So for the second version, we enhance it to handle unknown words. He does this by adding the string `\"<|unk|>\"` to the vocabulary before generating the word-to-ID mapping, so it gets an ID, and then modifying the encoder so that if it encounters a word that it doesn't recognise, it will output that token. He also adds on an `\"<|endoftext|>\"` token so that different documents in the same input stream can be separated.\n\nThis is a good solution, and works well, but the impression I've got from reading around previously is that it's not current best practice (as in, as of GPT-4). I imagine it's the system used here because all Raschka is trying to achieve with this code is an example of how tokenization works -- that is, the code is a simple implementation for pedagogical purposes.\n\nBut I'll explain what I understand to be the problem with it anyway, mostly as a way to make sure I really do understand it properly :-)\n\nIf `\"<|endoftext|>\"` appears as a string in a vocabulary for a naive encoder that maps from words to token IDs, then someone could put that exact string into a prompt for an LLM application, and get that token. I'm pretty sure I remember some early prompt injections/jailbreaks for the original launch of ChatGPT that used tricks that appeared to work along those lines.\n\nThere are ways to avoid that (see the next section for a good example), but it has the feel of something that someone sufficiently talented at jailbreaking might be able to work around. Whatever part of my brain sets off alarms at potential SQL injections in website code is certainly getting triggered when I see it.\n\nFrom what I've gatherered (and I may be completely wrong on this), the latest tokenizers don't have any specific string mapping to the special tokens -- that is, they're just token IDs. There would presumably be some way to output them in the `decode` method, but there would be no way to generate one by using specially formatted text in the `encode` method. So, for example, the only way to generate the token ID reserved for unknown words would be to provide an unknown word.\n\nAnyway, all of this is moot, because the tokenizer we'll be using for the LLM itself is not the one above, but something more sophisticated.\n\n### Tokenization: byte pair encoding\n\nThe problem with using tokenization with a fixed vocabulary like the one above is that either:\n\n-   You wind up having to scan your whole training set to find all of the unique words (if you're training on a scrape of the whole Internet you're going to find all kinds of things like `lksfdklkajfdfklj`). You get a massive vocabulary with a bunch of almost-never-used words, and yet, after training, you'll still have problems with inputs that contain words that you didn't see in training.\n-   Or you just accept that there will be unknown words during training too, which limits what your LLM can learn.\n\nWe can avoid that problem with a more sophisticated tokenizer. Rashka gives some sample code using OpenAI's [tiktoken](https://pypi.org/project/tiktoken/) library, which uses byte-pair encoding to do tokenization. Byte-pair encoding is a system where the tokenizer has learned its own set of tokens through a training process -- that is:\n\n-   It started off with tokens for all of the letters, numbers and punctuation marks\n-   It was then shown a lot of data, spotted common combinations of existing tokens, and created new tokens for them\n\nUnknown words can be represented by the tokens that it does have -- in the worst case, it just needs to spell them out letter-by-letter.\n\nThe sample code he shows gives an example:\n\n`In [64]: import tiktoken  In [65]: tokenizer = tiktoken.get_encoding(\"gpt2\")  In [66]: text = \"Hello, do you like tea? <|endoftext|> In the sunlit terraces of someunknownPlace.\"  In [67]: integers = tokenizer.encode(text, allowed_special={\"<|endoftext|>\"})  In [68]: tokenizer.decode(integers) Out[68]: 'Hello, do you like tea? <|endoftext|> In the sunlit terraces of someunknownPlace.'`\n\nI found it particularly interesting to see what the specific tokens were:\n\n`In [69]: [tokenizer.decode([ii]) for ii in integers] Out[69]: ['Hello',  ',',  ' do',  ' you',  ' like',  ' tea',  '?',  ' ',  '<|endoftext|>',  ' In',  ' the',  ' sun',  'lit',  ' terr',  'aces',  ' of',  ' some',  'unknown',  'Place',`\n\nSo you can see that `\"someunknownPlace\"` was broken up into the separate tokens for `\"some\"`, `\"unknown\"`, and `\"Place\"`\n\nThat also highlights something I'd gathered previously from reading elsewhere -- modern tokenisers often include leading spaces as part of the token itself, so -- for example -- there are different tokens for `\" do\"` and `\"do\"` -- they have different IDs:\n\n`In [70]: print(tokenizer.encode(\"do do\")) [4598, 466]`\n\n...and when decoded look different:\n\n`In [71]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"do do\")] Out[71]: ['do', ' do']`\n\nAnother thing that interested me was that `allowed_special` parameter in the `encode` call, which is a precaution to prevent the kind of jailbreaking issue I mentioned earlier. With it, it will happily parse the text as a special token:\n\n`In [72]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\", allowed_special={\"<|endoftext|>\"})] Out[72]: ['<|endoftext|>']`\n\nWithout it, the tokenizer recognizes it and rejects it:\n\n`In [73]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\")] --------------------------------------------------------------------------- ValueError                                Traceback (most recent call last) Cell In[73], line 1 ----> 1 [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\")]  File ~/.virtualenvs/llm-from-scratch/lib/python3.12/site-packages/tiktoken/core.py:117, in Encoding.encode(self, text, allowed_special, disallowed_special)     115         disallowed_special = frozenset(disallowed_special)     116     if match := _special_token_regex(disallowed_special).search(text): --> 117         raise_disallowed_special_token(match.group())     119 try:     120     return self._core_bpe.encode(text, allowed_special)  File ~/.virtualenvs/llm-from-scratch/lib/python3.12/site-packages/tiktoken/core.py:398, in raise_disallowed_special_token(token)     397 def raise_disallowed_special_token(token: str) -> NoReturn: --> 398     raise ValueError(     399         f\"Encountered text corresponding to disallowed special token {token!r}.\\n\"     400         \"If you want this text to be encoded as a special token, \"     401         f\"pass it to <!--CODE_BLOCK_4958-->, e.g. <!--CODE_BLOCK_4959-->.\\n\"     402         f\"If you want this text to be encoded as normal text, disable the check for this token \"     403         f\"by passing <!--CODE_BLOCK_4960-->.\\n\"     404         \"To disable this check for all special tokens, pass <!--CODE_BLOCK_4961-->.\\n\"     405     )  ValueError: Encountered text corresponding to disallowed special token '<|endoftext|>'. If you want this text to be encoded as a special token, pass it to <!--CODE_BLOCK_4962-->, e.g. <!--CODE_BLOCK_4963-->. If you want this text to be encoded as normal text, disable the check for this token by passing <!--CODE_BLOCK_4964-->. To disable this check for all special tokens, pass <!--CODE_BLOCK_4965-->.`\n\n...or, as per that (excellently detailed) error message, you can get it to be parsed as just a sequence of characters with no special meaning:\n\n`In [77]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\", disallowed_special=(tokenizer.special_tokens_set - {'<|endoftext|>'}))] Out[77]: ['<', '|', 'end', 'of', 'text', '|', '>']`\n\nThat last one feels a little risky! In a larger system (like some kind of framework with LLMs talking to each other) it's easy to imagine that stream of tokens being reassembled by naive code and then tokenized as if it were trusted.\n\n### Summary\n\nAnyway, I felt that this was quite enough for today. Interestingly, I think I've spent about twice as long typing up these notes as I did reading it, but perhaps that's a good balance. I'm certainly sure that I'll remember what I've read much better as a result of the writeup!\n\nThe next bit, which I expect to read tomorrow (though it's Christmas Eve, so I may not manage) is on data sampling, followed by creating the embeddings. I was expecting the latter to be pretty complicated, but on a quick scan ahead I see that what all we're doing at this stage is generating random embeddings, which makes sense -- the actual values will be learned when we get on to training.\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2024/12/llm-from-scratch-3).\n\n### Nits and oddities\n\nOnly one thing today -- it really is me nit-picking, but on p28 the output of a command `print(tokenizer.decode(ids))` is rendered as\n\n```\n'\" It\\' s the last to be painted, you know,\" Mrs. Gisburn said with pardonable pride.'\n```\n\nIt should be\n\n```\n\" It' s the last to be painted, you know,\" Mrs. Gisburn said with pardonable pride.\n```\n\n\\-- my guess is that it was copy-pasted from a CLI session where the command was just `tokenizer.decode(ids)`, so Python provided a repr.\n\nBasically a typo and I feel kind of bad about even mentioning it ;-)\n\n* * *\n\n1.  It later becomes clear that it's the 124M parameter model in the GPT-2 series that has 768 embedding dimensions -- there are other larger models in the series that have more, up to 1,600 for the \"XL\" model. So, still much fewer than GPT-3. [↩](#fnref-1)",
  "imageCount": 0,
  "wordCount": 2277,
  "readingMinutes": 10,
  "syncedAt": "2026-08-11T08:01:50.133Z",
  "license": "CC BY 4.0",
  "author": "Giles Thomas",
  "markdownBilingual": "I'm reading [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", and planning to post every day (or at least, every day I read some of it -- Christmas day I suspect I'll not be posting) with notes on what I found interesting. <span class=\"translation\">我正在阅读 [Sebastian Raschka](https://sebastianraschka.com/) 的著作《[从零开始构建大型语言模型](https://www.manning.com/books/build-a-large-language-model-from-scratch)》，计划每天（或者至少每天读到一些内容时）发文，记录我觉得有趣的东西——我猜圣诞节不会发。</span>\n\n[Here's a link to the previous post in this series](https://www.gilesthomas.com/2024/12/llm-from-scratch-1). <span class=\"translation\">[这是本系列上一篇文章的链接](https://www.gilesthomas.com/2024/12/llm-from-scratch-1)。</span>\n\nI had been planning to do a chapter a day, but that is looking optimistic for such a dense book! So today, I've read the first half or so of Chapter 2, \"Working with text data\". This gives an overview of the pre-processing that happens to text before it hits the LLM, goes on to describe a simple tokenization system (complete with source code), and then briefly covers the byte pair encoding method that we'll actually be using for the LLM. <span class=\"translation\">我原本计划每天读一章，但对于这样一本密度极高的书来说，这个目标看起来过于乐观！所以今天我读完了第 2 章《处理文本数据》前半章左右。本章概述了文本进入 LLM 之前的预处理过程，介绍了一个简单的分词系统（包含源代码），然后简要讲解了我们实际会用于 LLM 的字节对编码方法。</span>\n\n### The overview <span class=\"translation\">概览</span>\n\nThe core idea here is that LLMs cannot actually process text directly -- not even words. I think it's pretty common knowledge that the starting point for inputting text to one is to tokenize it (there was a lot of that during [my adventures with fine-tuning](https://www.gilesthomas.com/fine-tuning)), but it turns out that that is only the first step. <span class=\"translation\">这里的核心思想是：LLM 实际上不能直接处理文本——甚至不能直接处理单词。我想大家普遍知道，把文本输入 LLM 的起点是分词（我在[微调之旅](https://www.gilesthomas.com/fine-tuning)中就做了很多分词），但事实证明那只是第一步。</span>\n\nThe process is: <span class=\"translation\">流程是：</span>\n\n-   Get the raw text, which is a series of characters (the purist in me wants to either say \"bytes\" here, or to add a preliminary step which is \"work out what encoding was used for the text and convert it into something consistent across documents\"). <span class=\"translation\">获取原始文本，它是一系列字符（我内心的纯粹主义者要么想在这里说“字节”，要么想加一个预备步骤：“弄清文本使用的编码，并把它转换成跨文档一致的形式”）。</span>\n-   Tokenize that, so that you get a series of token IDs -- integers, one per word or sub-word. <span class=\"translation\">对文本进行分词，得到一系列词元 ID——整数，每个单词或子词对应一个。</span>\n-   For each token, generate an embedding. <span class=\"translation\">为每个词元生成一个嵌入。</span>\n\n...and then it's the embeddings that are actually sent to the LLM. <span class=\"translation\">……然后真正送入 LLM 的就是这些嵌入。</span>\n\n### Introduction to word embeddings <span class=\"translation\">词嵌入简介</span>\n\nThe embeddings in question are just one-per-token, not the big embeddings of a whole sentence or document that were being used in the encoder-decoder transformer presented in the [first chapter](https://www.gilesthomas.com/2024/12/llm-from-scratch-1). But they're still just high-dimensional vectors that represent in some abstract way the meaning of the tokens in question. <span class=\"translation\">这里说的嵌入是每个词元一个，而不是第一章介绍的编码器—解码器 Transformer 使用的整句或整篇文档的大嵌入。但它们仍然是高维向量，以某种抽象方式表示相关词元的含义。</span>\n\nRashka gives as an example the Word2vec embedding that was quite big news a while back, because you could do arithmetic with the vectors it produced and get reasonable results. For example, using `w2v` as an imaginary function that takes a word and returns the embedding vector, and treating `+` and `-` as element-wise operators on vectors, we can see stuff like this: <span class=\"translation\">Raschka 以曾经引起轰动的 Word2vec 嵌入为例，因为你可以对它生成的向量做算术运算，并得到合理结果。例如，把 `w2v` 当作一个接收单词并返回嵌入向量的虚构函数，把 `+` 和 `-` 视为向量按元素运算符，我们可以看到这样的结果：</span>\n\n```\nw2v(\"king\") - w2v(\"man\") + w2v(\"woman\") ~= w2v(\"queen\")\n```\n\nor: <span class=\"translation\">或者：</span>\n\n```\nw2v(\"Paris\") - w2v(\"France\") + w2v(\"Germany\") ~= w2v(\"Berlin\")\n```\n\n...but more problematically: <span class=\"translation\">……但更麻烦的是：</span>\n\n```\nw2v(\"doctor\") - w2v(\"man\") + w2v(\"woman\") ~= w2v(\"nurse\")\n```\n\n...which made it clear that keeping training material bias-free was going to be important. <span class=\"translation\">这说明，让训练材料不带偏见将非常重要。</span>\n\nAnyway, he notes that the embeddings used for LLMs are generally not pre-created ones like Word2vec but are instead generated by embedding engines that are trained alongside the LLM itself. One surprise for me was how many dimensions these have -- he says 768 dimensions for GPT-2 [1](#fn-1) but a massive 12,288 dimensions for GPT-3! <span class=\"translation\">无论如何，他指出，LLM 使用的嵌入通常不是像 Word2vec 那样预先创建的，而是由与 LLM 一起训练的嵌入引擎生成。令我惊讶的是它们的维度如此之多——他说 GPT-2 有 768 维[1](#fn-1)，而 GPT-3 则有惊人的 12,288 维！</span>\n\nHe also briefly touches on the existence of embedding models for audio and video, and while he doesn't mention it explicitly, that suggests to me how the multi-modal LLMs that we have now might work. After all, if an LLM just takes embeddings as an input, then so long as they're compatible in some sense, there's no reason why it might not reason about audio embeddings or image embeddings just like it does about word embeddings. That's not much more than speculation on my part, though. <span class=\"translation\">他还简要提到音频和视频嵌入模型的存在。虽然他没有明说，但这让我想到如今的多模态 LLM 可能就是这样工作的。毕竟，如果 LLM 只接收嵌入作为输入，那么只要它们在某种意义上兼容，就没有理由不能像处理词嵌入一样处理音频嵌入或图像嵌入并进行推理。不过这还只是我的推测。</span>\n\nSomething else that occurred to me while reading this is that if an LLM is working specifically with embeddings for its calculations, it seems plausible that the immediate output might be an embedding for the next token rather than the token itself. So we'd somehow need to map from embedding to token, which would be hard (finding the nearest token to a random embedding in a space with thousands of dimensions is an expensive operation) so perhaps that symmetry embedding -> LLM -> embedding is just a mistake on my part, and the LLM really does just produce a next token (or rather, a set of tokens with associated probabilities). We'll see! <span class=\"translation\">阅读时我还想到，如果 LLM 的计算专门基于嵌入，那么直接输出似乎可能是下一个词元的嵌入，而不是词元本身。这样我们就得把嵌入映射回词元，这会很困难（在数千维空间中寻找随机嵌入最近的词元是昂贵的操作）；所以“嵌入 → LLM → 嵌入”的对称结构也许只是我的误解，LLM 实际上确实直接生成下一个词元（或者说，带有相应概率的一组词元）。以后就知道了！</span>\n\n### Tokenization: writing your own <span class=\"translation\">分词：自己动手编写</span>\n\nThis part was something with less new stuff for me -- the concept of tokenization, converting words (or parts of words) into numbers is something that I think anyone reading this will be familiar with, and the implementation that Raschka works through is a pretty simple and clear one. <span class=\"translation\">这一部分对我来说新东西较少——把单词（或单词的一部分）转换成数字的分词概念，我想读到这里的人都很熟悉；Raschka 讲解的实现也相当简单清晰。</span>\n\nFor the first version, we split text into words and punctuation, eg. <span class=\"translation\">第一版实现是把文本拆成单词和标点，例如：</span>\n\n```\n\"Hello, how are you?\" -> [\"Hello\", \",\", \"how\", \"are\", \"you\", \"?\"]\n```\n\n(He notes that whitespace can be kept if required for the use case, eg. Python code.) <span class=\"translation\">（他指出，如果使用场景需要，也可以保留空白字符，例如 Python 代码。）</span>\n\nOnce you've done that, assign a unique integer for each unique word/punctuation mark, and build a mapping from one to the other. <span class=\"translation\">完成这一步后，为每个不同的单词/标点符号分配一个唯一整数，并建立二者之间的映射。</span>\n\nWith that, you can build a simple class with an encode method to map text to lists of token IDs and a decode method to convert such a list back to text. <span class=\"translation\">有了这些，就可以构建一个简单的类：用 encode 方法把文本映射为词元 ID 列表，再用 decode 方法把列表还原成文本。</span>\n\nHowever, if it encounters a word it hasn't seen before, it will break. So for the second version, we enhance it to handle unknown words. He does this by adding the string `\"<|unk|>\"` to the vocabulary before generating the word-to-ID mapping, so it gets an ID, and then modifying the encoder so that if it encounters a word that it doesn't recognise, it will output that token. He also adds on an `\"<|endoftext|>\"` token so that different documents in the same input stream can be separated. <span class=\"translation\">不过，一旦遇到之前没见过的单词，它就会崩溃。因此在第二版中，我们增强了它处理未知词的能力。具体做法是在生成单词到 ID 的映射前，把字符串 `\"&lt;|unk|&gt;\"` 加入词汇表，这样它就会获得一个 ID；然后修改编码器，使它遇到不认识的单词时输出这个词元。他还加入了 `\"&lt;|endoftext|&gt;\"` 词元，以便在同一个输入流中分隔不同文档。</span>\n\nThis is a good solution, and works well, but the impression I've got from reading around previously is that it's not current best practice (as in, as of GPT-4). I imagine it's the system used here because all Raschka is trying to achieve with this code is an example of how tokenization works -- that is, the code is a simple implementation for pedagogical purposes. <span class=\"translation\">这是一个不错且运行良好的方案，但根据我此前阅读资料的印象，它并不是当前最佳实践（至少在 GPT-4 时代不是）。我猜这里采用它，是因为 Raschka 只想用这段代码示范分词如何工作——也就是说，这是出于教学目的的简单实现。</span>\n\nBut I'll explain what I understand to be the problem with it anyway, mostly as a way to make sure I really do understand it properly :-) <span class=\"translation\">不过我还是解释一下我所理解的问题，主要是为了确认自己真的理解了 :-)。</span>\n\nIf `\"<|endoftext|>\"` appears as a string in a vocabulary for a naive encoder that maps from words to token IDs, then someone could put that exact string into a prompt for an LLM application, and get that token. I'm pretty sure I remember some early prompt injections/jailbreaks for the original launch of ChatGPT that used tricks that appeared to work along those lines. <span class=\"translation\">如果 `\"&lt;|endoftext|&gt;\"` 作为字符串出现在一个把单词映射到词元 ID 的朴素编码器词汇表中，那么有人就可以在 LLM 应用的提示词中直接输入这串字符，并得到对应词元。我很确定自己记得 ChatGPT 最初发布时的一些早期提示注入/越狱技巧，似乎就是沿着这个方向起作用的。</span>\n\nThere are ways to avoid that (see the next section for a good example), but it has the feel of something that someone sufficiently talented at jailbreaking might be able to work around. Whatever part of my brain sets off alarms at potential SQL injections in website code is certainly getting triggered when I see it. <span class=\"translation\">有办法避免这个问题（下一节有一个很好的例子），但它给人的感觉是：只要越狱者足够有才，或许就能绕过。每当我在网站代码里看到潜在的 SQL 注入时，大脑中负责拉响警报的那部分也会在这里被触发。</span>\n\nFrom what I've gatherered (and I may be completely wrong on this), the latest tokenizers don't have any specific string mapping to the special tokens -- that is, they're just token IDs. There would presumably be some way to output them in the `decode` method, but there would be no way to generate one by using specially formatted text in the `encode` method. So, for example, the only way to generate the token ID reserved for unknown words would be to provide an unknown word. <span class=\"translation\">据我了解（当然也可能完全错了），最新的分词器不会把任何特定字符串映射到特殊词元——它们只是词元 ID。decode 方法大概有办法输出这些词元，但 encode 方法无法通过特殊格式的文本生成它们。例如，生成保留给未知词的词元 ID，唯一途径应该是提供一个未知单词。</span>\n\nAnyway, all of this is moot, because the tokenizer we'll be using for the LLM itself is not the one above, but something more sophisticated. <span class=\"translation\">无论如何，这些讨论都无关紧要，因为 LLM 本身将使用的分词器不是上面这个，而是更复杂的东西。</span>\n\n### Tokenization: byte pair encoding <span class=\"translation\">分词：字节对编码</span>\n\nThe problem with using tokenization with a fixed vocabulary like the one above is that either: <span class=\"translation\">使用上面那种固定词汇表进行分词的问题在于，以下两者必居其一：</span>\n\n-   You wind up having to scan your whole training set to find all of the unique words (if you're training on a scrape of the whole Internet you're going to find all kinds of things like `lksfdklkajfdfklj`). You get a massive vocabulary with a bunch of almost-never-used words, and yet, after training, you'll still have problems with inputs that contain words that you didn't see in training. <span class=\"translation\">你必须扫描整个训练集来找出所有不同的单词（如果训练数据是从整个互联网抓取的，会遇到 `lksfdklkajfdfklj` 之类的东西）。这样会得到一个庞大的词汇表，其中充斥着几乎从未使用的单词；然而训练结束后，输入中出现训练时没见过的单词仍会造成问题。</span>\n-   Or you just accept that there will be unknown words during training too, which limits what your LLM can learn. <span class=\"translation\">或者你接受训练期间也会出现未知词，但这会限制 LLM 能学到的内容。</span>\n\nWe can avoid that problem with a more sophisticated tokenizer. Rashka gives some sample code using OpenAI's [tiktoken](https://pypi.org/project/tiktoken/) library, which uses byte-pair encoding to do tokenization. Byte-pair encoding is a system where the tokenizer has learned its own set of tokens through a training process -- that is: <span class=\"translation\">我们可以用更复杂的分词器避开这个问题。Raschka 给出了使用 OpenAI [tiktoken](https://pypi.org/project/tiktoken/) 库的示例代码，该库用字节对编码进行分词。字节对编码是一种通过训练过程学习自身词元集合的系统，也就是说：</span>\n\n-   It started off with tokens for all of the letters, numbers and punctuation marks <span class=\"translation\">它最初拥有所有字母、数字和标点符号对应的词元；</span>\n-   It was then shown a lot of data, spotted common combinations of existing tokens, and created new tokens for them <span class=\"translation\">然后接触大量数据，找出已有词元的常见组合，并为这些组合创建新词元。</span>\n\nUnknown words can be represented by the tokens that it does have -- in the worst case, it just needs to spell them out letter-by-letter. <span class=\"translation\">未知单词可以用已有词元表示——最坏情况下，只需逐字母拼写出来。</span>\n\nThe sample code he shows gives an example: <span class=\"translation\">他展示的示例代码如下：</span>\n\n`In [64]: import tiktoken  In [65]: tokenizer = tiktoken.get_encoding(\"gpt2\")  In [66]: text = \"Hello, do you like tea? <|endoftext|> In the sunlit terraces of someunknownPlace.\"  In [67]: integers = tokenizer.encode(text, allowed_special={\"<|endoftext|>\"})  In [68]: tokenizer.decode(integers) Out[68]: 'Hello, do you like tea? <|endoftext|> In the sunlit terraces of someunknownPlace.'`\n\nI found it particularly interesting to see what the specific tokens were: <span class=\"translation\">我觉得最有趣的是看到具体的词元是什么：</span>\n\n`In [69]: [tokenizer.decode([ii]) for ii in integers] Out[69]: ['Hello',  ',',  ' do',  ' you',  ' like',  ' tea',  '?',  ' ',  '<|endoftext|>',  ' In',  ' the',  ' sun',  'lit',  ' terr',  'aces',  ' of',  ' some',  'unknown',  'Place',`\n\nSo you can see that `\"someunknownPlace\"` was broken up into the separate tokens for `\"some\"`, `\"unknown\"`, and `\"Place\"` <span class=\"translation\">你可以看到，`\"someunknownPlace\"` 被拆成了 `\"some\"`、`\"unknown\"` 和 `\"Place\"` 三个独立词元。</span>\n\nThat also highlights something I'd gathered previously from reading elsewhere -- modern tokenisers often include leading spaces as part of the token itself, so -- for example -- there are different tokens for `\" do\"` and `\"do\"` -- they have different IDs: <span class=\"translation\">这也印证了我此前从其他资料中了解到的一点：现代分词器往往把前导空格作为词元本身的一部分，因此 `\" do\"` 和 `\"do\"` 是不同的词元，拥有不同的 ID：</span>\n\n`In [70]: print(tokenizer.encode(\"do do\")) [4598, 466]`\n\n...and when decoded look different: <span class=\"translation\">……解码后看起来也不同：</span>\n\n`In [71]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"do do\")] Out[71]: ['do', ' do']`\n\nAnother thing that interested me was that `allowed_special` parameter in the `encode` call, which is a precaution to prevent the kind of jailbreaking issue I mentioned earlier. With it, it will happily parse the text as a special token: <span class=\"translation\">另一个让我感兴趣的地方是 encode 调用中的 `allowed_special` 参数，这是为了防止我前面提到的越狱问题而采取的预防措施。使用它，分词器会很乐意把文本解析为特殊词元：</span>\n\n`In [72]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\", allowed_special={\"<|endoftext|>\"})] Out[72]: ['<|endoftext|>']`\n\nWithout it, the tokenizer recognizes it and rejects it: <span class=\"translation\">不加这个参数时，分词器会识别出它并拒绝处理：</span>\n\n`In [73]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\")] --------------------------------------------------------------------------- ValueError                                Traceback (most recent call last) Cell In[73], line 1 ----> 1 [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\")]  File ~/.virtualenvs/llm-from-scratch/lib/python3.12/site-packages/tiktoken/core.py:117, in Encoding.encode(self, text, allowed_special, disallowed_special)     115         disallowed_special = frozenset(disallowed_special)     116     if match := _special_token_regex(disallowed_special).search(text): --> 117         raise_disallowed_special_token(match.group())     119 try:     120     return self._core_bpe.encode(text, allowed_special)  File ~/.virtualenvs/llm-from-scratch/lib/python3.12/site-packages/tiktoken/core.py:398, in raise_disallowed_special_token(token)     397 def raise_disallowed_special_token(token: str) -> NoReturn: --> 398     raise ValueError(     399         f\"Encountered text corresponding to disallowed special token {token!r}.\\n\"     400         \"If you want this text to be encoded as a special token, \"     401         f\"pass it to <!--CODE_BLOCK_4958-->, e.g. <!--CODE_BLOCK_4959-->.\\n\"     402         f\"If you want this text to be encoded as normal text, disable the check for this token \"     403         f\"by passing <!--CODE_BLOCK_4960-->.\\n\"     404         \"To disable this check for all special tokens, pass <!--CODE_BLOCK_4961-->.\\n\"     405     )  ValueError: Encountered text corresponding to disallowed special token '<|endoftext|>'. If you want this text to be encoded as a special token, pass it to <!--CODE_BLOCK_4962-->, e.g. <!--CODE_BLOCK_4963-->. If you want this text to be encoded as normal text, disable the check for this token by passing <!--CODE_BLOCK_4964-->. To disable this check for all special tokens, pass <!--CODE_BLOCK_4965-->.`\n\n...or, as per that (excellently detailed) error message, you can get it to be parsed as just a sequence of characters with no special meaning: <span class=\"translation\">……或者按照那条（极其详尽的）错误消息所说，让它把文本解析成一串没有特殊含义的字符：</span>\n\n`In [77]: [tokenizer.decode([ii]) for ii in tokenizer.encode(\"<|endoftext|>\", disallowed_special=(tokenizer.special_tokens_set - {'<|endoftext|>'}))] Out[77]: ['<', '|', 'end', 'of', 'text', '|', '>']`\n\nThat last one feels a little risky! In a larger system (like some kind of framework with LLMs talking to each other) it's easy to imagine that stream of tokens being reassembled by naive code and then tokenized as if it were trusted. <span class=\"translation\">最后这种做法让我觉得有点冒险！在更大的系统中（例如某种让多个 LLM 相互通信的框架），很容易想象这串词元被朴素代码重新拼接，然后又被当作可信文本重新分词。</span>\n\n### Summary <span class=\"translation\">总结</span>\n\nAnyway, I felt that this was quite enough for today. Interestingly, I think I've spent about twice as long typing up these notes as I did reading it, but perhaps that's a good balance. I'm certainly sure that I'll remember what I've read much better as a result of the writeup! <span class=\"translation\">无论如何，我觉得今天读到这里已经够多了。有意思的是，我花在整理这些笔记上的时间大约是阅读时间的两倍，不过这或许是好事——我确信写下来后会记得牢得多！</span>\n\nThe next bit, which I expect to read tomorrow (though it's Christmas Eve, so I may not manage) is on data sampling, followed by creating the embeddings. I was expecting the latter to be pretty complicated, but on a quick scan ahead I see that what all we're doing at this stage is generating random embeddings, which makes sense -- the actual values will be learned when we get on to training. <span class=\"translation\">下一部分预计明天阅读（但明天是平安夜，可能做不到），内容是数据采样，之后是创建嵌入。我原以为后者会很复杂，但快速往后看了一眼，发现现阶段我们只是在生成随机嵌入，这很合理——真正的数值会在开始训练后学出来。</span>\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2024/12/llm-from-scratch-3). <span class=\"translation\">[这是本系列下一篇文章的链接](https://www.gilesthomas.com/2024/12/llm-from-scratch-3)。</span>\n\n### Nits and oddities <span class=\"translation\">小问题与奇闻</span>\n\nOnly one thing today -- it really is me nit-picking, but on p28 the output of a command `print(tokenizer.decode(ids))` is rendered as <span class=\"translation\">今天只有一件事——确实是我在吹毛求疵：第 28 页把命令 `print(tokenizer.decode(ids)` 的输出写成了：</span>\n\n```\n'\" It\\' s the last to be painted, you know,\" Mrs. Gisburn said with pardonable pride.'\n```\n\nIt should be <span class=\"translation\">正确的写法应该是：</span>\n\n```\n\" It' s the last to be painted, you know,\" Mrs. Gisburn said with pardonable pride.\n```\n\n\\-- my guess is that it was copy-pasted from a CLI session where the command was just `tokenizer.decode(ids)`, so Python provided a repr. <span class=\"translation\">我的猜测是，这是从命令行会话中复制粘贴过来的，当时执行的命令只有 `tokenizer.decode(ids)`，所以 Python 提供了 repr。</span>\n\nBasically a typo and I feel kind of bad about even mentioning it ;-) <span class=\"translation\">基本就是一个错别字，我甚至觉得提起它都有点过意不去 ;-)。</span>\n\n* * *\n\n1.  It later becomes clear that it's the 124M parameter model in the GPT-2 series that has 768 embedding dimensions -- there are other larger models in the series that have more, up to 1,600 for the \"XL\" model. So, still much fewer than GPT-3. [↩](#fnref-1) <span class=\"translation\">好消息是，后文确实会解释清楚。坏消息是，要到[第 15 部分](https://www.gilesthomas.com/2025/05/llm-from-scratch-15-from-context-vectors-to-logits)才会讲清楚。 [↩](#fnref-1)</span>",
  "titleZh": "从零开始编写大型语言模型，第 2 部分",
  "summaryZh": "嵌入和分词的基础知识。"
}
