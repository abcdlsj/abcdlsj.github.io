{"slug":"llm-from-scratch-32g-interventions-weight-tying","part":32,"title":"Writing an LLM from scratch, part 32g -- Interventions: weight tying","titleZh":"从零开始编写 LLM，第 32g 篇——干预：权重绑定","shortTitle":"part 32g -- Interventions: weight tying","date":"24 March 2026","summary":"Weight tying is apparently not used in modern LLMs, and intuitively would worsen performance in general. Does it?","summaryZh":"现代 LLM 显然不使用权重绑定，直觉上它通常会降低性能。事实真是如此吗？","href":"https://www.gilesthomas.com/2026/03/llm-from-scratch-32g-interventions-weight-tying","phaseId":"interventions","phaseLabel":"07 · 实验干预","markdown":"In [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", he writes that weight tying, while it reduces the parameter count of a model, in his experience makes it worse. As such, apparently people don't use it in modern LLMs. Intuitively, that makes sense -- I'll explain why in this post.\n\nBut as I'm trying various [interventions](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) to see if I can get my model -- based on Raschka's code, but trained for a fraction of the time that the original GPT-2 model was -- to perform as well as the original in terms of the loss it gets on a test set, I thought it would be worth seeing if it really is a negative for this particular tiny model of 163M parameters.\n\nAfter all, the original weights use weight tying, and I did find that [QKV bias](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias) appeared to help -- and that's another old-school technique that they used, which has since dropped out of fashion. Might this one help too?\n\nWorth a try! Let's give it a go.\n\n### Weight tying: a refresher\n\nI'll start with a quick refresher on what weight tying is, and how it works. This is really targeted at people who've been reading along with this series -- if it's all new to you, you might find my post on [Maths for LLMs](https://www.gilesthomas.com/2025/09/maths-for-llms) a useful catch-up guide first.\n\nIn our LLM code, right at the start, we use an embedding layer to take our input token IDs, and turn them into embeddings -- each token becomes a vector in a high-dimensional space (768 in our case), which we see as representing in some manner the \"meaning\" of the token.\n\nA useful way to think about that is that we could start with a one-hot vector for the token -- that is, with our 50,257-token vocabulary, it would be 50,257 items long, and have zeros in every position apart from the position corresponding to the token's ID. We'll treat that as being a vector in a \"vocab space\". The process of converting the token into an embedding turns out to be equivalent to multiplying that vocab space representation by an embedding matrix -- one with one row per possible token, the values in that row being the values for the appropriate embedding. [1](#fn-1)\n\nBecause matrix multiplications can be seen as projections between different spaces, we can see that as a projection from our vocab space to the embedding space.\n\nOnce we've projected our sequence of tokens into a sequence of embeddings, we do all of the steps required for the LLM -- we add in positional information, run it through the Transformers layers, normalise it, and then we have a new sequence of embeddings. The embedding at position n in that output sequence, if our model is working well, should be something that represents an appropriate next-token prediction for the portion of the input sequence from zero to position n.\n\nWhat we want as our final output is to map that back to the vocab space. We want logits: a list of numbers that (after being run through softmax) will represent the probability that our next token is a particular one.\n\nJust as we mapped from vocab space to embedding space with (conceptually) a matrix multiplication at the start of the process, we can map back with another one. More specifically, if we treat the embedding matrix as having the same number of rows as there are input tokens (which we'll call dvocab) and columns as there are embedding dimensions (demb), then the original vocab-space-to-embedding-space matrix will have this shape:\n\ndvocab×demb\n\nSo it's projecting from a dvocab\\-dimensional space to a demb\\-dimensional one. Similarly, our matrix to do the projection at the end is just a matrix with the numbers of rows and columns swapped around:\n\ndemb×dvocab\n\n...to do a projection in the other direction.\n\nThe trick with weight tying is to see that these two projections can *potentially* be just the opposite of each other. If we assume that the embedding space on the way in to the LLM is essentially the same as the embedding space on the way out, then we can use one projection to go into it from vocab space, and the opposite to go back. The \"opposite\" in this case is the transpose -- that is, if we use Wemb for our embedding matrix and Wout for the output one, we have:\n\nWout\\=WembT\n\nThat means we can re-use all of the embedding parameters for the output projection matrix, and fewer parameters means not only a smaller model, but hopefully faster training. Sounds like a win!\n\nBut of course, there's no such thing as a free lunch. By constraining the output head to be the transpose of the input one, we're essentially enforcing that assumption above: we're saying that the embedding space on the way out *must* be the same as the embedding space on the way in. That limits what the LLM can do -- if it were able to use different embedding spaces at each end, it would have more flexibility, which might help it learn to model things better.\n\nThat's the theory: what does it mean in practice?\n\n### The code\n\nLet's take a quick look at the GPT-2 code -- just the `__init__` for the top level class:\n\n`class GPTModel(nn.Module):      def __init__(self, cfg):         super().__init__()          self.tok_emb = nn.Embedding(cfg[\"vocab_size\"], cfg[\"emb_dim\"])         self.pos_emb = nn.Embedding(cfg[\"context_length\"], cfg[\"emb_dim\"])         self.drop_emb = nn.Dropout(cfg[\"drop_rate\"])          self.trf_blocks = nn.Sequential(             *[TransformersBlock(cfg) for _ in range(cfg[\"n_layers\"])]         )          self.final_norm = LayerNorm(cfg[\"emb_dim\"])          self.out_head = nn.Linear(             cfg[\"emb_dim\"], cfg[\"vocab_size\"], bias=False         )`\n\nFor our embedding layer, we use PyTorch's [`nn.Embedding`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Embedding.html) class, and for the output head we use [`nn.Linear`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Linear.html).\n\nNow, `nn.Embedding` provides us with access to the underlying matrix with a `weight` field:\n\n> `weight` (Tensor) -- the learnable weights of the module of shape (`num_embeddings`, `embedding_dim`) initialized from 𝒩(0,1).\n\nSo, that's exactly the dvocab×demb matrix that we'd expect -- it's the input dimension as the rows, and the output dimension as the columns.\n\nIf we look at `nn.Linear`, we see something very similar:\n\n> weight (torch.Tensor) – the learnable weights of the module of shape (`out_features`, `in_features`) The values are initialized from 𝒰(−k,k) where k\\=1in\\_features\n\nThat's actually the other way around, output dimension as the rows and input as the columns. If you're wondering why, remember that [we transpose the weights matrix for a neural network before using it](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2). But that's actually really convenient in our situation, because if we want to use the same weights for both, they're already \"compatible\"!\n\nAnd that means that adding weight tying to our code above is as simple as adding two lines at the end:\n\n        `if cfg.get(\"tie_weights\", False):             self.out_head.weight = self.tok_emb.weight`\n\nFor the model code, it literally is just that! There is a tiny inefficiency in that PyTorch is going to spend a bit of time initialising the weights in `self.out_head` to appropriately-sized random values, only to have them all replaced -- but that actually works in our favour, because it means that we'll use up the same amount of the random number stream when creating the LLM in both the weight-tying and non-weight-tying cases, which is a bit better for reproducibility.\n\nThere is one other change needed, though. I ran a test train with that code, and checkpointing failed like this:\n\n```\n[rank0]: RuntimeError:\n[rank0]:             Some tensors share memory, this will lead to duplicate memory on disk and potential differences when loading them again: [{'tok_emb.weight', 'out_hea\nd.weight'}].\n[rank0]:             A potential way to correctly save your model is to use <!--CODE_BLOCK_9763-->.\n[rank0]:             More information at https://huggingface.co/docs/safetensors/torch_shared_tensors\n```\n\n[Safetensors](https://huggingface.co/docs/safetensors/index) doesn't like it when you reuse weights like we're doing here. The good news is that [the help page the error links to](https://huggingface.co/docs/safetensors/torch_shared_tensors) is exactly about this problem with weight tying, and the suggested fix -- to replace\n\n`save_file(model.state_dict(), \"model.safetensors\")`\n\n...with\n\n`save_model(model, \"model.safetensors\")`\n\n...and similarly for loading -- appears to work fine. Saving and loading checkpoints works, and it's compatible with the old checkpoint files too. So that's good news :-)\n\nSo, that's how we code it. How much actual saving do we get in terms of the parameter count by doing this?\n\n### The parameters\n\nA quick-and-easy way to count the parameters is just to create an instance of the model and see:\n\n`In [1]: import json  In [2]: from gpt import GPTModel  In [3]: with open(\"runs/8xa100m40-baseline/model.json\", \"r\") as f:    ...:     model_conf = json.load(f)    ...:  In [4]: model = GPTModel(model_conf)  In [5]: sum(p.numel() for p in model.parameters()) Out[5]: 163009536  In [6]: with open(\"runs/8xa100m40-weight-tying/model.json\", \"r\") as f:    ...:     model_conf = json.load(f)    ...:  In [7]: model = GPTModel(model_conf)  In [8]: sum(p.numel() for p in model.parameters()) Out[8]: 124412160`\n\nSo, we've gone from a 163M-parameter model to a 124M-parameter one. That's certainly quite some saving -- 38,597,376 fewer parameters, which is a reduction of almost a quarter.\n\nWe can also sanity check the size of that saving -- our output head was, as we know, a demb×dvocab matrix, so it should have 50257×768 parameters -- which is, indeed, 38,597,376. Excellent.\n\nNow, there's one thing we should consider here. We're training on a Chinchilla-optimal number of tokens, 20x our parameter count. Is that what we want to keep stable? Or is the total number of training tokens the important bit, so we wind up technically overtraining?\n\nMy instinct is that the total training tokens is the important thing. Chinchilla optimality is a training heuristic rather than a true aspect of the model, so sticking with it would mean that we're training a model with fewer parameters on less data. It seems very unlikely that would do anything other than produce a worse model!\n\nSo: we'll keep the same number of training tokens, and just introduce weight tying. How does it train?\n\n### The train\n\nI kicked it off on the usual 8x A100 40 GiB machine, and after a little while I checked the loss chart. It looked like this:\n\n![Crazy high initial training loss with weight tying](https://www.gilesthomas.com/post-assets/llm-from-scratch-32g-interventions-weight-tying/high-initial-loss-loss-chart.png \"Crazy high initial training loss with weight tying\")\n\nYikes! It started off with a loss of about 460. Normally, we start with a loss of about 11.\n\nThe normal loss makes a lot of sense. If you consider it in terms of perplexity, that value of 11 comes out at e11≈59,874 -- that is, the model is giving pretty much equal probabilities to every one of the 50,257 possible tokens.\n\nA loss of 460 means that the model is making incorrect predictions and is very certain about them. How could that be? Well, let's look at the documentation again.\n\nFor `nn.Embedding`:\n\n> `weight` (Tensor) -- the learnable weights of the module of shape (`num_embeddings`, `embedding_dim`) initialized from 𝒩(0,1).\n\nFor `nn.Linear`:\n\n> weight (torch.Tensor) – the learnable weights of the module of shape (`out_features`, `in_features`) The values are initialized from 𝒰(−k,k) where k\\=1in\\_features\n\nThey're initialised completely differently. Embeddings are set to values in a normal distribution (that is, a Gaussian bell curve) with a mean of 0 and a standard deviation of 1. But linear layers are set to random values in a uniform distribution (that is, a completely flat one) within a range based on the number of input features.\n\nIn particular, those numbers for the linear layer are really small! Our output head has `in_features` set to 768, so that means that the k would be:\n\n1768≈0.0360\n\nSo instead of getting that kind of \"ideal\" linear layer initialisation within the range (−0.0360,0.0360), we're getting numbers which roughly 2/3 of the time will be in the range (−1,1), and the rest of the time will be even further from zero -- we could be getting -3 or +4, or potentially even crazier numbers!\n\nThat means that the output logits (coming from a linear layer with higher weights) will be larger, which in turn will push softmax to come up with higher probabilities:\n\n`In [1]: import torch  In [2]: torch.softmax(torch.tensor([1.0, 2.0, 3.0]), dim=0) Out[2]: tensor([0.0900, 0.2447, 0.6652])  In [3]: torch.softmax(torch.tensor([10.0, 20.0, 30.0]), dim=0) Out[3]: tensor([2.0611e-09, 4.5398e-05, 9.9995e-01])  In [4]: torch.softmax(torch.tensor([100.0, 200.0, 300.0]), dim=0) Out[4]: tensor([0.0000e+00, 3.7835e-44, 1.0000e+00])`\n\nI considered changing things to initialise the weights differently, but given that the loss had fallen to 8 or so by the second checkpoint, I decided to just let the run complete. Here's the final loss chart, with the Y axis fixed to run from 0 to 12:\n\n![Full train run loss with weight tying](https://www.gilesthomas.com/post-assets/llm-from-scratch-32g-interventions-weight-tying/complete-scaled-loss-chart.png \"Full train run loss with weight tying\")\n\nThat's a nice smooth curve, at least! The output is:\n\n`Training complete in 12,058.054 seconds Tokens seen: 3,260,252,160 Throughput: 270,380 tokens/second Final train loss: 3.952`\n\nTiming-wise, that's about 180 seconds faster than our baseline model training run, only a 1.5% speedup -- clearly the lower number of parameters doesn't actually save us much time. Loss-wise, the final train loss on the baseline model was 3.743, so that's not particularly promising. Still, the proof is, as ever, in the evals. Smoke test first:\n\n```\nEvery effort moves you are making, the same as if there have been hundreds or thousands of miles of people getting in one\n```\n\nBorderline coherent, but maybe worse than normal? Let's see what our test set loss looks like.\n\n```\nLoss against our test dataset: 3.874\n```\n\nThat's bad -- let's see it in our comparison table:\n\n|  | Test set loss | Improvement vs baseline |\n| --- | --- | --- |\n| 8xa100m40-weight-tying | 3.874 | \\-0.182 |\n| 8xa100m40-weight-decay-cerebras | 3.867 | \\-0.175 |\n| 8xa100m40-baseline | 3.692 | \\- |\n| 8xa100m40-gradient-clipping | 3.678 | 0.014 |\n| 8xa100m40-qkv-bias | 3.669 | 0.023 |\n| 8xa100m40-weight-decay-gpt2 | 3.643 | 0.049 |\n| 8xa100m40-remove-dropout | 3.641 | 0.051 |\n| 8xa100m40-schedule-learning-rate | 3.602 | 0.09 |\n\nOur worst model so far :-(\n\n### Conclusion\n\nWeight tying certainly didn't help our train. It is worth noting that the GPT-2 small weights -- which *do* use it -- got 3.500 on the same test set as we're using for that table, so it is possible to get a better model with weight tying. But there was clearly something different about their train, and my suspicion, as I've said before, is that it was trained for many more epochs ([I estimated 40](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch#epochs)), slowly grinding that loss down.\n\nBut what I'm trying to do in this mini-series of interventions is find tricks that will allow us to approach the original weights' loss without a very long training run. And for the purposes of that, I think we can safely say that weight-tying is not one of those.\n\nNext time around, our last intervention test! What happens if we switch off the use of automated mixed precision (AMP)? That is something I added right back at the start as a performance enhancement; it means that PyTorch can do certain calculations in 16-bit rather than 32-bit if it thinks there's no harm in doing so. Might we get better loss by training without it?\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32).\n\n* * *\n\n1.  In reality we don't multiply a one-hot vector by a matrix, as that would be extremely inefficient -- PyTorch just does a lookup into the embedding matrix. If we get token ID 1234, then it just reads out the contents of row 1234, and that's our embedding. But for the purposes of this post, it's best to see that as more of a (extremely effective) performance tweak rather than what's happening conceptually. [↩](#fnref-1)","markdownBilingual":"In [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", he writes that weight tying, while it reduces the parameter count of a model, in his experience makes it worse. As such, apparently people don't use it in modern LLMs. Intuitively, that makes sense -- I'll explain why in this post. <span class=\"translation\">在[塞巴斯蒂安·拉斯卡](https://sebastianraschka.com/)的书“[构建大型语言模型（从头开始）](https://www.manning.com/books/build-a-large-language-model-from-scratch)”中，他写道，权重绑定虽然减少了模型的参数数量，但根据他的经验，它使情况变得更糟。因此，显然人们在现代大语言模型（LLM）中不使用它。直观上，这是有道理的——我将在这篇文章中解释原因。</span>\n\nBut as I'm trying various [interventions](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) to see if I can get my model -- based on Raschka's code, but trained for a fraction of the time that the original GPT-2 model was -- to perform as well as the original in terms of the loss it gets on a test set, I thought it would be worth seeing if it really is a negative for this particular tiny model of 163M parameters. <span class=\"translation\">不过，我正在尝试各种[干预](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model)，看看能否让我的模型（基于 Raschka 的代码，但训练时间只有原始 GPT-2 的一小部分）在测试集损失上接近原始模型，因此我觉得值得研究：对这个只有 163M 参数的小模型来说，权重绑定是否真的有害？</span>\n\nAfter all, the original weights use weight tying, and I did find that [QKV bias](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias) appeared to help -- and that's another old-school technique that they used, which has since dropped out of fashion. Might this one help too? <span class=\"translation\">毕竟，原始模型的权重使用了权重绑定，而我发现[QKV 偏置](https://www.gilesthomas.com/2026/02/llm-from-scratch-32d-interventions-adding-attention-bias)似乎有帮助——这是另一种如今过时的老技术。权重绑定也会有帮助吗？</span>\n\nWorth a try! Let's give it a go. <span class=\"translation\">值得一试！让我们尝试一下吧。</span>\n\n### Weight tying: a refresher <span class=\"translation\">权重绑定：复习</span>\n\nI'll start with a quick refresher on what weight tying is, and how it works. This is really targeted at people who've been reading along with this series -- if it's all new to you, you might find my post on [Maths for LLMs](https://www.gilesthomas.com/2025/09/maths-for-llms) a useful catch-up guide first. <span class=\"translation\">我先快速回顾一下权重绑定是什么、如何工作。这主要写给一直阅读本系列的读者；如果你是第一次接触，可以先看看我关于[LLM 数学](https://www.gilesthomas.com/2025/09/maths-for-llms)的文章。</span>\n\nIn our LLM code, right at the start, we use an embedding layer to take our input token IDs, and turn them into embeddings -- each token becomes a vector in a high-dimensional space (768 in our case), which we see as representing in some manner the \"meaning\" of the token. <span class=\"translation\">在我们的大语言模型（LLM）代码中，从一开始，我们就使用嵌入层来获取输入词元ID，并将它们转换为嵌入——每个词元成为高维空间中的向量（在我们的例子中为768），我们将其视为以某种方式表示词元的“含义”。</span>\n\nA useful way to think about that is that we could start with a one-hot vector for the token -- that is, with our 50,257-token vocabulary, it would be 50,257 items long, and have zeros in every position apart from the position corresponding to the token's ID. We'll treat that as being a vector in a \"vocab space\". The process of converting the token into an embedding turns out to be equivalent to multiplying that vocab space representation by an embedding matrix -- one with one row per possible token, the values in that row being the values for the appropriate embedding. [1](#fn-1) <span class=\"translation\">思考这个问题的一种有用方法是，我们可以从词元的 one-hot 向量开始——也就是说，使用我们的 50,257-词元词汇表，它将有 50,257 个项目长，并且除了与词元ID 对应的位置之外的每个位置都有零。我们将其视为“词汇空间”中的向量。将词元转换为嵌入的过程相当于将该词汇空间表示乘以嵌入矩阵——每个可能的词元一行，该行中的值是适当嵌入的值。 [1](#fn-1)</span>\n\nBecause matrix multiplications can be seen as projections between different spaces, we can see that as a projection from our vocab space to the embedding space. <span class=\"translation\">因为矩阵乘法可以看作是不同空间之间的投影，所以我们可以将其视为从词汇空间到嵌入空间的投影。</span>\n\nOnce we've projected our sequence of tokens into a sequence of embeddings, we do all of the steps required for the LLM -- we add in positional information, run it through the Transformers layers, normalise it, and then we have a new sequence of embeddings. The embedding at position n in that output sequence, if our model is working well, should be something that represents an appropriate next-token prediction for the portion of the input sequence from zero to position n. <span class=\"translation\">将词元序列投影成嵌入序列后，我们执行 LLM 所需的全部步骤：加入位置信息，经过 Transformer 层，进行归一化，得到新的嵌入序列。如果模型运行良好，输出序列位置 n 的嵌入应表示输入序列从开头到位置 n 的内容，并据此预测下一个词元。</span>\n\nWhat we want as our final output is to map that back to the vocab space. We want logits: a list of numbers that (after being run through softmax) will represent the probability that our next token is a particular one. <span class=\"translation\">最终输出需要映射回词汇空间。我们要得到 logits：一串数字，经过 softmax 后表示下一个词元是各个候选词的概率。</span>\n\nJust as we mapped from vocab space to embedding space with (conceptually) a matrix multiplication at the start of the process, we can map back with another one. More specifically, if we treat the embedding matrix as having the same number of rows as there are input tokens (which we'll call dvocab) and columns as there are embedding dimensions (demb), then the original vocab-space-to-embedding-space matrix will have this shape: <span class=\"translation\">正如我们在过程开始时使用（概念上）矩阵乘法从词汇空间映射到嵌入空间一样，我们可以使用另一个映射回来。更具体地说，如果我们将嵌入矩阵视为具有与输入词元（我们称之为 dvocab）相同的行数和与嵌入维度 (demb) ​​相同的列数，则原始 vocab-space-to-embedding-space 矩阵将具有以下形状：</span>\n\ndvocab×demb\n\nSo it's projecting from a dvocab\\-dimensional space to a demb\\-dimensional one. Similarly, our matrix to do the projection at the end is just a matrix with the numbers of rows and columns swapped around: <span class=\"translation\">所以它从 dvocab\\ 维空间投影到 demb\\ 维空间。类似地，我们最后进行投影的矩阵只是一个行数和列数交换的矩阵：</span>\n\ndemb×dvocab\n\n...to do a projection in the other direction. <span class=\"translation\">...在另一个方向上进行投影。</span>\n\nThe trick with weight tying is to see that these two projections can *potentially* be just the opposite of each other. If we assume that the embedding space on the way in to the LLM is essentially the same as the embedding space on the way out, then we can use one projection to go into it from vocab space, and the opposite to go back. The \"opposite\" in this case is the transpose -- that is, if we use Wemb for our embedding matrix and Wout for the output one, we have: <span class=\"translation\">权重绑定的技巧是发现这两个投影“可能”彼此相反。如果我们假设进入大语言模型（LLM）的嵌入空间与出去的嵌入空间基本相同，那么我们可以使用一个投影从词汇空间进入它，并使用相反的投影返回。在这种情况下，“相反”是转置——也就是说，如果我们使用 Wemb 作为嵌入矩阵，使用 Wout 作为输出矩阵，我们有：</span>\n\nWout\\=WembT\n\nThat means we can re-use all of the embedding parameters for the output projection matrix, and fewer parameters means not only a smaller model, but hopefully faster training. Sounds like a win! <span class=\"translation\">这样我们就能复用输出投影矩阵所需的全部嵌入参数；参数更少不仅意味着模型更小，还有望加快训练。听起来不错！</span>\n\nBut of course, there's no such thing as a free lunch. By constraining the output head to be the transpose of the input one, we're essentially enforcing that assumption above: we're saying that the embedding space on the way out *must* be the same as the embedding space on the way in. That limits what the LLM can do -- if it were able to use different embedding spaces at each end, it would have more flexibility, which might help it learn to model things better. <span class=\"translation\">当然，天下没有免费的午餐。把输出头限制为输入头的转置，等于强制采用上面的假设：输出嵌入空间*必须*与输入嵌入空间相同。这限制了 LLM 的能力；如果两端可以使用不同嵌入空间，模型会更灵活，也许能学得更好。</span>\n\nThat's the theory: what does it mean in practice? <span class=\"translation\">这就是理论：它在实践中意味着什么？</span>\n\n### The code <span class=\"translation\">代码</span>\n\nLet's take a quick look at the GPT-2 code -- just the `__init__` for the top level class: <span class=\"translation\">让我们快速浏览一下 GPT-2 代码——只有顶级类的 `__init__` ：</span>\n\n`class GPTModel(nn.Module):      def __init__(self, cfg):         super().__init__()          self.tok_emb = nn.Embedding(cfg[\"vocab_size\"], cfg[\"emb_dim\"])         self.pos_emb = nn.Embedding(cfg[\"context_length\"], cfg[\"emb_dim\"])         self.drop_emb = nn.Dropout(cfg[\"drop_rate\"])          self.trf_blocks = nn.Sequential(             *[TransformersBlock(cfg) for _ in range(cfg[\"n_layers\"])]         )          self.final_norm = LayerNorm(cfg[\"emb_dim\"])          self.out_head = nn.Linear(             cfg[\"emb_dim\"], cfg[\"vocab_size\"], bias=False         )`\n\nFor our embedding layer, we use PyTorch's [`nn.Embedding`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Embedding.html) class, and for the output head we use [`nn.Linear`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Linear.html). <span class=\"translation\">对于我们的嵌入层，我们使用PyTorch的[`nn.Embedding`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Embedding.html)类，对于输出头，我们使用[`nn.Linear`](https://docs.pytorch.org/docs/stable/generated/torch.nn.Linear.html)。</span>\n\nNow, `nn.Embedding` provides us with access to the underlying matrix with a `weight` field: <span class=\"translation\">现在，`nn.Embedding`为​​我们提供了通过`weight`字段访问底层矩阵的权限：</span>\n\n> `weight` (Tensor) -- the learnable weights of the module of shape (`num_embeddings`, `embedding_dim`) initialized from 𝒩(0,1). <span class=\"translation\">`weight` (Tensor) -- 从 𝒩(0,1) 初始化的形状模块 (`num_embeddings`, `embedding_dim`) 的可学习权重。</span>\n\nSo, that's exactly the dvocab×demb matrix that we'd expect -- it's the input dimension as the rows, and the output dimension as the columns. <span class=\"translation\">所以，这正是我们所期望的 dvocab×demb 矩阵——它的输入维度为行，输出维度为列。</span>\n\nIf we look at `nn.Linear`, we see something very similar: <span class=\"translation\">如果我们看一下`nn.Linear`，我们会看到非常相似的东西：</span>\n\n> weight (torch.Tensor) – the learnable weights of the module of shape (`out_features`, `in_features`) The values are initialized from 𝒰(−k,k) where k\\=1in\\_features <span class=\"translation\">权重(torch.Tensor) – 形状模块的可学习权重(`out_features`, `in_features`) ​​值从 𝒰(−k,k) 初始化，其中 k\\=1in\\_features</span>\n\nThat's actually the other way around, output dimension as the rows and input as the columns. If you're wondering why, remember that [we transpose the weights matrix for a neural network before using it](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2). But that's actually really convenient in our situation, because if we want to use the same weights for both, they're already \"compatible\"! <span class=\"translation\">这实际上是相反的，输出维度作为行，输入作为列。如果您想知道为什么，请记住[我们在使用神经网络之前转置权重矩阵](https://www.gilesthomas.com/2025/02/basic-neural-network-matrix-maths-part-2)。但这在我们的情况下实际上非常方便，因为如果我们想对两者使用相同的权重，它们已经“兼容”了！</span>\n\nAnd that means that adding weight tying to our code above is as simple as adding two lines at the end: <span class=\"translation\">这意味着将权重绑定添加到上面的代码中就像在末尾添加两行一样简单：</span>\n\n        `if cfg.get(\"tie_weights\", False):             self.out_head.weight = self.tok_emb.weight`\n\nFor the model code, it literally is just that! There is a tiny inefficiency in that PyTorch is going to spend a bit of time initialising the weights in `self.out_head` to appropriately-sized random values, only to have them all replaced -- but that actually works in our favour, because it means that we'll use up the same amount of the random number stream when creating the LLM in both the weight-tying and non-weight-tying cases, which is a bit better for reproducibility. <span class=\"translation\">模型代码实际上就这么简单！PyTorch 会花一些时间为 `self.out_head` 初始化适当大小的随机权重，随后这些权重又会全部被替换；不过这对我们反而有好处，因为权重绑定和不绑定两种情况下使用的随机数流相同，可重复性更好。</span>\n\nThere is one other change needed, though. I ran a test train with that code, and checkpointing failed like this: <span class=\"translation\">不过，还需要进行另一项改变。我用该代码运行了测试训练，检查点失败如下：</span>\n\n```\n[rank0]: RuntimeError:\n[rank0]:             Some tensors share memory, this will lead to duplicate memory on disk and potential differences when loading them again: [{'tok_emb.weight', 'out_hea\nd.weight'}].\n[rank0]:             A potential way to correctly save your model is to use <!--CODE_BLOCK_9763-->.\n[rank0]:             More information at https://huggingface.co/docs/safetensors/torch_shared_tensors\n```\n\n[Safetensors](https://huggingface.co/docs/safetensors/index) doesn't like it when you reuse weights like we're doing here. The good news is that [the help page the error links to](https://huggingface.co/docs/safetensors/torch_shared_tensors) is exactly about this problem with weight tying, and the suggested fix -- to replace <span class=\"translation\">[Safetensors](https://huggingface.co/docs/safetensors/index) 不喜欢我们这样复用权重。好消息是，错误链接到的[帮助页面](https://huggingface.co/docs/safetensors/torch_shared_tensors)正好讨论了权重绑定，并给出了修复方案：把</span>\n\n`save_file(model.state_dict(), \"model.safetensors\")`\n\n...with <span class=\"translation\">...和</span>\n\n`save_model(model, \"model.safetensors\")`\n\n...and similarly for loading -- appears to work fine. Saving and loading checkpoints works, and it's compatible with the old checkpoint files too. So that's good news :-) <span class=\"translation\">...与加载类似 - 似乎工作正常。保存和加载检查点有效，并且它也与旧的检查点文件兼容。所以这是个好消息:-)</span>\n\nSo, that's how we code it. How much actual saving do we get in terms of the parameter count by doing this? <span class=\"translation\">所以，这就是我们编码的方式。通过这样做，我们在参数数量方面实际节省了多少？</span>\n\n### The parameters <span class=\"translation\">参数</span>\n\nA quick-and-easy way to count the parameters is just to create an instance of the model and see: <span class=\"translation\">一种快速简便的参数计数方法，是创建一个模型实例并查看：</span>\n\n`In [1]: import json  In [2]: from gpt import GPTModel  In [3]: with open(\"runs/8xa100m40-baseline/model.json\", \"r\") as f:    ...:     model_conf = json.load(f)    ...:  In [4]: model = GPTModel(model_conf)  In [5]: sum(p.numel() for p in model.parameters()) Out[5]: 163009536  In [6]: with open(\"runs/8xa100m40-weight-tying/model.json\", \"r\") as f:    ...:     model_conf = json.load(f)    ...:  In [7]: model = GPTModel(model_conf)  In [8]: sum(p.numel() for p in model.parameters()) Out[8]: 124412160`\n\nSo, we've gone from a 163M-parameter model to a 124M-parameter one. That's certainly quite some saving -- 38,597,376 fewer parameters, which is a reduction of almost a quarter. <span class=\"translation\">因此，我们已经从 163M 参数模型转变为 124M 参数模型。这当然是相当大的节省——减少了 38,597,376 个参数，几乎减少了四分之一。</span>\n\nWe can also sanity check the size of that saving -- our output head was, as we know, a demb×dvocab matrix, so it should have 50257×768 parameters -- which is, indeed, 38,597,376. Excellent. <span class=\"translation\">我们还可以合理地检查该保存的大小 - 正如我们所知，我们的输出头是一个 demb×dvocab 矩阵，因此它应该有 50257×768 个参数 - 这实际上是 38,597,376。出色的。</span>\n\nNow, there's one thing we should consider here. We're training on a Chinchilla-optimal number of tokens, 20x our parameter count. Is that what we want to keep stable? Or is the total number of training tokens the important bit, so we wind up technically overtraining? <span class=\"translation\">现在需要考虑一点：我们按 Chinchilla 最优数量训练词元，即参数数量的 20 倍。我们要保持稳定的是这个比例吗？还是训练词元总数更重要，以至于最终会在参数更少的模型上进行过度训练？</span>\n\nMy instinct is that the total training tokens is the important thing. Chinchilla optimality is a training heuristic rather than a true aspect of the model, so sticking with it would mean that we're training a model with fewer parameters on less data. It seems very unlikely that would do anything other than produce a worse model! <span class=\"translation\">我的直觉是训练词元总数更重要。Chinchilla 最优性是训练启发式规则，而非模型的固有属性；坚持这个比例意味着我们会在更少数据上训练更少参数的模型，除了得到更差的模型之外似乎没有好处。</span>\n\nSo: we'll keep the same number of training tokens, and just introduce weight tying. How does it train? <span class=\"translation\">所以，我们会保持训练词元总数不变，只引入权重绑定。它训练起来会怎样？</span>\n\n### The train <span class=\"translation\">训练</span>\n\nI kicked it off on the usual 8x A100 40 GiB machine, and after a little while I checked the loss chart. It looked like this: <span class=\"translation\">我在通常的 8x A100 40 GiB 机器上启动了它，过了一会儿我检查了损失图表。它看起来像这样：</span>\n\n![Crazy high initial training loss with weight tying](https://www.gilesthomas.com/post-assets/llm-from-scratch-32g-interventions-weight-tying/high-initial-loss-loss-chart.png \"Crazy high initial training loss with weight tying\")\n\nYikes! It started off with a loss of about 460. Normally, we start with a loss of about 11. <span class=\"translation\">哎呀！一开始的损失约为 460。通常情况下，我们一开始的损失约为 11。</span>\n\nThe normal loss makes a lot of sense. If you consider it in terms of perplexity, that value of 11 comes out at e11≈59,874 -- that is, the model is giving pretty much equal probabilities to every one of the 50,257 possible tokens. <span class=\"translation\">正常损失的数值很容易理解。从困惑度角度看，损失 11 对应 e¹¹≈59,874，也就是模型几乎平均地为 50,257 个可能词元赋予概率。</span>\n\nA loss of 460 means that the model is making incorrect predictions and is very certain about them. How could that be? Well, let's look at the documentation again. <span class=\"translation\">损失 460 意味着模型做出了错误的预测，并且对这些预测非常确定。怎么可能呢？好吧，我们再看一下文档。</span>\n\nFor `nn.Embedding`: <span class=\"translation\">对于`nn.Embedding`：</span>\n\n> `weight` (Tensor) -- the learnable weights of the module of shape (`num_embeddings`, `embedding_dim`) initialized from 𝒩(0,1). <span class=\"translation\">`weight` (Tensor) -- 从 𝒩(0,1) 初始化的形状模块 (`num_embeddings`, `embedding_dim`) 的可学习权重。</span>\n\nFor `nn.Linear`: <span class=\"translation\">对于`nn.Linear`：</span>\n\n> weight (torch.Tensor) – the learnable weights of the module of shape (`out_features`, `in_features`) The values are initialized from 𝒰(−k,k) where k\\=1in\\_features <span class=\"translation\">权重(torch.Tensor) – 形状模块的可学习权重(`out_features`, `in_features`) ​​值从 𝒰(−k,k) 初始化，其中 k\\=1in\\_features</span>\n\nThey're initialised completely differently. Embeddings are set to values in a normal distribution (that is, a Gaussian bell curve) with a mean of 0 and a standard deviation of 1. But linear layers are set to random values in a uniform distribution (that is, a completely flat one) within a range based on the number of input features. <span class=\"translation\">两者的初始化方式完全不同。嵌入层的值来自均值为 0、标准差为 1 的正态分布（高斯钟形曲线）；线性层的随机值则来自由输入特征数决定范围的均匀分布。</span>\n\nIn particular, those numbers for the linear layer are really small! Our output head has `in_features` set to 768, so that means that the k would be: <span class=\"translation\">特别是线性层的数字非常小！我们的输出头将 `in_features` 设置为 768，这意味着 k 将为：</span>\n\n1768≈0.0360\n\nSo instead of getting that kind of \"ideal\" linear layer initialisation within the range (−0.0360,0.0360), we're getting numbers which roughly 2/3 of the time will be in the range (−1,1), and the rest of the time will be even further from zero -- we could be getting -3 or +4, or potentially even crazier numbers! <span class=\"translation\">因此，我们得到的数字不是在 (−0.0360,0.0360) 范围内的那种“理想”线性层初始化，而是大约 2/3 的时间在 (−1,1) 范围内，其余时间将离零更远——我们可能会得到 -3 或 +4，甚至可能是更疯狂的数字！</span>\n\nThat means that the output logits (coming from a linear layer with higher weights) will be larger, which in turn will push softmax to come up with higher probabilities: <span class=\"translation\">这意味着输出 logits（来自具有较高权重的线性层）将会更大，这反过来又会推动 softmax 得出更高的概率：</span>\n\n`In [1]: import torch  In [2]: torch.softmax(torch.tensor([1.0, 2.0, 3.0]), dim=0) Out[2]: tensor([0.0900, 0.2447, 0.6652])  In [3]: torch.softmax(torch.tensor([10.0, 20.0, 30.0]), dim=0) Out[3]: tensor([2.0611e-09, 4.5398e-05, 9.9995e-01])  In [4]: torch.softmax(torch.tensor([100.0, 200.0, 300.0]), dim=0) Out[4]: tensor([0.0000e+00, 3.7835e-44, 1.0000e+00])`\n\nI considered changing things to initialise the weights differently, but given that the loss had fallen to 8 or so by the second checkpoint, I decided to just let the run complete. Here's the final loss chart, with the Y axis fixed to run from 0 to 12: <span class=\"translation\">我考虑过改变一些东西，以不同的方式初始化权重，但考虑到到第二个检查点时损失已降至 8 左右，我决定让运行完成。这是最终的损失图表，Y 轴固定为运行从 0 到 12：</span>\n\n![Full train run loss with weight tying](https://www.gilesthomas.com/post-assets/llm-from-scratch-32g-interventions-weight-tying/complete-scaled-loss-chart.png \"Full train run loss with weight tying\")\n\nThat's a nice smooth curve, at least! The output is: <span class=\"translation\">至少这是一条漂亮的平滑曲线！输出是：</span>\n\n`Training complete in 12,058.054 seconds Tokens seen: 3,260,252,160 Throughput: 270,380 tokens/second Final train loss: 3.952`\n\nTiming-wise, that's about 180 seconds faster than our baseline model training run, only a 1.5% speedup -- clearly the lower number of parameters doesn't actually save us much time. Loss-wise, the final train loss on the baseline model was 3.743, so that's not particularly promising. Still, the proof is, as ever, in the evals. Smoke test first: <span class=\"translation\">从时间上看，这比基线模型训练快约 180 秒，只提升了 1.5%；显然，参数减少并没有节省多少时间。损失方面，基线模型最终训练损失为 3.743，所以这并不特别理想。不过评估才是关键。先做冒烟测试：</span>\n\n```\nEvery effort moves you are making, the same as if there have been hundreds or thousands of miles of people getting in one\n```\n\nBorderline coherent, but maybe worse than normal? Let's see what our test set loss looks like. <span class=\"translation\">边缘连贯，但可能比正常情况更差？让我们看看我们的测试集损失是什么样的。</span>\n\n```\nLoss against our test dataset: 3.874\n```\n\nThat's bad -- let's see it in our comparison table: <span class=\"translation\">这很糟糕——让我们在比较表中看看：</span>\n\n|  | Test set loss | Improvement vs baseline | <span class=\"translation\"> | 测试集损失 | 与基线相比的改进</span>\n| --- | --- | --- |\n| 8xa100m40-weight-tying | 3.874 | \\-0.182 | <span class=\"translation\">8xa100m40-weight-tying | 3.874 | \\-0.182</span>\n| 8xa100m40-weight-decay-cerebras | 3.867 | \\-0.175 | <span class=\"translation\">8xa100m40-weight-decay-cerebras | 3.867 | \\-0.175</span>\n| 8xa100m40-baseline | 3.692 | \\- | <span class=\"translation\">8xa100m40-基线 | 3.692 | \\-</span>\n| 8xa100m40-gradient-clipping | 3.678 | 0.014 | <span class=\"translation\">8xa100m40-gradient-clipping | 3.678 | 0.014</span>\n| 8xa100m40-qkv-bias | 3.669 | 0.023 | <span class=\"translation\">8xa100m40-qkv-bias | 3.669 | 0.023</span>\n| 8xa100m40-weight-decay-gpt2 | 3.643 | 0.049 | <span class=\"translation\">8xa100m40-weight-decay-gpt2 | 3.643 | 0.049</span>\n| 8xa100m40-remove-dropout | 3.641 | 0.051 | <span class=\"translation\">8xa100m40-remove-dropout | 3.641 | 0.051</span>\n| 8xa100m40-schedule-learning-rate | 3.602 | 0.09 | <span class=\"translation\">8xa100m40-schedule-learning-rate | 3.602 | 0.09</span>\n\nOur worst model so far :-( <span class=\"translation\">迄今为止我们最糟糕的模型:-(</span>\n\n### Conclusion <span class=\"translation\">结论</span>\n\nWeight tying certainly didn't help our train. It is worth noting that the GPT-2 small weights -- which *do* use it -- got 3.500 on the same test set as we're using for that table, so it is possible to get a better model with weight tying. But there was clearly something different about their train, and my suspicion, as I've said before, is that it was trained for many more epochs ([I estimated 40](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch#epochs)), slowly grinding that loss down. <span class=\"translation\">权重绑定显然没有帮助我们的训练。值得注意的是，使用权重绑定的 GPT-2 small 在同一测试集上得到 3.500，因此权重绑定并非一定不能训练出更好的模型。不过它们的训练方式显然不同；正如我之前提到的，我怀疑 GPT-2 训练了更多轮（[我估计约 40 轮](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch#epochs)），通过长时间训练逐渐降低了损失。</span>\n\nBut what I'm trying to do in this mini-series of interventions is find tricks that will allow us to approach the original weights' loss without a very long training run. And for the purposes of that, I think we can safely say that weight-tying is not one of those. <span class=\"translation\">但这个干预系列的目标，是寻找无需极长训练就能接近原始权重损失的技巧。就此而言，我们可以相当有把握地说，权重绑定不是其中之一。</span>\n\nNext time around, our last intervention test! What happens if we switch off the use of automated mixed precision (AMP)? That is something I added right back at the start as a performance enhancement; it means that PyTorch can do certain calculations in 16-bit rather than 32-bit if it thinks there's no harm in doing so. Might we get better loss by training without it? <span class=\"translation\">下一篇就是最后一项干预测试！如果关闭自动混合精度（AMP），会发生什么？这是我一开始为提升性能而加入的功能：PyTorch 可以在合适的地方使用 16 位而非 32 位计算。关闭它会让训练损失更好吗？</span>\n\n[Here's a link to the next post in this series](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32). <span class=\"translation\">[这是本系列下一篇文章的链接](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32)。</span>\n\n* * *\n\n1.  In reality we don't multiply a one-hot vector by a matrix, as that would be extremely inefficient -- PyTorch just does a lookup into the embedding matrix. If we get token ID 1234, then it just reads out the contents of row 1234, and that's our embedding. But for the purposes of this post, it's best to see that as more of a (extremely effective) performance tweak rather than what's happening conceptually. [↩](#fnref-1) <span class=\"translation\">实际上，我们不会将 one-hot 向量与矩阵相乘，因为那样效率极低——PyTorch 只是查找嵌入矩阵。如果我们得到词元ID 1234，那么它只是读出第 1234 行的内容，这就是我们的嵌入。但就本文而言，最好将其视为（极其有效的）性能调整，而不是概念上发生的事情。 [↩](#fnref-1)</span>","imageCount":2,"wordCount":2558,"readingMinutes":12,"syncedAt":"2026-08-11T08:02:01.059Z","license":"CC BY 4.0","author":"Giles Thomas"}