{
  "slug": "llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax",
  "part": 34,
  "title": "Writing an LLM from scratch, part 34b -- from bigrams to GPT-2, one component at a time (in JAX)",
  "shortTitle": "part 34b -- from bigrams to GPT-2, one component at a time (in JAX)",
  "date": "8 July 2026",
  "summary": "The capstone of my LLM from scratch journey: building up from a bigram-style model to GPT-2 small in JAX, watching the loss fall as each component goes in.",
  "href": "https://www.gilesthomas.com/2026/07/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax",
  "phaseId": "jax",
  "phaseLabel": "08 · JAX 重构",
  "markdown": "This post is the capstone of [the most long-running series on my blog](https://www.gilesthomas.com/llm-from-scratch). In December 2024 (!), I started reading [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", and worked through it carefully. Being who I am, despite trying to apply a strict \"no side quests\" policy, I found myself zooming off and digging into all kinds of things.\n\nIt's time to wrap it up. I had decided that the endpoint would be to build and train an LLM from scratch just using my notes -- no reference to the book, no reference to the model code I'd written when following the book. After an [X/Twitter](https://x.com/gpjt/status/1985434030880293004) poll, I decided to use JAX for that, just to make sure that I really was building it from scratch and not regurgitating bits of PyTorch code like a bad coding LLM spitting out half-digested lumps of Stack Overflow.\n\nIn my [last post](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run), I showed how I built a JAX training script that mirrored what I had built for the original PyTorch version of the model. To test it as I went along, I used it to train a really dumb \"LLM\", which instead of trying to predict the next token for every token in an input sequence, instead predicted the input -- that is, if you fed it\n\n`The fat cat sat on the mat`\n\nIt would return the same thing. I called that an A-to-A model.\n\nIn this post, I'll show you how I turned it into a GPT-2 model, and then trained it from scratch on my RTX 3090 (using the parameter counts for the original paper's \"small\" size). What turned out really well with this is that I found a route that meant that almost every component I added made the model better! That's not guaranteed -- sometimes different aspects of an AI model depend on each other, so adding A without also adding B makes things worse. But (admittedly with a bit of backtracking in places) I was able to find a route that shows a nice clear progression.\n\nThe final training run took 37 hours 15 minutes -- compared to 40 hours, 38 minutes for [an equivalent PyTorch model](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation). That is despite it being full-fat 32-bit -- the PyTorch one was using Automatic Mixed Precision (AMP), which allowed it to use 16-bit calculations in places where it would be relatively harmless in terms of loss.\n\nWhen asked to continue \"Every effort moves you\", it came back with a decent response:\n\n`Every effort moves you closer to your goals, but if you are unsure of what it takes, you don’t`\n\nThe model got 3.418784 loss on my held-back test dataset, as compared to my PyTorch model's 3.538161, and even more impressively, it was better than the original GPT-2 small's result of 3.499677 on the same dataset! However, just as [I found previously](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests), the OpenAI weights still beat mine consistently in instruction fine-tuning challenges.\n\nLet's get started.\n\n## The starting point -- A-to-A\n\nAt the end of the last post, we had a solid training loop, using all of the tricks I'd picked up with my PyTorch code. The A-to-A model we were training with it looked like this:\n\n`from flax import nnx  class GPTModel(nnx.Module):      def __init__(         self,         vocab_size, context_length,         d_emb,         n_heads, n_layers,         qkv_bias,         drop_rate,         rngs,     ):         self.token_embedding = nnx.Embed(             num_embeddings=vocab_size,             features=d_emb,             rngs=rngs,         )          self.output_head = nnx.Linear(             in_features=d_emb,             out_features=vocab_size,             use_bias=False,             rngs=rngs,         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          return self.output_head(input_embeddings)`\n\nThat was based on my preferred model of [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work), where at the top level for a model, we feed in a sequence of token IDs, then:\n\n-   Firstly, we convert them into embeddings, so we get a sequence of vectors, one for each token. We do this by a lookup into a table, but we can see it conceptually as a projection via a matrix, from vocab space (where a particular token ID is a one-hot vector) to embedding space.\n-   Next, we do the magic with our Transformers layers, getting embeddings for the next token. After these layers, the embedding at position n in the output sequence is for the predicted token to come after the token at position n in the input sequence, considering that input token and all other tokens to its left.\n-   Finally, we project those back from embedding space to logits, this time actually using a real matrix (in the form of a linear layer), the output head. The logits (after being run through softmax) represent the probabilities for each token of it being the next one.\n\nThe A-to-A model basically skipped the second step completely: it would project to embedding space, then immediately project back to vocab space -- and after training, it was pretty good at mapping a sequence to itself.\n\nOne interesting question is, if we train the same code, but this time try to get it to make next-token predictions, how good will it be at that? Obviously it can't be as good as a full LLM. But there are correlations between tokens; full stops will generally be followed by spaces, adjectives will normally be followed by other adjectives or nouns (at least in English), and so on. It would be kind of like the predictive text systems on a phone, where (at least until recently) it would just use the last word you entered to generate a list of possible next words to select from.\n\nOld-school natural language processing has a name for this: bigrams. The idea is that you can work out statistically what the most common two-word pairs are, which allows you to make a guess at a next word from a single one. (There are also trigrams, where you look at the last two words when predicting the next, then 4-grams, 5-grams, and so on.) You'd build up a full probability table -- for every word in your vocab, you'd have the probability of every word coming next.\n\nSo maybe even with that minimal model, we could get it to learn something similar to a set of token-level (rather than word-level) bigrams, which would then get the loss down. Obviously it wouldn't be as good as a full bigram table -- for our GPT-2 vocab size of 50,257, that would need 50,2572\\=2,525,766,049 parameters -- but perhaps it could approximate one. (For comparison, the model we're using has just an embedding table and an output head, each mapping between 50,257 dimensions and 768, so that's 2×50,257×768≈77 million parameters -- about 3% of the full table.)\n\nAn uninitialised model would (hopefully) have a loss of about 10.82, implying a [perplexity](https://www.gilesthomas.com/2025/10/llm-from-scratch-21-perplexed-by-perplexity) equal to the vocab size. If we can train our dumb model to get better loss than that, then we'd have the beginnings of an LLM.\n\nThat was a simple test to run. In my training code, I had a dataset class that looked like this:\n\n`class BigTrainDataset:      def __init__(self, all_tokens, seq_length, microbatch_size):         self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)      def __getitem__(self, ix):         return self.xs[ix], self.ys[ix]      def __len__(self):         return self.xs.shape[0]`\n\nThat is, the inputs, the `xs`, were the same as the targets, the `ys`. If we fed it\n\n`The fat cat sat on the mat`\n\n...then we'd be training it to output exactly the same thing.\n\nThe modified version for a real LLM would involve feeding it something like this:\n\n`The fat cat sat on the`\n\n...and targeting this:\n\n `fat cat sat on the mat`\n\nThat's a simple change -- that `__init__` method became this:\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[1:].reshape(-1, microbatch_size, seq_length)`\n\nI did that, and kicked it off to train on the 92,209,152 tokens that I was (somewhat arbitrarily) using in the last post to test my training loop. The loss chart looked like this:\n\n![Next token prediction loss chart for the bigram-style model](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/a-to-a-next-token-loss-chart.png \"Next token prediction loss chart for the bigram-style model\")\n\nThat was pretty promising! Loss came down from roughly 10.82 down to a fairly stable 6 or so by global step 768, and seemed to flatten out there. It's possible that further training could have got it down a bit more, but I decided (again, somewhat arbitrarily) to use the average train loss in the checkpoint period ending at step 937 as my starting point. If we could make changes that reduced that, then we'd be moving forward. For this model, that value was 5.909.\n\nSo, what were the changes we needed to make to change our bigram-style model to a real, if small, LLM?\n\n## Building GPT-2: a checklist\n\nAdapting from my [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work) post, a GPT-2-style LLM looks like this. We receive our sequence of token IDs, and then:\n\n-   Convert them into embeddings. **✔ ️done**\n-   Add on position embeddings.\n-   Run these embeddings through multiple successive Transformers blocks.\n-   Layer normalisation\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings\n-   Layer normalisation\n-   Run multi-head attention\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original\n-   Take a second copy of that one\n-   Layer normalisation again\n-   Run it through a simple neural network\n-   Add the results of that back in.\n\nSo that gave me the checklist; looking at it, the most tempting next step was layer normalisation (henceforth LayerNorm). It's used at the end of the core loop, and then twice in the Transformers blocks.\n\nWhat would happen if we coded it up, and then added it to the core only?\n\n## LayerNorm\n\nThe purpose of [LayerNorm](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation) is to stabilise training. We constrain the values flowing through our model so that they have certain statistical properties that tend to make the whole thing more trainable.\n\nThat would mean that if it did help with this model -- placed in between the embedding layer at the start, and the output head at the end -- then we'd hope for loss to go down faster, and ideally finish at a lower level.\n\nTime to code it up!\n\n[NNX has its own LayerNorm implementation](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/nn/normalization.html#flax.nnx.LayerNorm), of course (as does [PyTorch](https://docs.pytorch.org/docs/2.12/generated/torch.nn.LayerNorm.html)), but in the book, we implement it ourselves, and that felt like the correct path to take.\n\nFirstly, I implemented a dummy version:\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         return xs`\n\n...and updated the core `GPTModel` to create and call one:\n\n`class GPTModel(nnx.Module):     def __init__(         ...     ):         self.token_embedding = nnx.Embed(                 ...         )          self.output_norm = LayerNorm()          self.output_head = nnx.Linear(                 ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          normalised = self.output_norm(input_embeddings)          return self.output_head(normalised)`\n\nAnd kicked off a training run for a few seconds just to make sure that it hadn't broken anything and that loss dropped -- being my first NNX module-inside-a-module, I worried that there might have been something non-intuitive that I had to do to get it to work. But everything seemed good -- loss was dropping, no errors.\n\nSo, following [the notes I made when I first learned about LayerNorm](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation), I needed to make the values flowing through centred around zero by subtracting their mean, and then scale them to have a variance of one by dividing by the standard deviation (details in those notes).\n\nThe shape of the `xs` I had coming into my `LayerNorm` class's `_call` was this:\n\n`xs.shape=(6, 1024, 768)`\n\nThat was `(batch_size, seq_len, d_emb)`. So we needed to do those operations strictly on the last axis, manipulating each embedding independently.\n\nJAX has a [`std` function](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.std.html) and a [`mean` one](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.mean.html#jax.numpy.mean), both of which take an `axis` parameter. The `Array` object repackaged those as methods, which was convenient, so I did a first cut test like this:\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1)         jax.debug.print(f\"{stds.shape=}\")          return xs`\n\nThat printed out these results:\n\n`xs.shape=(6, 1024, 768) means.shape=(6, 1024) stds.shape=(6, 1024)`\n\n...which looked plausible; one number for each embedding vector. Could we broadcast them across the array?\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          return normalized`\n\nThis blew up:\n\n`ValueError: Incompatible shapes for broadcasting: shapes=[(6, 1024, 768), (6, 1024)]`\n\nFair enough. But `mean` and `std` have a `keepdims` kwarg that looked like it would help:\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1, keepdims=True)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1, keepdims=True)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          return normalized`\n\n...and it did!\n\n`xs.shape=(6, 1024, 768) means.shape=(6, 1024, 1) stds.shape=(6, 1024, 1) normalized.shape=(6, 1024, 768)`\n\nExcellent. So the next step was to see if that would work even slightly. Interestingly loss started off a bit higher at 11.29 after the first global step -- so adding in the LayerNorm had actually made the model *worse* than it was -- but it seemed to be falling rapidly. Things weren't totally broken, at least.\n\nBut there was more to LayerNorm than just zeroing the mean and scaling to the variance; we also needed to scale them up by a learnable amount, and then shift/bias them by adding on a different trainable amount. More precisely, both of those trainable amounts were different for each of the (in this case) 768 embedding dimensions.\n\nWe needed two learnable vectors of length `d_emb`. I hadn't noted it down at the time but I figured (as it turned out, correctly) that a sensible starting point for those values would be all-zero for the bias, and all-one for the scale.\n\nFrom [this help page](https://flax.readthedocs.io/en/stable/key_concepts.html#traced-vs-static-data), the way you create a trainable array associated with an NNX module is this:\n\n`nnx.Param(jax.random.normal(rngs.param(), (dim, dim)))`\n\nThat code created a random vector, rather than the zeros/ones we needed, and we'd need to get the dimensions right. Because of the \"Incompatible shapes for broadcasting\" error I'd just had, I was feeling a bit paranoid about the latter, so I chose a shape of `(1, 1, d_emb)`, and wrote this:\n\n`class LayerNorm(nnx.Module):      def __init__(self, d_emb):         self.scale = nnx.Param(jnp.ones((1, 1, d_emb)))         self.bias = nnx.Param(jnp.zeros((1, 1, d_emb)))      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1, keepdims=True)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1, keepdims=True)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          scaled_and_biased = (normalized * self.scale) + self.bias          return scaled_and_biased`\n\nThat looked pretty plausible, though in retrospect I think I was being overly cautious and didn't need the leading two axes for the scale and bias.\n\nThe only thing I was unsure about was whether the `nnx.Param` wrappers I had put in were really making those arrays trainable. I put some code in to print them out and kicked off a run for a few minutes, and confirmed that they were changing in ways that seem plausible -- small non-zero bias, scale close to but not equal to one. That was all good!\n\nNext, I spotted one issue. What if one of the standard deviations was zero? That would lead to a divide-by-zero error here:\n\n`normalized = (xs - means) / stds`\n\nNow, the standard deviation, if it's not zero, has to be positive -- so adding on a small value would fix that [1](#fn-1):\n\n        `normalized = (xs - means) / (stds + 1e-5)`\n\nWith that in place, I felt that it was ready to go. Time to do a full training run!\n\nI kicked that off, and it completed with this output:\n\n`2026-06-20 19:08:17.189721 Tokens seen: 92,209,152 2026-06-20 19:08:17.189724 Throughput: 95,383 tokens/second 2026-06-20 19:08:17.189734 Final train loss: 5.736 2026-06-20 19:08:17.189737 Done`\n\nLoss looked like this:\n\n![Next token prediction loss chart for embeddings-LayerNorm-output](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/llm-shell-only-next-token-loss-chart.png \"Next token prediction loss chart for embeddings-LayerNorm-output\")\n\nLet's look at the results for the previous run without LayerNorm for comparison:\n\n![Next token prediction loss chart for the bigram-style model](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/a-to-a-next-token-loss-chart.png \"Next token prediction loss chart for the bigram-style model\")\n\nYou can see that the new run, the first one, drops faster. It's harder to see from the chart, but it also finished up with a lower training loss at 937 (my relatively arbitrary metric): 5.734 rather than 5.909.\n\nThat was interesting! The new model was basically doing the same thing -- predicting the next token based only on the \"current\" token, but loss was lower. My take is that if we had trained the non-LayerNorm model for longer, it might have managed to eventually grind out a better loss. But LayerNorm was doing its job -- it was stabilising training, and as a result we converged faster.\n\nThat was a win! I decided to run it through my old smoke test from the PyTorch training runs, and see how it completed \"Every effort moves you\":\n\n`Every effort moves you can be a few years. -year-year-year-year-year-year-`\n\nIt was kind of impressive that it managed to finish the first line before it got stuck in a loop -- but it was understandable that we couldn't expect anything good yet. Each predicted token was based entirely on the token before it.\n\nWhat next? Back to our checklist:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings.\n-   Run these embeddings through multiple successive Transformers blocks.\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings\n-   Layer normalisation\n-   Run multi-head attention\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original\n-   Take a second copy of that one\n-   Layer normalisation again\n-   Run it through a simple neural network\n-   Add the results of that back in.\n\nSo, at this stage, for each input token we were predicting the next one based on the input token only -- like I said earlier, we were doing a somewhat roundabout way of building an approximation of a table of bigram probabilities. What would happen if we started paying attention to the tokens to the left? And what would be the simplest, dumbest way to do that?\n\n## A single layer of single-head attention\n\nThe real LLM has multiple layers of multi-head attention, each one also having a feed-forward network, some LayerNorms, and some shortcut connections.\n\nSingle-head attention is easier to code, but even on its own, you'd expect it to be able to add some value. Each token would get at least some information from the ones to the left. And one layer, likewise, you'd expect might help a bit. I suspected that it wouldn't work on its own -- I expected I'd need shortcut connections too -- but decided to start with attention on its own.\n\nI modified the main class to have a single \"Transformers\" layer:\n\n`class GPTModel(nnx.Module):      def __init__(         ...     ):         self.token_embedding = nnx.Embed(             ...         )          self.transformers_layer = TransformersLayer(d_emb, qkv_bias, rngs)          self.output_norm = LayerNorm(d_emb)          self.output_head = nnx.Linear(             ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          transformed = self.transformers_layer(input_embeddings)          normalized = self.output_norm(transformed)          return self.output_head(normalized)`\n\n...where that layer was actually just single-head attention:\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.attention = Attention(d_emb, qkv_bias, rngs)      def __call__(self, xs):         return self.attention(xs)`\n\nNext, it was time for the `Attention` class. I'm not going to write yet another attention explainer -- I think my [\"How do LLMs work?\"](https://www.gilesthomas.com/2025/09/how-do-llms-work) one does a decent job of that, and [\"The 'why' of attention, or: attention heads are dumb\"](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb) works well too. So in the next bit I'll assume that you understand the basics.\n\nMy first cut was basically just the maths (up to the causal mask) to get the attention scores:\n\n`class Attention(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.d_emb = d_emb          self.W_q = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)      def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ K.T          omega /= jnp.sqrt(self.d_emb)          causal_omega = jnp.tril(omega)`\n\nIt did the projections into query, key and value space, worked out the attention scores with the array multiplication, normalised it by dividing by the square root of the number of dimensions in the Q-K embedding space, and then zeroed out the scores where a token was attending to tokens in its \"future\".\n\nThere were a couple of problems, though. Firstly, that wouldn't work if we were working with batches, and secondly, zeroing out the non-causal scores wasn't quite correct.\n\nThe batches first. Our incoming `xs` here would have the shape `(batch_length, seq_len, d_emb)`. After the projections to the Q-K embedding space, both `Q` and `K` would also be shaped `(batch_length, seq_len, d_emb)`. Now, the `.T` property on the JAX array class just reverses the axes, so the code above would give us `K.T` with the shape `(d_emb, seq_len, batch_length)`.\n\nThat would break! Matrix multiplication in JAX expects all but the last two axes to represent batches, so we actually wanted `K.T` to have the shape \\``(batch_length, d_emb, seq_len)`. That meant that what we actually wanted was to just transpose the last two axes.\n\nThe JAX [`transpose` function](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.transpose.html) takes an `axes` parameter that allows you to specify the specific re-ordering of the input axes that you want. So I could rewrite the code like this:\n\n    `def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_omega = jnp.tril(omega)`\n\nAs `Q` would have the shape `(batch_length, seq_len, d_emb)`, and the transposed version of `K` would be `(batch_length, d_emb, seq_len)`, they'd be compatible for matrix multiplication and give us a result that was `(batch_length, seq_len, seq_len)` -- just what we wanted for attention scores.\n\nThe next step was to fix the causal mask. The next step in this attention mechanism was going to be running the causal attention scores in `causal_omega` through softmax over the last dimension, to convert them into attention weights. Now, our current code was zeroing out unwanted acausal scores, but a zero still contributes to softmax. If you want a particular value to come out of softmax guaranteed to be zero, you need to set it to minus infinity.\n\nI decided that the easiest way to do this was to create a causal mask -- a boolean array that matched the size of `omega`, but was full of `True`s:\n\n`causal_mask = jnp.ones_like(omega, dtype=bool)`\n\nThen I could zero out (well, \"false out\") the cells in the mask related to unwanted future-facing scores, just like I was previously doing on the scores:\n\n`causal_mask = jnp.tril(causal_mask)`\n\n...and then I could apply that mask to omega with [`jnp.where`](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.where.html), telling it to create a new array, taking the value from `omega` where the mask had `True`, and `-jnp.inf` in places where it had `False`.\n\n`causal_omega = jnp.where(causal_mask, omega, -jnp.inf)`\n\nThat seemed solid, so I just needed to run the result through [`jax.nn.softmax`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.softmax.html), specifying that the last dimension was the one where it should apply the function, and that would give me the attention weights:\n\n`attention_weights = jax.nn.softmax(causal_omega, axis=-1)`\n\nFinally, I just needed to use those attention weights to get the attention output by mixing in appropriate portions of the projection of the inputs into value space, `V`:\n\n`return attention_weights @ V`\n\nAs `attention_weights` was shaped `(batch_length, seq_len, seq_len)`, and `V` (like `Q` and `K`) was shaped `(batch_length, seq_len, d_emb)`, the batch axes were at the start where they belonged, and the matrix multiplication would work and return something shaped `(batch_length, seq_len, d_emb)`.\n\nWith that, we were done! The final single-head attention class looked like this:\n\n`class Attention(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.d_emb = d_emb          self.W_q = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)      def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_mask = jnp.ones_like(omega, dtype=bool)         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          attention_weights = jax.nn.softmax(causal_omega, axis=-1)          return attention_weights @ V`\n\nI kicked off a training run with that, and it did work, in that loss went down over the course of the run -- but at the end of the run, the loss at step 937 was 5.934 -- significantly above the 5.734 I got on the previous run, with no attention.\n\nBut that made sense! As I'd said earlier, I suspected that this wouldn't help if we had no shortcut connection.\n\nIntuitively, if you want to work out what token should be at position n+1, on average the most important other token you need to know about is probably whichever one is at position n. Knowing about the tokens at n−1, n−2, and so on, could well be helpful -- maybe *very* helpful -- but not at the cost of not knowing about the one at n.\n\nNow, single attention heads are just simple pattern-matchers. They can't learn complex rules, it's only by working together -- \"horizontally\", in multi-head attention or \"vertically\" across multiple layers -- that they can do complex things.\n\nWhat we were asking this head to do was to learn some way of gathering information about previous tokens, and also to keep the knowledge about the \"current\" one. That's a tall order for a dumb attention head!\n\nIn my mind, this is a large part of the benefit of shortcut connections. They are often presented as a way to make sure that during training, gradients flow smoothly from the output end of the model to the earlier layers. But I prefer to think of them as preserving the original embeddings, so that each layer doesn't completely replace what came into it, but instead does something closer to adding on its own notes -- like [scholars adding commentary to a core text in the Talmud](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud).\n\nIn the training run above, the attention head was trying to learn how to preserve the meaning of the embedding it was working on, while also merging in information from earlier ones. If we added a shortcut connection, then it would only have to do the second of those two jobs.\n\nThe code was simple: I updated the `TransformersLayer` module to do a shortcut connection:\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.attention = Attention(d_emb, qkv_bias, rngs)      def __call__(self, xs):         shortcut = xs         att = self.attention(xs)         return shortcut + att`\n\nI kicked off a training run, and at the end it printed this:\n\n`2026-06-23 03:51:18.086097 Tokens seen: 92,209,152 2026-06-23 03:51:18.086099 Throughput: 90,442 tokens/second 2026-06-23 03:51:18.086108 Final train loss: 5.570 2026-06-23 03:51:18.086121 Done`\n\nThe loss chart looked like this:\n\n![Next token prediction loss chart for one layer of SHA with shortcut](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/sha-with-shortcut-transformers-layer-loss-chart.png \"Next token prediction loss chart for one layer of SHA with shortcut\")\n\nAnd, importantly, that training loss at step 937 which I was using as a metric was 5.553 -- a decent improvement over the previous best of 5.734. Even a dumb single attention head was able to do something useful, if it had a shortcut connection.\n\nI decided to run another qualitative smoke test:\n\n`Every effort moves you can be able to get to get to get to get to get a lot of the way to get`\n\nI mean, it was repetitive, but it was actually getting noticeably closer to making sense!\n\nSo that was excellent news. What next? Our checklist looked like this:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings.\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation\n-   Run multi-head attention **part-done -- single-head attention only**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one\n-   Layer normalisation again\n-   Run it through a simple neural network\n-   Add the results of that back in.\n\nNow, our single attention layer was lacking something. Without position embeddings, that layer has no idea what order the tokens before the one it's looking at come in. If it's considering the \" cat\" in\n\n`The fat cat`\n\n...it doesn't know if it's looking at \"The fat cat\" or \"fat The cat\".\n\nPosition embeddings are simple, and might help, so that was the next step.\n\n## Position embeddings\n\nThese were trivial to add. We had this core code:\n\n`class GPTModel(nnx.Module):      def __init__(         ...     ):         self.token_embedding = nnx.Embed(             ...         )          self.transformers_layer = TransformersLayer(d_emb, qkv_bias, rngs)          self.output_norm = LayerNorm(d_emb)          self.output_head = nnx.Linear(             ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          transformed = self.transformers_layer(input_embeddings)          normalized = self.output_norm(transformed)          return self.output_head(normalized)`\n\nSo I just added a position encoding module in `__init__`:\n\n        `self.position_embedding = nnx.Embed(             num_embeddings=context_length,             features=d_emb,             rngs=rngs,         )`\n\n...and mixed it in with the token embeddings to create new, improved `input_embeddings` to be used in our \"Transformers\" layer:\n\n        `token_embeddings = self.token_embedding(xs)         b, n = xs.shape         position_embeddings = self.position_embedding(jnp.arange(n))         input_embeddings = token_embeddings + position_embeddings`\n\nI kicked off a training run with that:\n\n`2026-06-23 04:44:44.759768 Tokens seen: 92,209,152 2026-06-23 04:44:44.759771 Throughput: 88,618 tokens/second 2026-06-23 04:44:44.759779 Final train loss: 5.386 2026-06-23 04:44:44.759781 Done`\n\n![Next token prediction loss chart for one layer of SHA with position embeddings](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/sha-with-position-embeddings-loss-chart.png \"Next token prediction loss chart for one layer of SHA with position embeddings\")\n\nPretty hard to distinguish from the previous one, but the metric I was tracking, that loss at step 937, had improved again! We were down to 5.354 from 5.553 :-)\n\nA quick qualitative smoke test didn't show that improvement, though:\n\n`Every effort moves you can be able to get to get to get back to get back to get back to get back to`\n\nPretty much indistinguishable to the previous one. But still, Loss Number Went Down, and that's what was important at this stage.\n\nIt was time to try the next step. From the checklist:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings. **✔ ️done**\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation\n-   Run multi-head attention **part-done -- single-head attention only**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one\n-   Layer normalisation again\n-   Run it through a simple neural network\n-   Add the results of that back in.\n\nWe had only one attention head right now. Individually, [attention heads are dumb](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb), so switching to multi-head attention seemed like a good thread to pull.\n\n## Multi-head attention\n\nAt this point, my single-head attention code looked like this:\n\n        `Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_mask = jnp.ones_like(omega, dtype=bool)         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          attention_weights = jax.nn.softmax(causal_omega, axis=-1)          return attention_weights @ V`\n\nI decided to re-implement multi-head attention (which I'll call MHA from here onwards) from first principles rather than working strictly from my notes, and then to come back and check it.\n\n> If you're looking at your browser's scrollbar with horror (\"*still* only 50%?!\") and really don't want to read a full derivation of MHA, you can [skip straight to the first complete version of the code](#first-mha-code).\n\nThe point of MHA is that we're running multiple copies of the calculation above in parallel -- let's pin down the name of the number of copies as `n_heads`. Now, we could naively implement it just by spinning off `n_heads` threads and running the existing code in each, but that wouldn't really take advantage of the GPU's inherent parallelism.\n\nI felt that we could rely on the fact that JAX's matrix multiplications treat all but the last two dimensions as \"batches\". For example, if you have two arrays with shapes:\n\n`(a, b, c, ..., l, m, n)`\n\nand\n\n`(a, b, c, ..., l, n, p)`\n\n...then you can multiply them. A m×n matrix multiplied by a n×p one will be m×p, so you'll get something that is\n\n`(a, b, c, ..., l, m, p)`\n\nThe other dimensions (so long as they match) will essentially act as an a×b×c×...×l batch.\n\nNow, right now we were just using a single batch dimension. Let's look at the core multiplication in the attention mechanism, which works out `omega`, the attention scores. I had this:\n\n`omega = Q @ jnp.transpose(K, axes=(0, 2, 1))`\n\nBreaking that apart into two steps:\n\n`K_transpose = jnp.transpose(K, axes=(0, 2, 1)) omega = Q @ K_transpose`\n\nWe got `K` from this line:\n\n`K = self.W_k(xs)`\n\nLet's look at the shapes here.\n\n`xs` is our input embeddings for this layer; its shape is `(batch_size, seq_len, d_emb)`. Projecting it through `W_k`, which is shaped `(d_emb, d_emb)` gives us a shape for `K` of `(batch_size, seq_len, d_emb)` again.\n\n`Q`, being a projection of `xs` through `W_q`, which is the same shape as `W_k`, will have the same shape as `K`.\n\nNow, that means that `K_transpose` is `(batch_size, d_emb, seq_len)`, and the calculation\n\n`omega = Q @ K_transpose`\n\n...is doing a batched matrix multiplication getting us the `omega` that we want, shaped `(batch_size, seq_len, seq_len)`.\n\nBut as I said above, there's no need to stop with just one batch dimension. Let's say that we have `n_heads` heads, and that they each work with embeddings sized `d_head`. Imagine that we've already somehow done multiple projections into the key and query spaces for each of our `n_heads` heads, and that the results have somehow been put into arrays such that `Q` and `K` are shaped `(batch_size, n_heads, seq_len, d_head)` -- that is, we've gained an extra axis that keeps the projections for each head into its query-key space separate.\n\nWe could use the fact that both of those two leading axes are basically just batch dimensions, and the existing single matrix multiplication will still work, with one tiny tweak: the current transpose is this:\n\n`K_transpose = jnp.transpose(K, axes=(0, 2, 1)) omega = Q @ K_transpose`\n\n...to swap around the last two axes of a three-axis array. With one extra batch dimension, we'll need to take account of that and do this instead:\n\n`K_transpose = jnp.transpose(K, axes=(0, 1, 3, 2)) omega = Q @ K_transpose`\n\nThat will be a multiplication of `Q`, shaped `(batch_size, n_heads, seq_len, d_head)`, with `K_transpose`, shaped `(batch_size, n_heads, d_head, seq_len)`, which gives us an `omega` of the right shape, `(batch_size, n_heads, seq_len, seq_len)`.\n\nSo, if we can start treating the heads as just another batch dimension, things seem simpler, at least for the attention score calculation. Let's continue down through the single-head code, and then come back later to how we might get the inputs into that double-batched shape.\n\nThe next line after the `omega` calculation just scales the attention scores by a scalar:\n\n`omega /= jnp.sqrt(self.d_emb)`\n\nThat looked fine, just a broadcast division-by-float. We'd need to change that `self.d_emb` to be `d_head` in some manner, but that's all.\n\nNext:\n\n`causal_mask = jnp.ones_like(omega, dtype=bool)`\n\nThe `jnp.ones_like` will give us an array that's `(batch_size, n_heads, seq_len, seq_len)` full of `True`s. That seems reasonable.\n\nThe next step:\n\n`causal_mask = jnp.tril(causal_mask)`\n\nWhat will that do? Well, per [the `tril` documentation](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.tril.html#jax.numpy.tril):\n\n> When `m.ndim > 2`, `jnp.tril` operates batch-wise on the trailing axes.\n\n...which sounded good. `batch_size` and `n_heads` would be treated as batch axes, which meant that the next line:\n\n`causal_omega = jnp.where(causal_mask, omega, -jnp.inf)`\n\n...would work. Likewise, with the next line:\n\n`attention_weights = jax.nn.softmax(causal_omega, axis=-1)`\n\n...the axis to apply `softmax` to is explicitly stated as the last one, which is what we wanted.\n\nSo at the end of all of those steps, we'd have `attention_weights` shaped `(batch_size, n_heads, seq_len, seq_len)`, where the last axis had been softmaxed (softmaxxed?).\n\nThe next line looked a little trickier:\n\n`return attention_weights @ V`\n\nIn the single-head version we had `attention_weights` of shape `(batch_size, seq_len, seq_len)`, and V of shape `(batch_size, seq_len, d_emb)`, so multiplying them gives us `(batch_size, seq_len, d_emb)`\n\nIn the new MHA code so far, we had our `attention_weights` shaped `(batch_size, n_heads, seq_len, seq_len)`. So in order for the matrix multiplication to work, we'd need `V` to be shaped `(batch_size, n_heads, seq_len, d_head)`. That would give us a result shaped as `(batch_size, n_heads, seq_len, d_head)`.\n\nAnd conveniently, we'd already decided that the correct shape for `Q` and for `K` was `(batch_size, n_heads, seq_len, d_head)`. If we could use the same \"magic\" to do the projection into value space -- that is, to get `V` such that the heads formed a new batch-like axis like we had for `Q` and `K` -- then we'd be all set.\n\nSo, at that point, I'd worked out the core of MHA. If we could get all of the inputs into the shape `(batch_size, n_heads, seq_len, d_head)`, and somehow handle an output of the shape `(batch_size, n_heads, seq_len, d_head)`, then we could use MHA code something like this:\n\n        `# Q and K are (batch_size, n_heads, len_sequence, d_head)         # We need to convert K to (batch_size, n_heads, d_head, len_sequence)         # and then we get omega (batch_size, n_heads, len_sequence, len_sequence)         omega = Q @ jnp.transpose(K, axes=(0, 1, 3, 2))          omega /= jnp.sqrt(self.d_head)          causal_mask = jnp.ones_like(omega, dtype=bool)         # tril treats all but the last two axes as batches so we're OK here.         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          # last axis is still OK.         attention_weights = jax.nn.softmax(causal_omega, axis=-1)          # attention_weights is (batch_size, n_heads, len_sequence, len_sequence)         # V is (batch_size, n_heads, len_sequence, d_head)         # So this will come out as (batch_size, n_heads, len_sequence, d_head)         weighted = attention_weights @ V`\n\nThe next question was, how do we get our inputs into that shape? We could run them all through separate per-head weights -- that is, have an array with one per head, like `W_q[0]`, `W_k[0]` and `W_v[0]` for the first one. But that, again, felt like it would be failing to take advantage of the GPU properly.\n\nThe solution was to think of how matrix multiplications work. If you multiply two matrices, X·Y, the value in the result, in row r, and column c, is the dot product of row r in X and column c in Y.\n\nSo, imagine if you wanted to multiply X by n different versions of Y, let's call them Y0, Y1, and so on up to Yn. If you imagine a new matrix, Yall, which is basically all the Yxs stacked side-by-side, then the dot-product understanding of multiplication makes it pretty clear that if you did X·Yall, you would get the results of all of those separate multiplications, also stacked side-by-side. I'll call that kind of matrix a \"striped\" one, for want of a better word.\n\nNow, when we project our inputs into the embedding spaces used for attention, we have code like this:\n\n`Q = self.W_q(xs)`\n\nWe've initialised the weights, `W_k` in this case, as an `nnx.Linear`, so what is happening under the hood here is basically:\n\nQ\\=xs·Wq\n\nThat is, it is just a matrix multiplication. [2](#fn-2)\n\nSo if we imagine that `W_q` is one of those \"striped\" matrices, holding all of the separate matrices to do the projections for all of the heads in a single one shaped `(d_emb, n_heads * d_head)`, then we could stick with the current code -- the\n\n`Q = self.W_q(xs)`\n\nOur input `xs` would be shaped `(batch_size, seq_len, d_emb)`, so the result would be `(batch_size, seq_len, n_heads * d_head)`, and would have the projections for each head in the same vertical stripes as the separate heads' projection weights.\n\nNow, like PyTorch, JAX allows you to [reshape](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.reshape.html) arrays. You can take one axis of length (say) m×n, and split it into two of lengths m and n respectively -- or, conversely, you can combine two axes of length m and n to one of m×n.\n\nIf our data had the shape `(batch_size, seq_len, n_heads * d_head)`, we could reshape it like this:\n\n`Q = self.W_q(xs).reshape((batch_size, seq_len, n_heads, d_head))`\n\n...and that would split things up. So we'd have Q shaped as `(batch_size, seq_len, n_heads, d_head)`.\n\nThat's almost what we wanted! We needed `(batch_size, n_heads, seq_len, d_head)`, and a simple transpose could sort that out:\n\n`Q = jnp.transpose(     self.W_q(xs).reshape(         (batch_size, seq_len, n_heads, self.d_head)     ),     (0, 2, 1, 3) )`\n\nLikewise for `K` and `V`, and that was our inputs sorted.\n\nMoving on to the output; it came from this:\n\n`weighted = attention_weights @ V`\n\n...and as we worked out above, it was shaped `(batch_size, n_heads, seq_len, d_head)`.\n\nI remembered that we wanted to run that through a single linear layer to combine all of the different heads' outputs into one. It felt like the best way to do that would be to get it back into a \"striped\" layout: `(batch_size, seq_len, n_heads * d_head)`. This would be something like the inverse of the input-wrangling.\n\nThat would need a reshape, but before I could do that, I'd need to get the axes that needed to be merged next to each other. If the input to the linear layer was going to be `(batch_size, seq_len, n_heads * d_head)`, we'd need to convert it from `(batch_size, n_heads, seq_len, d_head)` to `(batch_size, seq_len, n_heads, d_head)` first:\n\n`jnp.transpose(weighted, (0, 2, 1, 3))`\n\n... and then we could just reshape it to `batch_size, len_sequence, n_heads * d_head`:\n\n`striped_output = jnp.transpose(     weighted,     (0, 2, 1, 3) ).reshape(     batch_size, len_sequence, self.n_heads * self.d_head )`\n\nFinally, we could run it through a linear layer, with `in_features` set to `n_heads * d_head`, and `out_features` set to `d_emb`.\n\nI put that all together, and decided to throw something extra into the mix. I remembered that Raschka's code had various checks to make sure that `d_head * n_heads == d_emb`, which seemed a little artificial -- I'd read that this was true of GPT-2, but wasn't a necessary restriction for GPT-style models, which makes sense. There's no obvious reason per se why the heads' embedding dimensions should sum up to the higher-level embedding dimensions.\n\nSo I decided initially to just pass in `d_head` and `n_heads` to the constructor. In my training script I could force them to match the GPT-2 model, but if I wanted to use the code later for something different, I could vary them.\n\nThen I remembered that although the dimensionality of the embedding spaces for the query and the key vectors have to match (because otherwise you can't multiply them to work out attention scores with Ω\\=QKT), the value vector's dimensionality can in theory be different. So I decided to break `d_head` into two separate `d_qk` and `d_v` parameters.\n\nThe result was this:\n\n`class MultiHeadAttention(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.n_heads = n_heads         self.d_qk = d_qk         self.d_v = d_v          self.W_q = nnx.Linear(d_emb, self.d_qk * n_heads, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, self.d_qk * n_heads, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, self.d_v * n_heads, use_bias=qkv_bias, rngs=rngs)          self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=False, rngs=rngs)      def __call__(self, xs):         batch_size, len_sequence, d_emb = xs.shape          # For each of the below:         # * The initial linear layer projects them to         #   (batch_size, len_sequence, d_X * n_heads)         #   where X is qk or v as appropriate.         # * The reshape makes them (batch_size, len_sequence, n_heads, d_X)         # * The transpose makes them (batch_size, n_heads, len_sequence, d_X)         Q = jnp.transpose(             self.W_q(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_qk)             ),             (0, 2, 1, 3)         )         K = jnp.transpose(             self.W_k(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_qk)             ),             (0, 2, 1, 3)         )         V = jnp.transpose(             self.W_v(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_v)             ),             (0, 2, 1, 3)         )          # Q and K are (batch_size, n_heads, len_sequence, d_qk) per above         # We need to convert K to (batch_size, n_heads, d_qk, len_sequence)         # and then we get omega (batch_size, n_heads, len_sequence, len_sequence)         omega = Q @ jnp.transpose(K, axes=(0, 1, 3, 2))          omega /= jnp.sqrt(self.d_qk)          causal_mask = jnp.ones_like(omega, dtype=bool)         # tril treats all but the last two axes as batches so we're OK here.         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          # last axis is still OK.         attention_weights = jax.nn.softmax(causal_omega, axis=-1)          # attention_weights is (batch_size, n_heads, len_sequence, len_sequence)         # V is (batch_size, n_heads, len_sequence, d_v)         # So this will come out as (batch_size, n_heads, len_sequence, d_v)         weighted = attention_weights @ V          # Transpose to (batch_size, len_sequence, n_heads, d_v),         # then reshape to (batch_size, len_sequence, n_heads * d_v)         striped_output = jnp.transpose(             weighted,             (0, 2, 1, 3)         ).reshape(             batch_size, len_sequence, self.n_heads * self.d_v         )          # Final linear layer to combine         return self.output_projection(striped_output)`\n\nUnusually for a case where I went off the reservation like this, the whole thing with the embedding space dimensionality didn't cause any problems at all! But there was one small bug in this code, which I didn't discover until later -- we'll come to it by the end of the post.\n\nAt this point, I did another of my short training runs, and:\n\n`2026-06-23 17:51:32.094308 Tokens seen: 92,209,152 2026-06-23 17:51:32.094311 Throughput: 85,682 tokens/second 2026-06-23 17:51:32.094321 Final train loss: 5.358 2026-06-23 17:51:32.094323 Done`\n\n...with a loss chart that looked like this:\n\n![Next token prediction loss chart for one layer of MHA](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-loss-chart.png \"Next token prediction loss chart for one layer of MHA\")\n\nThe training loss at the 937th global step was 5.336, only a tiny bit better than the 5.354 with single-head attention. That was quite possibly within the noise. Even though (due to the `d_head * n_heads == d_emb` restriction I was enforcing in my training script) the `W_q`, `W_k`, and `W_v` arrays were the same size, I was creating that `output_projection`, which would consume randomness and make things vary.\n\nIf I were doing a proper scientific experiment to see if a single layer of MHA beat a single layer of single-head attention, I think I would have run both for more steps to see if the difference became more pronounced later.\n\nBut for the purposes of this post, I decided to move on. My checklist now looked like this:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings. **✔ ️done**\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation\n-   Run multi-head attention **✔ ️done**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one\n-   Layer normalisation again\n-   Run it through a simple neural network\n-   Add the results of that back in.\n\nAdding that simple neural network -- the FFN -- seemed like a good next step.\n\n## Adding the FFN to the Transformers block\n\nThe [feed forward network](https://www.gilesthomas.com/2025/08/llm-from-scratch-17-the-feed-forward-network) is simple; you take the output of the MHA block, run it through a biased linear layer to expand it from `d_emb` to `4 * d_emb`, then run it through the GELU activation function, then shrink it back down to `d_emb` with another linear layer. I didn't really see any value in writing my own implementation of GELU, given that even in the book we were just given code for an approximation to type in. So, using [`jax.nn.gelu`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.gelu.html), I wrote this:\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.attention = MultiHeadAttention(d_emb, n_heads, d_qk, d_v, qkv_bias, rngs)         self.ffn = nnx.Sequential(             nnx.Linear(                 in_features=d_emb,                 out_features=d_emb * 4,                 use_bias=True,                 rngs=rngs             ),             jax.nn.gelu,             nnx.Linear(                 in_features=d_emb * 4,                 out_features=d_emb,                 use_bias=True,                 rngs=rngs             ),         )      def __call__(self, xs):         shortcut = xs         att = self.attention(xs)         post_attention = shortcut + att         fed_forward = self.ffn(post_attention)         return fed_forward + post_attention`\n\nNote that I added in a shortcut connection around the FFN as well, so that it didn't overwrite what was there, but only \"added on its notes\".\n\nI kicked that off, and it ran for ten minutes or so, but then OOMed:\n\n`2026-06-23 18:20:01.376377 Saving checkpoint  51%|██████████████████████████████████████████████████████▊                                                    | 481/938 [10:20<11:47,  1.55s/it, loss=5.758, tps=76,185]W0623 18:20:12.602631 2860192 bfc_allocator.cc:514] Allocator (GPU_0_bfc) ran out of memory trying to allocate 2.93GiB (rounded to 3149744640)requested by op If the cause is memory fragmentation maybe the environment variable 'TF_GPU_ALLOCATOR=cuda_malloc_async' will improve the situation.`\n\nAdding `TF_GPU_ALLOCATOR=cuda_malloc_async` didn't help. I spent some time trying to dig into what might be causing it, but eventually noticed something interesting: in `nvtop`, the VRAM usage was consistently 75% throughout.\n\nNow I knew that JAX pre-allocates 75% of VRAM when it starts up, but I'd been assuming that it would try to grab more if it needed it. It turned out I was wrong with that assumption -- it grabs 75%, but that's all you ever get!\n\nThe solution turned out to be the [`XLA_PYTHON_CLIENT_MEM_FRACTION`](https://docs.jax.dev/en/latest/gpu_memory_allocation.html) environment variable. If you set that to, say, `0.90`, then JAX will pre-allocate 90% of the VRAM, and you can use all of that. (You can also make it allocate as-needed with `XLA_PYTHON_CLIENT_PREALLOCATE=false`, and there are various other settings you can control with other environment variables on that linked page).\n\nAnyway, setting it to `0.90` to grab 90% of VRAM worked, and I was able to get a successful run:\n\n`2026-06-24 00:29:34.864880 Tokens seen: 92,209,152 2026-06-24 00:29:34.864882 Throughput: 77,596 tokens/second 2026-06-24 00:29:34.864900 Final train loss: 5.341 2026-06-24 00:29:34.864902 Done`\n\nThe loss chart was this:\n\n![Next token prediction loss chart for one layer of MHA plus FFN](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-with-ffn-loss-chart.png \"Next token prediction loss chart for one layer of MHA plus FFN\")\n\n...and the training loss at global step 937 was 5.295, compared to the 5.336 from MHA alone. Another tiny improvement, another one that could have been in the noise. Again, if I were doing a proper experiment, I'd do a longer run, but for now, I decided to move on.\n\nThe checklist looked like this:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings. **✔ ️done**\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation\n-   Run multi-head attention **✔ ️done**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one **✔ ️done**\n-   Layer normalisation again\n-   Run it through a simple neural network **✔ ️done**\n-   Add the results of that back in. **✔ ️done**\n\nNow, my gut instinct was that the layer normalisation inside the Transformers blocks was of most value as a way of stabilising training over deep networks. And with one layer, it didn't seem like the right time to add it. Instead, I decided to add on multiple layers.\n\n## Multiple layers\n\nFor GPT-2 small, you have 12 layers. That was already being passed in to my `GPTModel`'s `__init__` method as `n_layers`, so I just replaced this:\n\n`self.transformers_layer = TransformersLayer(     d_emb, n_heads, d_qk, d_v, qkv_bias, rngs )`\n\n...with this:\n\n`self.transformers_layers = nnx.Sequential(     *(         TransformersLayer(             d_emb, n_heads, d_qk, d_v, qkv_bias, rngs         )         for _ in range(n_layers)     ) )`\n\n...and then just renamed it where it was called; this:\n\n`transformed = self.transformers_layer(input_embeddings)`\n\n...became this:\n\n`transformed = self.transformers_layers(input_embeddings)`\n\nI kicked it off, and it completed! However, the loss chart was telling:\n\n![Next token prediction loss chart for multiple full Transformers layers, no LayerNorm](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/multiple-full-layers-no-pre-norm-loss-chart.png \"Next token prediction loss chart for multiple full Transformers layers, no LayerNorm\")\n\nOuch. Loss started dropping quite nicely, but then things got out of control and it settled down at a loss that was essentially that of a random model. At step 937, we were at 10.75, so just a hair less than the 10.82 that randomly guessing next tokens would give.\n\nWell, LayerNorm is specifically meant to stabilise training, and the checklist looked like this:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings. **✔ ️done**\n-   Run these embeddings through multiple successive Transformers blocks. **✔ ️done**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation\n-   Run multi-head attention **✔ ️done**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one **✔ ️done**\n-   Layer normalisation again\n-   Run it through a simple neural network **✔ ️done**\n-   Add the results of that back in. **✔ ️done**\n\n...and the only remaining step was that LayerNorm in the Transformers blocks, so it was time to add it in!\n\n## Adding LayerNorm\n\nAs per the checklist, we do the LayerNorm after we've taken our copy for the shortcut connection, just before MHA, and then likewise after the second shortcut copy, before the FFN. As I understand it, this was a GPT-2 innovation -- previously, people had done normalisation after those steps, but this pre-norm setup turned out to work better.\n\nThe code changes were simple. I added two `LayerNorm` modules to the `TransformersLayer` class, and then called them in the appropriate places (taking the opportunity to tidy up the variable naming in the forward pass while I was there):\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.attention_norm = LayerNorm(d_emb)         self.attention = MultiHeadAttention(d_emb, n_heads, d_qk, d_v, qkv_bias, rngs)          self.ffn_norm = LayerNorm(d_emb)         self.ffn = nnx.Sequential(             ...         )      def __call__(self, xs):         shortcut = xs         xs = self.attention_norm(xs)         xs = self.attention(xs)         xs = xs + shortcut          shortcut = xs         xs = self.ffn_norm(xs)         xs = self.ffn(xs)         return xs + shortcut`\n\nI kicked it off and ran it, and got these results:\n\n`2026-06-24 03:07:51.128966 Tokens seen: 92,209,152 2026-06-24 03:07:51.128969 Throughput: 23,399 tokens/second 2026-06-24 03:07:51.128979 Final train loss: 5.359 2026-06-24 03:07:51.128981 Done`\n\n![Next token prediction loss chart for multiple full Transformers layers, with LayerNorm](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/multiple-full-layers-with-pre-norm-loss-chart.png \"Next token prediction loss chart for multiple full Transformers layers, with LayerNorm\")\n\nThat certainly looked much healthier!\n\nHowever, when I looked at the loss at step 937, it was 5.311 -- a tiny bit higher than the single-layer MHA example, which got 5.295.\n\nI'd been willing to play a bit fast and loose with this loss number and allow myself to accept a win when the loss went down a tiny bit, even if it was such a small amount that it could have been within the noise. But *increasing* loss -- even if it could *also* be within the noise -- was a step too far.\n\nI decided that in this specific case, I'd be strict and test the hypothesis that longer training runs would demonstrate an improvement between one single layer without pre-norm, and multiple layers with pre-norm.\n\nI had to remember that these training runs would not be comparable with the earlier ones. In the training script, I [had a learning rate schedule like this](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run#learning-rate-scheduling):\n\n![The learning rate chart for the first scheduled learning rate run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart.png \"The learning rate chart for the first scheduled learning rate run\")\n\nThat straight-line warmup period and the following cosine decay were 5% and 95% of the training run respectively, which meant that (for example) global step 937 of the short runs we had been doing would be at a completely different point in the schedule than the same step would in these longer runs.\n\nHowever, they would be comparable to each other, and that was what mattered.\n\nAfter some humming and hawing, I decided that a full Chinchilla-optimal (for the full model) training run over 3,260,190,720 tokens, rounded up to fit into a round number of global steps, would be a nice experiment. I expected it to run comfortably overnight for the single-layer run, and take a bit less than two days for the multi-layer one. So I kicked off the first.\n\nJust over 11 hours later:\n\n`2026-06-24 16:39:52.178045 Tokens seen: 3,260,252,160 2026-06-24 16:39:52.178049 Throughput: 81,733 tokens/second 2026-06-24 16:39:52.178058 Final train loss: 4.324 2026-06-24 16:39:52.178061 Done`\n\nHere's the loss chart:\n\n![Next token prediction loss chart for one layer of MHA plus FFN, extended run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-with-ffn-extended-run-loss-chart.png \"Next token prediction loss chart for one layer of MHA plus FFN, extended run\")\n\nThe last checkpointing period in that run ended at global step 33,164, and the training loss then was 4.165 -- indeed, it had been at around 4.17 for quite some time, though the trend still seemed to be a tiny bit downward.\n\nSo then I kicked off a run of the full version -- multiple layers, with pre-norm in the Transformers blocks. Just over 37 hours later:\n\n`2026-06-26 06:55:39.956609 Tokens seen: 3,260,252,160 2026-06-26 06:55:39.956614 Throughput: 24,151 tokens/second 2026-06-26 06:55:39.956625 Final train loss: 3.637 2026-06-26 06:55:39.956629 Done`\n\nThe \"Final train loss\" line at the end said it all, really! But here's the loss chart:\n\n![Next token prediction loss chart for the full LLM, no dropout](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-loss-chart.png \"Next token prediction loss chart for the full LLM, no dropout\")\n\n...and the loss at step 33,164 was 3.399. Definitely quite an improvement over the 4.165 that a single layer got.\n\nAgain, at some point I might do the equivalent tests for the earlier results where improvements appear to be pretty much in the noise. It would be good to be sure that the changes really did have the impact I think they did.\n\nBut for now: our checklist was looking like this:\n\n-   Convert token IDs into embeddings. **✔ ️done**\n-   Add on position embeddings. **✔ ️done**\n-   Run these embeddings through multiple successive Transformers blocks. **✔ ️done**\n-   Layer normalisation **✔ ️done**\n-   Project them back from embedding space to vocab space. **✔ ️done**\n\nInside the Transformers blocks, we:\n\n-   Take a copy of the input sequence of embeddings **✔ ️done**\n-   Layer normalisation **✔ ️done**\n-   Run multi-head attention **✔ ️done**\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done**\n-   Take a second copy of that one **✔ ️done**\n-   Layer normalisation again **✔ ️done**\n-   Run it through a simple neural network **✔ ️done**\n-   Add the results of that back in. **✔ ️done**\n\nEverything was checked off. So was this journey over?\n\nWell, there was one thing that the original PyTorch code had that my new code didn't: dropout.\n\n## Dropout\n\nI'd found in my lengthy [interventions experiments](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) that dropout seemed to make models worse. It was, I felt, a smart idea back in the days when people had little data and did multiple epochs, each sweeping over everything, but it made less sense nowadays with single-epoch training runs over very large datasets. (Though I do have [some intuitive ideas about why it could still help](https://www.gilesthomas.com/2025/03/dropout-and-mandatory-vacation).)\n\nStill, it would be good to show that it harmed loss for this model as well.\n\nChecking my notes, I found that there were four places where dropout was applied:\n\n-   Once in the main body, just after we've worked out the embeddings.\n-   Twice in the transformers block: once after attention (but before the shortcut is mixed back in), and once after the FFN (ditto)\n-   Inside multi-head attention, on the attention weights ([which surprised me](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)).\n\nThe changes are tiny and rather dotted around the code, so rather than showing you isolated bits of code, if you'd like to see it you can take a look at [the code at this point](https://github.com/gpjt/jax-gpt2-from-scratch/blob/5fe6e745b55a33a3b55d3d7ce39f480e29ed0d77/gpt.py) and search for \"dropout\".\n\nWhen I started running that, I got an error when saving the first checkpoint:\n\n`TypeError: JAX array with PRNGKey dtype cannot be converted to a NumPy array. Use jax.random.key_data(arr) if you wish to extract the underlying integer array.`\n\nThis was happening deep inside the bowels of Safetensors, but it made a lot of sense. The `nnx.Dropout` object needs to keep track of the state of the random number generator, and that meant that the `to_flat_state` function that [I was using](https://www.gilesthomas.com/2026/06/flax-and-safetensors) might return a structure that had something that contained that state, and was not compatible with Safetensors.\n\nI decided that I'd cheat a little bit here. If I skipped the dropout layers when I saved my checkpoints, like this:\n\n    `for tuple_key, array in flat_state:         key = \".\".join(str(key) for key in tuple_key)         if \"dropout\" not in key:             simple_dict[key] = array`\n\n...then I'd be able to save them. This would have a problem -- if I restarted from a checkpoint, the dropout pattern after the restart would mirror the dropout pattern from the start of the training run, because the random seed it started with would not have come from the checkpoint, but just the initialisation code.\n\nI felt that this would not have a serious impact, though, and given that I'd not had to restart from checkpoints so far, I (wrongly, as it turned out) decided it wouldn't matter.\n\nI kicked off the run, and... after four hours, it OOMed. I cursed, decided that I'd nurse this run through anyway (despite my dropout checkpointing concerns), and kicked it off again.\n\nThree hours later, it OOMed again. I happened to be away from home at the time, logging in to my machine remotely (thanks, [Tailscale](https://tailscale.com/)!), and on looking at `nvtop`, I realised that the X window system on my machine was using a gig or so of VRAM.\n\nI was running the training run in a `tmux` session, which meant that I could kill X and not lose state, so I did that, and adjusted the `XLA_PYTHON_CLIENT_MEM_FRACTION` environment variable I was using -- it had been 0.90, so I bumped it up to 0.95. I kicked it off again, and...\n\n`2026-06-28 22:06:47.669676 Tokens seen: 2,640,052,224 2026-06-28 22:06:47.669683 Throughput: 23,019 tokens/second 2026-06-28 22:06:47.669691 Final train loss: 3.776 2026-06-28 22:06:47.669694 Done`\n\nNote that the tokens seen only relates to the period since the restart, which is why it was lower.\n\nOne more loss chart:\n\n![Next token prediction loss chart for the full LLM, with dropout](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-with-dropout-loss-chart.png \"Next token prediction loss chart for the full LLM, with dropout\")\n\n...and the training loss at step 33,164 was 3.524, higher enough than the 3.399 I got without dropout that I was comfortable that it wasn't in the noise. That was very reassuring.\n\nOnce again, if this was a proper scientific experiment I'd fix the issue with saving dropout, and run it completely from scratch -- or, at least, run it all the way through from scratch without restarts, even if I had to try several times to get it done.\n\nBut I don't think that \"replaying\" dropout would make the loss any worse. And for this experiment, I felt this was enough.\n\nSo: checklist complete. GPT-2 model coded up. It was time for some evals!\n\n## Evals, first try -- and fixing an MHA bug\n\nI wanted to evaluate these models against the ones I got using the old PyTorch code: specifically, the [last local training run](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) that used exactly the same training hyperparameters, and only differed in that it was trained using AMP -- 32-bit floats in general, but using 16-bit where the framework thought it would not be harmful.\n\nIn order to do *exactly* the same evals, I decided it would be easiest to write a conversion script to take the Safetensors files written to my JAX checkpoints, and write out new files that were compatible with the PyTorch model code -- then I'd be able to use the original PyTorch eval code. I [put something together](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fd170272e54844229642619b9c69993f0f18778f/convert_model_to_pytorch.py), converted my last two models -- the full runs with and without dropout -- and tried to load them up.\n\nUnfortunately there was an error:\n\n`RuntimeError: Error(s) in loading state_dict for GPTModel:     Missing key(s) in state_dict: \"trf_blocks.0.att.out_proj.bias\", \"trf_blocks.1.att.out_proj.bias\", \"trf_blocks.10.att.out_proj.bias\", \"trf_blocks.11.att.out_proj.bias\", \"trf_blocks.2.att.out_proj.bias\", \"trf_blocks.3.att.out_proj.bias\", \"trf_blocks.4.att.out_proj.bias\", \"trf_blocks.5.att.out_proj.bias\", \"trf_blocks.6.att.out_proj.bias\", \"trf_blocks.7.att.out_proj.bias\", \"trf_blocks.8.att.out_proj.bias\", \"trf_blocks.9.att.out_proj.bias\"`\n\nYou might remember that back when I went through multi-head attention, I mentioned that I'd made a mistake. Somehow, I'd misremembered, and thought that the output projection -- the one that mixes together all of the different heads' outputs -- was a linear layer without bias, despite [my original notes](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention) being perfectly clear that it *did* have bias.\n\nThe good news was that if I disabled bias in the PyTorch code, I could load the safetensors files that I had. So the two models I'd trained so far were not useless, and could actually work as a kind of natural experiment into the benefits of having that bias there.\n\nBut anyway, in order to do things properly, I was going to need to fix the bug and train yet another model.\n\n## Adding bias to the MHA output projections\n\nThe fix was simple, I just replaced this (in `MultiHeadAttention`):\n\n`self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=False, rngs=rngs)`\n\n...with this:\n\n`self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=True, rngs=rngs)`\n\nThen it was time to kick off yet another training run. After another 37 hours:\n\n`2026-07-05 10:09:22.147819 Tokens seen: 3,260,252,160 2026-07-05 10:09:22.147823 Throughput: 24,072 tokens/second 2026-07-05 10:09:22.147832 Final train loss: 3.650 2026-07-05 10:09:22.147834 Done`\n\n...with this loss chart:\n\n![Next token prediction loss chart for the full LLM, no dropout, with MHA output bias](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-with-mha-output-bias-loss-chart.png \"Next token prediction loss chart for the full LLM, no dropout, with MHA output bias\")\n\n...and the training loss at step 33,164 was 3.398 -- almost exactly the same as the 3.399 that I got in the no-dropout training run without MHA bias above!\n\nWell, now it really was time for the evals.\n\n## Evals, take two\n\nI updated [my conversion script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/9ab47bc589973b3d8d18a5248b405ef3f17f09fe/convert_model_to_pytorch.py) to handle the bias on the MHA output projections, and used it to convert the three models -- the un-biased ones, with and without dropout, and the biased one, without -- to the PyTorch format, then ran the loss test that I had been using to compare the old models on each.\n\nHere are the results, compared to the previous models, and OpenAI's:\n\n|  | Test loss |\n| --- | --- |\n| OpenAI weights: medium | 3.231442 |\n| **JAX, with MHA bias, no dropout** | 3.418784 |\n| **JAX, no MHA bias, no dropout** | 3.420089 |\n| **JAX, no MHA bias, with dropout** | 3.476802 |\n| OpenAI weights: small | 3.499677 |\n| `1xrtx3090-stacked-interventions` | 3.538161 |\n| `8xa100m40-stacked-interventions-1` | 3.577761 |\n| Cloud FineWeb, 8x A100 40 GiB | 3.673623 |\n| `1xrtx3090-baseline` | 3.683835 |\n| `8xa100m40-baseline` | 3.691526 |\n| Cloud FineWeb, 8x H100 80 GiB | 3.724507 |\n| Cloud FineWeb, 8x A100 80 GiB | 3.729900 |\n| Cloud FineWeb, 8x B200 160 GiB | 3.771478 |\n| Local FineWeb train | 3.943522 |\n| Local FineWeb-Edu extended train | 4.134991 |\n| Local FineWeb-Edu train | 4.166892 |\n\nThat was a pretty amazing result -- I'd clearly proven that JAX trains much better models than PyTorch! 3.5% better in the best case.\n\nWell, OK, no.\n\nMy guess is that the difference was probably something like better luck with the initial weights on the JAX side, plus [the improvement from not using AMP](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32). Anyway, the important thing was that the JAX models were in the same kind of loss range as the PyTorch ones -- and while a 3.5% improvement in loss was more variation than I'd been expecting, it was definitely the right ballpark.\n\nNow, one thing I had found in the past was that the OpenAI weights -- and some of my own models, like the Fineweb-Edu ones -- were [consistently better](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests) at an instruction fine-tuning test than their test loss scores would indicate. Would that hold here?\n\nThe IFT eval code fine-tuned each model on the [Alpaca](https://crfm.stanford.edu/2023/03/13/alpaca.html) dataset until validation loss started rising, then used the model prior to the start of the rise to generate responses for a test set. These were saved, and then run past an OpenAI model so that they could be compared with each other:\n\n```\nYou are judging the comparative capabilities of a number of different LLM\nmodels.  They have been trained to follow instructions.\n\nThe input was this:\n\n`\n{input}\n`\n\nAn example correct output is this:\n\n`\n{correct_output}\n`\n\nPlease produce a score of between 0 and 100 for each model, and respond\nwith a JSON structure like this (note that the number of models may differ\nfrom this example):\n\n`\n{\n    \"Model 1\": {\"score\": XXX, \"comments\": \"optional comments\"},\n    \"Model 2\": {\"score\": YYY, \"comments\": \"optional comments\"},\n    \"Model 3\": {\"score\": ZZZ, \"comments\": \"optional comments\"}\n}\n`\n\n...where the XXX, YYY and ZZZ are the scores for the respective models.\nYou can optionally add the \"comments\" field if you want to explain your\nreasoning.\n\nHere are the models' responses:\n\n# Model 1\n\n{model 1 response}\n\n\n# Model 2\n\n{model 2 response}\n\n\n# Model 3\n\n{model 3 response}\n```\n\n...with the model order randomly changed for each query to avoid any position bias.\n\nThe methodology seemed solid, but I was uncertain about the \"train until loss starts rising\", as it meant that different models had wildly different amounts of fine-tuning -- between two and seven epochs.\n\nOn the one hand it felt \"unfair\" to certain models that they'd get less training than others. On the other hand, if the less-trained models had been trained past the point where their validation loss started rising, then assuming that loss would continue to rise, further training would actually be a disadvantage rather than an advantage.\n\nI decided to stick with the original plan, and train until validation loss started rising. I did, however, switch the judge model from the GPT 5.4 that I used in my last IFT test to GPT 5.5. Here are the results:\n\n|  | Test loss | IFT epochs | IFT score | IFT rank |\n| --- | --- | --- | --- | --- |\n| OpenAI weights: medium | 3.231442 | 2 | 41.62 | 1 |\n| **JAX, with MHA bias, no dropout** | 3.418784 | 4 | 19.25 | 4 |\n| **JAX, no MHA bias, no dropout** | 3.420089 | 3 | 14.66 | 11 |\n| **JAX, no MHA bias, with dropout** | 3.476802 | 4 | 12.94 | 15 |\n| OpenAI weights: small | 3.499677 | 2 | 26.73 | 2 |\n| `1xrtx3090-stacked-interventions` | 3.538161 | 4 | 17.79 | 6 |\n| `8xa100m40-stacked-interventions-1` | 3.577761 | 4 | 10.29 | 16 |\n| Cloud FineWeb, 8x A100 40 GiB | 3.673623 | 7 | 20.71 | 3 |\n| `1xrtx3090-baseline` | 3.683835 | 6 | 15.11 | 9 |\n| `8xa100m40-baseline` | 3.691526 | 4 | 14.74 | 10 |\n| Cloud FineWeb, 8x H100 80 GiB | 3.724507 | 4 | 13.25 | 14 |\n| Cloud FineWeb, 8x A100 80 GiB | 3.729900 | 4 | 14.50 | 12 |\n| Cloud FineWeb, 8x B200 160 GiB | 3.771478 | 4 | 16.03 | 8 |\n| Local FineWeb train | 3.943522 | 7 | 13.73 | 13 |\n| Local FineWeb-Edu extended train | 4.134991 | 7 | 16.70 | 7 |\n| Local FineWeb-Edu train | 4.166892 | 7 | 18.68 | 5 |\n\nMore interesting datapoints! As before, you can see that low loss is not particularly well-correlated with a high score on this instruction fine-tuning test. The OpenAI weights continue to lead the pack, and while one of our new JAX models did quite well, it's still beaten by the Cloud FineWeb, 8x A100 40 GiB model.\n\nBut what was important here, just as with the loss, was that the new JAX models landed in the same ballpark as the PyTorch ones. They did, and so I could be confident that they were doing essentially the same thing.\n\nAnd that meant that, after 18 months, I had reached the end of my LLM from scratch journey.\n\n## Conclusion\n\nIt's been [a long trek](https://www.gilesthomas.com/llm-from-scratch).\n\nI [started reading](https://www.gilesthomas.com/2024/12/llm-from-scratch-1) \"Build a Large Language Model (from Scratch)\" on 22 December 2024. I was planning to breeze through over the Christmas break, but somehow it morphed into being a curriculum onto which I could hang projects to learn the fundamentals of LLMs, beyond what was in the book.\n\nIn May 2025, I had my first real conceptual breakthrough when I realised that [attention heads are (individually) dumb](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb), and as I continued, the second big one came later on in the same month, when the concept of embeddings as being [projections between vocab space and embedding space](https://www.gilesthomas.com/2025/05/llm-from-scratch-15-from-context-vectors-to-logits) (and the converse projection in the other direction that happens in the LLM's output head) became clear.\n\nIn August I had the first moment where I felt that the standard teaching approach to LLMs might not be the full story; shortcut connections are normally explained as a way to fix vanishing gradients, while I felt that a better way to see them was a way to allow attention and the FFN to \"annotate\" the existing information, similarly to how Jewish scholars have [annotated the original text of the Talmud](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud). (The results in this post seem to point in that direction, given how even a single layer of attention was massively helped by adding them.)\n\nBy early December, I had essentially finished the book, and felt I wanted to try to [train my first base model from scratch on my RTX 3090](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch). It worked, and wasn't far off the quality of the original GPT-2 small. I was really surprised that I could do that with consumer hardware, and became interested (perhaps obsessively so) with whether I could match OpenAI's weights.\n\nIn January 2026, I [trained a model using DDP on Lambda Labs](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud), and then spent the following months training model after model, trying to work out which interventions -- learning rate scheduling, gradient clipping, etc -- would improve the loss. I wrapped that up in [late April](https://www.gilesthomas.com/2026/04/llm-from-scratch-32m-interventions-conclusion), with the interesting finding that although I'd been able to get the test loss pretty low, that didn't seem to map cleanly to performance in my instruction fine-tuning tests. In other words, Loss Number Goes Down is an interesting technical game to play, but doesn't cleanly map to real-world performance.\n\nThe final step was this post, and [the previous one](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run) -- could I, using my notes, implement GPT-2 completely from scratch in JAX without referencing the book?\n\nAnd as you've read, the answer was a definite yes!\n\nOf course, as with any long-running project, there are some loose ends -- from this post alone, there's the interesting fact that JAX trained faster than PyTorch (perhaps `torch.compile` could close the gap?) and had a larger possible batch size for full-fat 32-bit. And the fact that fixing the multi-head attention bias bug didn't seem to help with the loss much was interesting too.\n\nBut those are really details, and there's so much beyond them to learn. Longer-context LLMs: position embedding improvements like RoPE, efficiency tricks like flash attention and attention variants like DSA. Mixture of experts models. How do optimisers really work? ([Do they work?](https://x.com/mgostIH/status/2024087619756318866)) And plenty more.\n\nSo it's time to draw a line under this series, and start thinking about what comes next. It's been a blast; if you've been reading along, I hope it's been as useful (and fun) to read as it was to write.\n\nAnd as always, comments, questions and corrections very welcome below.\n\n* * *\n\n1.  On looking back at Raschka's code, after having worked through all of this, there's a slight difference. I do this:\n    \n    `normalized = (xs - means) / (stds + 1e-5)`\n    \n    ...whereas he does this:\n    \n    `norm_x = (x - mean) / torch.sqrt(var + self.eps)`\n    \n    Now, the standard deviation is the square root of the variance, so if you ignore the small numbers -- `1e-5` in my case, and `self.eps` in his -- the calculations are the same. But there is a difference once those are taken account of. I don't think it's large enough to have any serious effect in these runs, though. [↩](#fnref-1)\n    \n2.  In PyTorch, linear layers are stored as the transpose of the matrix that would allow you to do that, so it would be:\n    \n    Q\\=xs×WqT\n    \n    Also, note that for simplicity (heh) I'm disregarding bias in this discussion. [↩](#fnref-2)",
  "imageCount": 14,
  "wordCount": 12652,
  "readingMinutes": 58,
  "syncedAt": "2026-08-11T08:02:06.724Z",
  "license": "CC BY 4.0",
  "author": "Giles Thomas",
  "titleZh": "从零开始编写 LLM，第 34b 篇——在 JAX 中从二元模型逐步构建并训练 GPT-2 small",
  "summaryZh": "从二元模型一路构建到 JAX 中的 GPT-2 small：我的 LLM 从零开始旅程的收官篇，看着每个组件加入后损失逐步下降。",
  "markdownBilingual": "This post is the capstone of [the most long-running series on my blog](https://www.gilesthomas.com/llm-from-scratch). In December 2024 (!), I started reading [Sebastian Raschka](https://sebastianraschka.com/)'s book \"[Build a Large Language Model (from Scratch)](https://www.manning.com/books/build-a-large-language-model-from-scratch)\", and worked through it carefully. Being who I am, despite trying to apply a strict \"no side quests\" policy, I found myself zooming off and digging into all kinds of things. <span class=\"translation\">这篇文章是[我博客上持续时间最长的系列](https://www.gilesthomas.com/llm-from-scratch)的收官篇。2024 年 12 月（没错！），我开始阅读 [Sebastian Raschka](https://sebastianraschka.com/) 的书《[从零开始构建大型语言模型](https://www.manning.com/books/build-a-large-language-model-from-scratch)》，并认真跟着做。以我的性格，尽管努力执行严格的“不做支线任务”政策，还是不知不觉跑去研究了各种各样的东西。</span>\n\nIt's time to wrap it up. I had decided that the endpoint would be to build and train an LLM from scratch just using my notes -- no reference to the book, no reference to the model code I'd written when following the book. After an [X/Twitter](https://x.com/gpjt/status/1985434030880293004) poll, I decided to use JAX for that, just to make sure that I really was building it from scratch and not regurgitating bits of PyTorch code like a bad coding LLM spitting out half-digested lumps of Stack Overflow. <span class=\"translation\">是时候收尾了。我决定最终目标是只用自己的笔记从零开始构建并训练一个 LLM——不参考书，也不参考跟着书写代码时形成的模型实现。经过一次 [X/Twitter](https://x.com/gpjt/status/1985434030880293004) 投票，我决定使用 JAX，这样可以确认自己确实是在从零构建，而不是像一个吐出半消化 Stack Overflow 代码块的糟糕编程 LLM 一样复述 PyTorch 代码。</span>\n\nIn my [last post](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run), I showed how I built a JAX training script that mirrored what I had built for the original PyTorch version of the model. To test it as I went along, I used it to train a really dumb \"LLM\", which instead of trying to predict the next token for every token in an input sequence, instead predicted the input -- that is, if you fed it <span class=\"translation\">在[上一篇文章](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run)中，我展示了如何构建一份 JAX 训练脚本，复刻原始 PyTorch 模型版本的训练脚本。为了边做边测试，我用它训练了一个非常笨的“LLM”：它不为输入序列中的每个词元预测下一个词元，而是预测输入本身——也就是说，如果输入：</span>\n\n`The fat cat sat on the mat`\n\nIt would return the same thing. I called that an A-to-A model. <span class=\"translation\">它会返回同样的内容。我把它叫作 A 到 A 模型。</span>\n\nIn this post, I'll show you how I turned it into a GPT-2 model, and then trained it from scratch on my RTX 3090 (using the parameter counts for the original paper's \"small\" size). What turned out really well with this is that I found a route that meant that almost every component I added made the model better! That's not guaranteed -- sometimes different aspects of an AI model depend on each other, so adding A without also adding B makes things worse. But (admittedly with a bit of backtracking in places) I was able to find a route that shows a nice clear progression. <span class=\"translation\">这篇文章会展示我如何把它变成 GPT-2 模型，然后在 RTX 3090 上从零训练它（参数数量采用原论文中“small”尺寸的设置）。结果非常理想：我找到了一条路线，几乎每个加入的组件都会让模型变好！这并非必然——AI 模型的不同方面有时相互依赖，只加入 A 而不加入 B 反而会变差。但（虽然中间确实有过一些回退）我找到了一条能清晰展示逐步改进的路线。</span>\n\nThe final training run took 37 hours 15 minutes -- compared to 40 hours, 38 minutes for [an equivalent PyTorch model](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation). That is despite it being full-fat 32-bit -- the PyTorch one was using Automatic Mixed Precision (AMP), which allowed it to use 16-bit calculations in places where it would be relatively harmless in terms of loss. <span class=\"translation\">最终训练运行耗时 37 小时 15 分钟，而[等价的 PyTorch 模型](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation)耗时 40 小时 38 分钟。尽管 JAX 版本使用的是完整的 32 位计算；PyTorch 版本使用了自动混合精度（AMP），可以在对损失影响相对无害的地方使用 16 位计算。</span>\n\nWhen asked to continue \"Every effort moves you\", it came back with a decent response: <span class=\"translation\">让它续写“Every effort moves you”时，它给出了还不错的回答：</span>\n\n`Every effort moves you closer to your goals, but if you are unsure of what it takes, you don’t`\n\nThe model got 3.418784 loss on my held-back test dataset, as compared to my PyTorch model's 3.538161, and even more impressively, it was better than the original GPT-2 small's result of 3.499677 on the same dataset! However, just as [I found previously](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests), the OpenAI weights still beat mine consistently in instruction fine-tuning challenges. <span class=\"translation\">这个模型在我留出的测试集上取得了 3.418784 的损失，而我的 PyTorch 模型是 3.538161；更令人惊讶的是，在同一数据集上，它甚至优于原始 GPT-2 small 的 3.499677！不过，正如我[之前发现的](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests)，OpenAI 权重在指令微调挑战中仍然稳定胜过我的模型。</span>\n\nLet's get started. <span class=\"translation\">开始吧。</span>\n\n## The starting point -- A-to-A <span class=\"translation\">起点——A 到 A</span>\n\nAt the end of the last post, we had a solid training loop, using all of the tricks I'd picked up with my PyTorch code. The A-to-A model we were training with it looked like this: <span class=\"translation\">上一篇文章结束时，我们已经有了一个扎实的训练循环，使用了我在 PyTorch 代码中学到的所有技巧。用它训练的 A 到 A 模型如下：</span>\n\n`from flax import nnx  class GPTModel(nnx.Module):      def __init__(         self,         vocab_size, context_length,         d_emb,         n_heads, n_layers,         qkv_bias,         drop_rate,         rngs,     ):         self.token_embedding = nnx.Embed(             num_embeddings=vocab_size,             features=d_emb,             rngs=rngs,         )          self.output_head = nnx.Linear(             in_features=d_emb,             out_features=vocab_size,             use_bias=False,             rngs=rngs,         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          return self.output_head(input_embeddings)`\n\nThat was based on my preferred model of [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work), where at the top level for a model, we feed in a sequence of token IDs, then: <span class=\"translation\">它基于我偏好的[LLM 工作方式模型](https://www.gilesthomas.com/2025/09/how-do-llms-work)：在模型的顶层，我们输入一串词元 ID，然后：</span>\n\n-   Firstly, we convert them into embeddings, so we get a sequence of vectors, one for each token. We do this by a lookup into a table, but we can see it conceptually as a projection via a matrix, from vocab space (where a particular token ID is a one-hot vector) to embedding space. <span class=\"translation\">首先，把它们转换为嵌入，得到一个向量序列，每个词元对应一个向量。具体是查表，但从概念上可以看作通过矩阵从词汇表空间（某个词元 ID 是 one-hot 向量）投影到嵌入空间。</span>\n-   Next, we do the magic with our Transformers layers, getting embeddings for the next token. After these layers, the embedding at position n in the output sequence is for the predicted token to come after the token at position n in the input sequence, considering that input token and all other tokens to its left. <span class=\"translation\">接下来，在 Transformer 层中施展魔法，得到下一个词元的嵌入。经过这些层后，输出序列位置 n 的嵌入表示：在考虑输入位置 n 的词元及其左侧所有词元后，预测应该出现在该输入词元之后的词元。</span>\n-   Finally, we project those back from embedding space to logits, this time actually using a real matrix (in the form of a linear layer), the output head. The logits (after being run through softmax) represent the probabilities for each token of it being the next one. <span class=\"translation\">最后，把这些嵌入从嵌入空间投影回 logits，这次真正使用矩阵（以线性层、即输出头的形式）。logits 经过 softmax 后，表示每个词元成为下一个词元的概率。</span>\n\nThe A-to-A model basically skipped the second step completely: it would project to embedding space, then immediately project back to vocab space -- and after training, it was pretty good at mapping a sequence to itself. <span class=\"translation\">A 到 A 模型基本完全跳过了第二步：它投影到嵌入空间，然后马上投影回词汇表空间；训练之后，它很擅长把一个序列映射回自身。</span>\n\nOne interesting question is, if we train the same code, but this time try to get it to make next-token predictions, how good will it be at that? Obviously it can't be as good as a full LLM. But there are correlations between tokens; full stops will generally be followed by spaces, adjectives will normally be followed by other adjectives or nouns (at least in English), and so on. It would be kind of like the predictive text systems on a phone, where (at least until recently) it would just use the last word you entered to generate a list of possible next words to select from. <span class=\"translation\">一个有趣的问题是：如果训练同一份代码，但这次让它做下一个词元预测，它会有多好？显然不可能和完整 LLM 一样好。不过词元之间存在相关性：句号通常后面跟空格，形容词通常后面跟另一个形容词或名词（至少英语是这样），等等。这有点像手机上的预测文本系统，至少直到最近，它只会用你输入的最后一个词生成一列可能的下一个词供选择。</span>\n\nOld-school natural language processing has a name for this: bigrams. The idea is that you can work out statistically what the most common two-word pairs are, which allows you to make a guess at a next word from a single one. (There are also trigrams, where you look at the last two words when predicting the next, then 4-grams, 5-grams, and so on.) You'd build up a full probability table -- for every word in your vocab, you'd have the probability of every word coming next. <span class=\"translation\">老派自然语言处理给这种东西起了一个名字：二元组（bigrams）。想法是统计出最常见的两个词组合，从而根据一个词猜下一个词。（还有三元组，即预测下一个词时查看前两个词；还有 4-gram、5-gram，依此类推。）你可以建立一张完整的概率表——词汇表中的每个词，都有后面出现每个词的概率。</span>\n\nSo maybe even with that minimal model, we could get it to learn something similar to a set of token-level (rather than word-level) bigrams, which would then get the loss down. Obviously it wouldn't be as good as a full bigram table -- for our GPT-2 vocab size of 50,257, that would need 50,2572\\=2,525,766,049 parameters -- but perhaps it could approximate one. (For comparison, the model we're using has just an embedding table and an output head, each mapping between 50,257 dimensions and 768, so that's 2×50,257×768≈77 million parameters -- about 3% of the full table.) <span class=\"translation\">所以，即使模型如此简单，也许我们能让它学习一组词元级（而非词语级）的二元关系，从而降低损失。显然它不会和完整的二元概率表一样好——对于 GPT-2 的 50,257 词汇表，需要 50,257²=2,525,766,049 个参数——但也许可以近似它。（相比之下，我们使用的模型只有一个嵌入表和一个输出头，每个都在 50,257 和 768 维之间映射，因此参数量为 2×50,257×768≈7,700 万，约为完整表的 3%。）</span>\n\nAn uninitialised model would (hopefully) have a loss of about 10.82, implying a [perplexity](https://www.gilesthomas.com/2025/10/llm-from-scratch-21-perplexed-by-perplexity) equal to the vocab size. If we can train our dumb model to get better loss than that, then we'd have the beginnings of an LLM. <span class=\"translation\">一个未初始化的模型（希望）会有约 10.82 的损失，这意味着困惑度等于词汇表大小。如果能训练这个笨模型，使损失低于这个值，就算是迈出了构建 LLM 的第一步。</span>\n\nThat was a simple test to run. In my training code, I had a dataset class that looked like this: <span class=\"translation\">这是一个很容易运行的测试。在训练代码中，我有一个数据集类，看起来是这样：</span>\n\n`class BigTrainDataset:      def __init__(self, all_tokens, seq_length, microbatch_size):         self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)      def __getitem__(self, ix):         return self.xs[ix], self.ys[ix]      def __len__(self):         return self.xs.shape[0]`\n\nThat is, the inputs, the `xs`, were the same as the targets, the `ys`. If we fed it <span class=\"translation\">也就是说，输入 `xs` 和目标 `ys` 相同。如果输入：</span>\n\n`The fat cat sat on the mat`\n\n...then we'd be training it to output exactly the same thing. <span class=\"translation\">……那我们训练的就是让它输出完全相同的内容。</span>\n\nThe modified version for a real LLM would involve feeding it something like this: <span class=\"translation\">真正 LLM 的修改版本，会输入类似这样的内容：</span>\n\n`The fat cat sat on the`\n\n...and targeting this: <span class=\"translation\">……目标则是：</span>\n\n `fat cat sat on the mat`\n\nThat's a simple change -- that `__init__` method became this: <span class=\"translation\">这是一个简单改动——`__init__` 方法变成：</span>\n\n        `self.xs = all_tokens[:-1].reshape(-1, microbatch_size, seq_length)         self.ys = all_tokens[1:].reshape(-1, microbatch_size, seq_length)`\n\nI did that, and kicked it off to train on the 92,209,152 tokens that I was (somewhat arbitrarily) using in the last post to test my training loop. The loss chart looked like this: <span class=\"translation\">我这样做了，然后启动训练，使用上一篇文章中（有些随意地）用于测试训练循环的 92,209,152 个词元。损失图如下：</span>\n\n![Next token prediction loss chart for the bigram-style model](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/a-to-a-next-token-loss-chart.png \"Next token prediction loss chart for the bigram-style model\")\n\nThat was pretty promising! Loss came down from roughly 10.82 down to a fairly stable 6 or so by global step 768, and seemed to flatten out there. It's possible that further training could have got it down a bit more, but I decided (again, somewhat arbitrarily) to use the average train loss in the checkpoint period ending at step 937 as my starting point. If we could make changes that reduced that, then we'd be moving forward. For this model, that value was 5.909. <span class=\"translation\">这非常有希望！损失从约 10.82 降到全局步 768 时相当稳定的 6 左右，之后似乎趋于平坦。继续训练可能会让它再低一些，但我决定（同样有些随意地）把第 937 步结束的检查点区间平均训练损失作为起点。如果能通过改动降低这个值，就说明我们在前进。这个模型的起点值是 5.909。</span>\n\nSo, what were the changes we needed to make to change our bigram-style model to a real, if small, LLM? <span class=\"translation\">那么，要把二元模型变成一个真正的（尽管很小的）LLM，我们需要做哪些改动？</span>\n\n## Building GPT-2: a checklist <span class=\"translation\">构建 GPT-2：检查清单</span>\n\nAdapting from my [how LLMs work](https://www.gilesthomas.com/2025/09/how-do-llms-work) post, a GPT-2-style LLM looks like this. We receive our sequence of token IDs, and then: <span class=\"translation\">根据我的[LLM 工作原理](https://www.gilesthomas.com/2025/09/how-do-llms-work)文章，GPT-2 风格 LLM 的结构如下。我们接收一串词元 ID，然后：</span>\n\n-   Convert them into embeddings. **✔ ️done** <span class=\"translation\">将它们转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. <span class=\"translation\">添加位置嵌入。</span>\n-   Run these embeddings through multiple successive Transformers blocks. <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。</span>\n-   Layer normalisation <span class=\"translation\">层归一化</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings <span class=\"translation\">复制一份输入嵌入序列</span>\n-   Layer normalisation <span class=\"translation\">层归一化</span>\n-   Run multi-head attention <span class=\"translation\">运行多头注意力</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”</span>\n-   Take a second copy of that one <span class=\"translation\">再复制一份这个结果</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化</span>\n-   Run it through a simple neural network <span class=\"translation\">让它通过一个简单的神经网络</span>\n-   Add the results of that back in. <span class=\"translation\">把结果加回去。</span>\n\nSo that gave me the checklist; looking at it, the most tempting next step was layer normalisation (henceforth LayerNorm). It's used at the end of the core loop, and then twice in the Transformers blocks. <span class=\"translation\">于是我得到了这张清单；看起来最诱人的下一步是层归一化（下文简称 LayerNorm）。它在核心循环的末尾使用一次，在 Transformer 块中再使用两次。</span>\n\nWhat would happen if we coded it up, and then added it to the core only? <span class=\"translation\">如果我们把它写出来，然后只添加到核心部分，会发生什么？</span>\n\n## LayerNorm <span class=\"translation\">LayerNorm</span>\n\nThe purpose of [LayerNorm](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation) is to stabilise training. We constrain the values flowing through our model so that they have certain statistical properties that tend to make the whole thing more trainable. <span class=\"translation\">[LayerNorm](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation) 的目的是稳定训练。我们约束流经模型的值，使其具备某些统计属性，从而让整个模型更容易训练。</span>\n\nThat would mean that if it did help with this model -- placed in between the embedding layer at the start, and the output head at the end -- then we'd hope for loss to go down faster, and ideally finish at a lower level. <span class=\"translation\">也就是说，如果它确实能帮助这个模型——放在开头的嵌入层和末尾的输出头之间——我们希望损失下降得更快，理想情况下最终也更低。</span>\n\nTime to code it up! <span class=\"translation\">开始写代码！</span>\n\n[NNX has its own LayerNorm implementation](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/nn/normalization.html#flax.nnx.LayerNorm), of course (as does [PyTorch](https://docs.pytorch.org/docs/2.12/generated/torch.nn.LayerNorm.html)), but in the book, we implement it ourselves, and that felt like the correct path to take. <span class=\"translation\">当然，[NNX 有自己的 LayerNorm 实现](https://flax.readthedocs.io/en/stable/api_reference/flax.nnx/nn/normalization.html#flax.nnx.LayerNorm)（[PyTorch](https://docs.pytorch.org/docs/2.12/generated/torch.nn.LayerNorm.html) 也是如此），但书中是自己实现的，我觉得沿用这条路线更合适。</span>\n\nFirstly, I implemented a dummy version: <span class=\"translation\">首先，我实现了一个占位版本：</span>\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         return xs`\n\n...and updated the core `GPTModel` to create and call one: <span class=\"translation\">……并更新核心 `GPTModel`，创建并调用它：</span>\n\n`class GPTModel(nnx.Module):     def __init__(         ...     ):         self.token_embedding = nnx.Embed(                 ...         )          self.output_norm = LayerNorm()          self.output_head = nnx.Linear(                 ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          normalised = self.output_norm(input_embeddings)          return self.output_head(normalised)`\n\nAnd kicked off a training run for a few seconds just to make sure that it hadn't broken anything and that loss dropped -- being my first NNX module-inside-a-module, I worried that there might have been something non-intuitive that I had to do to get it to work. But everything seemed good -- loss was dropping, no errors. <span class=\"translation\">我启动训练运行了几秒，确认没有破坏任何东西且损失会下降——这是我第一次在模块内部嵌套 NNX 模块，担心需要做一些不直观的操作才能让它工作。但一切似乎都很好：损失下降，没有错误。</span>\n\nSo, following [the notes I made when I first learned about LayerNorm](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation), I needed to make the values flowing through centred around zero by subtracting their mean, and then scale them to have a variance of one by dividing by the standard deviation (details in those notes). <span class=\"translation\">因此，根据[我初次学习 LayerNorm 时做的笔记](https://www.gilesthomas.com/2025/07/llm-from-scratch-16-layer-normalisation)，我需要通过减去均值，让流经模型的值以零为中心；再除以标准差，把它们缩放到方差为 1（笔记中有详细说明）。</span>\n\nThe shape of the `xs` I had coming into my `LayerNorm` class's `_call` was this: <span class=\"translation\">进入 `LayerNorm` 类 `_call` 的 `xs` 形状是：</span>\n\n`xs.shape=(6, 1024, 768)`\n\nThat was `(batch_size, seq_len, d_emb)`. So we needed to do those operations strictly on the last axis, manipulating each embedding independently. <span class=\"translation\">也就是 `(batch_size, seq_len, d_emb)`。因此必须严格沿最后一个轴执行这些操作，独立处理每个嵌入。</span>\n\nJAX has a [`std` function](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.std.html) and a [`mean` one](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.mean.html#jax.numpy.mean), both of which take an `axis` parameter. The `Array` object repackaged those as methods, which was convenient, so I did a first cut test like this: <span class=\"translation\">JAX 有 [`std` 函数](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.std.html)和 [`mean` 函数](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.mean.html#jax.numpy.mean)，两者都接收 `axis` 参数。`Array` 对象把它们重新包装成方法，这很方便，所以我先做了这样的测试：</span>\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1)         jax.debug.print(f\"{stds.shape=}\")          return xs`\n\nThat printed out these results: <span class=\"translation\">它打印出了下面的结果：</span>\n\n`xs.shape=(6, 1024, 768) means.shape=(6, 1024) stds.shape=(6, 1024)`\n\n...which looked plausible; one number for each embedding vector. Could we broadcast them across the array? <span class=\"translation\">……看起来合理：每个嵌入向量有一个数字。我们能把它们广播到整个数组上吗？</span>\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          return normalized`\n\nThis blew up: <span class=\"translation\">结果崩溃了：</span>\n\n`ValueError: Incompatible shapes for broadcasting: shapes=[(6, 1024, 768), (6, 1024)]`\n\nFair enough. But `mean` and `std` have a `keepdims` kwarg that looked like it would help: <span class=\"translation\">也有道理。不过 `mean` 和 `std` 有一个看起来能帮上忙的 `keepdims` 关键字参数：</span>\n\n`class LayerNorm(nnx.Module):      def __init__(self):         ...      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1, keepdims=True)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1, keepdims=True)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          return normalized`\n\n...and it did! <span class=\"translation\">……而且确实成功了！</span>\n\n`xs.shape=(6, 1024, 768) means.shape=(6, 1024, 1) stds.shape=(6, 1024, 1) normalized.shape=(6, 1024, 768)`\n\nExcellent. So the next step was to see if that would work even slightly. Interestingly loss started off a bit higher at 11.29 after the first global step -- so adding in the LayerNorm had actually made the model *worse* than it was -- but it seemed to be falling rapidly. Things weren't totally broken, at least. <span class=\"translation\">太好了。下一步是看看它哪怕能否带来一点效果。有趣的是，第一步之后损失起始值略高，为 11.29——加入 LayerNorm 实际上让模型比以前*更差*——但损失似乎下降得很快。至少没有完全坏掉。</span>\n\nBut there was more to LayerNorm than just zeroing the mean and scaling to the variance; we also needed to scale them up by a learnable amount, and then shift/bias them by adding on a different trainable amount. More precisely, both of those trainable amounts were different for each of the (in this case) 768 embedding dimensions. <span class=\"translation\">不过 LayerNorm 不只是把均值变为零、把方差缩放为 1；我们还需要用可学习的量将它们放大，再加上另一个可训练的量进行平移/偏置。更准确地说，这两个可训练量对每个嵌入维度（这里是 768 个）都不同。</span>\n\nWe needed two learnable vectors of length `d_emb`. I hadn't noted it down at the time but I figured (as it turned out, correctly) that a sensible starting point for those values would be all-zero for the bias, and all-one for the scale. <span class=\"translation\">我们需要两个长度为 `d_emb` 的可学习向量。当时我没有记下来，但我猜（事实证明是正确的），合理的初始值应该是偏置全零、缩放全一。</span>\n\nFrom [this help page](https://flax.readthedocs.io/en/stable/key_concepts.html#traced-vs-static-data), the way you create a trainable array associated with an NNX module is this: <span class=\"translation\">根据[这个帮助页面](https://flax.readthedocs.io/en/stable/key_concepts.html#traced-vs-static-data)，创建与 NNX 模块关联的可训练数组的方法是：</span>\n\n`nnx.Param(jax.random.normal(rngs.param(), (dim, dim)))`\n\nThat code created a random vector, rather than the zeros/ones we needed, and we'd need to get the dimensions right. Because of the \"Incompatible shapes for broadcasting\" error I'd just had, I was feeling a bit paranoid about the latter, so I chose a shape of `(1, 1, d_emb)`, and wrote this: <span class=\"translation\">那段代码创建的是随机向量，而我们需要全零/全一，还需要确定正确的维度。刚遇到“广播形状不兼容”错误，我对后者有点疑神疑鬼，所以选择了 `(1, 1, d_emb)` 的形状，并写成：</span>\n\n`class LayerNorm(nnx.Module):      def __init__(self, d_emb):         self.scale = nnx.Param(jnp.ones((1, 1, d_emb)))         self.bias = nnx.Param(jnp.zeros((1, 1, d_emb)))      def __call__(self, xs):         jax.debug.print(f\"{xs.shape=}\")          means = xs.mean(axis=-1, keepdims=True)         jax.debug.print(f\"{means.shape=}\")          stds = xs.std(axis=-1, keepdims=True)         jax.debug.print(f\"{stds.shape=}\")          normalized = (xs - means) / stds         jax.debug.print(f\"{normalized.shape=}\")          scaled_and_biased = (normalized * self.scale) + self.bias          return scaled_and_biased`\n\nThat looked pretty plausible, though in retrospect I think I was being overly cautious and didn't need the leading two axes for the scale and bias. <span class=\"translation\">看起来相当合理，不过现在回头看，我当时过于谨慎了，缩放和偏置其实不需要前面那两个轴。</span>\n\nThe only thing I was unsure about was whether the `nnx.Param` wrappers I had put in were really making those arrays trainable. I put some code in to print them out and kicked off a run for a few minutes, and confirmed that they were changing in ways that seem plausible -- small non-zero bias, scale close to but not equal to one. That was all good! <span class=\"translation\">唯一不确定的是，我加入的 `nnx.Param` 包装器是否真的让数组变成了可训练参数。我加入代码把它们打印出来，运行了几分钟，确认它们以合理的方式变化：偏置是小的非零值，缩放接近但不等于 1。一切都很好！</span>\n\nNext, I spotted one issue. What if one of the standard deviations was zero? That would lead to a divide-by-zero error here: <span class=\"translation\">接下来我发现一个问题。如果某个标准差为零呢？这里就会除以零：</span>\n\n`normalized = (xs - means) / stds`\n\nNow, the standard deviation, if it's not zero, has to be positive -- so adding on a small value would fix that [1](#fn-1): <span class=\"translation\">标准差只要不为零就必定为正，因此加上一个很小的值就能修复这个问题 [1](#fn-1)：</span>\n\n        `normalized = (xs - means) / (stds + 1e-5)`\n\nWith that in place, I felt that it was ready to go. Time to do a full training run! <span class=\"translation\">加上之后，我觉得已经准备好了。该进行完整训练了！</span>\n\nI kicked that off, and it completed with this output: <span class=\"translation\">我启动了训练，输出如下：</span>\n\n`2026-06-20 19:08:17.189721 Tokens seen: 92,209,152 2026-06-20 19:08:17.189724 Throughput: 95,383 tokens/second 2026-06-20 19:08:17.189734 Final train loss: 5.736 2026-06-20 19:08:17.189737 Done`\n\nLoss looked like this: <span class=\"translation\">损失图如下：</span>\n\n![Next token prediction loss chart for embeddings-LayerNorm-output](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/llm-shell-only-next-token-loss-chart.png \"Next token prediction loss chart for embeddings-LayerNorm-output\")\n\nLet's look at the results for the previous run without LayerNorm for comparison: <span class=\"translation\">作为对比，看看之前没有 LayerNorm 的运行结果：</span>\n\n![Next token prediction loss chart for the bigram-style model](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/a-to-a-next-token-loss-chart.png \"Next token prediction loss chart for the bigram-style model\")\n\nYou can see that the new run, the first one, drops faster. It's harder to see from the chart, but it also finished up with a lower training loss at 937 (my relatively arbitrary metric): 5.734 rather than 5.909. <span class=\"translation\">你可以看到，新运行（第一张图）下降得更快。图上不太容易看出来，但在第 937 步，它最终的训练损失也更低（这是我相当随意的指标）：5.734，而不是 5.909。</span>\n\nThat was interesting! The new model was basically doing the same thing -- predicting the next token based only on the \"current\" token, but loss was lower. My take is that if we had trained the non-LayerNorm model for longer, it might have managed to eventually grind out a better loss. But LayerNorm was doing its job -- it was stabilising training, and as a result we converged faster. <span class=\"translation\">这很有意思！新模型基本做着同样的事——仅根据“当前”词元预测下一个词元，但损失更低。我的看法是，如果让没有 LayerNorm 的模型训练更久，它最终也许能磨出更低的损失。但 LayerNorm 完成了自己的工作：稳定训练，因此让我们更快收敛。</span>\n\nThat was a win! I decided to run it through my old smoke test from the PyTorch training runs, and see how it completed \"Every effort moves you\": <span class=\"translation\">这是一次胜利！我决定用 PyTorch 训练运行中的旧冒烟测试运行它，看看它如何续写“Every effort moves you”：</span>\n\n`Every effort moves you can be a few years. -year-year-year-year-year-year-`\n\nIt was kind of impressive that it managed to finish the first line before it got stuck in a loop -- but it was understandable that we couldn't expect anything good yet. Each predicted token was based entirely on the token before it. <span class=\"translation\">它能在陷入循环前完成第一行，确实有点令人印象深刻；但我们还不能期待好结果也很容易理解。每个预测词元完全基于它前面的那个词元。</span>\n\nWhat next? Back to our checklist: <span class=\"translation\">接下来是什么？回到清单：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换成嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. <span class=\"translation\">添加位置嵌入。</span>\n-   Run these embeddings through multiple successive Transformers blocks. <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化 **✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings <span class=\"translation\">复制一份输入嵌入序列</span>\n-   Layer normalisation <span class=\"translation\">层归一化</span>\n-   Run multi-head attention <span class=\"translation\">运行多头注意力</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”</span>\n-   Take a second copy of that one <span class=\"translation\">再复制一份这个结果</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化</span>\n-   Run it through a simple neural network <span class=\"translation\">让它通过一个简单的神经网络</span>\n-   Add the results of that back in. <span class=\"translation\">把结果加回去。</span>\n\nSo, at this stage, for each input token we were predicting the next one based on the input token only -- like I said earlier, we were doing a somewhat roundabout way of building an approximation of a table of bigram probabilities. What would happen if we started paying attention to the tokens to the left? And what would be the simplest, dumbest way to do that? <span class=\"translation\">在这个阶段，我们为每个输入词元预测下一个词元，而且只基于输入词元本身——如前所述，我们在用一种有些迂回的方式近似一张二元概率表。如果开始关注左侧词元会怎样？最简单、最笨的做法是什么？</span>\n\n## A single layer of single-head attention <span class=\"translation\">单层单头注意力</span>\n\nThe real LLM has multiple layers of multi-head attention, each one also having a feed-forward network, some LayerNorms, and some shortcut connections. <span class=\"translation\">真正的 LLM 有多层多头注意力，每层还带有前馈网络、一些 LayerNorm 和一些快捷连接。</span>\n\nSingle-head attention is easier to code, but even on its own, you'd expect it to be able to add some value. Each token would get at least some information from the ones to the left. And one layer, likewise, you'd expect might help a bit. I suspected that it wouldn't work on its own -- I expected I'd need shortcut connections too -- but decided to start with attention on its own. <span class=\"translation\">单头注意力更容易编码，但即使单独使用，也应该能带来一些价值。每个词元至少能从左侧词元获得一点信息；一层注意力同样应该能提供一些帮助。我怀疑它单独使用不会奏效——预计还需要快捷连接——但决定先只加入注意力。</span>\n\nI modified the main class to have a single \"Transformers\" layer: <span class=\"translation\">我修改主类，让它拥有一个“Transformer”层：</span>\n\n`class GPTModel(nnx.Module):      def __init__(         ...     ):         self.token_embedding = nnx.Embed(             ...         )          self.transformers_layer = TransformersLayer(d_emb, qkv_bias, rngs)          self.output_norm = LayerNorm(d_emb)          self.output_head = nnx.Linear(             ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          transformed = self.transformers_layer(input_embeddings)          normalized = self.output_norm(transformed)          return self.output_head(normalized)`\n\n...where that layer was actually just single-head attention: <span class=\"translation\">其中这个层实际上只是单头注意力：</span>\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.attention = Attention(d_emb, qkv_bias, rngs)      def __call__(self, xs):         return self.attention(xs)`\n\nNext, it was time for the `Attention` class. I'm not going to write yet another attention explainer -- I think my [\"How do LLMs work?\"](https://www.gilesthomas.com/2025/09/how-do-llms-work) one does a decent job of that, and [\"The 'why' of attention, or: attention heads are dumb\"](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb) works well too. So in the next bit I'll assume that you understand the basics. <span class=\"translation\">接下来轮到 `Attention` 类。我不打算再写一篇注意力解释器——我认为[“LLM 如何工作？”](https://www.gilesthomas.com/2025/09/how-do-llms-work)那篇已经讲得不错，[“注意力的‘为什么’，或者：注意力头很笨”](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb)也很有用。因此下面默认你已经理解基础知识。</span>\n\nMy first cut was basically just the maths (up to the causal mask) to get the attention scores: <span class=\"translation\">我的第一版基本只是计算注意力分数所需的数学（一直到因果掩码）：</span>\n\n`class Attention(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.d_emb = d_emb          self.W_q = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)      def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ K.T          omega /= jnp.sqrt(self.d_emb)          causal_omega = jnp.tril(omega)`\n\nIt did the projections into query, key and value space, worked out the attention scores with the array multiplication, normalised it by dividing by the square root of the number of dimensions in the Q-K embedding space, and then zeroed out the scores where a token was attending to tokens in its \"future\". <span class=\"translation\">它把输入投影到 query、key 和 value 空间，通过数组乘法计算注意力分数，再除以 Q-K 嵌入空间维度数的平方根进行归一化，最后将词元关注“未来”词元的分数置零。</span>\n\nThere were a couple of problems, though. Firstly, that wouldn't work if we were working with batches, and secondly, zeroing out the non-causal scores wasn't quite correct. <span class=\"translation\">不过有两个问题。第一，处理批次时它无法工作；第二，把非因果分数置零并不完全正确。</span>\n\nThe batches first. Our incoming `xs` here would have the shape `(batch_length, seq_len, d_emb)`. After the projections to the Q-K embedding space, both `Q` and `K` would also be shaped `(batch_length, seq_len, d_emb)`. Now, the `.T` property on the JAX array class just reverses the axes, so the code above would give us `K.T` with the shape `(d_emb, seq_len, batch_length)`. <span class=\"translation\">先看批次。这里传入的 `xs` 形状是 `(batch_length, seq_len, d_emb)`。投影到 Q-K 嵌入空间后，`Q` 和 `K` 也都是 `(batch_length, seq_len, d_emb)`。而 JAX 数组类的 `.T` 属性只是反转轴，所以前面的代码会得到形状为 `(d_emb, seq_len, batch_length)` 的 `K.T`。</span>\n\nThat would break! Matrix multiplication in JAX expects all but the last two axes to represent batches, so we actually wanted `K.T` to have the shape \\``(batch_length, d_emb, seq_len)`. That meant that what we actually wanted was to just transpose the last two axes. <span class=\"translation\">这会出问题！JAX 的矩阵乘法要求除最后两个轴外的轴表示批次，所以我们真正想要的 `K.T` 形状是 `(batch_length, d_emb, seq_len)`。也就是说，实际只需要转置最后两个轴。</span>\n\nThe JAX [`transpose` function](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.transpose.html) takes an `axes` parameter that allows you to specify the specific re-ordering of the input axes that you want. So I could rewrite the code like this: <span class=\"translation\">JAX 的 [`transpose` 函数](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.transpose.html)接收 `axes` 参数，可以指定输入轴的具体重排方式。因此可以把代码改成：</span>\n\n    `def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_omega = jnp.tril(omega)`\n\nAs `Q` would have the shape `(batch_length, seq_len, d_emb)`, and the transposed version of `K` would be `(batch_length, d_emb, seq_len)`, they'd be compatible for matrix multiplication and give us a result that was `(batch_length, seq_len, seq_len)` -- just what we wanted for attention scores. <span class=\"translation\">由于 `Q` 的形状是 `(batch_length, seq_len, d_emb)`，转置后的 `K` 是 `(batch_length, d_emb, seq_len)`，两者可以进行矩阵乘法，结果形状为 `(batch_length, seq_len, seq_len)`——正是我们想要的注意力分数形状。</span>\n\nThe next step was to fix the causal mask. The next step in this attention mechanism was going to be running the causal attention scores in `causal_omega` through softmax over the last dimension, to convert them into attention weights. Now, our current code was zeroing out unwanted acausal scores, but a zero still contributes to softmax. If you want a particular value to come out of softmax guaranteed to be zero, you need to set it to minus infinity. <span class=\"translation\">下一步修复因果掩码。这个注意力机制接下来会在最后一个维度上对 `causal_omega` 中的因果注意力分数执行 softmax，把它们转换为注意力权重。当前代码把不需要的非因果分数置零，但零仍会参与 softmax。如果希望 softmax 的某个值保证为零，就需要把它设为负无穷。</span>\n\nI decided that the easiest way to do this was to create a causal mask -- a boolean array that matched the size of `omega`, but was full of `True`s: <span class=\"translation\">我觉得最容易的办法是创建一个因果掩码——一个和 `omega` 大小相同、但全部为 `True` 的布尔数组：</span>\n\n`causal_mask = jnp.ones_like(omega, dtype=bool)`\n\nThen I could zero out (well, \"false out\") the cells in the mask related to unwanted future-facing scores, just like I was previously doing on the scores: <span class=\"translation\">然后可以像之前处理分数那样，把掩码中对应未来分数的单元置零（也就是“置假”）：</span>\n\n`causal_mask = jnp.tril(causal_mask)`\n\n...and then I could apply that mask to omega with [`jnp.where`](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.where.html), telling it to create a new array, taking the value from `omega` where the mask had `True`, and `-jnp.inf` in places where it had `False`. <span class=\"translation\">然后用 [`jnp.where`](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.where.html) 把掩码应用到 `omega`：掩码为 `True` 的位置取 `omega` 的值，为 `False` 的位置取 `-jnp.inf`，生成新数组。</span>\n\n`causal_omega = jnp.where(causal_mask, omega, -jnp.inf)`\n\nThat seemed solid, so I just needed to run the result through [`jax.nn.softmax`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.softmax.html), specifying that the last dimension was the one where it should apply the function, and that would give me the attention weights: <span class=\"translation\">看起来很扎实。现在只需将结果传给 [`jax.nn.softmax`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.softmax.html)，指定最后一个维度作为函数作用的维度，就能得到注意力权重：</span>\n\n`attention_weights = jax.nn.softmax(causal_omega, axis=-1)`\n\nFinally, I just needed to use those attention weights to get the attention output by mixing in appropriate portions of the projection of the inputs into value space, `V`: <span class=\"translation\">最后，使用这些注意力权重，把输入投影到 value 空间的结果 `V` 按适当比例混合起来，从而得到注意力输出：</span>\n\n`return attention_weights @ V`\n\nAs `attention_weights` was shaped `(batch_length, seq_len, seq_len)`, and `V` (like `Q` and `K`) was shaped `(batch_length, seq_len, d_emb)`, the batch axes were at the start where they belonged, and the matrix multiplication would work and return something shaped `(batch_length, seq_len, d_emb)`. <span class=\"translation\">`attention_weights` 的形状是 `(batch_length, seq_len, seq_len)`，而 `V`（和 `Q`、`K` 一样）的形状是 `(batch_length, seq_len, d_emb)`。批次轴在开头，位置正确，矩阵乘法可以正常工作，返回形状为 `(batch_length, seq_len, d_emb)` 的结果。</span>\n\nWith that, we were done! The final single-head attention class looked like this: <span class=\"translation\">这样就完成了！最终的单头注意力类如下：</span>\n\n`class Attention(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.d_emb = d_emb          self.W_q = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, d_emb, use_bias=qkv_bias, rngs=rngs)      def __call__(self, xs):         Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_mask = jnp.ones_like(omega, dtype=bool)         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          attention_weights = jax.nn.softmax(causal_omega, axis=-1)          return attention_weights @ V`\n\nI kicked off a training run with that, and it did work, in that loss went down over the course of the run -- but at the end of the run, the loss at step 937 was 5.934 -- significantly above the 5.734 I got on the previous run, with no attention. <span class=\"translation\">我用它启动训练，确实能工作，训练过程中损失下降了；但运行结束时第 937 步的损失为 5.934，明显高于上一次没有注意力时的 5.734。</span>\n\nBut that made sense! As I'd said earlier, I suspected that this wouldn't help if we had no shortcut connection. <span class=\"translation\">但这很合理！正如前面所说，我怀疑没有快捷连接时它不会带来帮助。</span>\n\nIntuitively, if you want to work out what token should be at position n+1, on average the most important other token you need to know about is probably whichever one is at position n. Knowing about the tokens at n−1, n−2, and so on, could well be helpful -- maybe *very* helpful -- but not at the cost of not knowing about the one at n. <span class=\"translation\">直觉上，如果要判断位置 n+1 应该是什么词元，平均来说最重要的其他词元可能是位置 n 的词元。了解 n−1、n−2 等位置当然可能有帮助——甚至很有帮助——但不能以不了解位置 n 的词元为代价。</span>\n\nNow, single attention heads are just simple pattern-matchers. They can't learn complex rules, it's only by working together -- \"horizontally\", in multi-head attention or \"vertically\" across multiple layers -- that they can do complex things. <span class=\"translation\">单个注意力头只是简单的模式匹配器。它们无法学习复杂规则；只有通过协作——在多头注意力中“横向”协作，或在多层之间“纵向”协作——才能做复杂的事情。</span>\n\nWhat we were asking this head to do was to learn some way of gathering information about previous tokens, and also to keep the knowledge about the \"current\" one. That's a tall order for a dumb attention head! <span class=\"translation\">我们要求这个注意力头既学会收集前面词元的信息，又保留“当前”词元的知识。这对一个笨拙的注意力头来说要求太高了！</span>\n\nIn my mind, this is a large part of the benefit of shortcut connections. They are often presented as a way to make sure that during training, gradients flow smoothly from the output end of the model to the earlier layers. But I prefer to think of them as preserving the original embeddings, so that each layer doesn't completely replace what came into it, but instead does something closer to adding on its own notes -- like [scholars adding commentary to a core text in the Talmud](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud). <span class=\"translation\">在我看来，这正是快捷连接好处的很大一部分。人们通常把它们解释成一种确保训练过程中梯度能从模型输出端顺畅流向早期层的方法。但我更愿意把它们看成保留原始嵌入的机制，让每一层不会完全替换收到的内容，而是更像添加自己的笔记——类似[学者给《塔木德》核心文本添加注释](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud)。</span>\n\nIn the training run above, the attention head was trying to learn how to preserve the meaning of the embedding it was working on, while also merging in information from earlier ones. If we added a shortcut connection, then it would only have to do the second of those two jobs. <span class=\"translation\">在上面的训练运行中，注意力头既要学习保留它正在处理的嵌入含义，又要合并更早词元的信息。如果加入快捷连接，它只需要完成其中第二项工作。</span>\n\nThe code was simple: I updated the `TransformersLayer` module to do a shortcut connection: <span class=\"translation\">代码很简单：我修改 `TransformersLayer` 模块，加入快捷连接：</span>\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, qkv_bias, rngs):         self.attention = Attention(d_emb, qkv_bias, rngs)      def __call__(self, xs):         shortcut = xs         att = self.attention(xs)         return shortcut + att`\n\nI kicked off a training run, and at the end it printed this: <span class=\"translation\">我启动了一次训练，结束时打印出：</span>\n\n`2026-06-23 03:51:18.086097 Tokens seen: 92,209,152 2026-06-23 03:51:18.086099 Throughput: 90,442 tokens/second 2026-06-23 03:51:18.086108 Final train loss: 5.570 2026-06-23 03:51:18.086121 Done`\n\nThe loss chart looked like this: <span class=\"translation\">损失图如下：</span>\n\n![Next token prediction loss chart for one layer of SHA with shortcut](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/sha-with-shortcut-transformers-layer-loss-chart.png \"Next token prediction loss chart for one layer of SHA with shortcut\")\n\nAnd, importantly, that training loss at step 937 which I was using as a metric was 5.553 -- a decent improvement over the previous best of 5.734. Even a dumb single attention head was able to do something useful, if it had a shortcut connection. <span class=\"translation\">更重要的是，我用作指标的第 937 步训练损失是 5.553，比之前最佳的 5.734 有了不错的改善。即便是一个笨拙的单头注意力，只要有快捷连接，也能做出有用的事情。</span>\n\nI decided to run another qualitative smoke test: <span class=\"translation\">我决定再进行一次定性冒烟测试：</span>\n\n`Every effort moves you can be able to get to get to get to get to get a lot of the way to get`\n\nI mean, it was repetitive, but it was actually getting noticeably closer to making sense! <span class=\"translation\">我的意思是，它虽然很重复，但明显更接近有意义的文本了！</span>\n\nSo that was excellent news. What next? Our checklist looked like this: <span class=\"translation\">这是个好消息。下一步是什么？检查清单如下：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. <span class=\"translation\">添加位置嵌入。</span>\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**部分完成——只有一层**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation <span class=\"translation\">层归一化。</span>\n-   Run multi-head attention **part-done -- single-head attention only** <span class=\"translation\">运行多头注意力。**部分完成——目前只有单头注意力**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one <span class=\"translation\">再复制一份这个结果。</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化。</span>\n-   Run it through a simple neural network <span class=\"translation\">让它通过一个简单的神经网络。</span>\n-   Add the results of that back in. <span class=\"translation\">把结果加回去。</span>\n\nNow, our single attention layer was lacking something. Without position embeddings, that layer has no idea what order the tokens before the one it's looking at come in. If it's considering the \" cat\" in <span class=\"translation\">现在，我们的单个注意力层还缺少一些东西。没有位置嵌入时，它不知道所关注词元之前的词元顺序。如果它正在考虑 “ cat”：</span>\n\n`The fat cat`\n\n...it doesn't know if it's looking at \"The fat cat\" or \"fat The cat\". <span class=\"translation\">……它不知道自己看到的是 “The fat cat” 还是 “fat The cat”。</span>\n\nPosition embeddings are simple, and might help, so that was the next step. <span class=\"translation\">位置嵌入很简单，也许会有帮助，所以这是下一步。</span>\n\n## Position embeddings <span class=\"translation\">位置嵌入</span>\n\nThese were trivial to add. We had this core code: <span class=\"translation\">添加位置嵌入非常简单。我们有这样的核心代码：</span>\n\n`class GPTModel(nnx.Module):      def __init__(         ...     ):         self.token_embedding = nnx.Embed(             ...         )          self.transformers_layer = TransformersLayer(d_emb, qkv_bias, rngs)          self.output_norm = LayerNorm(d_emb)          self.output_head = nnx.Linear(             ...         )      def __call__(self, xs):         input_embeddings = self.token_embedding(xs)          transformed = self.transformers_layer(input_embeddings)          normalized = self.output_norm(transformed)          return self.output_head(normalized)`\n\nSo I just added a position encoding module in `__init__`: <span class=\"translation\">所以我只需在 `__init__` 中添加一个位置编码模块：</span>\n\n        `self.position_embedding = nnx.Embed(             num_embeddings=context_length,             features=d_emb,             rngs=rngs,         )`\n\n...and mixed it in with the token embeddings to create new, improved `input_embeddings` to be used in our \"Transformers\" layer: <span class=\"translation\">……并将它与词元嵌入混合，创建新的、改进后的 `input_embeddings`，供“Transformer”层使用：</span>\n\n        `token_embeddings = self.token_embedding(xs)         b, n = xs.shape         position_embeddings = self.position_embedding(jnp.arange(n))         input_embeddings = token_embeddings + position_embeddings`\n\nI kicked off a training run with that: <span class=\"translation\">我用它启动了一次训练：</span>\n\n`2026-06-23 04:44:44.759768 Tokens seen: 92,209,152 2026-06-23 04:44:44.759771 Throughput: 88,618 tokens/second 2026-06-23 04:44:44.759779 Final train loss: 5.386 2026-06-23 04:44:44.759781 Done`\n\n![Next token prediction loss chart for one layer of SHA with position embeddings](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/sha-with-position-embeddings-loss-chart.png \"Next token prediction loss chart for one layer of SHA with position embeddings\")\n\nPretty hard to distinguish from the previous one, but the metric I was tracking, that loss at step 937, had improved again! We were down to 5.354 from 5.553 :-) <span class=\"translation\">很难和上一张图区分，但我跟踪的指标——第 937 步的损失——再次改善！从 5.553 降到了 5.354 :-)</span>\n\nA quick qualitative smoke test didn't show that improvement, though: <span class=\"translation\">不过，快速定性冒烟测试没有体现这一改善：</span>\n\n`Every effort moves you can be able to get to get to get back to get back to get back to get back to`\n\nPretty much indistinguishable to the previous one. But still, Loss Number Went Down, and that's what was important at this stage. <span class=\"translation\">和上一次几乎无法区分。不过损失数字下降了，这才是现阶段重要的事情。</span>\n\nIt was time to try the next step. From the checklist: <span class=\"translation\">是时候尝试下一步了。根据清单：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. **✔ ️done** <span class=\"translation\">添加位置嵌入。**✔ ️已完成**</span>\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**部分完成——只有一层**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation <span class=\"translation\">层归一化。</span>\n-   Run multi-head attention **part-done -- single-head attention only** <span class=\"translation\">运行多头注意力。**部分完成——目前只有单头注意力**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one <span class=\"translation\">再复制一份这个结果。</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化。</span>\n-   Run it through a simple neural network <span class=\"translation\">让它通过一个简单的神经网络。</span>\n-   Add the results of that back in. <span class=\"translation\">把结果加回去。</span>\n\nWe had only one attention head right now. Individually, [attention heads are dumb](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb), so switching to multi-head attention seemed like a good thread to pull. <span class=\"translation\">我们目前只有一个注意力头。单独来看，[注意力头很笨](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb)，所以切换到多头注意力似乎是值得尝试的方向。</span>\n\n## Multi-head attention <span class=\"translation\">多头注意力</span>\n\nAt this point, my single-head attention code looked like this: <span class=\"translation\">此时，我的单头注意力代码如下：</span>\n\n        `Q = self.W_q(xs)         K = self.W_k(xs)         V = self.W_v(xs)          omega = Q @ jnp.transpose(K, axes=(0, 2, 1))          omega /= jnp.sqrt(self.d_emb)          causal_mask = jnp.ones_like(omega, dtype=bool)         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          attention_weights = jax.nn.softmax(causal_omega, axis=-1)          return attention_weights @ V`\n\nI decided to re-implement multi-head attention (which I'll call MHA from here onwards) from first principles rather than working strictly from my notes, and then to come back and check it. <span class=\"translation\">我决定从第一性原理重新实现多头注意力（之后简称 MHA），而不是严格照抄笔记，然后再回来核对。</span>\n\n> If you're looking at your browser's scrollbar with horror (\"*still* only 50%?!\") and really don't want to read a full derivation of MHA, you can [skip straight to the first complete version of the code](#first-mha-code). <span class=\"translation\">如果你正惊恐地看着浏览器滚动条（“怎么*还*只有 50%？！”），实在不想读完 MHA 的完整推导，可以[直接跳到第一份完整代码](#first-mha-code)。</span>\n\nThe point of MHA is that we're running multiple copies of the calculation above in parallel -- let's pin down the name of the number of copies as `n_heads`. Now, we could naively implement it just by spinning off `n_heads` threads and running the existing code in each, but that wouldn't really take advantage of the GPU's inherent parallelism. <span class=\"translation\">MHA 的要点是并行运行上面计算的多份副本——我们把副本数量命名为 `n_heads`。当然可以简单地启动 `n_heads` 个线程、每个线程运行现有代码，但那样不会真正利用 GPU 的固有并行能力。</span>\n\nI felt that we could rely on the fact that JAX's matrix multiplications treat all but the last two dimensions as \"batches\". For example, if you have two arrays with shapes: <span class=\"translation\">我觉得可以利用 JAX 矩阵乘法的特性：除了最后两个维度外，其他维度都被视为“批次”。例如，如果有两个形状分别为：</span>\n\n`(a, b, c, ..., l, m, n)`\n\nand <span class=\"translation\">以及</span>\n\n`(a, b, c, ..., l, n, p)`\n\n...then you can multiply them. A m×n matrix multiplied by a n×p one will be m×p, so you'll get something that is <span class=\"translation\">……那么就可以相乘。m×n 矩阵乘 n×p 矩阵会得到 m×p，所以结果形状是：</span>\n\n`(a, b, c, ..., l, m, p)`\n\nThe other dimensions (so long as they match) will essentially act as an a×b×c×...×l batch. <span class=\"translation\">其他维度（只要彼此匹配）基本上就会充当 a×b×c×…×l 的批次。</span>\n\nNow, right now we were just using a single batch dimension. Let's look at the core multiplication in the attention mechanism, which works out `omega`, the attention scores. I had this: <span class=\"translation\">现在我们只有一个批次维度。来看看注意力机制中计算 `omega`（注意力分数）的核心乘法。我有：</span>\n\n`omega = Q @ jnp.transpose(K, axes=(0, 2, 1))`\n\nBreaking that apart into two steps: <span class=\"translation\">把它拆成两步：</span>\n\n`K_transpose = jnp.transpose(K, axes=(0, 2, 1)) omega = Q @ K_transpose`\n\nWe got `K` from this line: <span class=\"translation\">我们通过这一行得到 `K`：</span>\n\n`K = self.W_k(xs)`\n\nLet's look at the shapes here. <span class=\"translation\">看看这里的形状。</span>\n\n`xs` is our input embeddings for this layer; its shape is `(batch_size, seq_len, d_emb)`. Projecting it through `W_k`, which is shaped `(d_emb, d_emb)` gives us a shape for `K` of `(batch_size, seq_len, d_emb)` again. <span class=\"translation\">`xs` 是这一层的输入嵌入，形状为 `(batch_size, seq_len, d_emb)`。通过形状为 `(d_emb, d_emb)` 的 `W_k` 投影后，`K` 的形状仍然是 `(batch_size, seq_len, d_emb)`。</span>\n\n`Q`, being a projection of `xs` through `W_q`, which is the same shape as `W_k`, will have the same shape as `K`. <span class=\"translation\">`Q` 是 `xs` 经过 `W_q` 的投影；`W_q` 和 `W_k` 形状相同，所以 `Q` 和 `K` 形状也相同。</span>\n\nNow, that means that `K_transpose` is `(batch_size, d_emb, seq_len)`, and the calculation <span class=\"translation\">这意味着 `K_transpose` 的形状是 `(batch_size, d_emb, seq_len)`，而计算：</span>\n\n`omega = Q @ K_transpose`\n\n...is doing a batched matrix multiplication getting us the `omega` that we want, shaped `(batch_size, seq_len, seq_len)`. <span class=\"translation\">……执行的是批次矩阵乘法，得到我们想要的形状为 `(batch_size, seq_len, seq_len)` 的 `omega`。</span>\n\nBut as I said above, there's no need to stop with just one batch dimension. Let's say that we have `n_heads` heads, and that they each work with embeddings sized `d_head`. Imagine that we've already somehow done multiple projections into the key and query spaces for each of our `n_heads` heads, and that the results have somehow been put into arrays such that `Q` and `K` are shaped `(batch_size, n_heads, seq_len, d_head)` -- that is, we've gained an extra axis that keeps the projections for each head into its query-key space separate. <span class=\"translation\">不过如前所述，不必止步于一个批次维度。假设有 `n_heads` 个头，每个头使用大小为 `d_head` 的嵌入。设想我们已经以某种方式为每个头投影到 key 和 query 空间，并把结果放进形状为 `(batch_size, n_heads, seq_len, d_head)` 的数组中——也就是增加了一个轴，将各头在 query-key 空间中的投影分开。</span>\n\nWe could use the fact that both of those two leading axes are basically just batch dimensions, and the existing single matrix multiplication will still work, with one tiny tweak: the current transpose is this: <span class=\"translation\">可以利用前两个轴本质上都是批次维度这一事实，现有的单矩阵乘法只需一个小改动就能继续工作。当前转置是：</span>\n\n`K_transpose = jnp.transpose(K, axes=(0, 2, 1)) omega = Q @ K_transpose`\n\n...to swap around the last two axes of a three-axis array. With one extra batch dimension, we'll need to take account of that and do this instead: <span class=\"translation\">……它交换三轴数组的最后两个轴。增加一个批次维度后，需要考虑它，改成：</span>\n\n`K_transpose = jnp.transpose(K, axes=(0, 1, 3, 2)) omega = Q @ K_transpose`\n\nThat will be a multiplication of `Q`, shaped `(batch_size, n_heads, seq_len, d_head)`, with `K_transpose`, shaped `(batch_size, n_heads, d_head, seq_len)`, which gives us an `omega` of the right shape, `(batch_size, n_heads, seq_len, seq_len)`. <span class=\"translation\">这会将形状为 `(batch_size, n_heads, seq_len, d_head)` 的 `Q` 与形状为 `(batch_size, n_heads, d_head, seq_len)` 的 `K_transpose` 相乘，得到正确形状 `(batch_size, n_heads, seq_len, seq_len)` 的 `omega`。</span>\n\nSo, if we can start treating the heads as just another batch dimension, things seem simpler, at least for the attention score calculation. Let's continue down through the single-head code, and then come back later to how we might get the inputs into that double-batched shape. <span class=\"translation\">所以，只要把各个头当成另一个批次维度，至少在计算注意力分数时事情就简单了。继续沿着单头代码往下看，稍后再回头处理如何把输入变成双批次形状。</span>\n\nThe next line after the `omega` calculation just scales the attention scores by a scalar: <span class=\"translation\">`omega` 计算后的下一行只是用一个标量缩放注意力分数：</span>\n\n`omega /= jnp.sqrt(self.d_emb)`\n\nThat looked fine, just a broadcast division-by-float. We'd need to change that `self.d_emb` to be `d_head` in some manner, but that's all. <span class=\"translation\">看起来没问题，就是广播除以一个浮点数。需要以某种方式把 `self.d_emb` 换成 `d_head`，但除此之外就没有了。</span>\n\nNext: <span class=\"translation\">接下来：</span>\n\n`causal_mask = jnp.ones_like(omega, dtype=bool)`\n\nThe `jnp.ones_like` will give us an array that's `(batch_size, n_heads, seq_len, seq_len)` full of `True`s. That seems reasonable. <span class=\"translation\">`jnp.ones_like` 会生成一个 `(batch_size, n_heads, seq_len, seq_len)` 的数组，全部填充 `True`。看起来合理。</span>\n\nThe next step: <span class=\"translation\">下一步：</span>\n\n`causal_mask = jnp.tril(causal_mask)`\n\nWhat will that do? Well, per [the `tril` documentation](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.tril.html#jax.numpy.tril): <span class=\"translation\">根据 [`tril` 文档](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.tril.html#jax.numpy.tril)：</span>\n\n> When `m.ndim > 2`, `jnp.tril` operates batch-wise on the trailing axes. <span class=\"translation\">当 `m.ndim &gt; 2` 时，`jnp.tril` 会对最后两个轴按批次运行。</span>\n\n...which sounded good. `batch_size` and `n_heads` would be treated as batch axes, which meant that the next line: <span class=\"translation\">……听起来不错。`batch_size` 和 `n_heads` 会被视为批次轴，因此下一行：</span>\n\n`causal_omega = jnp.where(causal_mask, omega, -jnp.inf)`\n\n...would work. Likewise, with the next line: <span class=\"translation\">同样，下一行：</span>\n\n`attention_weights = jax.nn.softmax(causal_omega, axis=-1)`\n\n...the axis to apply `softmax` to is explicitly stated as the last one, which is what we wanted. <span class=\"translation\">……要应用 `softmax` 的轴明确指定为最后一个，这正是我们想要的。</span>\n\nSo at the end of all of those steps, we'd have `attention_weights` shaped `(batch_size, n_heads, seq_len, seq_len)`, where the last axis had been softmaxed (softmaxxed?). <span class=\"translation\">完成这些步骤后，`attention_weights` 的形状就是 `(batch_size, n_heads, seq_len, seq_len)`，最后一个轴已经进行了 softmax（softmax 过？）。</span>\n\nThe next line looked a little trickier: <span class=\"translation\">下一行看起来有点棘手：</span>\n\n`return attention_weights @ V`\n\nIn the single-head version we had `attention_weights` of shape `(batch_size, seq_len, seq_len)`, and V of shape `(batch_size, seq_len, d_emb)`, so multiplying them gives us `(batch_size, seq_len, d_emb)` <span class=\"translation\">在单头版本中，`attention_weights` 的形状是 `(batch_size, seq_len, seq_len)`，`V` 的形状是 `(batch_size, seq_len, d_emb)`，相乘得到 `(batch_size, seq_len, d_emb)`。</span>\n\nIn the new MHA code so far, we had our `attention_weights` shaped `(batch_size, n_heads, seq_len, seq_len)`. So in order for the matrix multiplication to work, we'd need `V` to be shaped `(batch_size, n_heads, seq_len, d_head)`. That would give us a result shaped as `(batch_size, n_heads, seq_len, d_head)`. <span class=\"translation\">到目前为止，在新 MHA 代码中，`attention_weights` 的形状是 `(batch_size, n_heads, seq_len, seq_len)`。为了让矩阵乘法正常工作，需要让 `V` 的形状变为 `(batch_size, n_heads, seq_len, d_head)`。这样结果就是 `(batch_size, n_heads, seq_len, d_head)`。</span>\n\nAnd conveniently, we'd already decided that the correct shape for `Q` and for `K` was `(batch_size, n_heads, seq_len, d_head)`. If we could use the same \"magic\" to do the projection into value space -- that is, to get `V` such that the heads formed a new batch-like axis like we had for `Q` and `K` -- then we'd be all set. <span class=\"translation\">方便的是，我们已经决定 `Q` 和 `K` 的正确形状都是 `(batch_size, n_heads, seq_len, d_head)`。如果可以用同样的“魔法”投影到 value 空间——也就是让 `V` 的各头形成类似 `Q`、`K` 的新批次轴——那就大功告成了。</span>\n\nSo, at that point, I'd worked out the core of MHA. If we could get all of the inputs into the shape `(batch_size, n_heads, seq_len, d_head)`, and somehow handle an output of the shape `(batch_size, n_heads, seq_len, d_head)`, then we could use MHA code something like this: <span class=\"translation\">到这里，我已经推导出了 MHA 的核心。如果能把所有输入变成 `(batch_size, n_heads, seq_len, d_head)`，并以某种方式处理同样形状的输出，就可以使用类似下面的 MHA 代码：</span>\n\n        `# Q and K are (batch_size, n_heads, len_sequence, d_head)         # We need to convert K to (batch_size, n_heads, d_head, len_sequence)         # and then we get omega (batch_size, n_heads, len_sequence, len_sequence)         omega = Q @ jnp.transpose(K, axes=(0, 1, 3, 2))          omega /= jnp.sqrt(self.d_head)          causal_mask = jnp.ones_like(omega, dtype=bool)         # tril treats all but the last two axes as batches so we're OK here.         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          # last axis is still OK.         attention_weights = jax.nn.softmax(causal_omega, axis=-1)          # attention_weights is (batch_size, n_heads, len_sequence, len_sequence)         # V is (batch_size, n_heads, len_sequence, d_head)         # So this will come out as (batch_size, n_heads, len_sequence, d_head)         weighted = attention_weights @ V`\n\nThe next question was, how do we get our inputs into that shape? We could run them all through separate per-head weights -- that is, have an array with one per head, like `W_q[0]`, `W_k[0]` and `W_v[0]` for the first one. But that, again, felt like it would be failing to take advantage of the GPU properly. <span class=\"translation\">下一个问题是，如何把输入变成这种形状？我们可以让输入分别通过每个头的权重——也就是每个头都有一组数组，例如第一个头使用 `W_q[0]`、`W_k[0]` 和 `W_v[0]`。但这样又无法充分利用 GPU。</span>\n\nThe solution was to think of how matrix multiplications work. If you multiply two matrices, X·Y, the value in the result, in row r, and column c, is the dot product of row r in X and column c in Y. <span class=\"translation\">解决方案是思考矩阵乘法的工作方式。两个矩阵 X·Y 相乘时，结果第 r 行第 c 列的值，是 X 第 r 行与 Y 第 c 列的点积。</span>\n\nSo, imagine if you wanted to multiply X by n different versions of Y, let's call them Y0, Y1, and so on up to Yn. If you imagine a new matrix, Yall, which is basically all the Yxs stacked side-by-side, then the dot-product understanding of multiplication makes it pretty clear that if you did X·Yall, you would get the results of all of those separate multiplications, also stacked side-by-side. I'll call that kind of matrix a \"striped\" one, for want of a better word. <span class=\"translation\">想象一下，要把 X 分别乘以 n 个不同版本的 Y，叫作 Y0、Y1，一直到 Yn。设想一个新矩阵 Yall，它基本上是把所有 Y 横向并排堆叠；根据矩阵乘法的点积定义，很容易看出，计算 X·Yall 就会得到所有单独乘法的结果，同样横向堆叠。我姑且把这种矩阵叫作“条纹矩阵”。</span>\n\nNow, when we project our inputs into the embedding spaces used for attention, we have code like this: <span class=\"translation\">现在，把输入投影到注意力使用的嵌入空间时，代码类似这样：</span>\n\n`Q = self.W_q(xs)`\n\nWe've initialised the weights, `W_k` in this case, as an `nnx.Linear`, so what is happening under the hood here is basically: <span class=\"translation\">我们把权重（这里是 `W_k`）初始化为 `nnx.Linear`，所以底层实际做的基本是：</span>\n\nQ\\=xs·Wq\n\nThat is, it is just a matrix multiplication. [2](#fn-2) <span class=\"translation\">也就是说，它只是一次矩阵乘法。[2](#fn-2)</span>\n\nSo if we imagine that `W_q` is one of those \"striped\" matrices, holding all of the separate matrices to do the projections for all of the heads in a single one shaped `(d_emb, n_heads * d_head)`, then we could stick with the current code -- the <span class=\"translation\">如果把 `W_q` 想成这种“条纹矩阵”，在一个形状为 `(d_emb, n_heads * d_head)` 的矩阵中保存所有头的投影矩阵，那么就可以继续使用当前代码：</span>\n\n`Q = self.W_q(xs)`\n\nOur input `xs` would be shaped `(batch_size, seq_len, d_emb)`, so the result would be `(batch_size, seq_len, n_heads * d_head)`, and would have the projections for each head in the same vertical stripes as the separate heads' projection weights. <span class=\"translation\">输入 `xs` 的形状是 `(batch_size, seq_len, d_emb)`，所以结果形状为 `(batch_size, seq_len, n_heads * d_head)`，各头的投影结果会按与各头投影权重相同的垂直条纹排列。</span>\n\nNow, like PyTorch, JAX allows you to [reshape](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.reshape.html) arrays. You can take one axis of length (say) m×n, and split it into two of lengths m and n respectively -- or, conversely, you can combine two axes of length m and n to one of m×n. <span class=\"translation\">和 PyTorch 一样，JAX 允许对数组进行[重塑](https://docs.jax.dev/en/latest/_autosummary/jax.numpy.reshape.html)。可以把长度为（比如）m×n 的一个轴拆成长度分别为 m 和 n 的两个轴，也可以反过来把长度为 m 和 n 的两个轴合并成长度 m×n 的一个轴。</span>\n\nIf our data had the shape `(batch_size, seq_len, n_heads * d_head)`, we could reshape it like this: <span class=\"translation\">如果数据形状为 `(batch_size, seq_len, n_heads * d_head)`，就可以这样重塑：</span>\n\n`Q = self.W_q(xs).reshape((batch_size, seq_len, n_heads, d_head))`\n\n...and that would split things up. So we'd have Q shaped as `(batch_size, seq_len, n_heads, d_head)`. <span class=\"translation\">……这样就把它拆开了，得到形状为 `(batch_size, seq_len, n_heads, d_head)` 的 Q。</span>\n\nThat's almost what we wanted! We needed `(batch_size, n_heads, seq_len, d_head)`, and a simple transpose could sort that out: <span class=\"translation\">这几乎就是我们想要的！我们需要 `(batch_size, n_heads, seq_len, d_head)`，只需简单转置即可：</span>\n\n`Q = jnp.transpose(     self.W_q(xs).reshape(         (batch_size, seq_len, n_heads, self.d_head)     ),     (0, 2, 1, 3) )`\n\nLikewise for `K` and `V`, and that was our inputs sorted. <span class=\"translation\">`K` 和 `V` 同理，这样输入就整理好了。</span>\n\nMoving on to the output; it came from this: <span class=\"translation\">继续处理输出；它来自：</span>\n\n`weighted = attention_weights @ V`\n\n...and as we worked out above, it was shaped `(batch_size, n_heads, seq_len, d_head)`. <span class=\"translation\">……正如上面推导的，形状为 `(batch_size, n_heads, seq_len, d_head)`。</span>\n\nI remembered that we wanted to run that through a single linear layer to combine all of the different heads' outputs into one. It felt like the best way to do that would be to get it back into a \"striped\" layout: `(batch_size, seq_len, n_heads * d_head)`. This would be something like the inverse of the input-wrangling. <span class=\"translation\">我记得需要用一个线性层，把不同头的输出合并成一个。最好的办法似乎是把它重新变回“条纹”布局：`(batch_size, seq_len, n_heads * d_head)`。这和整理输入的过程应该互为逆操作。</span>\n\nThat would need a reshape, but before I could do that, I'd need to get the axes that needed to be merged next to each other. If the input to the linear layer was going to be `(batch_size, seq_len, n_heads * d_head)`, we'd need to convert it from `(batch_size, n_heads, seq_len, d_head)` to `(batch_size, seq_len, n_heads, d_head)` first: <span class=\"translation\">这需要一次重塑，但在此之前，必须先把要合并的轴放到相邻位置。如果线性层输入要是 `(batch_size, seq_len, n_heads * d_head)`，就要先把 `(batch_size, n_heads, seq_len, d_head)` 转换成 `(batch_size, seq_len, n_heads, d_head)`：</span>\n\n`jnp.transpose(weighted, (0, 2, 1, 3))`\n\n... and then we could just reshape it to `batch_size, len_sequence, n_heads * d_head`: <span class=\"translation\">……然后就可以把它重塑为 `batch_size, len_sequence, n_heads * d_head`：</span>\n\n`striped_output = jnp.transpose(     weighted,     (0, 2, 1, 3) ).reshape(     batch_size, len_sequence, self.n_heads * self.d_head )`\n\nFinally, we could run it through a linear layer, with `in_features` set to `n_heads * d_head`, and `out_features` set to `d_emb`. <span class=\"translation\">最后把它传过一个线性层，`in_features` 设为 `n_heads * d_head`，`out_features` 设为 `d_emb`。</span>\n\nI put that all together, and decided to throw something extra into the mix. I remembered that Raschka's code had various checks to make sure that `d_head * n_heads == d_emb`, which seemed a little artificial -- I'd read that this was true of GPT-2, but wasn't a necessary restriction for GPT-style models, which makes sense. There's no obvious reason per se why the heads' embedding dimensions should sum up to the higher-level embedding dimensions. <span class=\"translation\">我把这些组合起来，并决定再加入一点额外内容。我记得 Raschka 的代码有多处检查，确保 `d_head * n_heads == d_emb`，这似乎有些人为——我读到 GPT-2 确实满足这一点，但 GPT 风格模型并不必须满足，这也很合理。没有明显理由要求各头嵌入维度之和必须等于更高层的嵌入维度。</span>\n\nSo I decided initially to just pass in `d_head` and `n_heads` to the constructor. In my training script I could force them to match the GPT-2 model, but if I wanted to use the code later for something different, I could vary them. <span class=\"translation\">所以我起初决定只把 `d_head` 和 `n_heads` 传给构造函数。在训练脚本中可以强制它们匹配 GPT-2 模型，但以后如果想把代码用于其他东西，也可以改变它们。</span>\n\nThen I remembered that although the dimensionality of the embedding spaces for the query and the key vectors have to match (because otherwise you can't multiply them to work out attention scores with Ω\\=QKT), the value vector's dimensionality can in theory be different. So I decided to break `d_head` into two separate `d_qk` and `d_v` parameters. <span class=\"translation\">然后我想起，query 和 key 向量的嵌入空间维度必须相同（否则无法用 Ω=QKᵀ 相乘计算注意力分数），但 value 向量的维度理论上可以不同。因此我决定把 `d_head` 拆成两个参数：`d_qk` 和 `d_v`。</span>\n\nThe result was this: <span class=\"translation\">结果如下：</span>\n\n`class MultiHeadAttention(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.n_heads = n_heads         self.d_qk = d_qk         self.d_v = d_v          self.W_q = nnx.Linear(d_emb, self.d_qk * n_heads, use_bias=qkv_bias, rngs=rngs)         self.W_k = nnx.Linear(d_emb, self.d_qk * n_heads, use_bias=qkv_bias, rngs=rngs)         self.W_v = nnx.Linear(d_emb, self.d_v * n_heads, use_bias=qkv_bias, rngs=rngs)          self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=False, rngs=rngs)      def __call__(self, xs):         batch_size, len_sequence, d_emb = xs.shape          # For each of the below:         # * The initial linear layer projects them to         #   (batch_size, len_sequence, d_X * n_heads)         #   where X is qk or v as appropriate.         # * The reshape makes them (batch_size, len_sequence, n_heads, d_X)         # * The transpose makes them (batch_size, n_heads, len_sequence, d_X)         Q = jnp.transpose(             self.W_q(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_qk)             ),             (0, 2, 1, 3)         )         K = jnp.transpose(             self.W_k(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_qk)             ),             (0, 2, 1, 3)         )         V = jnp.transpose(             self.W_v(xs).reshape(                 (batch_size, len_sequence, self.n_heads, self.d_v)             ),             (0, 2, 1, 3)         )          # Q and K are (batch_size, n_heads, len_sequence, d_qk) per above         # We need to convert K to (batch_size, n_heads, d_qk, len_sequence)         # and then we get omega (batch_size, n_heads, len_sequence, len_sequence)         omega = Q @ jnp.transpose(K, axes=(0, 1, 3, 2))          omega /= jnp.sqrt(self.d_qk)          causal_mask = jnp.ones_like(omega, dtype=bool)         # tril treats all but the last two axes as batches so we're OK here.         causal_mask = jnp.tril(causal_mask)          causal_omega = jnp.where(causal_mask, omega, -jnp.inf)          # last axis is still OK.         attention_weights = jax.nn.softmax(causal_omega, axis=-1)          # attention_weights is (batch_size, n_heads, len_sequence, len_sequence)         # V is (batch_size, n_heads, len_sequence, d_v)         # So this will come out as (batch_size, n_heads, len_sequence, d_v)         weighted = attention_weights @ V          # Transpose to (batch_size, len_sequence, n_heads, d_v),         # then reshape to (batch_size, len_sequence, n_heads * d_v)         striped_output = jnp.transpose(             weighted,             (0, 2, 1, 3)         ).reshape(             batch_size, len_sequence, self.n_heads * self.d_v         )          # Final linear layer to combine         return self.output_projection(striped_output)`\n\nUnusually for a case where I went off the reservation like this, the whole thing with the embedding space dimensionality didn't cause any problems at all! But there was one small bug in this code, which I didn't discover until later -- we'll come to it by the end of the post. <span class=\"translation\">不同寻常的是，在这次偏离原路线的情况下，嵌入空间维度完全没有造成问题！不过代码中有一个小 bug，我到后来才发现——文章结尾会讲到。</span>\n\nAt this point, I did another of my short training runs, and: <span class=\"translation\">此时我又进行了一次短训练：</span>\n\n`2026-06-23 17:51:32.094308 Tokens seen: 92,209,152 2026-06-23 17:51:32.094311 Throughput: 85,682 tokens/second 2026-06-23 17:51:32.094321 Final train loss: 5.358 2026-06-23 17:51:32.094323 Done`\n\n...with a loss chart that looked like this: <span class=\"translation\">损失图如下：</span>\n\n![Next token prediction loss chart for one layer of MHA](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-loss-chart.png \"Next token prediction loss chart for one layer of MHA\")\n\nThe training loss at the 937th global step was 5.336, only a tiny bit better than the 5.354 with single-head attention. That was quite possibly within the noise. Even though (due to the `d_head * n_heads == d_emb` restriction I was enforcing in my training script) the `W_q`, `W_k`, and `W_v` arrays were the same size, I was creating that `output_projection`, which would consume randomness and make things vary. <span class=\"translation\">第 937 个全局步的训练损失是 5.336，只比单头注意力的 5.354 好一点点，很可能处于噪声范围内。虽然（由于训练脚本强制的 `d_head * n_heads == d_emb` 限制）`W_q`、`W_k` 和 `W_v` 数组大小相同，但我创建了 `output_projection`，它会消耗随机性，使结果发生变化。</span>\n\nIf I were doing a proper scientific experiment to see if a single layer of MHA beat a single layer of single-head attention, I think I would have run both for more steps to see if the difference became more pronounced later. <span class=\"translation\">如果要做严格的科学实验，判断单层 MHA 是否胜过单层单头注意力，我会让两者运行更多步，看看差距之后是否会变得明显。</span>\n\nBut for the purposes of this post, I decided to move on. My checklist now looked like this: <span class=\"translation\">不过为了这篇文章，我决定继续前进。现在的检查清单是：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. **✔ ️done** <span class=\"translation\">添加位置嵌入。**✔ ️已完成**</span>\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**部分完成——只有一层**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation <span class=\"translation\">层归一化。</span>\n-   Run multi-head attention **✔ ️done** <span class=\"translation\">运行多头注意力。**✔ ️已完成**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one <span class=\"translation\">再复制一份这个结果。</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化。</span>\n-   Run it through a simple neural network <span class=\"translation\">让它通过一个简单的神经网络。</span>\n-   Add the results of that back in. <span class=\"translation\">把结果加回去。</span>\n\nAdding that simple neural network -- the FFN -- seemed like a good next step. <span class=\"translation\">添加这个简单的神经网络——FFN——似乎是很好的下一步。</span>\n\n## Adding the FFN to the Transformers block <span class=\"translation\">向 Transformer 块中添加 FFN</span>\n\nThe [feed forward network](https://www.gilesthomas.com/2025/08/llm-from-scratch-17-the-feed-forward-network) is simple; you take the output of the MHA block, run it through a biased linear layer to expand it from `d_emb` to `4 * d_emb`, then run it through the GELU activation function, then shrink it back down to `d_emb` with another linear layer. I didn't really see any value in writing my own implementation of GELU, given that even in the book we were just given code for an approximation to type in. So, using [`jax.nn.gelu`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.gelu.html), I wrote this: <span class=\"translation\">[前馈网络](https://www.gilesthomas.com/2025/08/llm-from-scratch-17-the-feed-forward-network)很简单：取 MHA 块的输出，让它经过一个带偏置的线性层，把维度从 `d_emb` 扩大到 `4 * d_emb`；再通过 GELU 激活函数；最后用另一个线性层缩回 `d_emb`。考虑到即使在书中，我们也只是拿到一个近似实现的代码来输入，我不觉得自己实现 GELU 有什么价值。因此使用 [`jax.nn.gelu`](https://docs.jax.dev/en/latest/_autosummary/jax.nn.gelu.html)，我写成：</span>\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.attention = MultiHeadAttention(d_emb, n_heads, d_qk, d_v, qkv_bias, rngs)         self.ffn = nnx.Sequential(             nnx.Linear(                 in_features=d_emb,                 out_features=d_emb * 4,                 use_bias=True,                 rngs=rngs             ),             jax.nn.gelu,             nnx.Linear(                 in_features=d_emb * 4,                 out_features=d_emb,                 use_bias=True,                 rngs=rngs             ),         )      def __call__(self, xs):         shortcut = xs         att = self.attention(xs)         post_attention = shortcut + att         fed_forward = self.ffn(post_attention)         return fed_forward + post_attention`\n\nNote that I added in a shortcut connection around the FFN as well, so that it didn't overwrite what was there, but only \"added on its notes\". <span class=\"translation\">注意，我也在 FFN 周围加入了快捷连接，使它不会覆盖已有内容，而只会“添加自己的注释”。</span>\n\nI kicked that off, and it ran for ten minutes or so, but then OOMed: <span class=\"translation\">我启动了训练，运行了大约十分钟，然后 OOM：</span>\n\n`2026-06-23 18:20:01.376377 Saving checkpoint  51%|██████████████████████████████████████████████████████▊                                                    | 481/938 [10:20<11:47,  1.55s/it, loss=5.758, tps=76,185]W0623 18:20:12.602631 2860192 bfc_allocator.cc:514] Allocator (GPU_0_bfc) ran out of memory trying to allocate 2.93GiB (rounded to 3149744640)requested by op If the cause is memory fragmentation maybe the environment variable 'TF_GPU_ALLOCATOR=cuda_malloc_async' will improve the situation.`\n\nAdding `TF_GPU_ALLOCATOR=cuda_malloc_async` didn't help. I spent some time trying to dig into what might be causing it, but eventually noticed something interesting: in `nvtop`, the VRAM usage was consistently 75% throughout. <span class=\"translation\">设置 `TF_GPU_ALLOCATOR=cuda_malloc_async` 也没有帮助。我花了一些时间研究原因，但最终注意到一个有趣现象：在 `nvtop` 中，VRAM 使用率始终是 75%。</span>\n\nNow I knew that JAX pre-allocates 75% of VRAM when it starts up, but I'd been assuming that it would try to grab more if it needed it. It turned out I was wrong with that assumption -- it grabs 75%, but that's all you ever get! <span class=\"translation\">我知道 JAX 启动时会预分配 75% 的 VRAM，但一直以为需要时它会尝试获取更多。事实证明这个假设错了——它获取 75%，而你永远只能得到这些！</span>\n\nThe solution turned out to be the [`XLA_PYTHON_CLIENT_MEM_FRACTION`](https://docs.jax.dev/en/latest/gpu_memory_allocation.html) environment variable. If you set that to, say, `0.90`, then JAX will pre-allocate 90% of the VRAM, and you can use all of that. (You can also make it allocate as-needed with `XLA_PYTHON_CLIENT_PREALLOCATE=false`, and there are various other settings you can control with other environment variables on that linked page). <span class=\"translation\">最终的解决方案是 [`XLA_PYTHON_CLIENT_MEM_FRACTION`](https://docs.jax.dev/en/latest/gpu_memory_allocation.html) 环境变量。把它设为 `0.90`，JAX 就会预分配 90% 的 VRAM，你可以使用这部分全部空间。（也可以用 `XLA_PYTHON_CLIENT_PREALLOCATE=false` 让它按需分配；链接页面还介绍了其他环境变量设置。）</span>\n\nAnyway, setting it to `0.90` to grab 90% of VRAM worked, and I was able to get a successful run: <span class=\"translation\">无论如何，把它设为 `0.90`、获取 90% VRAM 后就能正常运行：</span>\n\n`2026-06-24 00:29:34.864880 Tokens seen: 92,209,152 2026-06-24 00:29:34.864882 Throughput: 77,596 tokens/second 2026-06-24 00:29:34.864900 Final train loss: 5.341 2026-06-24 00:29:34.864902 Done`\n\nThe loss chart was this: <span class=\"translation\">损失图如下：</span>\n\n![Next token prediction loss chart for one layer of MHA plus FFN](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-with-ffn-loss-chart.png \"Next token prediction loss chart for one layer of MHA plus FFN\")\n\n...and the training loss at global step 937 was 5.295, compared to the 5.336 from MHA alone. Another tiny improvement, another one that could have been in the noise. Again, if I were doing a proper experiment, I'd do a longer run, but for now, I decided to move on. <span class=\"translation\">……第 937 个全局步的训练损失为 5.295，相比单独 MHA 的 5.336 又有一点小改善。又一个微小提升，也可能只是噪声。同样，如果要做严格实验，我会运行更长时间，但现在决定继续前进。</span>\n\nThe checklist looked like this: <span class=\"translation\">检查清单如下：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. **✔ ️done** <span class=\"translation\">添加位置嵌入。**✔ ️已完成**</span>\n-   Run these embeddings through multiple successive Transformers blocks. **part-done -- one layer only** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**部分完成——只有一层**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation <span class=\"translation\">层归一化。</span>\n-   Run multi-head attention **✔ ️done** <span class=\"translation\">运行多头注意力。**✔ ️已完成**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one **✔ ️done** <span class=\"translation\">再复制一份这个结果。**✔ ️已完成**</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化。</span>\n-   Run it through a simple neural network **✔ ️done** <span class=\"translation\">让它通过一个简单的神经网络。**✔ ️已完成**</span>\n-   Add the results of that back in. **✔ ️done** <span class=\"translation\">把结果加回去。**✔ ️已完成**</span>\n\nNow, my gut instinct was that the layer normalisation inside the Transformers blocks was of most value as a way of stabilising training over deep networks. And with one layer, it didn't seem like the right time to add it. Instead, I decided to add on multiple layers. <span class=\"translation\">我的直觉是，Transformer 块内部的层归一化主要用于稳定深层网络的训练。只有一层时，似乎还不是加入它的正确时机。因此我决定增加层数。</span>\n\n## Multiple layers <span class=\"translation\">多层</span>\n\nFor GPT-2 small, you have 12 layers. That was already being passed in to my `GPTModel`'s `__init__` method as `n_layers`, so I just replaced this: <span class=\"translation\">GPT-2 small 有 12 层。这已经作为 `n_layers` 传给 `GPTModel` 的 `__init__` 方法，所以我只需把下面这段：</span>\n\n`self.transformers_layer = TransformersLayer(     d_emb, n_heads, d_qk, d_v, qkv_bias, rngs )`\n\n...with this: <span class=\"translation\">……替换成：</span>\n\n`self.transformers_layers = nnx.Sequential(     *(         TransformersLayer(             d_emb, n_heads, d_qk, d_v, qkv_bias, rngs         )         for _ in range(n_layers)     ) )`\n\n...and then just renamed it where it was called; this: <span class=\"translation\">然后在调用处重命名即可；这段：</span>\n\n`transformed = self.transformers_layer(input_embeddings)`\n\n...became this: <span class=\"translation\">……变成：</span>\n\n`transformed = self.transformers_layers(input_embeddings)`\n\nI kicked it off, and it completed! However, the loss chart was telling: <span class=\"translation\">我启动训练，而且它完成了！不过损失图很能说明问题：</span>\n\n![Next token prediction loss chart for multiple full Transformers layers, no LayerNorm](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/multiple-full-layers-no-pre-norm-loss-chart.png \"Next token prediction loss chart for multiple full Transformers layers, no LayerNorm\")\n\nOuch. Loss started dropping quite nicely, but then things got out of control and it settled down at a loss that was essentially that of a random model. At step 937, we were at 10.75, so just a hair less than the 10.82 that randomly guessing next tokens would give. <span class=\"translation\">哎。损失开始下降得相当不错，但随后失控，最终稳定在几乎等于随机模型的损失。第 937 步时是 10.75，只比随机猜测下一个词元会得到的 10.82 好一点点。</span>\n\nWell, LayerNorm is specifically meant to stabilise training, and the checklist looked like this: <span class=\"translation\">LayerNorm 本来就是用来稳定训练的，清单如下：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. **✔ ️done** <span class=\"translation\">添加位置嵌入。**✔ ️已完成**</span>\n-   Run these embeddings through multiple successive Transformers blocks. **✔ ️done** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**✔ ️已完成**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation <span class=\"translation\">层归一化。</span>\n-   Run multi-head attention **✔ ️done** <span class=\"translation\">运行多头注意力。**✔ ️已完成**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one **✔ ️done** <span class=\"translation\">再复制一份这个结果。**✔ ️已完成**</span>\n-   Layer normalisation again <span class=\"translation\">再次进行层归一化。</span>\n-   Run it through a simple neural network **✔ ️done** <span class=\"translation\">让它通过一个简单的神经网络。**✔ ️已完成**</span>\n-   Add the results of that back in. **✔ ️done** <span class=\"translation\">把结果加回去。**✔ ️已完成**</span>\n\n...and the only remaining step was that LayerNorm in the Transformers blocks, so it was time to add it in! <span class=\"translation\">……唯一剩下的步骤就是 Transformer 块中的 LayerNorm，因此是时候把它加入了！</span>\n\n## Adding LayerNorm <span class=\"translation\">添加 LayerNorm</span>\n\nAs per the checklist, we do the LayerNorm after we've taken our copy for the shortcut connection, just before MHA, and then likewise after the second shortcut copy, before the FFN. As I understand it, this was a GPT-2 innovation -- previously, people had done normalisation after those steps, but this pre-norm setup turned out to work better. <span class=\"translation\">按照清单，我们在为快捷连接复制输入之后、MHA 之前执行 LayerNorm；同样，在第二次快捷连接复制之后、FFN 之前再执行一次。据我理解，这是 GPT-2 的创新——此前人们在这些步骤之后做归一化，但这种 pre-norm 设置效果更好。</span>\n\nThe code changes were simple. I added two `LayerNorm` modules to the `TransformersLayer` class, and then called them in the appropriate places (taking the opportunity to tidy up the variable naming in the forward pass while I was there): <span class=\"translation\">代码改动很简单。我在 `TransformersLayer` 类中添加两个 `LayerNorm` 模块，并在合适的位置调用它们（同时顺便整理了前向传播中的变量命名）：</span>\n\n`class TransformersLayer(nnx.Module):      def __init__(self, d_emb, n_heads, d_qk, d_v, qkv_bias, rngs):         self.attention_norm = LayerNorm(d_emb)         self.attention = MultiHeadAttention(d_emb, n_heads, d_qk, d_v, qkv_bias, rngs)          self.ffn_norm = LayerNorm(d_emb)         self.ffn = nnx.Sequential(             ...         )      def __call__(self, xs):         shortcut = xs         xs = self.attention_norm(xs)         xs = self.attention(xs)         xs = xs + shortcut          shortcut = xs         xs = self.ffn_norm(xs)         xs = self.ffn(xs)         return xs + shortcut`\n\nI kicked it off and ran it, and got these results: <span class=\"translation\">我启动并运行它，得到这些结果：</span>\n\n`2026-06-24 03:07:51.128966 Tokens seen: 92,209,152 2026-06-24 03:07:51.128969 Throughput: 23,399 tokens/second 2026-06-24 03:07:51.128979 Final train loss: 5.359 2026-06-24 03:07:51.128981 Done`\n\n![Next token prediction loss chart for multiple full Transformers layers, with LayerNorm](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/multiple-full-layers-with-pre-norm-loss-chart.png \"Next token prediction loss chart for multiple full Transformers layers, with LayerNorm\")\n\nThat certainly looked much healthier! <span class=\"translation\">看起来健康多了！</span>\n\nHowever, when I looked at the loss at step 937, it was 5.311 -- a tiny bit higher than the single-layer MHA example, which got 5.295. <span class=\"translation\">不过查看第 937 步的损失时，结果是 5.311——比单层 MHA 示例的 5.295 略高。</span>\n\nI'd been willing to play a bit fast and loose with this loss number and allow myself to accept a win when the loss went down a tiny bit, even if it was such a small amount that it could have been within the noise. But *increasing* loss -- even if it could *also* be within the noise -- was a step too far. <span class=\"translation\">之前即使提升非常小、可能只是噪声，我也愿意对损失数字稍微放宽标准，接受它作为胜利。但损失上升——哪怕也可能在噪声范围内——就有些过分了。</span>\n\nI decided that in this specific case, I'd be strict and test the hypothesis that longer training runs would demonstrate an improvement between one single layer without pre-norm, and multiple layers with pre-norm. <span class=\"translation\">我决定在这个特定案例中严格一点，测试这样一个假设：更长的训练运行能证明“单层无 pre-norm”和“多层有 pre-norm”之间存在提升。</span>\n\nI had to remember that these training runs would not be comparable with the earlier ones. In the training script, I [had a learning rate schedule like this](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run#learning-rate-scheduling): <span class=\"translation\">我必须记住，这些训练运行不能和之前的运行直接比较。在训练脚本中，我[有这样的学习率调度](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run#learning-rate-scheduling)：</span>\n\n![The learning rate chart for the first scheduled learning rate run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run/learning-rate-chart.png \"The learning rate chart for the first scheduled learning rate run\")\n\nThat straight-line warmup period and the following cosine decay were 5% and 95% of the training run respectively, which meant that (for example) global step 937 of the short runs we had been doing would be at a completely different point in the schedule than the same step would in these longer runs. <span class=\"translation\">直线预热阶段和随后的余弦衰减分别占训练运行的 5% 和 95%，这意味着（例如）短运行中的全局步 937，与这些长运行中同一步数处于调度的完全不同位置。</span>\n\nHowever, they would be comparable to each other, and that was what mattered. <span class=\"translation\">不过它们彼此之间可以比较，而这才是关键。</span>\n\nAfter some humming and hawing, I decided that a full Chinchilla-optimal (for the full model) training run over 3,260,190,720 tokens, rounded up to fit into a round number of global steps, would be a nice experiment. I expected it to run comfortably overnight for the single-layer run, and take a bit less than two days for the multi-layer one. So I kicked off the first. <span class=\"translation\">经过一番犹豫，我决定进行一次完整的 Chinchilla 最优（针对完整模型）训练运行，使用 3,260,190,720 个词元，并向上取整以适应整数个全局步，这是个不错的实验。我预计单层版本可以舒适地跑一夜，多层版本耗时不到两天。因此我启动了第一个。</span>\n\nJust over 11 hours later: <span class=\"translation\">11 个多小时后：</span>\n\n`2026-06-24 16:39:52.178045 Tokens seen: 3,260,252,160 2026-06-24 16:39:52.178049 Throughput: 81,733 tokens/second 2026-06-24 16:39:52.178058 Final train loss: 4.324 2026-06-24 16:39:52.178061 Done`\n\nHere's the loss chart: <span class=\"translation\">这是损失图：</span>\n\n![Next token prediction loss chart for one layer of MHA plus FFN, extended run](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/mha-single-layer-with-ffn-extended-run-loss-chart.png \"Next token prediction loss chart for one layer of MHA plus FFN, extended run\")\n\nThe last checkpointing period in that run ended at global step 33,164, and the training loss then was 4.165 -- indeed, it had been at around 4.17 for quite some time, though the trend still seemed to be a tiny bit downward. <span class=\"translation\">这次运行最后一个检查点区间结束于全局步 33,164，当时训练损失是 4.165——确实已经在约 4.17 停留了相当一段时间，不过趋势似乎仍在微微下降。</span>\n\nSo then I kicked off a run of the full version -- multiple layers, with pre-norm in the Transformers blocks. Just over 37 hours later: <span class=\"translation\">于是我启动了完整版本的运行——多层，并在 Transformer 块中使用 pre-norm。37 个多小时后：</span>\n\n`2026-06-26 06:55:39.956609 Tokens seen: 3,260,252,160 2026-06-26 06:55:39.956614 Throughput: 24,151 tokens/second 2026-06-26 06:55:39.956625 Final train loss: 3.637 2026-06-26 06:55:39.956629 Done`\n\nThe \"Final train loss\" line at the end said it all, really! But here's the loss chart: <span class=\"translation\">最后的“Final train loss”一行已经说明了一切！不过还是来看看损失图：</span>\n\n![Next token prediction loss chart for the full LLM, no dropout](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-loss-chart.png \"Next token prediction loss chart for the full LLM, no dropout\")\n\n...and the loss at step 33,164 was 3.399. Definitely quite an improvement over the 4.165 that a single layer got. <span class=\"translation\">……第 33,164 步的损失是 3.399，确实比单层得到的 4.165 好很多。</span>\n\nAgain, at some point I might do the equivalent tests for the earlier results where improvements appear to be pretty much in the noise. It would be good to be sure that the changes really did have the impact I think they did. <span class=\"translation\">以后某个时候，我也许会为早期那些提升看起来完全处于噪声范围的结果做同样的测试。确认这些改动确实产生了我认为的影响，会很有帮助。</span>\n\nBut for now: our checklist was looking like this: <span class=\"translation\">不过现在，清单看起来是这样：</span>\n\n-   Convert token IDs into embeddings. **✔ ️done** <span class=\"translation\">把词元 ID 转换为嵌入。**✔ ️已完成**</span>\n-   Add on position embeddings. **✔ ️done** <span class=\"translation\">添加位置嵌入。**✔ ️已完成**</span>\n-   Run these embeddings through multiple successive Transformers blocks. **✔ ️done** <span class=\"translation\">让这些嵌入依次通过多个 Transformer 块。**✔ ️已完成**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Project them back from embedding space to vocab space. **✔ ️done** <span class=\"translation\">把它们从嵌入空间投影回词汇表空间。**✔ ️已完成**</span>\n\nInside the Transformers blocks, we: <span class=\"translation\">在 Transformer 块内部，我们：</span>\n\n-   Take a copy of the input sequence of embeddings **✔ ️done** <span class=\"translation\">复制一份输入嵌入序列。**✔ ️已完成**</span>\n-   Layer normalisation **✔ ️done** <span class=\"translation\">层归一化。**✔ ️已完成**</span>\n-   Run multi-head attention **✔ ️done** <span class=\"translation\">运行多头注意力。**✔ ️已完成**</span>\n-   Add the copy back in so that the version that came out of MHA is something more like an \"annotation\" of the original **✔ ️done** <span class=\"translation\">把副本加回来，使 MHA 输出的版本更像是对原始信息的“注释”。**✔ ️已完成**</span>\n-   Take a second copy of that one **✔ ️done** <span class=\"translation\">再复制一份这个结果。**✔ ️已完成**</span>\n-   Layer normalisation again **✔ ️done** <span class=\"translation\">再次进行层归一化。**✔ ️已完成**</span>\n-   Run it through a simple neural network **✔ ️done** <span class=\"translation\">让它通过一个简单的神经网络。**✔ ️已完成**</span>\n-   Add the results of that back in. **✔ ️done** <span class=\"translation\">把结果加回去。**✔ ️已完成**</span>\n\nEverything was checked off. So was this journey over? <span class=\"translation\">所有项目都打勾了。这段旅程结束了吗？</span>\n\nWell, there was one thing that the original PyTorch code had that my new code didn't: dropout. <span class=\"translation\">不过，原始 PyTorch 代码中有一样东西，我的新代码还没有：dropout。</span>\n\n## Dropout <span class=\"translation\">Dropout</span>\n\nI'd found in my lengthy [interventions experiments](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model) that dropout seemed to make models worse. It was, I felt, a smart idea back in the days when people had little data and did multiple epochs, each sweeping over everything, but it made less sense nowadays with single-epoch training runs over very large datasets. (Though I do have [some intuitive ideas about why it could still help](https://www.gilesthomas.com/2025/03/dropout-and-mandatory-vacation).) <span class=\"translation\">在漫长的[干预实验](https://www.gilesthomas.com/2026/02/llm-from-scratch-32a-interventions-baseline-model)中，我发现 dropout 似乎会让模型变差。我觉得，在数据很少、要进行多个 epoch、每个 epoch 都遍历全部数据的年代，这曾是个聪明想法；但如今在超大数据集上进行单 epoch 训练，它就没那么合理了。（不过我确实有[一些直觉上的想法，认为它仍可能有帮助](https://www.gilesthomas.com/2025/03/dropout-and-mandatory-vacation)。）</span>\n\nStill, it would be good to show that it harmed loss for this model as well. <span class=\"translation\">不过，证明它对这个模型也会损害损失，仍然很有意义。</span>\n\nChecking my notes, I found that there were four places where dropout was applied: <span class=\"translation\">查看笔记后，我发现 dropout 应用在四个地方：</span>\n\n-   Once in the main body, just after we've worked out the embeddings. <span class=\"translation\">主体部分一次，就在计算完嵌入之后。</span>\n-   Twice in the transformers block: once after attention (but before the shortcut is mixed back in), and once after the FFN (ditto) <span class=\"translation\">Transformer 块中两次：一次在注意力之后（但在加回快捷连接之前），一次在 FFN 之后（同样如此）。</span>\n-   Inside multi-head attention, on the attention weights ([which surprised me](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)). <span class=\"translation\">多头注意力内部，在注意力权重上（[这点让我很惊讶](https://www.gilesthomas.com/2025/03/llm-from-scratch-10-dropout)）。</span>\n\nThe changes are tiny and rather dotted around the code, so rather than showing you isolated bits of code, if you'd like to see it you can take a look at [the code at this point](https://github.com/gpjt/jax-gpt2-from-scratch/blob/5fe6e745b55a33a3b55d3d7ce39f480e29ed0d77/gpt.py) and search for \"dropout\". <span class=\"translation\">改动很小，而且分散在代码各处，因此我不逐段展示；如果想看，可以查看[这一阶段的代码](https://github.com/gpjt/jax-gpt2-from-scratch/blob/5fe6e745b55a33a3b55d3d7ce39f480e29ed0d77/gpt.py)，搜索“dropout”。</span>\n\nWhen I started running that, I got an error when saving the first checkpoint: <span class=\"translation\">启动后，我在保存第一个检查点时遇到了错误：</span>\n\n`TypeError: JAX array with PRNGKey dtype cannot be converted to a NumPy array. Use jax.random.key_data(arr) if you wish to extract the underlying integer array.`\n\nThis was happening deep inside the bowels of Safetensors, but it made a lot of sense. The `nnx.Dropout` object needs to keep track of the state of the random number generator, and that meant that the `to_flat_state` function that [I was using](https://www.gilesthomas.com/2026/06/flax-and-safetensors) might return a structure that had something that contained that state, and was not compatible with Safetensors. <span class=\"translation\">错误发生在 Safetensors 的深层内部，但很合理。`nnx.Dropout` 对象需要跟踪随机数生成器的状态，这意味着我正在使用的 `to_flat_state` 函数[代码在这里](https://www.gilesthomas.com/2026/06/flax-and-safetensors)可能返回包含该状态的结构，而这种结构与 Safetensors 不兼容。</span>\n\nI decided that I'd cheat a little bit here. If I skipped the dropout layers when I saved my checkpoints, like this: <span class=\"translation\">我决定在这里稍微作弊。如果保存检查点时跳过 dropout 层，像这样：</span>\n\n    `for tuple_key, array in flat_state:         key = \".\".join(str(key) for key in tuple_key)         if \"dropout\" not in key:             simple_dict[key] = array`\n\n...then I'd be able to save them. This would have a problem -- if I restarted from a checkpoint, the dropout pattern after the restart would mirror the dropout pattern from the start of the training run, because the random seed it started with would not have come from the checkpoint, but just the initialisation code. <span class=\"translation\">……这样就能保存了。但这会带来一个问题——如果从检查点重启，重启后的 dropout 模式会和训练开始时的模式相同，因为它使用的随机种子不是从检查点恢复，而是来自初始化代码。</span>\n\nI felt that this would not have a serious impact, though, and given that I'd not had to restart from checkpoints so far, I (wrongly, as it turned out) decided it wouldn't matter. <span class=\"translation\">我觉得这不会产生严重影响，而且此前从未需要从检查点重启，所以（事实证明是错误地）认为这没关系。</span>\n\nI kicked off the run, and... after four hours, it OOMed. I cursed, decided that I'd nurse this run through anyway (despite my dropout checkpointing concerns), and kicked it off again. <span class=\"translation\">我启动运行后……四小时后 OOM 了。我骂了一句，决定无论如何照顾着把它跑完（尽管仍担心 dropout 检查点问题），然后再次启动。</span>\n\nThree hours later, it OOMed again. I happened to be away from home at the time, logging in to my machine remotely (thanks, [Tailscale](https://tailscale.com/)!), and on looking at `nvtop`, I realised that the X window system on my machine was using a gig or so of VRAM. <span class=\"translation\">三小时后又 OOM 了。当时我恰好不在家，通过远程登录机器（感谢 [Tailscale](https://tailscale.com/)！）；查看 `nvtop` 时，我发现机器上的 X 窗口系统占用了大约一 GB 的 VRAM。</span>\n\nI was running the training run in a `tmux` session, which meant that I could kill X and not lose state, so I did that, and adjusted the `XLA_PYTHON_CLIENT_MEM_FRACTION` environment variable I was using -- it had been 0.90, so I bumped it up to 0.95. I kicked it off again, and... <span class=\"translation\">训练在 `tmux` 会话中运行，所以可以杀掉 X 而不丢失状态。我这样做了，并调整了使用的 `XLA_PYTHON_CLIENT_MEM_FRACTION` 环境变量——原来是 0.90，我把它调到 0.95。然后再次启动……</span>\n\n`2026-06-28 22:06:47.669676 Tokens seen: 2,640,052,224 2026-06-28 22:06:47.669683 Throughput: 23,019 tokens/second 2026-06-28 22:06:47.669691 Final train loss: 3.776 2026-06-28 22:06:47.669694 Done`\n\nNote that the tokens seen only relates to the period since the restart, which is why it was lower. <span class=\"translation\">注意，tokens seen 只涉及重启之后的这段时间，所以数值更低。</span>\n\nOne more loss chart: <span class=\"translation\">再看一张损失图：</span>\n\n![Next token prediction loss chart for the full LLM, with dropout](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-with-dropout-loss-chart.png \"Next token prediction loss chart for the full LLM, with dropout\")\n\n...and the training loss at step 33,164 was 3.524, higher enough than the 3.399 I got without dropout that I was comfortable that it wasn't in the noise. That was very reassuring. <span class=\"translation\">……第 33,164 步的训练损失是 3.524，比不使用 dropout 时的 3.399 高出足够多，我可以确定这不是噪声。这让人非常放心。</span>\n\nOnce again, if this was a proper scientific experiment I'd fix the issue with saving dropout, and run it completely from scratch -- or, at least, run it all the way through from scratch without restarts, even if I had to try several times to get it done. <span class=\"translation\">再次强调，如果这是严谨的科学实验，我会修复保存 dropout 的问题，从头完整运行——或者至少不重启、从头跑到底，即使需要多次尝试才能成功。</span>\n\nBut I don't think that \"replaying\" dropout would make the loss any worse. And for this experiment, I felt this was enough. <span class=\"translation\">不过我不认为“重放” dropout 会让损失变差。对于这次实验，我觉得这样已经足够。</span>\n\nSo: checklist complete. GPT-2 model coded up. It was time for some evals! <span class=\"translation\">所以：检查清单完成。GPT-2 模型写好了。是时候做一些评估了！</span>\n\n## Evals, first try -- and fixing an MHA bug <span class=\"translation\">第一次评估——以及修复 MHA bug</span>\n\nI wanted to evaluate these models against the ones I got using the old PyTorch code: specifically, the [last local training run](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation) that used exactly the same training hyperparameters, and only differed in that it was trained using AMP -- 32-bit floats in general, but using 16-bit where the framework thought it would not be harmful. <span class=\"translation\">我想把这些模型和旧 PyTorch 代码得到的模型进行比较：具体来说，是和[上一次本地训练运行](https://www.gilesthomas.com/2026/04/llm-from-scratch-32k-interventions-training-our-best-model-locally-gradient-accumulation)比较，它使用完全相同的训练超参数，唯一差异是使用 AMP 训练——通常为 32 位浮点，但在框架认为不会有害的地方使用 16 位。</span>\n\nIn order to do *exactly* the same evals, I decided it would be easiest to write a conversion script to take the Safetensors files written to my JAX checkpoints, and write out new files that were compatible with the PyTorch model code -- then I'd be able to use the original PyTorch eval code. I [put something together](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fd170272e54844229642619b9c69993f0f18778f/convert_model_to_pytorch.py), converted my last two models -- the full runs with and without dropout -- and tried to load them up. <span class=\"translation\">为了进行*完全相同*的评估，我决定最好写一个转换脚本，把写入 JAX 检查点的 Safetensors 文件转换成与 PyTorch 模型代码兼容的新文件，这样就能使用原来的 PyTorch 评估代码。我[写了一个脚本](https://github.com/gpjt/jax-gpt2-from-scratch/blob/fd170272e54844229642619b9c69993f0f18778f/convert_model_to_pytorch.py)，转换了最近的两个模型——有 dropout 和无 dropout 的完整运行——然后尝试加载它们。</span>\n\nUnfortunately there was an error: <span class=\"translation\">不幸的是，出现了错误：</span>\n\n`RuntimeError: Error(s) in loading state_dict for GPTModel:     Missing key(s) in state_dict: \"trf_blocks.0.att.out_proj.bias\", \"trf_blocks.1.att.out_proj.bias\", \"trf_blocks.10.att.out_proj.bias\", \"trf_blocks.11.att.out_proj.bias\", \"trf_blocks.2.att.out_proj.bias\", \"trf_blocks.3.att.out_proj.bias\", \"trf_blocks.4.att.out_proj.bias\", \"trf_blocks.5.att.out_proj.bias\", \"trf_blocks.6.att.out_proj.bias\", \"trf_blocks.7.att.out_proj.bias\", \"trf_blocks.8.att.out_proj.bias\", \"trf_blocks.9.att.out_proj.bias\"`\n\nYou might remember that back when I went through multi-head attention, I mentioned that I'd made a mistake. Somehow, I'd misremembered, and thought that the output projection -- the one that mixes together all of the different heads' outputs -- was a linear layer without bias, despite [my original notes](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention) being perfectly clear that it *did* have bias. <span class=\"translation\">你可能记得，之前讲多头注意力时我提到过自己犯了一个错误。不知怎么的，我记错了，以为输出投影——把不同注意力头的输出混合起来的那一层——是没有偏置的线性层，尽管[我最初的笔记](https://www.gilesthomas.com/2025/04/llm-from-scratch-12-multi-head-attention)写得非常清楚，它*有*偏置。</span>\n\nThe good news was that if I disabled bias in the PyTorch code, I could load the safetensors files that I had. So the two models I'd trained so far were not useless, and could actually work as a kind of natural experiment into the benefits of having that bias there. <span class=\"translation\">好消息是，如果在 PyTorch 代码中禁用偏置，就可以加载已有的 Safetensors 文件。因此，已经训练的两个模型并非无用，反而可以作为一种自然实验，用来研究加入偏置的好处。</span>\n\nBut anyway, in order to do things properly, I was going to need to fix the bug and train yet another model. <span class=\"translation\">不过，为了正确完成工作，我需要修复 bug，再训练一个模型。</span>\n\n## Adding bias to the MHA output projections <span class=\"translation\">给 MHA 输出投影添加偏置</span>\n\nThe fix was simple, I just replaced this (in `MultiHeadAttention`): <span class=\"translation\">修复很简单，只需把 `MultiHeadAttention` 中的这段：</span>\n\n`self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=False, rngs=rngs)`\n\n...with this: <span class=\"translation\">……替换成：</span>\n\n`self.output_projection = nnx.Linear(self.d_v * n_heads, d_emb, use_bias=True, rngs=rngs)`\n\nThen it was time to kick off yet another training run. After another 37 hours: <span class=\"translation\">然后再次启动训练。又过了 37 小时：</span>\n\n`2026-07-05 10:09:22.147819 Tokens seen: 3,260,252,160 2026-07-05 10:09:22.147823 Throughput: 24,072 tokens/second 2026-07-05 10:09:22.147832 Final train loss: 3.650 2026-07-05 10:09:22.147834 Done`\n\n...with this loss chart: <span class=\"translation\">……损失图如下：</span>\n\n![Next token prediction loss chart for the full LLM, no dropout, with MHA output bias](https://www.gilesthomas.com/post-assets/llm-from-scratch-34b-building-and-training-gpt-2-small-in-jax/full-llm-full-train-with-mha-output-bias-loss-chart.png \"Next token prediction loss chart for the full LLM, no dropout, with MHA output bias\")\n\n...and the training loss at step 33,164 was 3.398 -- almost exactly the same as the 3.399 that I got in the no-dropout training run without MHA bias above! <span class=\"translation\">……第 33,164 步的训练损失是 3.398，几乎和上面无 dropout、无 MHA 偏置的训练运行得到的 3.399 完全相同！</span>\n\nWell, now it really was time for the evals. <span class=\"translation\">好吧，现在真的该评估了。</span>\n\n## Evals, take two <span class=\"translation\">第二次评估</span>\n\nI updated [my conversion script](https://github.com/gpjt/jax-gpt2-from-scratch/blob/9ab47bc589973b3d8d18a5248b405ef3f17f09fe/convert_model_to_pytorch.py) to handle the bias on the MHA output projections, and used it to convert the three models -- the un-biased ones, with and without dropout, and the biased one, without -- to the PyTorch format, then ran the loss test that I had been using to compare the old models on each. <span class=\"translation\">我更新了[转换脚本](https://github.com/gpjt/jax-gpt2-from-scratch/blob/9ab47bc589973b3d8d18a5248b405ef3f17f09fe/convert_model_to_pytorch.py)，让它处理 MHA 输出投影上的偏置；用它把三个模型转换成 PyTorch 格式——两个无偏置模型（有 dropout 和无 dropout），以及一个有偏置、无 dropout 的模型——然后对每个运行过去用来比较旧模型的损失测试。</span>\n\nHere are the results, compared to the previous models, and OpenAI's: <span class=\"translation\">结果如下，并与之前的模型和 OpenAI 模型进行比较：</span>\n\n|  | Test loss | <span class=\"translation\">| 测试损失 |</span>\n| --- | --- |\n| OpenAI weights: medium | 3.231442 | <span class=\"translation\">| OpenAI 权重：中等 | 3.231442 |</span>\n| **JAX, with MHA bias, no dropout** | 3.418784 | <span class=\"translation\">| **JAX，有 MHA 偏置，无 dropout** | 3.418784 |</span>\n| **JAX, no MHA bias, no dropout** | 3.420089 | <span class=\"translation\">| **JAX，无 MHA 偏置，无 dropout** | 3.420089 |</span>\n| **JAX, no MHA bias, with dropout** | 3.476802 | <span class=\"translation\">| **JAX，无 MHA 偏置，有 dropout** | 3.476802 |</span>\n| OpenAI weights: small | 3.499677 | <span class=\"translation\">| OpenAI 权重：小 | 3.499677 |</span>\n| `1xrtx3090-stacked-interventions` | 3.538161 | <span class=\"translation\">| `1xrtx3090-stacked-interventions` | 3.538161 |</span>\n| `8xa100m40-stacked-interventions-1` | 3.577761 | <span class=\"translation\">| `8xa100m40-stacked-interventions-1` | 3.577761 |</span>\n| Cloud FineWeb, 8x A100 40 GiB | 3.673623 | <span class=\"translation\">| 云端 FineWeb，8x A100 40 GiB | 3.673623 |</span>\n| `1xrtx3090-baseline` | 3.683835 | <span class=\"translation\">| `1xrtx3090-baseline` | 3.683835 |</span>\n| `8xa100m40-baseline` | 3.691526 | <span class=\"translation\">| `8xa100m40-baseline` | 3.691526 |</span>\n| Cloud FineWeb, 8x H100 80 GiB | 3.724507 | <span class=\"translation\">| 云端 FineWeb，8x H100 80 GiB | 3.724507 |</span>\n| Cloud FineWeb, 8x A100 80 GiB | 3.729900 | <span class=\"translation\">| 云端 FineWeb，8x A100 80 GiB | 3.729900 |</span>\n| Cloud FineWeb, 8x B200 160 GiB | 3.771478 | <span class=\"translation\">| 云端 FineWeb，8x B200 160 GiB | 3.771478 |</span>\n| Local FineWeb train | 3.943522 | <span class=\"translation\">| 本地 FineWeb 训练 | 3.943522 |</span>\n| Local FineWeb-Edu extended train | 4.134991 | <span class=\"translation\">| 本地 FineWeb-Edu 扩展训练 | 4.134991 |</span>\n| Local FineWeb-Edu train | 4.166892 | <span class=\"translation\">| 本地 FineWeb-Edu 训练 | 4.166892 |</span>\n\nThat was a pretty amazing result -- I'd clearly proven that JAX trains much better models than PyTorch! 3.5% better in the best case. <span class=\"translation\">这个结果相当惊人——我显然证明了 JAX 训练出的模型比 PyTorch 好得多！最佳情况下好 3.5%。</span>\n\nWell, OK, no. <span class=\"translation\">好吧，不是。</span>\n\nMy guess is that the difference was probably something like better luck with the initial weights on the JAX side, plus [the improvement from not using AMP](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32). Anyway, the important thing was that the JAX models were in the same kind of loss range as the PyTorch ones -- and while a 3.5% improvement in loss was more variation than I'd been expecting, it was definitely the right ballpark. <span class=\"translation\">我猜差异可能来自 JAX 侧初始权重运气更好，再加上[不使用 AMP 带来的提升](https://www.gilesthomas.com/2026/04/llm-from-scratch-32h-interventions-full-fat-float32)。无论如何，重要的是 JAX 模型和 PyTorch 模型处于同一损失范围；虽然 3.5% 的改善比我预期的变化更大，但确实在合理区间内。</span>\n\nNow, one thing I had found in the past was that the OpenAI weights -- and some of my own models, like the Fineweb-Edu ones -- were [consistently better](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests) at an instruction fine-tuning test than their test loss scores would indicate. Would that hold here? <span class=\"translation\">我以前发现，OpenAI 权重以及我的一些模型（例如 FineWeb-Edu 模型）在指令微调测试中的表现[始终好于](https://www.gilesthomas.com/2026/04/llm-from-scratch-32l-interventions-instruction-fine-tuning-tests)其测试损失所暗示的水平。这里也会如此吗？</span>\n\nThe IFT eval code fine-tuned each model on the [Alpaca](https://crfm.stanford.edu/2023/03/13/alpaca.html) dataset until validation loss started rising, then used the model prior to the start of the rise to generate responses for a test set. These were saved, and then run past an OpenAI model so that they could be compared with each other: <span class=\"translation\">IFT 评估代码在 Alpaca 数据集上微调每个模型，直到验证损失开始上升；然后使用上升开始前的模型，为测试集生成响应。这些响应会保存下来，再交给一个 OpenAI 模型，使它们能够相互比较：</span>\n\n```\nYou are judging the comparative capabilities of a number of different LLM\nmodels.  They have been trained to follow instructions.\n\nThe input was this:\n\n`\n{input}\n`\n\nAn example correct output is this:\n\n`\n{correct_output}\n`\n\nPlease produce a score of between 0 and 100 for each model, and respond\nwith a JSON structure like this (note that the number of models may differ\nfrom this example):\n\n`\n{\n    \"Model 1\": {\"score\": XXX, \"comments\": \"optional comments\"},\n    \"Model 2\": {\"score\": YYY, \"comments\": \"optional comments\"},\n    \"Model 3\": {\"score\": ZZZ, \"comments\": \"optional comments\"}\n}\n`\n\n...where the XXX, YYY and ZZZ are the scores for the respective models.\nYou can optionally add the \"comments\" field if you want to explain your\nreasoning.\n\nHere are the models' responses:\n\n# Model 1\n\n{model 1 response}\n\n\n# Model 2\n\n{model 2 response}\n\n\n# Model 3\n\n{model 3 response}\n```\n\n...with the model order randomly changed for each query to avoid any position bias. <span class=\"translation\">……并且每次查询都会随机改变模型顺序，以避免位置偏差。</span>\n\nThe methodology seemed solid, but I was uncertain about the \"train until loss starts rising\", as it meant that different models had wildly different amounts of fine-tuning -- between two and seven epochs. <span class=\"translation\">这个方法看起来很扎实，但我对“训练到损失开始上升”感到不确定，因为这意味着不同模型的微调次数差异很大——从两个 epoch 到七个 epoch。</span>\n\nOn the one hand it felt \"unfair\" to certain models that they'd get less training than others. On the other hand, if the less-trained models had been trained past the point where their validation loss started rising, then assuming that loss would continue to rise, further training would actually be a disadvantage rather than an advantage. <span class=\"translation\">一方面，这对得到较少训练的模型来说感觉“不公平”。另一方面，如果让训练较少的模型继续训练到验证损失开始上升之后，并假设损失会继续上升，那么继续训练反而会成为劣势，而不是优势。</span>\n\nI decided to stick with the original plan, and train until validation loss started rising. I did, however, switch the judge model from the GPT 5.4 that I used in my last IFT test to GPT 5.5. Here are the results: <span class=\"translation\">我决定坚持原计划，训练到验证损失开始上升。不过，我把评判模型从上次 IFT 测试使用的 GPT 5.4 换成了 GPT 5.5。结果如下：</span>\n\n|  | Test loss | IFT epochs | IFT score | IFT rank | <span class=\"translation\">| 测试损失 | IFT 轮次 | IFT 分数 | IFT 排名 |</span>\n| --- | --- | --- | --- | --- |\n| OpenAI weights: medium | 3.231442 | 2 | 41.62 | 1 | <span class=\"translation\">| OpenAI 权重：中等 | 3.231442 | 2 | 41.62 | 1 |</span>\n| **JAX, with MHA bias, no dropout** | 3.418784 | 4 | 19.25 | 4 | <span class=\"translation\">| **JAX，有 MHA 偏置，无 dropout** | 3.418784 | 4 | 19.25 | 4 |</span>\n| **JAX, no MHA bias, no dropout** | 3.420089 | 3 | 14.66 | 11 | <span class=\"translation\">| **JAX，无 MHA 偏置，无 dropout** | 3.420089 | 3 | 14.66 | 11 |</span>\n| **JAX, no MHA bias, with dropout** | 3.476802 | 4 | 12.94 | 15 | <span class=\"translation\">| **JAX，无 MHA 偏置，有 dropout** | 3.476802 | 4 | 12.94 | 15 |</span>\n| OpenAI weights: small | 3.499677 | 2 | 26.73 | 2 | <span class=\"translation\">| OpenAI 权重：小 | 3.499677 | 2 | 26.73 | 2 |</span>\n| `1xrtx3090-stacked-interventions` | 3.538161 | 4 | 17.79 | 6 | <span class=\"translation\">| `1xrtx3090-stacked-interventions` | 3.538161 | 4 | 17.79 | 6 |</span>\n| `8xa100m40-stacked-interventions-1` | 3.577761 | 4 | 10.29 | 16 | <span class=\"translation\">| `8xa100m40-stacked-interventions-1` | 3.577761 | 4 | 10.29 | 16 |</span>\n| Cloud FineWeb, 8x A100 40 GiB | 3.673623 | 7 | 20.71 | 3 | <span class=\"translation\">| 云端 FineWeb，8x A100 40 GiB | 3.673623 | 7 | 20.71 | 3 |</span>\n| `1xrtx3090-baseline` | 3.683835 | 6 | 15.11 | 9 | <span class=\"translation\">| `1xrtx3090-baseline` | 3.683835 | 6 | 15.11 | 9 |</span>\n| `8xa100m40-baseline` | 3.691526 | 4 | 14.74 | 10 | <span class=\"translation\">| `8xa100m40-baseline` | 3.691526 | 4 | 14.74 | 10 |</span>\n| Cloud FineWeb, 8x H100 80 GiB | 3.724507 | 4 | 13.25 | 14 | <span class=\"translation\">| 云端 FineWeb，8x H100 80 GiB | 3.724507 | 4 | 13.25 | 14 |</span>\n| Cloud FineWeb, 8x A100 80 GiB | 3.729900 | 4 | 14.50 | 12 | <span class=\"translation\">| 云端 FineWeb，8x A100 80 GiB | 3.729900 | 4 | 14.50 | 12 |</span>\n| Cloud FineWeb, 8x B200 160 GiB | 3.771478 | 4 | 16.03 | 8 | <span class=\"translation\">| 云端 FineWeb，8x B200 160 GiB | 3.771478 | 4 | 16.03 | 8 |</span>\n| Local FineWeb train | 3.943522 | 7 | 13.73 | 13 | <span class=\"translation\">| 本地 FineWeb 训练 | 3.943522 | 7 | 13.73 | 13 |</span>\n| Local FineWeb-Edu extended train | 4.134991 | 7 | 16.70 | 7 | <span class=\"translation\">| 本地 FineWeb-Edu 扩展训练 | 4.134991 | 7 | 16.70 | 7 |</span>\n| Local FineWeb-Edu train | 4.166892 | 7 | 18.68 | 5 | <span class=\"translation\">| 本地 FineWeb-Edu 训练 | 4.166892 | 7 | 18.68 | 5 |</span>\n\nMore interesting datapoints! As before, you can see that low loss is not particularly well-correlated with a high score on this instruction fine-tuning test. The OpenAI weights continue to lead the pack, and while one of our new JAX models did quite well, it's still beaten by the Cloud FineWeb, 8x A100 40 GiB model. <span class=\"translation\">更有意思的数据点！和之前一样，低损失与这项指令微调测试的高分并没有特别强的相关性。OpenAI 权重继续领先；虽然我们的一个新 JAX 模型表现不错，但仍然输给了 Cloud FineWeb、8x A100 40 GiB 模型。</span>\n\nBut what was important here, just as with the loss, was that the new JAX models landed in the same ballpark as the PyTorch ones. They did, and so I could be confident that they were doing essentially the same thing. <span class=\"translation\">这里重要的地方和损失一样：新的 JAX 模型落在与 PyTorch 模型相同的区间。确实如此，因此我可以确信它们本质上做的是同一件事。</span>\n\nAnd that meant that, after 18 months, I had reached the end of my LLM from scratch journey. <span class=\"translation\">这意味着，经过 18 个月，我终于走完了从零开始构建 LLM 的旅程。</span>\n\n## Conclusion <span class=\"translation\">结语</span>\n\nIt's been [a long trek](https://www.gilesthomas.com/llm-from-scratch). <span class=\"translation\">这是一段[漫长的跋涉](https://www.gilesthomas.com/llm-from-scratch)。</span>\n\nI [started reading](https://www.gilesthomas.com/2024/12/llm-from-scratch-1) \"Build a Large Language Model (from Scratch)\" on 22 December 2024. I was planning to breeze through over the Christmas break, but somehow it morphed into being a curriculum onto which I could hang projects to learn the fundamentals of LLMs, beyond what was in the book. <span class=\"translation\">2024 年 12 月 22 日，我[开始阅读](https://www.gilesthomas.com/2024/12/llm-from-scratch-1)《从零开始构建大型语言模型》。原本计划在圣诞假期轻松读完，但不知怎么它变成了一套课程，我可以在其上挂载项目，学习书本之外的 LLM 基础知识。</span>\n\nIn May 2025, I had my first real conceptual breakthrough when I realised that [attention heads are (individually) dumb](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb), and as I continued, the second big one came later on in the same month, when the concept of embeddings as being [projections between vocab space and embedding space](https://www.gilesthomas.com/2025/05/llm-from-scratch-15-from-context-vectors-to-logits) (and the converse projection in the other direction that happens in the LLM's output head) became clear. <span class=\"translation\">2025 年 5 月，我有了第一次真正的概念突破：意识到[注意力头（各自）很笨](https://www.gilesthomas.com/2025/05/llm-from-scratch-13-taking-stock-part-1-attention-heads-are-dumb)。继续学习后，同月晚些时候又迎来了第二个重大突破：我理解了嵌入是[词汇表空间与嵌入空间之间的投影](https://www.gilesthomas.com/2025/05/llm-from-scratch-15-from-context-vectors-to-logits)，以及 LLM 输出头中发生的反向投影。</span>\n\nIn August I had the first moment where I felt that the standard teaching approach to LLMs might not be the full story; shortcut connections are normally explained as a way to fix vanishing gradients, while I felt that a better way to see them was a way to allow attention and the FFN to \"annotate\" the existing information, similarly to how Jewish scholars have [annotated the original text of the Talmud](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud). (The results in this post seem to point in that direction, given how even a single layer of attention was massively helped by adding them.) <span class=\"translation\">8 月，我第一次觉得 LLM 的标准教学方式可能不是全部真相；快捷连接通常被解释为修复梯度消失的方法，而我觉得更好的理解方式是：它让注意力和 FFN 能够“注释”已有信息，类似犹太学者[给《塔木德》原文添加注释](https://www.gilesthomas.com/2025/08/llm-from-scratch-18-residuals-shortcut-connections-and-the-talmud)。（本文结果似乎支持这个方向，因为即使只有一层注意力，加入快捷连接也带来了巨大帮助。）</span>\n\nBy early December, I had essentially finished the book, and felt I wanted to try to [train my first base model from scratch on my RTX 3090](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch). It worked, and wasn't far off the quality of the original GPT-2 small. I was really surprised that I could do that with consumer hardware, and became interested (perhaps obsessively so) with whether I could match OpenAI's weights. <span class=\"translation\">到 12 月初，我基本读完了这本书，想尝试[在 RTX 3090 上从零训练自己的第一个基础模型](https://www.gilesthomas.com/2025/12/llm-from-scratch-28-training-a-base-model-from-scratch)。它成功了，质量和原始 GPT-2 small 相差不远。我很惊讶消费级硬件也能做到这点，并开始（或许有些着迷）思考能否匹配 OpenAI 的权重。</span>\n\nIn January 2026, I [trained a model using DDP on Lambda Labs](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud), and then spent the following months training model after model, trying to work out which interventions -- learning rate scheduling, gradient clipping, etc -- would improve the loss. I wrapped that up in [late April](https://www.gilesthomas.com/2026/04/llm-from-scratch-32m-interventions-conclusion), with the interesting finding that although I'd been able to get the test loss pretty low, that didn't seem to map cleanly to performance in my instruction fine-tuning tests. In other words, Loss Number Goes Down is an interesting technical game to play, but doesn't cleanly map to real-world performance. <span class=\"translation\">2026 年 1 月，我[在 Lambda Labs 上用 DDP 训练了一个模型](https://www.gilesthomas.com/2026/01/llm-from-scratch-29-ddp-training-a-base-model-in-the-cloud)，之后几个月不断训练模型，试图找出哪些干预——学习率调度、梯度裁剪等——能改善损失。我在[4 月底](https://www.gilesthomas.com/2026/04/llm-from-scratch-32m-interventions-conclusion)收束了这部分，得到一个有趣发现：尽管我能把测试损失降得很低，但它似乎无法清晰映射到指令微调测试中的表现。换句话说，“损失数字下降”是一个有趣的技术游戏，但并不能直接对应现实表现。</span>\n\nThe final step was this post, and [the previous one](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run) -- could I, using my notes, implement GPT-2 completely from scratch in JAX without referencing the book? <span class=\"translation\">最后一步就是这篇文章和[上一篇](https://www.gilesthomas.com/2026/06/llm-from-scratch-34a-building-a-jax-training-loop-for-an-llm-training-run)：只用笔记、不参考书，我能否在 JAX 中完全从零实现 GPT-2？</span>\n\nAnd as you've read, the answer was a definite yes! <span class=\"translation\">正如你已经读到的，答案当然是肯定的！</span>\n\nOf course, as with any long-running project, there are some loose ends -- from this post alone, there's the interesting fact that JAX trained faster than PyTorch (perhaps `torch.compile` could close the gap?) and had a larger possible batch size for full-fat 32-bit. And the fact that fixing the multi-head attention bias bug didn't seem to help with the loss much was interesting too. <span class=\"translation\">当然，和任何长期项目一样，还有一些收尾事项。仅从这篇文章来看，就有两个有趣事实：JAX 比 PyTorch 训练更快（也许 `torch.compile` 能缩小差距？），而且完整 32 位训练可以使用更大的批大小。另一个有趣点是，修复多头注意力偏置 bug 似乎并没有显著改善损失。</span>\n\nBut those are really details, and there's so much beyond them to learn. Longer-context LLMs: position embedding improvements like RoPE, efficiency tricks like flash attention and attention variants like DSA. Mixture of experts models. How do optimisers really work? ([Do they work?](https://x.com/mgostIH/status/2024087619756318866)) And plenty more. <span class=\"translation\">不过那都是细节，除此之外还有太多东西值得学习。更长上下文的 LLM；RoPE 等位置嵌入改进；flash attention 等效率技巧；DSA 等注意力变体；混合专家模型；优化器究竟如何工作？（[它们真的有效吗？](https://x.com/mgostIH/status/2024087619756318866)）还有很多很多。</span>\n\nSo it's time to draw a line under this series, and start thinking about what comes next. It's been a blast; if you've been reading along, I hope it's been as useful (and fun) to read as it was to write. <span class=\"translation\">所以是时候给这个系列画上句号，开始思考接下来做什么了。这段经历非常精彩；如果你一直读到这里，希望阅读它和写作它一样有用、一样有趣。</span>\n\nAnd as always, comments, questions and corrections very welcome below. <span class=\"translation\">和往常一样，非常欢迎在下面留下评论、问题和更正。</span>\n\n* * *\n\n1.  On looking back at Raschka's code, after having worked through all of this, there's a slight difference. I do this: <span class=\"translation\">回头看完 Raschka 的代码并经历这一切后，我发现有一个小差异。我做的是：</span>\n    \n    `normalized = (xs - means) / (stds + 1e-5)`\n    \n    ...whereas he does this: <span class=\"translation\">而他做的是：</span>\n    \n    `norm_x = (x - mean) / torch.sqrt(var + self.eps)`\n    \n    Now, the standard deviation is the square root of the variance, so if you ignore the small numbers -- `1e-5` in my case, and `self.eps` in his -- the calculations are the same. But there is a difference once those are taken account of. I don't think it's large enough to have any serious effect in these runs, though. [↩](#fnref-1) <span class=\"translation\">标准差是方差的平方根，所以忽略小数值——我这里是 `1e-5`，他那里是 `self.eps`——两种计算相同。但把这些小数值考虑进去后就有差异了。不过我不认为差异大到会对这些运行产生严重影响。[↩](#fnref-1)</span>\n    \n2.  In PyTorch, linear layers are stored as the transpose of the matrix that would allow you to do that, so it would be: <span class=\"translation\">在 PyTorch 中，线性层保存的是矩阵的转置形式，因此应该是：</span>\n    \n    Q\\=xs×WqT\n    \n    Also, note that for simplicity (heh) I'm disregarding bias in this discussion. [↩](#fnref-2) <span class=\"translation\">另外，为了简单起见（呵），这里忽略偏置。[↩](#fnref-2)</span>"
}
